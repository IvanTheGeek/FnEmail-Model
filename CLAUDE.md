# CLAUDE.md

The working rules for this repository live in [`AGENTS.md`](AGENTS.md), so that every agent and
every tool reads the same file rather than drifting copies.

@AGENTS.md

---

**If nothing else survives the read: US spelling.** color, modeling, behavior, organize, analyze.
It is rule 1 in `AGENTS.md` because it has been corrected repeatedly and keeps coming back.
