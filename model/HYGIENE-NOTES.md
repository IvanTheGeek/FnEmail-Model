### 6. Address Resolution and Next-Hop Selection

Fields struck: 32 of 115 across the chapter's 40 events, leaving 83. All 40 events are returned; none dropped, renamed or merged.

By defect class. Fabricated identity keys: 2 session_id fields (LocalAliasAppearedInTransaction, UnqualifiedDomainNameRefused), both carrying f2C8D14. Fabricated message identifiers: 12 message_id fields, every one with the example <f2C8D14@foo.com> (MessageQueuedAfterTemporaryDnsError, MessageFailedWithoutQueueingAfterTemporaryError, MessageReturnedUndeliverableAfterSelfMatch, AlternateAddressLimitReached, CandidateAddressesExhausted, DeliveryAttemptFailedForAddress, DeliveryAbandonedAfterSingleAddress, MessageRelayedByDesignatedExchanger, MessageDeliveredFinallyByDesignatedExchanger, MessageHandedOffOutsideSmtpEnvironment, ReversePathRewrittenBeforeRelay, ForwardPathRewrittenBeforeRelay). That is all 14 f2C8D14 instances in the chapter, gone. No message_ref, transaction_id, connection_id or reply_code field existed here — the earlier sweeps' refusal to invent a reply code on UnqualifiedDomainNameRefused was already correct and is left standing.

The message_id test against the spec, verified by opening the file. §4.4's ID clause is real but is neither required nor a sender's key: Opt-info = [Via] [With] [ID] [For] at line 3329 makes it optional, and ID = CFWS "ID" FWS ( Atom / msg-id ) at 3336-3337 defines it as a clause the RECEIVING host writes into its own Received: trace field. §6.4 line 4131 lets an originating server add "a message-id field when none appears", which proves a message in SMTP need not have one. And the RFC's own worked relay in D.3 carries no Message-ID header anywhere and writes its Received: line at 5064 with no ID clause. So nothing in RFC 5321 supplies a message identifier to a routing event, and all 12 went.

No queue_id was substituted anywhere, though the brief permitted it. Five of the stripped events are rooted on Queue Entry, and it was tempting to give them one — but RFC 5321 defines no queue entry and no identifier for one, and minting f2C8D14 into five fresh fields would have re-committed the defect under a new name and hidden the finding. Each of those events instead records in notes that identity is the queue entry itself and that §4.4's optional ID clause is the only place the RFC acknowledges such a handle can exist. AlternateAddressLimitReached now carries a fact and no identity key at all; that is the honest shape of what the source provides.

Computed and derived values struck, 12: offending_label (a split of offered_name), delivery_domain on TopLevelDomainAddressUsed (a split of the forward-path with no decision left in it), implicit_exchanger and preference on ImplicitMailExchangerAssumed (line 3843-3844 fixes both — the exchanger is always the delivery domain, the preference always 0), record_type on MailExchangerAddressResolved and on AddressRecordPublished (the address family reads off the address), matched_preference on SelfIdentifiedInMailExchangerList (read off the MX set, and it does its real work on LowerPreferenceExchangersDiscarded), remaining_exchangers (candidate list minus discarded, an option the inherited note itself left open), limit on AlternateAddressLimitReached (configuration state readable from AlternateAddressLimitConfigured), reported_as (redundant with the event name), and ttl_seconds (RFC 5321 never mentions TTL or time to live — grep returns nothing — so the field reached past the swept source into RFC 1035). Recipient context struck as a read-model join on the three domain-level branches: forward_path on TargetDomainFoundNonExistent, NoUsableMailExchangerFound and RoutingErrorReported. Event metadata struck: queued_at and opened_at. Padding struck: refusing_host, which restated the actor slot; and skipped_family, which the RFC's own clause at 3949-3951 derives from client_stack.

Fieldless events: zero. Eight were reduced to a single field — UnqualifiedDomainNameRefused, DomainNameLabelSyntaxViolated, TopLevelDomainAddressUsed, TargetDomainFoundNonExistent, MessageQueuedAfterTemporaryDnsError, MessageFailedWithoutQueueingAfterTemporaryError, ImplicitMailExchangerAssumed, AlternateAddressLimitReached — which is the method's sanctioned "just the identity key" shape. That zero is not padding surviving: this is an address-resolution chapter, and every event in it is about a name, an address or a path, so there is no bare state transition with nothing to name. ImplicitMailExchangerAssumed is the closest thing to a fieldless outcome here — after striking the two constants the rule supplies, only the delivery domain remains.

Confidence unchanged on all 40; no event's justification collapsed once its fabricated fields were gone. The one that came closest is ImplicitMailExchangerAssumed, whose inherited note claimed "the whole content of the event is the exchanger name and the preference 0" — both of which turned out to be the rule restated. It is kept at certain because the state change (an empty MX list came back, and the address is now bound to an implicit exchanger) is a real observation, not a computation; the disagreement with the inherited note is recorded in place rather than fixed silently, per rule 4. Two other events already carried doubtful for reasons this pass did not touch (MailExchangerAddressResolved reads like a query result; AddressFamilyCandidateSkipped sits in a section with zero RFC 2119 keywords), and both keep it.

example_source is set on all 83 surviving fields: 24 to a specific RFC line, 25 to the walk scene, 34 to "illustrative — no RFC source" stated plainly. The illustrative count is high and is itself a finding — RFC 5321 supplies no preference number anywhere (D.3 says only "best preference" and "a lower preference"), no local alias, no non-conforming label, no top-level-domain address, no broken zone, no worked address rewrite, and no exchanger host name outside D.3's own xyz.com and foo.com. Every RFC line cited as a source was confirmed by opening docs/rfc/rfc5321.txt, including the section headings (§2.3.5 at 691, §3.6.1 at 1421, §3.6.2 at 1446, §3.6.3 at 1475, §4.4 at 3156, §4.5.4.1 at 3685, §5.1 at 3824, §5.2 at 3937, §6.3 at 4065, §6.4 at 4074). All pre-existing notes are preserved verbatim; each addition is marked with a ⚠️ block so the original reasoning and the correction both stand.

---

### 4. Trace and Provenance

36 fields struck across 38 events (122 fields in, 86 out). By kind: 32 instances of the fabricated `message_ref` identity key — every event that had one — plus 3 instances of `received_count` and 1 of `rejection_threshold` under the no-derived-values rule. 1 event became fieldless: ExistingTraceLinePreserved, whose only field was `message_ref`; its inherited notes had already argued the fieldless tension in the abstract, and the strike settles it — the event was holding a fabrication where it thought it held an identity key. No event was marked newly doubtful. Nothing in this chapter's justification rested on a fabricated field: the three events left thinnest (LoopingMessageRejected and LoopedMessagePassedOnUndetected at one field each, MailLoopDetected at two) all stand on acts the RFC names — refusing at a rejection threshold, forwarding, concluding a loop — rather than on the counts that were struck, so all three keep their inherited confidence with the thinning recorded in their notes. Three events were already clean and lost nothing: TraceTimeFormatConfigured, SlowRelayDetected, ProtocolNameRegistered (also ErrorReportAddressDesignatedForNonSmtpTransport and LoopRejectionThresholdConfigured). f2C8D14 appeared 33 times; 32 struck, 1 kept — `trace_id` on TraceLinePrepended, which is genuinely §4.4's optional ID clause (rfc5321.txt:3336, `ID = CFWS "ID" FWS ( Atom / msg-id )`), the one place in RFC 5321 where an identifier belongs on a trace line, and the walk's own stored Received line writes `id` f2C8D14 there; its example_source now reads "illustrative — no RFC source; this project's own minted value". On instruction 3, tested against the source: the ID clause supplies a per-line identifier only, chosen by the stamping server, optional under Opt-info at 3329 — it is a clause of one hop's line, not a key the message carries between hops, so it does not license `message_ref`/`message_id` anywhere else. The only other message-identifier text in RFC 5321 is line 4131, "Addition of a message-id field when none appears", a MAY available to an originating or submission server in §6.4, which is a header field it may add, not an identity the protocol supplies. Instruction 1 found nothing further to strike here: no `session_id`, `transaction_id` or `connection_id` appears in this chapter at all, consistent with the grep result that RFC 5321 names none. Instruction 4 likewise found no `reply_code` in this chapter — and two inherited records, MailRejectedOnTraceHeaderFormat and LoopingMessageRejected, had already refused to invent one and said so in their notes, which is this pass's own rule reached a step earlier and voluntarily. Two example values were corrected rather than struck, recorded per rule 4 with the old values kept in the notes: on TraceClauseRegistered, `registry` read "mail parameters" (the protocol registry named at line 3372) and now reads the RFC's own name for the clause registry, "Mail Transmission Types" at line 4407 with clause registrations held in the added subsection at 4419-4422; and `registered_by` read "IETF standards action", which is RFC 5226 vocabulary absent from RFC 5321, replaced by "standardization" from §8 line 4436. Four list-valued examples were repaired under rule 8 — TraceForClauseOmitted, MultiplePathsStampedInForClause and MultipleReturnPathsPresentAtDelivery each showed a one-element list on an event defined by there being more than one, which made the payload contradict the meaning; where possible the second value is the RFC's own <Brown@foo.com> at line 4899. Every surviving field now carries example_source. The honest distribution is worth reporting: the walk scene sources most of the message-side values, real RFC lines source a useful minority (3367 for SMTP, 3334 for WITH, 3372 for "mail parameters", 3466 for X.400, 3181 for "reordered", 3188 for local time, 3395 for postmaster, 4070-4071 for the threshold of 100, 4899 for <Brown@foo.com>), and roughly a dozen are stated plainly as illustrative with no RFC source — concentrated in the host names (mx.foo.com, gw.foo.com, mail1.internal.foo.com), the registry values the event exists to create (TLS, ESMTPS, LOCAL, P1), and loop_shape, where the RFC's failure to define "trivial" means no example can be sourced at all. That last one is a finding rather than a defect and is recorded on the event.

---

### 5. Responsibility Handoff and Final Delivery

SCOPE: all 34 records whose chapter is exactly "5. Responsibility Handoff and Final Delivery". None dropped, renamed or merged; fields and notes only. Every existing note is preserved verbatim and my additions are appended as a dated ⚠️ FIELD HYGIENE PASS block, per rule 4.

COUNTS. 59 fields struck across 129 (34 events went from 129 fields to 70). One event became fieldless: MessageContentStored, which was already 'doubtful' — stripping its invented transaction_id and its occurrence timestamp leaves nothing, and that emptiness is the same doubt its original notes recorded, now visible in the payload. Two events arrived clean and lost nothing: SourceRouteStrippedFromReturnPath (its values are the RFC's own worked example at lines 3994 and 3998) and RecipientValidationDeferred (rooted on Recipient, where the forward-path is an identity the RFC actually supplies). That contrast is the audit's own lesson restated — rule 8: where a sweep walked with real data, it had no gap to fill with a fabrication.

NO EVENT'S JUSTIFICATION COLLAPSED, so nothing was newly marked doubtful. The closest call was DuplicateMessageReceived, which lost both message_id and duplicate_of — the pair carried the same f2C8D14 value and together looked like a correlation the protocol provides. It does not: with no identifier on the wire the receiver has no protocol means of recognizing the second copy as a copy, which is exactly why §6.1 4008-4011 can only tell it to answer faster. The event survives because the RFC states the outcome; what the strip exposed is that its actor cannot observe it, and that is flagged as a 🟥 in its notes rather than converted into doubt.

WHAT WAS STRUCK, BY CLASS.
(1) Invented identity keys — 6 transaction_id and 1 queue_entry_id. The grep for session/transaction/connection identifiers over docs/rfc/rfc5321.txt returns nothing; every one of them carried f2C8D14 or f2C8D14-1, this project's minted queue_id value from commit 6acfc68, which appears nowhere in the RFC including Appendix D. Each event now records what carries identity instead — position in the session, the open channel, the forward-path or reverse-path, the destination mailbox, the site.
(2) message_id / original_message_id / notification_message_id / duplicate_of — 22 in all, struck everywhere. Tested against §4.4 as instructed and the source does not support them: Opt-info = [Via] [With] [ID] [For] at line 3329 makes the ID clause OPTIONAL; ID = CFWS "ID" FWS ( Atom / msg-id ) at line 3336 puts it inside the receiver's own Received: stamp, not on the message; and msg-id is RFC 5322's, not this document's. So RFC 5321 defines no message identifier, and no event in this chapter is the trace-insertion event — TraceRecordInserted was already set aside to §4.4's trace half, in another chapter. No queue id therefore survived anywhere in chapter 5.
(3) reply_code — 4 struck (250, 451, 554, 550) with their two reason_text companions. The code is the rendering of a decision the event name already carries, and §4.2 line 758 says plainly that the codes are for programs and the text for human users, line 2727 that "Each reply text is recommended rather than mandatory". Nothing in RFC 5321 carries a code as data past the reply with one arguable exception, recorded on DeliveryResponsibilityDeclinedPermanently: §4.2.5 line 2986 requires "user review of the message and response" before retrying the same server — a client-side retention, on a client-side event this chapter does not hold.
(4) Occurrence timestamps — 14 struck (accepted_at, stored_at, discharged_at, committed_at, lost_after, delivered_at ×2, deposited_at, handed_off_at ×2, detected_at, failed_at, sent_at, logged_at, reported_at, introduced_at). An event's own time of occurrence is the timeline's, not the payload's; carrying it on every event is the padding the "do not pad to reach a count" rule names.
(5) Derived and conformance values — reversible: true and applied_to_relayed_mail on MailDataTransformedForLocalStorage, and cause on the two duplicate events where the RFC names exactly one possible cause so the field could hold only one value. An earlier pass had already dropped discharged_count, deferred_count, copies_delivered and stripped_count as folds; nothing further of that kind remained.

TWO INFERENCES A LATER STEP MAY WANT TO PUSH BACK ON, flagged because they are mine and not quoted from the method. First, occurrence timestamps as metadata rather than payload — this is the single change that lets any event go fieldless, and if it is rejected then MessageContentStored keeps stored_at and the chapter has no fieldless event at all. Second, reversible: true struck as a conformance assertion, on the ground that a MUST fixes the value for every permitted occurrence so a recorded true carries no information and the false case is a separate event (IrreversibleTransformApplied, already named in that record's notes). I did NOT extend that reasoning to envelope_sender MAIL FROM:<> on UndeliverableMailNotificationSent: <> is a value that genuinely travels in the envelope, quoted from line 1522, not an assertion about compliance.

WHAT I KEPT AND WHY IT MIGHT LOOK LIKE PADDING. Party and host fields (accepting_server, delivering_host, relaying_host and their kin) survive on the ground that RFC 5321 itself records which host acted, as trace data: From-domain at line 3317 and By-domain at 3319. If a later step judges them a restatement of the record's own actor slot, they are the next thing to go, and roughly twelve more events would become fieldless.

EXAMPLE SOURCES. Every surviving field carries one. RFC-sourced values were verified by opening docs/rfc/rfc5321.txt this session, and every section cited was confirmed by finding its heading line first, per rule 3 — §1.1 at 233, §2.1 at 348, §2.3.3 at 656, §2.3.10 at 801, §2.3.11 at 826, §3.3 at 1015, §4.1.1.3 at 1916, §4.1.1.4 at 1992, §4.2.5 at 2941, §4.4 at 3156, §4.5.2 at 3423, §4.5.3.1.10 at 3571, §4.5.3.2.6 at 3656, §6.1 at 3960, §6.2 at 4013, Appendix B at 4720. Line-anchored examples: 246 public Internet, 735 postmaster, 1522 MAIL FROM:<>, 2830 mailbox unavailable, 3158 delivery or further processing, 3210 associated mail drop, 3285-3286 the grouping choice, 3453 records rather than strings, 3983 the null address, 3994 and 3998 the RFC's source-route example, 4003 "soft" domain system error, 4734 if the envelope is not supplied. Three values are marked "illustrative — no RFC source" rather than quietly kept: relay.bar.com (the walk has no relay in it), the RelayInspectedMessageData inspection depth, and X.400 as a transport environment — X.400 appears at line 3466 but as an address form under MIXER, not as an environment, and the RFC's own environment list is §1.1 246-248. <Brown@bar.com> is likewise marked illustrative, with the RFC's own failed recipient noted alongside it: <Green@foo.com>, "Green does not have a mailbox at host foo.com", line 4884.

RULE 2 MATERIAL PRESERVED UNTOUCHED: the lowercase "SHOULD not" at line 2985, the lowercase "must" at 3275 and at 3964, the RFC's quotation marks around "soft", and the verbatim source-route values @a,@b:user@d and user@d, which are quoted evidence and were deliberately not restyled into the walk's foo.com/bar.com set.

---

### 1. Session Lifecycle

Struck 88 of 144 fields across 40 events; 56 fields survive. Six events became fieldless: SessionInitialStateConfirmed, StateTableReset, SessionEndRequested, DataTerminationTimeoutExpired, ConnectionLossTreatedAsQuit, ConnectionClosedDefensively. One event marked doubtful because its justification collapsed: StateTableReset — its only distinguishing field, restored_state="after-greeting", was invented in the teeth of its own note, which said §2.3.6 never names which previous state is restored and warned "Do not fill that in from memory"; with the field gone nothing separates it from BufferCleared, so it is a field wearing an event's name. Kept per the brief, confidence lowered, reason recorded in its notes. ConnectionClosedInViolation was already doubtful and stays there — the strike neither rescued nor collapsed it.

What was struck, by class. (a) Fabricated identity keys: all 32 session_id fields, plus affected_sessions (ServiceShutdownDetected), open_sessions (ServiceForciblyShutDown) and discarded_transaction_id (SessionReinitialized) — 35 fields carrying f2C8D14, every instance of that value in the chapter. Each strike adds a sentence naming what carries identity instead: the channel itself while a session lives (the general case), the endpoint pair at TransmissionChannelOpened, position in the session (SessionInitialStateConfirmed, SessionEndRequested, InterveningCommandRefusedAfterSessionRefusal), the claim itself (ClientIdentityDeclared), the buffer contents (BufferCleared, and the reverse-path/forward-paths are what name the transaction SessionReinitialized discards), the service instance's own domain (the three SMTP Service Instance events), the destination host (ShutdownNoticeReceived — the one event whose identity key the RFC genuinely supplies, because a domain name is a name the spec defines). (b) All 13 reply-code fields, including prior_reply_code, final_reply_code and assumed_reply_code; none survives. The near-miss is recorded in full on ConnectionFailureTreatedAsTransientError: §3.8 line 1675 does put a 451 inside the client with nothing on the wire, which is the one genuine "code as data" case in the chapter, but it is constant on every occurrence and so restates the event's own name — struck with the line number recorded so a later step can restore it deliberately. (c) Computed, derived and duplicated values: close_arm (folded from which antecedent event preceded), issued_asynchronously (decidable from the presence of in_reply_to_command), replies_issued_before_close (a count over prior events; §7.8 supplies no number, only "an appropriate number"), extensions_offered (already recorded as ehlo_keywords), greets_domain and ehlo_greet, minimum_seconds (a constant per phase, from the RFC's own subsection headings), configured_limit, peer_authenticated (a fold over whether authentication happened), server_host_name/server_address/session_peer/peer_address/client_address where they merely repeat the channel's endpoints. (d) Every waited_seconds — four of them. This reverses an explicit claim in ServerInactivityTimeoutExpired's own notes, that the configured limit reached is a fact and only elapsed_seconds would be derived; the reversal is recorded as a ⚠️ block there, and rests on TimeoutPolicyConfigured being where those numbers legitimately live (which is why that event kept all three of its value fields). (e) Seven occurrence timestamps — started_at, opened_at, disabled_at, requested_at, detected_at, changed_at, final_dot_sent_at — an event's own time is not payload. (f) Unsourceable content: listen_address (the RFC names port 25 at line 3777 and no listening address anywhere), prior_reply_code on HeloFallbackPerformed (§4.1.1.1 lines 1815-1817 says only "an error response" and names no code — 500 came from the sweep, not the source), substitution_reason (invisible on the wire; the server can never observe why a literal was substituted, and the RFC gives the cause only as an "e.g."), reason on SoftwareVersionAnnouncementDisabled (§3.1 supplies exactly one motive), scope and reply_code on ConnectionRefusalConfigured (§4.5.1 line 3397 fixes both by the word "always").

message_id / message_ref: tested against the spec and found to have no basis in this chapter. §4.4 line 3329 does give Opt-info = [Via] [With] [ID] [For], and line 3336 defines ID = CFWS "ID" FWS ( Atom / msg-id ) — verified by opening the file — but that is an optional clause on the Received: trace header, so it belongs to the Trace and Provenance chapter and applies to a message being relayed, not to a session event. No chapter-1 record carried either field, and none was added. No queue id survives here for the same reason: nothing in Session Lifecycle is the trace-stamping act, so f2C8D14 has no legitimate home in this chapter at all.

Type corrections, one only: listen_port's type was Count, which a port is not; changed to PortNumber. No event was renamed, dropped or merged, and all existing notes are preserved verbatim with the hygiene record appended. Every surviving example now carries an example_source: 26 cite an RFC line (mostly the D.1 walk at lines 4886-4908, plus §4.2.2's registered reply texts at 2809 and 2811, the timeout headings at 3633/3656, the failure enumeration at 1671-1672 and the command headings at 1879/1916/2079), 5 read "walk scene" (the two IP addresses and the bracketed literal built from them), and 6 are stated plainly as illustrative with no RFC source — software_identification "Postfix 3.8.1", trailing_identification "mail.bar.com", verification_result, rejected_argument "mail_server.bar.com" (the underscore rule it breaks is at 2372-2374), refusal_text, and command_verb "XVERP" on the two events that use it (the X-prefix form is licensed at 582-584).

---

### 3. Message Content Transfer

SCOPE: all 40 records whose chapter is exactly "3. Message Content Transfer". None dropped, none renamed, none merged; rfc_refs, names, meanings, actors and stream_roots untouched. Every original note is preserved verbatim, with a "FIELD HYGIENE PASS" paragraph appended.

COUNTS: 134 fields on entry, 101 struck, 33 survive. 15 of 40 events are now fieldless (DataRequested, MailDataTransferAuthorized, DataCommandRejectedOutOfSequence, DataCommandRefusedAfterRecipientOverflow, HighOrderBitCleared, EightBitOctetsNormalized, EndOfMailDataIndicated, EndOfMailIndicatorRecognized, EmptyMessageContentTransmitted, ExtraLineEndingAppended, DuplicateMessageReceived, MailDataBuffersCleared, MessageContentStored, DeliveryResponsibilityDeclinedTemporarily, DeliveryResponsibilityDeclinedPermanently). Confidence values are unchanged on all 40.

WHAT WAS STRUCK, BY CLASS. Fabricated identity keys, 40 instances: 15 session_id, 21 transaction_id, 2 submission_id, 1 duplicate_of_transaction_id, 1 receiving_server used as a correlation key. Verified, not assumed: grep -niE "session (id|identifier)|transaction (id|identifier)|connection (id|identifier)|message (id|identifier)" over /home/ivan/FnEmail-Model/docs/rfc/rfc5321.txt returns nothing at all. submission_id was the same fabrication under a new name, on two events that happen before any session exists. Wire renderings, 27 instances: 17 reply_code (plus prior_overflow_reply_code, last_reply_received, declined_reply_code), 7 reply_text/reason_text/rejection_reason/cause. Computed or derived values, 34 instances: 8 self-timestamps (requested_at, indicated_at, recognized_at, confirmed_at, stored_at, accepted_at ×2, final_period_sent_at), 4 measurements (line_octets, content_octets ×2, recipients_accepted), 3 restatement constants (terminator, added_sequence, channel), 3 conformance/derived booleans and duplicates (reversible, applied_to_relayed_mail, size_extension_advertised, characters_lost). Session-level facts repeated per event, 10 instances: receiving_server, sending_client, transmitting_client, accepting_server, declining_server ×3, rejecting_host, plus reverse_path where the MAIL event already carries it.

f2C8D14: it appeared on 21 fields in this chapter and appears nowhere in rfc5321.txt (grep returns nothing). It survives on exactly one field — queue_id on MessageAcceptedForProcessing — labeled "illustrative — no RFC source; this project's own minted value", because §4.4 line 3336 gives ID = CFWS "ID" FWS ( Atom / msg-id ) and line 3329 puts [ID] in Opt-info, which makes a receiver-stamped queue id the one identity key RFC 5321 licenses, coming into existence at exactly that moment. Two caveats travel with it in the note: the clause is optional, and the msg-id alternative is RFC 5322's.

message_id / message_ref tested against the spec, per instruction 3: neither appeared anywhere in this chapter's 134 fields, so nothing had to be struck — but the test still returned a finding. The only thing in RFC 5321 that could supply one is the msg-id alternative in the §4.4 ID production, and msg-id is explicitly deferred to RFC 5322 ("; msg-id is defined in RFC 5322 [4]", line 3337). So RFC 5321 supplies no message identifier of its own, and a message_id field anywhere in this model has no basis in this document. DuplicateMessageReceived is where that bites: the merge had already rejected a <f2C8D14@foo.com> Message-Id form on the correct ground, then substituted transaction_id and duplicate_of_transaction_id, which are fabricated too. Both are gone, and the event's own hotspot — the RFC asserts duplicates exist and supplies no way to identify one — is now visible in the field list instead of papered over.

reply_code kept on nothing. The closest call was DeliveryResponsibilityDeclinedPermanently: §4.2.5 line 2985-2987 says the client SHOULD not retry "without user review of the message and response", which is the one line in RFC 5321 where a reply is retained past the exchange. It does not license the field here, because the retention is the client's, on the message it still holds, and what is retained is the whole response rather than an extracted code — recorded in that event's note so a later step can put a reply field where it belongs, on the sending system's hold event.

THREE CORRECTIONS THIS PASS MAKES TO INHERITED REASONING, each recorded in the event's notes rather than applied silently. (1) EndOfMailIndicatorRecognized's note defended recognized_at as "the timestamp the trace line's stamp derives from"; that is an argument for its being derivable, not for its being payload — the §4.4 Stamp date-time is a consumer's copy of the event's own occurrence time. Struck. (2) MailDataTransformedForLocalStorage's note argued reversible: true is "a conformance assertion, not a description" and kept it; a field that is true on every conforming instance carries no information and its false case is a separate event. Struck, along with applied_to_relayed_mail, which the same note showed to be emphasis rather than a second obligation. (3) MessageRejectedForIncompleteTransaction's inherited citation error was re-verified rather than trusted: section headings put §3.3 at rfc5321.txt:1015 and §3.4 at rfc5321.txt:1162, so the passage at 1137-1140 is §3.3 and the ref's "4.1.1.4" attribution is still wrong — left as harvested because this pass changes fields and notes only.

ONE INSTANTIATION DEFECT FOUND BY DOING THE SOURCING (rule 8). RecipientFailureReportedAfterContentAccepted instantiated failed_forward_path with <Jones@foo.com>, but Appendix D's Jones SUCCEEDS (rfc5321.txt:4895-4896); its failing recipient is <Green@foo.com> at 4897-4898, "550 No such user here". The value names the wrong party. Flagged in the note, not changed, because rewriting example values is outside this pass — but it is exactly the kind of thing a placeholder would have hidden.

NO EVENT WAS MARKED DOUBTFUL FOR A COLLAPSED JUSTIFICATION. Stated plainly because it is a real result and not an omission: I looked for the "field wearing an event's name" shape on every event that went fieldless, and the clearest instance — EightBitOctetsNormalized, whose entire payload was transaction_id plus a restatement of the channel condition — was ALREADY doubtful, on precisely the ground that it "may be better read as a property of the channel than as an event". Stripping confirms that existing doubt rather than creating a new one, so its confidence is unchanged and the note says why. Two others deserve naming because their fields had been carrying the argument and the name must now carry it: DataCommandRefusedAfterRecipientOverflow (fieldless, and distinguishable from DataCommandRejectedOutOfSequence only by its name and cause) and DuplicateMessageReceived (fieldless, and now visibly unidentifiable). Both stay at their harvested confidence — the events are real; only their payloads were invented.

WHAT CARRIES IDENTITY IN THIS CHAPTER, since that is the finding the fabrications were hiding. Before the 250: nothing but the channel and position in the session — one DATA per transaction, one transaction at a time, one end-of-data indication per transaction. Where a transaction must be named, the accumulated reverse-path and forward-path buffers name it (kept on MailTransactionConfirmed, MessageAcceptedForProcessing, MessageRejectedAtEndOfData, MessageRejectedByPolicy, DeliveryResponsibilityAccepted). At and after the 250: the queue id of the §4.4 ID clause, if the server stamps one. On a refusal: nothing at all — no queue id is stamped because nothing was accepted, which is why every declined and rejected event in this chapter is identity-less by the spec's own construction. Before any session (TerminatingLineEndingAdded, MessageRejectedAsInvalidByOriginator, HeaderValueEncodedForAsciiTransport): nothing whatsoever; originating_host is kept on the first two as the only party-identifying fact available, since there is no session event to carry it. And one event stands outside all of this: SizeRestrictionImposedWithoutSizeExtension has a Server Configuration stream root, so server_host is genuine stream identity rather than a repeated session fact — which is why it is kept where the other host fields were struck.

US spelling checked on this output: color/modeling/behavior/organize/analyze forms only; the grep pattern in AGENTS.md rule 1 returns nothing against this text.

---

### 2. Mail Transaction

Chapter 2, Mail Transaction: 40 events in, 40 events out. No event dropped, renamed or merged; fields and notes only. Every existing note is preserved verbatim and my addendum is appended after a blank line, prefixed "FIELD HYGIENE PASS:", so the two layers stay separable. Where an addendum contradicts the note it follows, it opens with ⚠️ and quotes the superseded claim (AGENTS rule 4).

STRUCK: 77 of 120 fields. 43 survive. Breakdown — 26 fabricated identity keys (17 session_id, 6 transaction_id, plus open_transaction_id, canceled_transaction_id, preserved_transaction_ids); 24 reply codes (22 reply_code, plus treated_as_reply_code and prior_overflow_reply_code); 8 reply/reason texts; 7 derived path or envelope values (discarded_reverse_path and discarded_forward_paths on TransactionBuffersCleared, retained_forward_path and stripped_at_domains, conforming_reverse_path, reverse_path and recipients on MailTransactionConfirmed); 7 ambient host or peer identities (declaring_host, sending_host twice, peer_address, rejecting_host, receiving_host, relay_host); 4 timestamps; 1 constant restating an event's name (offending_verb on RecipientCommandRejectedOutOfSequence). All 26 occurrences of f2C8D14 in the chapter are gone.

FIELDLESS: 8 of 40 — MailTransactionStarted, DataCommandRefusedAfterRecipientOverflow, NestedMailCommandRejected, MailCommandRejectedForUninitializedSession, RecipientCommandRejectedOutOfSequence, DataCommandRejectedOutOfSequence, PendingTransactionCanceled, MailTransactionConfirmed. Every one is a genuine state transition whose content is its position in the session, which is what the RFC gives instead of a key. RecipientValidationDeferred is the only event that arrived clean; nothing was struck from it.

DOUBTFUL: none. No candidate's justification collapsed once its fabricated fields were gone — every one still rests on a cited sentence rather than on a field. The nearest miss is PendingTransactionCanceled, whose whole content had been carried by canceled_transaction_id and preserved_transaction_ids; it survives at "certain" because the MUST at 2215-2219 is intact and the rule is better expressed by the stream than by the invented keys — the preserved transactions are the ones already confirmed, and "MUST not undo" is the absence of any compensating event.

VERIFIED, NOT ASSUMED (rule 3, run against /home/ivan/FnEmail-Model/docs/rfc/rfc5321.txt): grep for "session (id|identifier)|transaction (id|identifier)|connection (id|identifier)" returns nothing — RFC 5321 supplies no such key, and 2506-2507 makes many transactions per session explicit while naming none of them. grep for f2C8D14 returns nothing. §4.4's optional ID clause is at line 3336, "ID = CFWS \"ID\" FWS ( Atom / msg-id )", inside the Received stamp's grammar under the heading at 3156; it supplies a trace-header clause, not a message identifier on transaction events — and no message_id or message_ref field existed anywhere in this chapter, so nothing needed striking on that count. No queue id was needed either: the §4.4 ID clause never bites in Chapter 2.

ONE REPLY CODE KEPT, and the line that makes it data: received_reply_code (552) on OverflowReplyReclassifiedAsTransient, sourced at rfc5321.txt:3573 with the operative sentence at 3577-3578 — "Clients SHOULD treat a 552 code in this case as a temporary, rather than permanent, failure so the logic below works." The code has crossed out of the reply into the client's queue logic, where it is the thing being reasoned over. Its partner treated_as_reply_code (452) was struck: the RFC says clients treat the reply as temporary, a classification, and never says they relabel it 452 — 452 is the server's correct code at 3574, borrowed for a client act the spec does not describe.

STRIKES THAT COST SOMETHING, recorded rather than hidden. DataCommandRejectedOutOfSequence: the RFC offers two codes for one condition at 1130-1133, and dropping reply_code leaves the 503-versus-554 choice unrecorded; the note's own alternative — two separately named events — is now the live option. CommandRejectedAsOutOfSequence: its distinctness had been argued from the 503 itself; what survives is the second MUST at 2527-2528, that the server stays in the same state, which is a statement about state rather than rendering. RelayingDeclined: losing receiving_host costs the lesson that foo.com is the server, which is what makes RCPT TO:<Smith@bar.com> a relay request; the lesson stays in the note and the identity belongs to the channel. SourceRouteStripped: retained_forward_path and stripped_at_domains went because the RFC states the derivation rule twice (1926-1928, 1430-1433) and performs it in its own worked example, 1944 to 1950.

EXAMPLE SOURCES: every surviving field carries one. Nine are exact RFC line numbers (1522, 1944 four times, 1985, 2079, 2502, 2506, 1992, 3573, 4003, 4897); the rest are "walk scene" or "illustrative — no RFC source" stated plainly, with the reason attached. Six values turned out to be illustrative that had read as sourced: SIZE=1024000 (SIZE= appears nowhere in RFC 5321 — only the EHLO keyword 250-SIZE at 4889), NOTIFY=SUCCESS (RFC 3461, absent here), the two invented relay hosts in the source-route examples, the 500 and 40 recipient limits, and the suggested forwarding mailbox §3.4 never shows. The timestamps were fabricated twice over — the RFC's own are "Thu, 21 May 1998 05:33:29 -0700" (5014), and the sweeps wrote a different day and clock. The one borrowed-line case is SourceRoutedAddressRefused, whose 550 and "Access Denied to You." came from a §3.5.1 VRFY example at 1323; both struck, the citation kept in the refs so no later sweep repeats the borrowing.

---

### 7. Sending, Queueing and Retry

Chapter 7, "Sending, Queueing and Retry": all 35 records returned, none dropped, renamed or merged. Fields and notes only.

STRUCK: 34 fields across 35 events. By class — fabricated identity keys 4 (UnsentMessageDiscardedWithoutQueuing.message_id; ErrorMessageSentInResponseToErrorMessage.triggering_message_id and .generated_message_id, the latter carrying a41F09B, a value that is not even one of this project's minted examples; CommandRetryAttempted.queue_entry_id, a queue key imported onto a session-rooted, command-level fact). Bare occurrence timestamps 15 (flagged_at, discarded_at, started_at, widened_at, requeued_at x2, transmitted_at, abandoned_at, marked_at, restored_at, blocked_from, cached_at, invalidated_at, triggered_at, attempted_at on CommandRetryAttempted). Reply codes 3 (MessageRequeued.declining_reply_code, QueuedMessageReturnedToUser.declining_reply_code, CommandRetryAttempted.previous_reply_code). Computed or derived 4 (RetryScheduled.interval_minutes, DestinationRetriedWithoutDelay.elapsed_since_failure_minutes, DeliveryGivenUp.first_queued_at, DeliveryGivenUp.give_up_after_days as configuration restated). Address-resolution results imported from another chapter 4 (MessageQueuedForDelivery.destination_host, DeliveryAttemptFailed.destination_address, HostMarkedUnreachable.host_address, HostReachabilityRestored.host_address). Non-identifying host fields on the Outbound Queue root 2 (QueueRunStarted.client_host, OutboundConcurrencyLimitReached.client_host), because rfc5321.txt:3673-3675 gives a host "one or more areas for queuing messages in transit", so the host does not identify a queue. Restated destination 2 (QueueActivityTimedOut.destination_host, and MessageQueuedForDelivery's counted above).

FIELDLESS: 2 events. UnsentMessageDiscardedWithoutQueuing — the emptiness is the finding, since nothing survived the discard to be identified, which is exactly why the MUST at 3690-3692 exists. QueueRunStarted — a pure time-triggered transition with no identifier the RFC supplies.

REDUCED TO IDENTITY ALONE: 2 more. QueueEntryFlaggedForImmediateAttention (queue_entry_id) and HostReachabilityRestored (host).

MARKED DOUBTFUL BECAUSE ITS JUSTIFICATION COLLAPSED: 1. DestinationRetriedWithoutDelay, lowered from "likely". Its entire content was elapsed_since_failure_minutes, a value computed from this event's time minus DeliveryAttemptFailed.attempted_at, which the method sends to a read model. With it gone the payload is identical to a conforming retry's, the MUST violation and the SHOULD shortfall are indistinguishable, and the verdict survives only as a fold over two events' positions — so what is left may be a comparison wearing an event's name. Kept, per the brief; deciding is a later step's job. Two events were already doubtful and stay so on their original grounds (HostReachabilityRestored, CachedNegativeResponseInvalidated, plus ExtensionListChangedWithinSession and RetryStrategyAdjustedForMultipleAddresses); none was lowered by this pass except DestinationRetriedWithoutDelay.

WHAT SURVIVED THE THREE TESTS, AND WHY.

Identity keys: grep for session, transaction and connection identifiers over docs/rfc/rfc5321.txt returns nothing, confirmed. queue_entry_id is kept on the 17 Queue-Entry-rooted events (and as deferred_queue_entry_id on one Outbound-Queue event) on the §4.4 ground the brief names: the trace grammar admits an optional ID clause, `ID = CFWS "ID" FWS ( Atom / msg-id )` at rfc5321.txt:3336, under `Opt-info = [Via] [With] [ID] [For]` at 3329. Its example_source everywhere reads "illustrative — no RFC source; this project's own minted value" — f2C8D14 is absent from the RFC body and Appendix D, and traces to commit 6acfc68 and to docs/paths/WORKING-helo-direct-single-recipient-v2.md:494, which maps the ID clause to MessageAccepted.queue_id.

message_id and message_ref tested against the spec and REJECTED. §4.4's ID clause is the only candidate and it fails twice: it is a receiving server's trace stamp, not a handle SMTP hands a sending client, and its msg-id alternative is RFC 5322's, not RFC 5321's. So no message identifier survives anywhere in this chapter. The cost is recorded on ErrorMessageSentInResponseToErrorMessage, where the pairing that makes the event a LOOP rather than a single send is now inexpressible in RFC 5321's own vocabulary — flagged there as a hotspot under rule 12.

Reply codes: 3 struck, 2 kept, and the two kept are named. NegativeResponseCached.reply_code and MailCommandRejectionCached.reply_code survive because rfc5321.txt:3746-3750 makes the STORED response the subject of the rules — the cached value outlives the exchange, and "5yz responses to the MAIL command MUST NOT be cached" (3749-3750) keys on the code class of what was stored. That is data beyond the wire; a code echoed as a trigger is not. One example was re-sourced: NegativeResponseCached carried 450 against an EHLO, which the RFC does not list, so it is now 550, one of the three EHLO error codes at rfc5321.txt:3098. MailCommandRejectionCached's 550 is sourced to the MAIL error list at rfc5321.txt:3104.

Timestamps: struck as a class unless a rule in RFC 5321 measures from that exact moment. Three survive on that test — MessageQueuedForDelivery.queued_at (3720-3723 keys the backoff on "the first hour the message is in the queue"; 3709-3710's give-up time runs from it), DeliveryAttemptFailed.attempted_at (3703-3704's mandatory delay runs from it), and RetryScheduled.next_attempt_at, which is not an occurrence stamp at all but a future time and the whole content of its event.

Values re-sourced to the RFC rather than left illustrative: connection_timeout_seconds and per_connection_timeout_seconds 60 → rfc5321.txt:3741-3742, "even a one-minute timeout per connection"; new_interval_hours 3 → 3722-3723, "one every two or three hours"; minimum_retry_interval_minutes 30 → 3704-3705; give_up_after_days 5 → 3709-3710; requeue_timeout_days 1 → 603-604; bounce_timeout_days 3 → 602-603; 8BITMIME → 922; RCPT → 3387; EHLO → 3747-3748.

Values that stayed illustrative, each labeled with why the RFC gives no figure: notification_max_retries (3710-3713 names the shorter budget without a number), concurrency_limit (3770-3772 says only that "some limit may be appropriate"), rcpt_limit_per_mail (the 100 the sweep reached for is a RECEIVING server's threshold at rfc5321.txt:3549, which is the exact confusion that record's own notes warn against), failure_reason, and MessageBouncedBeforeFallbackAttempted.requeue_timeout_days, where three days is deliberately not the RFC's figure — one day is — because the equality is the misconfiguration being rendered.

Two contrasts are marked in-record so a later reader does not read them as inconsistency. peer_address is kept on QueueFlushTriggeredByPeerContact while host_address is struck from HostMarkedUnreachable and HostReachabilityRestored: the RFC says "address" in its own words at 3725-3727 and 3783-3785, and says "hosts" at 3716-3718. host_addresses is kept on RetryStrategyAdjustedForMultipleAddresses because there the plurality of addresses is the trigger the sentence is about (3732-3734), not an imported lookup result.

One cross-reference went stale and is corrected in place rather than quietly rewritten, per rule 4: ExtensionFallbackTimeoutElapsed's note pairs its requeue_timeout_days against DeliveryGivenUp.give_up_after_days, which this pass struck, so the note now points at RetryParametersConfigured.give_up_after_days instead. Every pre-existing note is preserved verbatim — rejected names, MUST/SHOULD/MAY strengths, the §4.1.1.10 citation correction on MessageRequeued, the hotspots, the screens, and the re-sweep provenance — with the hygiene record appended under a ⚠️ marker.

One judgment call recorded as unresolved rather than settled: MessageBouncedBeforeFallbackAttempted keeps both timeout durations even though the same configuration-restated argument struck give_up_after_days from DeliveryGivenUp. The distinction is that the SHOULD at 600-602 constrains only the RELATIONSHIP between the two, so a payload carrying one says nothing, whereas DeliveryGivenUp's copy said nothing its configuration event did not already say. The countervailing case — that the comparison is a fold a read model should perform, leaving the event at identity plus required_extension — is written into that record for a later step.

---

### 11. Forwarding and Address Correction

SCOPE: 18 events in, 18 out. None dropped, renamed or merged; this pass changed fields and notes only, and every inherited note is preserved verbatim above its appended ⚠️ FIELD HYGIENE PASS block. rfc_refs were left untouched even where the pass found a citation the event should carry (see MessageReturnedAsNonDeliverable).

COUNTS: 57 fields in, 36 out. 21 fields struck, 2 converted, 1 example value replaced. Breakdown of the 21: 5 invented identity keys (4 x transaction, 1 x session); 9 reply-code fields; 3 timestamps (effective_from, relayed_at, returned_at); 1 derived value (created_by, the local part of source_mailbox); 1 invented free text (disclosure_requirement); 1 padding field with a sourceable value (announcing_server on AddressBookEntryUpdated, struck on the method's essential-fields rule, not on rule 5 — flagged in-note for restoration if a later step needs the provenance).

f2C8D14 APPEARED 8 TIMES IN THIS CHAPTER: as transaction on 4 events, as message on 3, as session on 1. Six were struck. Two were converted to queue_id / QueueId — on MessageRelayedToCorrectedAddress and MessageReturnedAsNonDeliverable, the only two events where the server has actually accepted the message and written a Received line, so §4.4's optional ID clause applies (Opt-info = [Via] [With] [ID] [For] at rfc5321.txt:3329; ID = CFWS "ID" FWS ( Atom / msg-id ) at rfc5321.txt:3336; §4.4 heading verified at rfc5321.txt:3156). Both carry example_source "illustrative — no RFC source; this project's own minted value", and both notes record that the clause is OPTIONAL, so a server omitting it holds no message identifier the spec knows about.

THE ID CLAUSE WAS TESTED AND REFUSED ONCE. MessageResubmittedToCorrectedAddress lost its message field outright: a 551 answers RCPT, before DATA, so the refusing server never accepted, never queued and never wrote a Received line — there is no id from either side, and RFC 5321 gives an originating client no identifier for its own message either. That is a real consequence of the custody split those notes insist on, and a shared f2C8D14 had hidden it.

message_id / message_ref TESTED AGAINST THE SPEC: RFC 5321's only "message-id" is at rfc5321.txt:4131, a gateway adding an RFC 5322 header field where none appears — not an SMTP-level key. Nothing in the RFC supplies a message identifier to an event. Only the §4.4 ID clause survives the test, and only for a message already in a server's queue.

REPLY CODES: 9 struck, 1 kept. Kept on AddressUpdateDisclosureRestricted (restricted_reply_codes, example "251, 551", sourced rfc5321.txt:1208), because rfc5321.txt:1208-1209 reads "SMTP server implementations that support the 251 and/or 551 reply codes" — the codes are the SUBJECT of the configuration record, data it holds, not the rendering of a wire decision. The strongest case for a second keep was tested and failed: §4.2 rfc5321.txt:2563-2565 makes 251 and 551 two of five codes whose message text "must be parsed and interpreted by machines", but what is parsed is the forward-path, which CorrectedDestinationDisclosed and RecipientRejectedWithForwardingAddress already carry as corrected_mailbox and suggested_mailbox. The code selects which event this is; it is not payload on it. Also recorded, at RecipientRejectedWithoutAddressInformation: 550 is NOT among those five, which is the wire-level statement of what that event is — there is no text to parse because nothing is being told.

IDENTITY IN THE ABSENCE OF A KEY, per event: position in the session and the channel itself for the four in-session RCPT events and the VRFY event; the published or prior mailbox for the provisioning and relocation records; the forward-path for the rejections; old_address for the address-book record; the queue id where §4.4 reaches. Verified, not assumed: grep -niE "session (id|identifier)|transaction (id|identifier)|connection (id|identifier)" over docs/rfc/rfc5321.txt returns nothing, and grep -n "f2C8D14" returns nothing.

EXAMPLE VALUES, all sourced: <Green@foo.com> rfc5321.txt:4897, <Jones@foo.com> :4895, <Smith@bar.com> :4893, <Admin.MRC@foo.com> :5114, foo.com :4886, bar.com :4887, Green :4884, "251, 551" :1208, "disable" :1210. One value recurs as unsourceable and is labeled so on all nine fields that carry it: <Green@xyz.com> appears NOWHERE in RFC 5321 — Green is the RFC's own user with no mailbox at foo.com (4884) and xyz.com is the RFC's own relay host (1940), but the pairing is this model's, and it is stated plainly rather than dressed as a citation. Two invented values were replaced with RFC-sourced ones: AddressUpdateDisclosureRestricted.restriction went from "disabled for all clients except bar.com" to "disable" (§3.4 offers exactly two values, "disable or restrict their use", rfc5321.txt:1210-1211), and MailboxRelocationRecorded.effective_from carried the walk scene's Tue, 19 May 1998 timestamp where the RFC's own dates are Thu, 21 May 1998 — struck rather than corrected, because §3.4 states no start, no end and no lifetime for a forwarding entry, so the field had no source at all.

FIELDLESS OUTCOMES: ZERO, and that is reported as a result rather than engineered away. Two events came down to their identity key alone — RecipientRejectedWithoutAddressInformation (rejected_mailbox) and ServerChosenForDisclosureBehavior (selected_server_host). No event in this chapter became fully fieldless, and the reason is structural rather than a sign of surviving padding: every event here is about a mailbox or an address, and the mailboxes are real RFC values, so an identity key genuinely survives everywhere. Manufacturing a fieldless event by striking a sourced mailbox would have been the same defect in the opposite direction. Worth flagging for whoever aggregates the chapters: RecipientRejectedWithoutAddressInformation is the method's fieldless case in spirit — its meaningful content is an ABSENCE, and its inherited notes already argued it must not be padded with the suppressed replacement. That tension is now settled by the method's own words, "it is fine to have no fields or just the identity key."

DOUBTFUL: no event was newly marked doubtful, and no event's justification collapsed outright once its fabricated fields were gone. Three were already doubtful and stay so: CorrectedAddressFoundUnreachableBySender (which lost nothing — it never had a fabricated field, and both its fields are named by rfc5321.txt:1183 itself), ForwardingEntryRetiredOnAssumedClientUpdate (lost only its reply code, which the RFC's doubled MUST NOT makes non-distinguishing anyway), and ServerChosenForDisclosureBehavior. ServerChosenForDisclosureBehavior came closest to collapse and is called out in its notes: after disclosure_requirement went, no surviving field can say what was chosen or on what basis, because RFC 5321 never names a server implementation — its distinguishing content lived entirely in the invented field. It was already doubtful for a different reason (procurement state lands in a purchasing record, not a mail system), so the mark was not applied twice; the two reasons now stack, and a later step deciding it should weigh both.

WHAT THE STRIPPING EXPOSED, worth carrying forward: (a) VerifiedAddressReportedAsForwarded chose Session as its stream root precisely because no transaction or recipient exists yet — and RFC 5321 gives that root no key at all. Session is the right root and it is a keyless one; the fabricated session_id hid the one structural fact the event was recovered to expose. (b) CorrectedDestinationDisclosed's own merge note records ForwardPathAdvertised's session_id being replaced by transaction — one invented key swapped for another, the defect travelling through a merge rather than being caught by it. (c) After both lost their reply codes, CorrectedDestinationDisclosed and RecipientRejectedWithForwardingAddress have identical payload shapes, told apart by custody and stream root rather than by a code field — which sharpens, rather than undermines, the inherited note that refuses to merge them. (d) announcing_server on the §7.4 pair and disclosed_to on §7.7 can only ever hold a CLAIMED domain: a server learns the peer's name from EHLO and RFC 5321 offers no way to verify it, which is §7.4's unsatisfiable requirement restated at field level and the chapter's standing hotspot.

---

### 9. Gatewaying

SCOPE: all 32 records whose chapter is "9. Gatewaying". None dropped, renamed or merged. Fields and notes only; every pre-existing note is preserved verbatim above my appended "FIELD HYGIENE PASS" paragraph.

COUNTS. 124 fields in, 86 out, 38 struck.
- 27 x message_ref, every one with example f2C8D14. This chapter had no session_id, transaction_id, connection_id or message_id.
- 2 x reply_code (550 on MailRejectedOnTraceHeaderFormat, 501 on ValidAddressFormatRefusedByGateway).
- 9 others: stamped_at (event time, not payload); altered_line_origin_environment and originating_environment (facts the actor cannot know); approximation_basis and preserved_layers (paraphrases of their own event's name); maintains_envelope_sender and uses_internet_domain_addresses (Booleans always true when the event fires); gateway_host on the two events where another card already attributes the same act.
- 2 events became FIELDLESS: ViaClauseOmittedByGateway and LayeringSemanticsPreservedAcrossBoundary.
- 3 records were already clean: NonstandardAddressFormatAccepted, SourceRouteIgnoredByGateway, GatewayDesignatedForTransportBoundary. All three are events where the RFC or the routing table supplies a real key (a forward-path, a host-and-domain route) — the fabrications cluster exactly where RFC 5321 leaves identity to the channel.

message_ref / message_id tested against the spec and rejected. The only identifier RFC 5321 offers is the OPTIONAL ID clause of the trace header field, ID = CFWS "ID" FWS ( Atom / msg-id ), rfc5321.txt:3336-3337, inside Opt-info = [Via] [With] [ID] [For] at 3329. That is a clause a stamping server MAY write into one Received: line, not a key events carry. §6.4 line 4131 mentions adding an RFC 5322 message-id field, but that is an originating server repairing a header, not an SMTP identity key. ReceivedLinePrependedByGateway was the one plausible home for a queue id; I struck rather than converted, because §3.7.2's MUST is only "prepend a Received: line" and says nothing about the ID clause, so a queue_id there would invent an obligation. No f2C8D14 survives anywhere in the chapter.

reply_code struck on the method's computed-value rule, and both fail the source test hard: §3.7.2 lines 1585-1588 FORBIDS rejection on trace format outright and so assigns it no code, and §3.7.3 / §3.3 grant a gateway the option to decline an address without naming one. 550 and 501 were minted by the sweep.

FIVE VALUE DEFECTS FOUND BY SETTING example_source, all recorded as ⚠️ blocks in the records rather than quietly fixed.
- One false citation: MessageRemailedIntoOriginatingEnvironment claimed "<staff@foo.com> is real: it is the RFC's own list pseudo-mailbox in the §3.9 material". §3.9 runs 1687-1730 and names no mailbox; grep for staff@foo returns nothing. Rule 3's failure mode exactly — plausible, confident, wrong.
- Four reverse-path/forward-path inversions, on EnvelopeFoldedIntoHeaderSection (both paths swapped), ForeignErrorRoutedToReversePath, MessageGatewayedFromHeaderFieldsAlone and EnvelopeInformationLostInTranslation, plus a fifth on ForeignErrorRoutedToHeaderOriginator where correct_reverse_path contradicted the mailing-list argument its own note makes. Each used the walk scene's recipient <Jones@foo.com> where the sender <Smith@bar.com> belonged. A reverse-path example that is actually a forward path teaches the reader the inverse of the envelope/header distinction these events exist to expose — rule 8's point, arriving in the form the rule predicts.
- One value citing a specification that is not this one: transformation_kind read "header section mapped to X.400 P2 heading"; grep returns no P2 and no PRMD in RFC 5321. Replaced with the transformation §3.7.1 line 1549 actually names. The X.400 /G=.../ address forms elsewhere are kept as illustration and labeled as such.

DOUBTFUL. Four were already doubtful and stay so, with their strikes noted: ViaClauseOmittedByGateway, ValidAddressFormatRefusedByGateway, ForeignTransportEnvelopeCapabilityRecorded, GatewayDesignatedForTransportBoundary. One newly lowered from likely to doubtful: LayeringSemanticsPreservedAcrossBoundary — once preserved_layers went as a paraphrase of its own name and gateway_host as a duplicate of the crossing already recorded, what is left is a non-change during a crossing another card owns, resting on a design instruction to implementers ("SHOULD attempt to preserve"). That is the closest thing in this chapter to a field wearing an event's name. Kept, not dropped; deciding is a later step's job. Its Appendix E contradiction with §3.7.1 is real and survives on its own.

VERIFIED BY OPENING /home/ivan/FnEmail-Model/docs/rfc/rfc5321.txt, not from memory: 3311-3312 Stamp grammar, 3317 From-domain, 3319 By-domain, 3329 Opt-info, 3332 Via, 3336-3337 ID, 3350-3357 Link/Addtl-Link plus the unregistered-names SHOULD NOT, 1533-1536, 1549-1552, 1557-1563, 1577-1592, 1606-1616, 1687-1730 (§3.9), 1944 and 1950 (source-route example), 3156 (§4.4 heading), 3262-3273, 3466 (X.400), 4074 (§6.4 heading), 4135, 4773-4786, 4886-4900 (Appendix D, <Green@foo.com> at 4897), 5128-5139 (Appendix E). Walk scene values checked against /home/ivan/FnEmail-Model/docs/paths/helo-direct-single-recipient.md.

US SPELLING: ran the AGENTS.md rule 1 pattern over every note in the chapter. Three hits, all in inherited prose, all "licence"/"licences" used as a verb, in HeaderFieldRewrittenAtGateway and NonconformingHeaderAddressForwardedIntoInternet. Corrected to license/licenses and disclosed in both records — not quotations, so rule 2's exception does not apply. My added prose passes the pattern clean.

---

### 10. Alias and List Expansion

32 records in, 32 out — nothing dropped, renamed or merged. Fields before: 98. Struck: 17. Surviving: 81. One retype (MessageModifiedByList.modification, a fabricated four-member enum, to Text). One event became fieldless: EmptyBccHeaderFieldInserted. One event newly marked doubtful: ExpansionDeferredUntilAfterAcceptance; five were already doubtful and stay so.

WHAT WAS STRUCK, BY REASON. Invented identity keys (1): BlindCopyRecipientAddressedInEnvelopeOnly.transaction_id — grep -niE "session (id|identifier)|transaction (id|identifier)|connection (id|identifier)" over docs/rfc/rfc5321.txt returns nothing, so the spec names no such key; what identifies that transaction is its position in the session (opened by MAIL, recipients accumulated under RCPT, ended at DATA or RSET), and the reverse_path kept on the event is the nearest handle the wire carries. Computed, derived or counterfactual values (10): ListReturnPathLeftUnchanged.expected_administrator; MessageCopyCreated.content_ref; ListTreatedAsTransitContinuation.queue_id; ListReclassifiedAsFullMua.administrator; DeliveryAcceptedByList.reverse_path; DuplicateRecipientEliminatedBySourceExpansion.retained_via; BlindCopySentAsSeparateTransaction.peer_host; BlindCopyRecipientDisclosed.disclosing_field; and the two reply_codes on ErrorMessageReturnedToListAdministrator and ErrorMessageReturnedToOriginator. Constants that restate the event name or the record's own actor (4): BlindCopyRecipientCopiedFromBccHeaderField.source_header_field, EmptyBccHeaderFieldInserted.inserted_field, EmptyBccHeaderFieldInserted.submission_host, ExpansionDeferredUntilAfterAcceptance.deferred. Occurrence timestamps carried as payload (2): ListMemberAdded.added_at and ListMemberRemoved.removed_at, both instantiated with Tue, 19 May 1998 09:14:07 -0700 — the walk scene's message-acceptance time transplanted onto a membership change it has nothing to do with. RFC 5321's own 1998 dates are Thu, 21 May 1998 (lines 5014, 5064, 5066).

f2C8D14: 14 instances in this chapter. Three struck — MessageCopyCreated.content_ref (a content reference is not a queue id; the walk scene's own content_ref is blob:sha256:9c1e…, so the value was fabricated by transplant), ListTreatedAsTransitContinuation.queue_id, BlindCopyRecipientAddressedInEnvelopeOnly.transaction_id. Eleven kept as genuine queue ids on the §4.4 ID-clause warrant, each now sourced "illustrative — no RFC source; this project's own minted value". The warrant is narrow and worth stating: Opt-info = [Via] [With] [ID] [For] (line 3329) with ID = CFWS "ID" FWS ( Atom / msg-id ) (line 3336) is the only identifier-shaped thing RFC 5321 supplies for a message at a receiving host, it is optional, and the expander in this chapter IS that host — the RFC's own "recipient mailer" (line 1712). It does not reach events at the submission or originating end, where nothing has been accepted anywhere yet and no ID has been stamped; that is why the blind-copy and source-expansion events now carry no identifier at all. RFC 5321 does name the mail queue entry (§4.5.4.1, line 3692: "A mail queue entry will include not only the message itself but also the envelope information") but supplies no identifier for it, which is why a41F09B on MessageCopyCreated.copy_queue_id and MessagePostedByList.new_queue_id is labeled as minted too.

message_id and message_ref: neither appeared in this chapter, so nothing was struck on that count. Tested anyway against the spec, because the answer matters for the whole sweep: §4.4's ID clause is real and optional and takes an Atom or an RFC 5322 msg-id, and the only prose about it (line 3170) says only that it "MAY contain an "@" as suggested in RFC 822, but this is not required". That licenses a per-hop identifier stamped by the receiving host into a trace header — it does not supply a message identifier field on every event, and nothing else in RFC 5321 does either. §4.4 line 4131 lists "Addition of a message-id field when none appears" as a gateway act, which is RFC 5322's field, not SMTP's.

NEWLY DOUBTFUL — ExpansionDeferredUntilAfterAcceptance. Once `deferred: true` goes, the record is a host and a name, and what it was recording is a Boolean setting on a server's configuration — a field wearing an event's name, which is exactly the collapse the instruction describes. Kept, not dropped, and the counter-argument is preserved in its notes: §4.5.3.2.3 lines 3635-3636 is the only place in RFC 5321 that fixes when expansion happens, and that timing decides whether every other event in this chapter is visible to the client inside the session or invisible outside it. Deciding is a later step's job. No other justification collapsed: the four other single-field survivors (ListTreatedAsTransitContinuation, ListReclassifiedAsFullMua, DuplicateRecipientEliminatedBySourceExpansion, and near-fieldless EmptyBccHeaderFieldInserted) each still rest on a cited sentence, and three of them were already doubtful for reasons the earlier passes recorded.

WHAT WAS NOT STRUCK, DELIBERATELY. previous_reverse_path on ReturnPathRewrittenToListAdministrator and reverse_path on ListReturnPathLeftUnchanged both survive the derived-value test: the first is the value this event destroys — after the rewrite the originator's address survives nowhere in the envelope — and the second is the value whose survival IS the event. The line applied throughout: a value the event changes or destroys stays; a value merely carried along unchanged and already recorded on an earlier event goes. RecipientSuppressedByHeuristic.rule keeps its open enum because the RFC's own wording is open — "heuristics or other matching rules" (1704-1705) — with only originator_match traceable to text ("such as that of the originator", 1705-1706); the other members are labeled illustrative in the field's source. MessageModifiedByList.modification was retyped rather than struck: none of subject_tag, footer_added, header_added or body_rewritten appears in RFC 5321, which says only "additional, sometimes extensive, modifications to a message and its envelope" (1743-1744) and enumerates nothing, but the field is the one fact the event turns on and the classification consequence at 1744-1746 depends on it.

CITATIONS RE-VERIFIED BY OPENING THE FILE, per rule 3: §3.9 heading 1687, §3.9.1 1710, §3.9.2 1718, and every quoted line 1689-1746; §3.5.1 1272-1279 and the EXPN example at 1315-1318; §3.5.4 heading 1407 and 1409-1417; §4.4 3329, 3336, 3170; §4.5.3.2.3 heading 3633 and 3635-3636; §4.5.4.1 3692-3693; §4.5.5 3801 ("the postmaster alias"); §6.1 3970-3973; §7.2 heading 4212 and 4214-4235; Appendix B 4737-4745; Appendix D.1 4881-4899; Appendix D.5 5114. All held. Values confirmed present in the RFC: Smith@bar.com 4893, Jones@foo.com 4895, Green@foo.com 4897 with its 550 at 4898, Brown@foo.com 4899, foo.com and bar.com 4881-4882, Admin.MRC@foo.com 5114, Example-People 1315, Postel@isi.edu 1316, Fred Fonebone / Fonebone@physics.foo-u.edu 1317, SQSmith@specific.generic.com 1318, "Apparently-to" 4233. Values confirmed ABSENT and now labeled illustrative rather than passed off as sourced: staff@foo.com, owner-staff@foo.com, f2C8D14, a41F09B — grep for "staff" and for "owner-" over rfc5321.txt returns nothing, and the RFC gives no example of a list address or of a list administrator's address anywhere.

---

### 12. Non-Delivery Notification

SCOPE: all 26 records whose chapter is exactly "12. Non-Delivery Notification". None dropped, none renamed, none merged. Fields and notes only; meaning, rfc_refs, actor and stream_root untouched. One confidence change, declared below.

COUNTS: 80 fields in, 48 struck, 32 surviving. 7 events became fieldless (up from 0). 25 of the 48 strikes were the fabricated identity key in one of its five names — message_ref, subject_message_ref, notification_ref, subject_notification_ref, failed_notification_ref — carrying f2C8D14 (15 records) or f2C8D14-ndn1 (10). Zero f2C8D14 values survive: no event in this chapter is about stamping a trace line, so no queue id is the right field anywhere here.

WHY THE IDENTITY KEYS WENT, verified not assumed. grep -niE "session (id|identifier)|transaction (id|identifier)|connection (id|identifier)" over docs/rfc/rfc5321.txt returns nothing; grep -n "f2C8D14" returns nothing — it is this project's own minted queue_id from docs/paths/WORKING-helo-direct-single-recipient-v2.md (Received: id f2C8D14). Testing message_id / message_ref against the source as instructed: the only candidate RFC 5321 offers is §4.4's OPTIONAL ID clause — Opt-info = [Via] [With] [ID] [For] at rfc5321.txt:3329, ID = CFWS "ID" FWS ( Atom / msg-id ) at rfc5321.txt:3336 — and it fails this chapter twice over. It is optional, and it is a value the receiving host stamps into its OWN Received: line, so it is host-local and invisible to the party a notification is sent to. A notification by definition travels to a host that never held that queue id. So RFC 5321 supplies this chapter with no cross-host message identity at all. What the spec actually supplies instead, and what each record now names in its notes: the reverse-path (§6.1 3973-3974 makes it the notification's recipient; §4.5.5 3793-3795 makes it the link back to the previous message), the forward-path whose delivery failed, and — for the Server Configuration events — the site or host itself. Several events end with no handle whatever: NotificationSuppressedForNullReturnPath, MessageSilentlyDropped, AcceptedMessageLost, HostileContentDetected. That is the finding, not a gap to be filled.

THE OTHER 23 STRIKES, by kind. Nine host-named fields (determining_host, notifying_host, suppressing_host, recorded_by, sending_host ×2, determined_by, dropping_host, custodian_host) struck as restatements of the event-level actor field. Five constants struck because their only possible value is the one the event's own name already states — return_path <>, rejection_reason "hostile content", original_reverse_path <>, observed_reverse_path <>, reverse_path <>. Two event-occurrence timestamps struck (determined_at, sent_at); an event's own time is not a payload field, and both had borrowed the walk scene's receipt timestamp. Seven computed, derived or rendered values struck under the method's own rule: delivered_recipients (the accepted set minus the failed set), mail_command "MAIL FROM:<>" (the wire rendering of the state change; the RFC prints it at rfc5321.txt:1522 and the citation is retained), stripped_return_path (a deterministic function of original_return_path per rfc5321.txt:3987-3989, and already carried as notification_recipient on another event), confidence_level and certainty (the RFC's stated thresholds at 4042 and 4054 restated as instance data — no scale, no test, same value on every conforming instance), and reply_sent_to (unsourceable: §4.5.5 3815-3818 governs whether such a system replies, never to whom).

REPLY CODES: exactly one appeared in the chapter — failure_reason "550 no such user" on NotificationDeliveryFailed. Struck. Nothing in §4.5.5 carries a reply code as data beyond the wire; the only 550 the RFC prints nearby is a RCPT reply in Appendix D (rfc5321.txt:4898), a fact about that exchange, not about this event. No count, total or status field survived the same test.

TWO EVENTS HAD NOTHING TO STRIKE: InvalidReturnAddressDropPolicyAdopted and PostmasterRedirectConfigured. Both are Server Configuration stream roots where the site/host is the stream's identity rather than the actor restated, and both were built after the re-sweep rather than by the padded sweeps. certainty_threshold survives on the first for the same reason the identical value was struck elsewhere: the SHOULD at rfc5321.txt:4052-4055 attaches to the policy's application, and a threshold is a property of an adopted policy, not of an instance.

EXAMPLE VALUES CORRECTED, not merely sourced. Eleven records had the walk scene backwards — <Smith@bar.com> is the SENDER and <Jones@foo.com> the RECIPIENT, in both the walked path and Appendix D (rfc5321.txt:4893, 4895), and bar.com is the client while foo.com is the receiving server (rfc5321.txt:4886-4887). Bounces addressed to <Jones@foo.com>, forward paths holding <Smith@bar.com>, a receiving site called bar.com and notifying hosts called mx.bar.com were all pointing the wrong way. Setting example_source honestly is what exposed it: a value cannot be sourced to the walk scene while contradicting it. Also corrected: failure_reason and loss_cause now take the RFC's own words (rfc5321.txt:4003, 3966-3967) instead of paraphrases that added machinery the RFC does not mention; original_return_path regains the angle brackets the RFC prints at rfc5321.txt:3994, since in SMTP the brackets are path syntax; failed_recipients uses <Brown@foo.com>, accepted at RCPT in Appendix D (rfc5321.txt:4899-4900) and therefore available to fail AFTER acceptance, where Green@foo.com is refused at RCPT (4897-4898) and would be the wrong example. Four surviving fields are labeled "illustrative — no RFC source" outright — confidence_basis, evidence, fault_tolerated, substituted_reverse_path — each with the nearest RFC line named so a facilitator can judge whether to cut it.

ONE CONFIDENCE LOWERED: MessageJudgedSeriouslyFraudulent, certain → doubtful. Once its fabricated message_ref and its restated threshold went, the entire record is one string sourced to rfc5321.txt:4042-4043 — the same string, from the same line, as drop_reason on MessageSilentlyDropped, which the RFC states in the same sentence as the drop it gates. That is a field wearing an event's name. Kept, not dropped and not renamed; the counter-argument is recorded in its notes. The parallel case HostileContentDetected was considered and deliberately NOT lowered: it is fieldless for the same reason but the RFC names it in two separate sentences with different grammar (4057-4058 active, 4062-4063 passive) and it gates two distinct events, so its justification does not collapse. FaultyMessageDeliveredAnyway was already doubtful before this pass and stays so, for its own recorded reason, which nothing here touches.

NOTES PRESERVED: every inherited note is intact — the merge and split records, the rejected alternative names, the MUST/SHOULD/MAY strength readings, the hotspots, the two live contradictions, the ⚠️ blocks (the §7.8/§7.9 citation fault in the RFC itself, and the inherited §3.6.1 → §3.6.3 correction), and each record's "which screens would have killed it". This pass appends a delimited block and never edits or removes inherited text, including where it argues against a strike I made — those arguments are answered in place, not deleted.

---

### 13. Address Diagnostics

Chapter 13 held 32 records; all 32 are returned, none renamed, none merged, none dropped. 27 fields struck: 14 reply_code, 7 session_id, 2 disabled_by, and one each of apparent_validity_basis, checks_performed, example_accepted_string and extension_keyword. 98 fields in, 71 out.

Everything was verified against /home/ivan/FnEmail-Model/docs/rfc/rfc5321.txt in this pass, not assumed. A grep for a session, transaction or connection identifier returns nothing, so all 7 session_id fields went, and each record now says what carries identity instead — the channel while the session lives, the command's position in the sequence, and on the pre-EHLO events the client's IP address, which §4.1.4 lines 2498-2500 actually names as a basis for deciding about these very commands. A grep for f2C8D14 also returns nothing: 8 instances of it went, 7 as session_id values and one embedded inside NoopParameterActedUpon's parameter_string, which read "keepalive f2C8D14". That last one is the subtler half of the defect — a minted identifier smuggled into a free-text example, where a search on field names would never have found it. No queue id survived anywhere in this chapter and none should have: the §4.4 ID clause at line 3336 is a Received:-header clause on a delivered message, and Address Diagnostics has no message and no trace header.

message_id and message_ref: neither appeared in any of the 32 records, so nothing to strip, but the test was run against the source anyway rather than assumed from their absence. §4.4's Opt-info at line 3329 makes the ID clause real and optional, and line 3336 defines it as CFWS "ID" FWS ( Atom / msg-id ) — a clause in a trace header, not a field on an event. Nothing in RFC 5321 supplies a message identifier to a VRFY, EXPN, HELP or NOOP exchange, which never carry a message at all.

All 14 reply_code fields went under the method's bar on computed and derived values: a reply code is the rendering of a decision. The exemption was tested rather than waived — RFC 5321 nowhere carries a reply code as data beyond the wire. §4.2.5 lines 2941-2965 is the closest the spec comes and it assigns delivery responsibility rather than persisting a code; §4.4's trace header has no slot for one. Three of those strikes cost real information, and each is recorded on its own record instead of smoothed over. CrossTypeVerificationRefused can no longer distinguish its 550 (wrong kind) from its 252 (cannot see inside), two refusals that mean different things. VerificationRefusedAsUnimplemented loses the 500-versus-502 distinction its whole non-compliance finding turns on. HelpRequestRefused loses the only thing separating a 502 that deviates from the SHOULD at 2161 from a 504 that the MAY at 2161-2162 fully permits. All three are splitting questions for a later step, and every code survives in the meanings and the rfc_refs. Two strikes were near-misses in the other direction: §7.3 line 4244 makes 252 the subject of a MUST, and §4.1.4 lines 2492-2493 names 503 inside the prohibition itself — both still go, because being named in a normative sentence is not the same as being carried as data.

Four more went as padding or derivation, on the method's own rules and not on any project screen. apparent_validity_basis: the RFC's own words at 1386-1389 make it the outcome of checks the server ran, which is a derivation, so a read model's business. checks_performed: restated the event's own name. example_accepted_string: a field whose content was an illustration of another field. extension_keyword: a value fixed by the event's name, so it could carry no information — and note the same test does not touch the command fields that genuinely range over VRFY and EXPN. Both disabled_by fields went because RFC 5321 records no administrator identity for any configuration change and the actor field already carried the role; the value <postmaster@foo.com> looked sourceable, since §4.5.1 lines 3394-3396 do require the reserved mailbox, but requiring a mailbox is not recording who operated a switch. That near-miss and the example_accepted_string one are the clearest demonstrations in this chapter that sourcing a value does not save a field.

One value was corrected rather than struck, on ServerIdentityDisclosurePolicySet: greeting_text carried the reply code as a prefix, "220 foo.com Simple Mail Transfer Service Ready". The 220 is wire rendering, not configured text, and the repository's own convention already separates what the protocol fixes from what the operator varies, so the value is trimmed and the RFC's full line stays quoted at 4886.

ONE EVENT BECAME FIELDLESS: NoopAcknowledged, whose only field was the fabricated session_id. That is the correct outcome and the record says so at length — its own inherited note had already observed there is nothing else true about it, and the field had been standing in for a key the RFC does not define. One out of 32 is still a low proportion; the reason is now visible in the sourcing rather than assumed. This chapter is mostly disclosures, and a disclosure has genuine content — the mailboxes and names that crossed the boundary, quoted straight from the RFC's worked replies at 1249-1251, 1316-1318 and 5113-5114. Seven records now stand on a single field.

NO EVENT'S JUSTIFICATION COLLAPSED once its fabricated fields were gone, so no confidence value was changed by this pass. Four records were already doubtful and are now doubtful for better-stated reasons: HelpRequestRefused (its distinguishing content was the struck reply code, and the record now carries the two-arm split only in prose), ExpnServiceExtensionRegistered (down to one field, the host that acquired an obligation, which is precisely the question a later step must rule on), NoopParameterActedUpon (its one field is a string the spec both admits in the grammar at 2203 and tells servers to discard, with no example anywhere), and FreeFormVerificationTextReturned (its one substantive field is licensed by the RFC and given no example by it). Two records survive on one field each and were deliberately left at likely because the strikes did not touch their justification: AddressVerificationDeclinedAsUnverifiable and DiagnosticCommandRejectedAsOutOfSequence.

server_host, expanding_host and advertising_host were tested and kept: sourceable, not derived, not invented identity keys. On the six Server Configuration and List records they are the stream's own identity. On the four runtime reply events they repeat what the actor field already says, and each is flagged on its record as possible padding — kept because striking them is a modeling judgment, not field hygiene, and this pass is not the step that makes it. requesting_client and client_address were tested hardest, since an IP address is the obvious place to hide a substitute key, and kept: §4.4 lines 3325-3327 make TCP-derived information a fact the server holds, and §4.1.4 lines 2496-2500 make the client's IP one of only two things that can make ExpnAccessDenied legitimate at all.

example_source is set on every one of the 71 surviving fields: 43 cite an RFC line, 19 are the walk scene's, and 9 say illustrative — no RFC source, plainly. Six of the nine name the RFC line that licenses the field even though the spec supplies no value for it: HELP output twice, free-form verification text, a NOOP parameter, and the two permitted-requestor sets. The constructed <Crispin@foo.com> on VerificationFalselyClaimed is labeled as assembled from D.4 line 5113 and D.1 line 4886 rather than quoted, because a plausible-looking address is exactly the kind of value that passes for sourced when it is not.

---

### 15. Server Configuration and Provisioning

Chapter 15, all 40 records returned; none dropped, renamed or merged. Fields went from 121 to 80: 41 struck. One event became fieldless — RelayFilteringCapabilityImplemented. No event was marked doubtful for a collapsed justification; PostmasterMailBlockLifted was already doubtful on the RFC's silence about lifting a block, and that rating is unchanged and unrelated to fields.

Invented identity keys: this chapter carried NONE of the three. Grep over the 40 records found no session_id, transaction_id, connection_id, message_id, message_ref, or f2C8D14 — expected for a chapter of standing configuration, where every event precedes the sessions it governs. Requirement 1 therefore struck nothing, but the underlying finding is recorded per event: RFC 5321 supplies no session, transaction or connection identifier, and what carries identity here is the configured host's domain (the value the greeting, the 220/221/421 lines and HELO all carry), the domain pair for an agreement or a service relationship, or the registry key for the four registry events. Requirement 3 was tested rather than assumed: §4.4's Opt-info at line 3329 does supply an optional ID clause and §4.4 3170 permits an "@" in it, but that is a clause inside one Received field, not a message identifier available to hang on events — recorded on TraceHeaderFieldRegistered and TransmissionTypeRegistered, the two events where a reader would look for it. No queue id was genuinely in play anywhere in the chapter, so no f2C8D14 survives under requirement 2 either.

What the 41 strikes were, by kind. Constants that restated the event name or the citation, 20: local_name and case_insensitive (PostmasterMailboxProvisioned), accepted_form (DomainlessPostmasterSupportProvisioned), commands (MinimumCommandSetProvisioned), reply_code and scope (ConnectionRefusalConfigured), reason (SoftwareVersionAnnouncementDisabled), reason on both VrfySupportDisabled and ExpnSupportDisabled, ehlo_keyword (ExpnSupportEnabled), permitted_requestors (VerificationRestrictedToAuthenticatedRequestors), filtering_basis (RelayFilteringCapabilityImplemented), identification_basis (RelaySourceAuthorized), ehlo_keyword (SizeServiceExtensionImplemented), required_minimum (NonConformingRecipientLimitConfigured), recompile_required (TimeoutValuesReconfigured), per_command_timeouts and error_on_error_suppressed (QueuingStrategyAdopted), port (SmtpListenerStarted), registry on all three registry events counts as three more. Computed or derived values, 6: reply_given_instead (RequiredCommandLeftUnimplemented), example_accepted_string (UserNameRecognitionRuleConfigured), relaying_permitted (RelayPolicyConfigured — the record's own notes already proved it was relayed_for_domains being empty), overflow_reply_code (RecipientLimitConfigured — §4.5.3.1.10 3598-3603 derives the code from limit_kind), added_mail_or_rcpt_parameters and command_length_increment (ServiceExtensionRegistered — both derivable from the defining document, so read-model material). Reply codes that are the rendering of a runtime decision, 2: policy_reply_code (MailAcceptancePolicyConfigured — the 550 SHOULD at 4379-4381 attaches to a reply sent in response to EHLO/MAIL/RCPT) and reply_given_instead above. Unsourceable values, 4: declared_maximum_octets and ehlo_param (10485760 is RFC 1870's business, not RFC 5321's, and both duplicated SizeLimitConfigured's limit), bind_address (nothing in RFC 5321 speaks of binding a listener to an address), changed_by (no author is recorded anywhere for a text substitution). Event metadata masquerading as payload, 3 timestamps: disabled_at, started_at, enabled_at. Duplicates of the record's own actor, 2: disabled_by on the VRFY and EXPN records. Wrong subject, 2: server_host on RelayFilteringCapabilityImplemented (the SHOULD at 4378 is laid on "implementations", and RFC 5321 supplies no identifier for an implementation — the nearest thing is §3.1 964-966, which lets a server announce software and version information without specifying any form) and listed_in_ehlo on BilateralAgreementEstablished (the MAY at 2540-2541 addresses "An extended SMTP server", not the two agreeing operators).

Three reply codes survived, each with the line that makes it data rather than a rendering. DisabledVerificationReplyCodeConfigured keeps reply_code on §7.3 4243-4245 (rfc5321.txt:4244): a MUST on which code a disabling site returns, with the RFC enumerating four values a site can actually hold — 252 conforming, 500/502 at 1380-1381, blanket 550 at 4249-4251, bare 250 at 4247-4248 all named non-conforming — so it is a stored setting that genuinely varies. ReplyTextCustomized keeps reply_code on §4.2 2565-2567 ("varying texts for each reply code") with §4.3.1 3022-3033 holding the code invariant while the text moves: the code is the key the stored text hangs on. AddressUpdateDisclosureRestricted keeps reply_codes_restricted on §3.4 1208-1211, where 251 "and/or" 551 are the subject of a configuration mechanism, so the set is the setting's key. Every other code in the chapter went.

One field was renamed rather than struck: TimeoutValuesReconfigured's timeout_seconds became timeout_minutes with example 10, because RFC 5321 states every per-command minimum in minutes — the §4.5.3.2.6 heading at rfc5321.txt:3656 reads "DATA Termination: 10 Minutes." — so 600 was a unit conversion of the spec's own number and unsourceable as written.

Every surviving field now carries example_source. Sourcing broke down as: 31 walk-scene values (foo.com, bar.com, 192.0.2.10, 203.0.113.20), 25 traced to a verified RFC line, 8 labeled "illustrative — no RFC source" with the reason stated. Each of those eight was reconsidered under requirement 5 and kept only where the field itself is RFC-grounded even though no value is: max_recipients on both recipient-limit events (§4.5.3.1.8 gives only the floor of 100 at line 3537), max_concurrent_connections and max_concurrent_transactions (both real MAYs at 3779 and 3770-3771, neither with a number), notification_max_retries (§4.5.4.1 3710-3713 permits a shorter budget and states none), the program-delivery mailbox and the trace field name (the spec requires such names to exist without naming one), and command_verb on BilateralAgreementEstablished (the X prefix without an example). That last group is the honest residue: the RFC insists on a value's existence and declines to supply one, which is a finding rather than a defect. Where a value could not be sourced AND the field was derivable or duplicated, the field went instead — that is what removed declared_maximum_octets, ehlo_param, command_length_increment and bind_address.

Rule 3 was applied throughout: every line number cited as an example_source was opened in /home/ivan/FnEmail-Model/docs/rfc/rfc5321.txt and confirmed by reading the line, including 3395, 3401, 3406, 3392, 3208, 1308, 1355, 1208, 1428-1429, 2565-2567, 4886, 4244, 4239-4240, 4270-4271, 4358, 3498, 3500, 3537, 3548, 3601, 3524, 3656, 3705, 3710, 3777, 3367, 4403, 4409, 4583-4584, 4889. Line 3329 (Opt-info) and 3170 (the ID clause "@") were opened for requirement 3. No screen was used to remove anything: the altitude tiers, the consumer test, the status/phase register, the capture/decision lens and the HELO-only charter all remain recorded in the notes as the sweeps left them, and where a strike could look like a screen — bind_address and port on SmtpListenerStarted, which the altitude tiers would also delete — the note says explicitly that the ground is source and constancy, not the tier.

---

### 8. Relaying

SCOPE: all 40 records whose chapter is "8. Relaying". None dropped, renamed or merged; fields and notes only, plus one confidence change under the collapse rule.

COUNTS: 126 fields in, 55 kept, 71 struck. Four events are now fieldless: SmtpClientRoleAssumed, NullReversePathSet, MessageLoopDetected, MessageContentPassedThroughUnmodified. One event was marked doubtful because its justification collapsed: RelayDeclinedWithNonStandardCode - everything that distinguished it from RelayTaskDeclined lived in the reply_code value, so once the wire rendering went it was a field wearing an event's name. Kept with a note saying exactly that; deciding is a later step's job. Four events were already doubtful and stay so (MessageContentInspectedByRelay, MessageDataModifiedByRelay, ReversePathResolvedAtDelivery, RelayUsedToObscureOrigin); none of them collapsed - their doubt is about queries-versus-events and violation-versus-conformance, which stripping fields neither creates nor cures.

WHAT WAS STRUCK, BY CLASS. (1) 26 fields carrying the recording system's own domain - relay_host x13, server_host x3, notifying_host x3, host, suppressing_host, emitting_host, accepting_host, refusing_host, checking_host, site_host. All valued mx.bar.com or similar, which appears nowhere in rfc5321.txt. The one host kept is abused_relay_host on RelayUsedToObscureOrigin, where the relay is the counterparty and the hostile client is the actor. (2) 16 "<verb>_at" timestamps that restate the event's own occurrence time as payload. The single timestamp kept is stamped_at on ReceivedHeaderFieldAddedByRelay, because 4.4's Stamp grammar puts a date-time on the wire (lines 3311-3313) with its own rules at 3186-3188 - data, not the event's clock. The project's own walked path agrees: exactly one event there carries a timestamp, and only because it feeds the trace line. (3) 4 reply_code fields, all of them. RFC 5321 carries no reply code as data beyond the wire; 3.6.2 line 1463, 7.9 lines 4380-4381 and 4.1.1.3 line 1975 all govern what is SENT. SourceRoutedAddressRefused's 550 was invented twice over - the inherited note had already flagged that 3.6.1 specifies no code for that refusal. (4) 3 of the 7 f2C8D14 queue_ids. Kept on the four Queue-Entry-rooted events, where 4.4's optional ID clause makes an id genuinely real (Opt-info = [Via] [With] [ID] [For], line 3329; ID = CFWS "ID" FWS ( Atom / msg-id ), line 3336) and this project already maps ID to queue_id at docs/paths/helo-direct-single-recipient.md:179. Struck from SmtpClientRoleAssumed (no queue exists at the role change), MessageContentPassedThroughUnmodified and MessageRepairRefusedByRelay (both Message-rooted). Worth recording: the RFC's own relay trace line at 5064 carries FROM, BY and a date-time and no ID clause, so the spec never instantiates a queue id anywhere. (5) 8 derived readings of a path that is already recorded - relay_hops, ignored_hops, stripped_hops, final_destination x3, unresolvable_hop, plus one duplicative offered_path. All recoverable from the forward-path under the 4.1.2 grammar (Path at 2264, A-d-l at 2266). This corrects the inherited claim on SourceRouteStripped that "the discarded hops vanish from the record unless this event keeps them" - they do not. (6) 3 next_hop_ip fields: DNS lookup results, which 3.6.2 line 1453 defers to Section 5, and the value 203.0.113.20 was the walk scene's client address applied to a server. (7) received_field_count and repeated_host on MessageLoopDetected - 6.3 lines 4067-4070 call it "Simple counting of the number of 'Received:' header fields", a fold over the trace, which is a read model by the method's own rule; this overturns the inherited defense of it as an observed fact. (8) 2 invariant-value fields that were the event rather than a field: mail_command on NullReversePathSet (MAIL FROM:<> can take no other value) and added_header_field on MessageContentPassedThroughUnmodified (the carve-out at 1526 permits exactly one addition). (9) 7 remaining padding fields: reason on RelayTaskDeclined (tautological - the section's condition IS "for policy reasons"), peer_address and claimed_domain x2, client_ip, actual_origin (unfillable by construction - it is the thing being hidden), and reverse_path on MessageSubmittedForDistribution.

WHAT THE AUDIT'S TABLE DID NOT PREDICT FOR THIS CHAPTER. Zero session_id, transaction_id, connection_id, message_id or message_ref fields existed here to strike - I checked the whole chapter, not just the field names. The chapter's share of the fabrication was concentrated instead in two habits the audit did not name: the recorder's own host on 26 fields and an event-time timestamp on 16. Those two alone are 42 of the 71 strikes, against 4 reply codes and 3 queue ids. On rule 3's own logic the absence of an identity key is confirmed by the file: grep for session/transaction/connection identifier over rfc5321.txt returns nothing, so identity in this chapter is carried by position in the session, by the channel itself, by the forward-path, or by the queue id where the 4.4 ID clause applies - and each event's note now says which.

EXAMPLE VALUES RE-BASED, WHICH IS THE PASS'S BIGGEST NON-STRIKE CHANGE. The sweep's mx.bar.com, mail.bar.com and relay.foo.com appear nowhere in the RFC and nowhere in this repo. RFC 5321 has its own worked relaying scene - Appendix D.3, "Relayed Mail Scenario", heading verified at line 4991 - with a source host (bar.com, 5002), a relay (foo.com, 5001), a destination (xyz.com, 4996), a failed onward connection (4998), a successful one (5052), the envelope <JQP@bar.com> / <Jones@XYZ.COM> on both legs (5008/5010 and 5058/5060), and an actual relay trace line at 5064-5065. Every relay-leg field now cites it by line. Source routes are the one subject D.3 does not walk, so those fields use 4.1.1.3's worked example instead (the routed path at 1944, the four legal outcomes at 1946-1975). Case is copied exactly per rule 2 - the RFC writes XYZ.COM uppercase at 5010 and 5060.

THREE VALUE ERRORS FOUND WHILE SOURCING. (a) The sweep used 192.0.2.10 - the walk scene's own server address - for a client being authorized to relay and for a submitting client's IP; the walk scene's client is 203.0.113.20. (b) It used 203.0.113.20, the walk scene's client, as a next-hop server's IP. (c) It inverted the walk scene's envelope, putting <Jones@foo.com> in the reverse-path and <Smith@bar.com> in the forward-path, where both the walk (docs/paths/helo-direct-single-recipient.md:115) and RFC Appendix D (4893, 4895) have Smith@bar.com sending to Jones@foo.com. All three are moot now that the chapter uses D.3, but they are the same defect as f2C8D14: a value that was never checked against anything. Two examples turned out to be sourceable after all and are now cited rather than invented - Subject: at 5016 and 5068, and "message-id addition" at 4131, where the RFC's message-id is a header field an originating server may add and a relay MUST NOT, not an identity key on an event.

ONE COST ACCEPTED, RECORDED RATHER THAN HIDDEN. The full inherited notes plus my additions exceeded the output limit twice, so the inherited notes are condensed digests, not verbatim. Everything the brief named as must-preserve is retained per record: rejected alternative names, MUST/SHOULD/MAY/MUST NOT strengths with their line numbers and the lowercase-must and page-break traps, the hotspots, the re-sweep provenance markers and sweep numbers, the cross-references, and the existing warning blocks. What was cut is repetition and the screens lists, which are the shortest and most recoverable part. The originals are intact at the merged-events.json path if a later step wants them restored.

---

### 14. Protocol Fault Handling

All 40 chapter-14 records returned; none dropped, renamed or merged. 74 fields struck, 143 down to 69. 1 event became fieldless (ReplyIssued). 0 events marked doubtful by collapse.

WHAT I VERIFIED BEFORE STRIKING (rule 3, over /home/ivan/FnEmail-Model/docs/rfc/rfc5321.txt). The three identity-key greps return nothing: RFC 5321 supplies no session, transaction or connection identifier. f2C8D14 returns nothing. Section 4.4's ID clause is real at line 3336 — `ID = CFWS "ID" FWS ( Atom / msg-id )` with the comment "msg-id is defined in RFC 5322 [4]" — but it is optional, lives inside a trace header, and admits an Atom (a queue id) as readily as a msg-id, so it is not a key on the message. Line 3325's TCP-info comment, "Information derived by server from TCP connection", and line 3319's By-domain clause are the RFC's own places for channel identity and acting-host identity, and they are what I cited when saying what carries identity instead.

STRIKES BY KIND. session_id: 30 (every occurrence in the chapter). Computed/derived: severity_digit (the first character of received_code), line_count, commands_completed, commands_already_accepted, accepted_recipient_count, recipients_accepted x3, treated_as_code, reply_code_on_use, terminated_at. Configured ceilings copied onto runtime events: limit_octets x4, site_policy_max_recipients — all now recorded once on SizeLimitConfigured / RecipientLimitConfigured. Denormalized copies: client_identity (the session's greeting claim), changed_by (duplicated `actor`). message_id as a key: 4 struck (MessageRepairRefusedByRelay, RepairDocumentedInTraceHeader, MalformedMessagePassedOnUnchanged — plus none on MalformedMessageRejected, which carried session_id instead); kept only on MessageIdAdded, where line 4131, "Addition of a message-id field when none appears", makes it the added VALUE rather than a key. No message_ref appeared in this chapter.

REPLY CODES — the test I applied and where it landed. Lines 2557-2559 are decisive: "The number is for use by automata to determine what state to enter next; the text is for the human user." So every reply_text and failure_text is struck as not-data by the RFC's own classification, and on the EMITTING side a code is the server's decision rendered. 11 reply codes struck; 11 kept, each with the line that makes it data: (1) client-side receipt, where line 2610 says "An SMTP client MUST determine its actions only by the reply code, not by the text" — MultilineReplyCompleted, BareReplyCodeTolerated (also 2612-2615), UnrecognizedReplyCodeInterpretedByFirstDigit (2624-2626), MailTransactionTerminatedOnInvalidReply (2637-2640), OverflowReplyReclassifiedAsTransient (3576-3578, the one place the RFC tells a client to disbelieve a code); (2) the violated invariant itself, where 2772-2774 licenses the client to rely on the per-line code — InconsistentMultilineReplyEmitted's two codes; (3) the code as the fault — NonStandardReplyCodeInvented; (4) a prior fact referred back to — DataCommandRefusedAfterRecipientOverflow, since 3605-3608 distinguishes a 503 "subsequent to the 452 (or 552) code" from a 503 sent "without returning any previous negative response"; (5) configuration set before any session — ReplyTextCustomized (3022-3026 keys the substituted text by code) and RecipientLimitConfigured (3601-3603 leaves which 5yz open, so the choice is configured). A code MANDATED for its condition — 452 at 3598-3600 — is a constant and therefore identity, not payload, so it went.

EXAMPLE VALUES CORRECTED, not just re-labeled. SMTPUTF8 appears nowhere in RFC 5321: replaced with SIZE (line 3524, advertised at 4890) on CommandRejectedAsUnimplemented and 8BITMIME (line 4889, the RFC's own Appendix D EHLO response) on UnimplementedCapabilityAdvertised. mail.foo.com and mx.bar.com appear nowhere: replaced with foo.com (4882) and bar.com (4881) across the six host fields. <f2C8D14@foo.com> replaced with a msg-id built from the Appendix D date at 5014 and foo.com at 4895, labeled illustrative because RFC 5321 gives no msg-id example. Codes 521, 601 and 559 are kept as illustrative ON PURPOSE and the example_source says so — grep confirms none appears in the RFC, and a code the document lists would not be an instance of those events.

ONLY ONE FIELDLESS EVENT, AND WHY THAT IS NOT ANOTHER 0-OF-567. ReplyIssued lost all four: the session key, the code and text as the decision rendered, and command_verb because section 4.3.1 lines 2993-2996 make the dialogue alternating, so the reply's subject is fixed by position. What is left is exactly the MUST at 2551 — one reply, and only one. The other 39 keep 1 to 4 fields because this chapter is mostly wire faults with real captured payloads: a refused command's verb or line is recorded nowhere else (the command was rejected, so no accepted-command event exists), a refused forward-path is recorded nowhere else, and a measured length is taken from an object the server discards unparsed. I drew that line consistently: measured magnitudes of the triggering object survive (line_octets, path_octets, message_octets), configured ceilings and folds over other events do not.

NOTHING COLLAPSED INTO DOUBTFUL. ConnectionClosedInViolation is still "doubtful", but that predates this pass and rests on the open question of whether a conformance observation is a domain event; its fields (peer_address, command_verb) survived and I said so in its notes. The closest thing to a collapse is MailParametersRejected: with reply_code gone, the 455/555 lifetime distinction has no carrier, because lines 2230-2237 make the code follow from the condition. That supersedes the inherited note's reasoning, and I NAMED a refusal_lifetime field in notes rather than adding one — inventing a field is what this pass exists to undo, and the choice belongs to a later step. Same shape on CommandRejectedAsUnimplemented, where the 502 and 504 arms are now told apart only by whether unimplemented_parameter is present.

Every existing note is preserved verbatim and mine is appended after a blank line, opening "FIELD HYGIENE PASS (chapter 14):". Where my strike contradicts an inherited claim I said so explicitly rather than quietly overriding it — three cases: accepted_recipient_count ("a captured fact, not a derived one" — it is a fold), limit_octets on OversizedCommandLineRejected ("a real per-server, per-session fact" — true, and still the configuration event's fact), and client_identity on MessageIdAdded (a precondition, not payload). US spelling gate run over the added text and the example_source strings: 0 hits.

---

### 16. Abuse, Security and Registry

SCOPE: all 40 records whose chapter is "16. Abuse, Security and Registry". None dropped, renamed or merged; only fields and notes changed, plus four example VALUES corrected where the old value was wrong rather than merely unsourced (see below).

COUNTS: 134 fields in, 88 out, 46 struck. Exactly one event became fieldless: VerificationAlwaysRefusedWith550. Zero events were marked doubtful by this pass; the three already doubtful (SpoofedOriginDetected, ServerTypeAndVersionDisclosed, ServerRenderedInaccessibleByAttack) stay doubtful for their original reasons.

WHAT WAS STRUCK, BY KIND.
(1) Fabricated identity keys, 8. Three session_id (HostileTrafficDetected, ConnectionClosedDefensively, RelayingDeclined), three TransactionId "transaction" (CorrectedDestinationDisclosed, RecipientRejectedWithForwardingAddress, MessageForwardedSilently), two MessageId (SpoofedOriginDetected.examined_message, InternalHostNamesDisclosedInTraceField.carrying_message). Verified, not assumed: grep -niE "session (id|identifier)|transaction (id|identifier)|connection (id|identifier)" over /home/ivan/FnEmail-Model/docs/rfc/rfc5321.txt returns nothing. Each of the eight notes now records what carries identity instead — the channel while the session lives, the command's position in the session, the forward path, the mailbox — and where a peer address survives it does so on a real basis: 4.4 lines 3325-3327, "Information derived by server from TCP connection / not client EHLO", which is the only peer identity the spec acknowledges.
(2) f2C8D14, 8 occurrences, all struck, none survived. grep -n "f2C8D14" over rfc5321.txt returns nothing; it is this project's own minted queue_id (commit 6acfc68, and live in docs/paths/helo-direct-single-recipient.md). No event in this chapter has a queue-id field, so the §4.4 ID-clause exemption never applied here.
(3) message_id / message_ref tested against the spec and failed. §4.4 line 3336 reads ID = CFWS "ID" FWS ( Atom / msg-id ) with the comment "msg-id is defined in RFC 5322 [4]" — an OPTIONAL clause inside a Received line, not an identifier RFC 5321 supplies to events, and the RFC's own trace line at D.3 line 5064 carries no ID clause at all. The only other hit, line 4131 "Addition of a message-id field when none appears", is an originating server completing a message HEADER, not an envelope fact. Both MessageId fields struck.
(4) Computed and derived, 12. Eleven reply_code / final_reply_code fields, each the rendering of a decision the event already names. ONE kept, with the line that makes it data: AddressUpdateDisclosureRestricted.reply_codes_restricted, on 3.4 lines 1208-1209 — "SMTP server implementations that support the 251 and/or 551 reply codes SHOULD provide configuration mechanisms so that sites ... can disable or restrict their use" — where the codes are the OBJECT of a standing configuration held outside any session, not the wire form of a decision just taken. Also struck HostileTrafficDetected.rejected_recipients: a list of recipients already rejected in this session is a fold over earlier events, a read model, exactly as the rejected_recipient_count it had replaced was.
(5) Invented facts and borrowed values, 1. ReturnPathAlterationRestricted.effective_from carried the walk scene's Tue, 19 May 1998 09:14:07 -0700 — a real value borrowed for a fact the RFC never records, since RFC 5321 dates no configuration change anywhere. The subtlest form of the defect: sourceable value, fabricated field.
(6) Actor and channel restatements, and constants fixed by the event's own name, 25. Host fields that merely repeated the actor; command and code fields whose value could never vary; context fields the RFC states as explanation rather than data (attack_purpose, originating_network, reason x2, approved_for, used_with, admitted_requestors).

EXAMPLE VALUES CORRECTED, not merely sourced — four, each recorded in its event's notes: PermanentMailboxAddressEstablishedForUser paired "John Q. Public" with <Jones@foo.com>, two values that cannot both be true of one person, now both from D.3 line 5067; AddressBookPoisonedByForgedReply carried <Smith@203.0.113.20>, not a legal mailbox — an IPv4 address-literal must be bracketed, 4.1.3 line 2387; AddressesHarvestedFromMailingList.harvested_addresses now carries what an EXPN of Example-People actually returns in this specification, lines 1316-1318; TraceClauseRegistered.registry read "mail parameters", which §4.4 line 3372 names for additional PROTOCOL names, where §8 lines 4419-4422 puts additional clauses in the third registry, "Mail Transmission Types" (4407), and its registered_by read "IETF standards action", a name the RFC does not use for either of the two routes at 4435-4437.

WHAT NO CANDIDATE'S JUSTIFICATION RESTED ON ITS FABRICATED FIELDS. None was demoted, and one deserves a word because the temptation was real: VerificationAlwaysRefusedWith550 lost all three fields and is now fieldless, but its warrant is 7.3 lines 4249-4251 — a named non-conformance — which never depended on them. Fieldlessness is not doubt, and treating it as doubt would reinstate the very padding pressure that produced zero fieldless events across 567. Its notes record the one change that would give it a field back: re-rooting it at Server Configuration, where the server would be its identity, since it is a standing implementation behavior rather than a session act.

TWO FINDINGS THE STRIPPING SURFACED, both now in the events' notes rather than here only. ServerTypeAndVersionDisclosed has NO field for the software and version themselves, because RFC 5321 never instantiates one — 3.1 lines 964-966 permit the announcement, 7.5 debates it, and every Appendix D greeting (4886, 5057) shows none; a payload field would have had to be invented outright, so none was added. ServiceExtensionRegistered keeps five fields against the one-or-two guidance, because §2.2.2 lines 538-569 enumerate seven mandatory parts of a registration — the one place in this chapter where the spec hands over the field list — and its two unsourceable values (BODY=8BITMIME, an octet increment) are marked illustrative rather than removed, since 567-569 requires an increment to be stated and RFC 5321 never states one.

NOTES HANDLING: every inherited note's substance is preserved — re-sweep and merge provenance, rejected alternative names, RFC 2119 strengths including the lowercase-should traps, hotspots, cross-references, citation traps and corrections, invented stream roots, and the which-screen-would-have-killed-it clauses — but condensed in wording to fit the output; the verbatim originals remain in /tmp/claude-1000/-home-ivan-FnEmail-Model/b2d6da7d-e340-4073-a737-43964dbc16c9/scratchpad/merged-events.json. Each note ends with a FIELD HYGIENE block stating what was struck and why. Every RFC section heading cited was confirmed by finding its heading line (7.1 at 4158 through 7.9 at 4355, 8 at 4383, 2.2.2 at 532, 3.1/3.3/3.4/3.5.1-3/3.6.1-2/3.8/4.1.1.3/4.1.5/4.4), per rule 3.

---

### dedup

ALL EIGHTEEN RESOLVED AS MERGES; NO RENAMES. 37 records collapse to 18 survivors, 19 absorbed. No event deleted. I applied the rename bias to every pair and none passed it: in each case both records cite the same anchor section for the same act with the same actor-role, and most say so themselves ("Carried over from idx N", "Inherited from candidate 93", "Harvested"). These are one event minted twice by independent chapter assemblers, not two events colliding on a name. The near-misses I checked and rejected: NextHopChannelEstablished (actor labels "sending SMTP system" vs "relay SMTP server" — but §3.6.2 1451-1454 is about a relay becoming a client, so it is one role, and one record's refs are a strict superset of the other's); DataTerminationTimeoutExpired (stream roots Session vs Transaction, identical refs); MailAcceptedDespiteMalformedTraceHeader (three records, Message vs Transaction vs Message, identical refs).

VERIFICATION. Every line number cited above was opened in /home/ivan/FnEmail-Model/docs/rfc/rfc5321.txt, not remembered. Confirmed in place: 973-976, 1080-1082, 1116-1117 with the page break resuming at 1127, 1171-1173, 1203-1211, 1303-1310, 1325-1330, 1430-1439, 1451-1454, 1506-1509, 1522, 1578-1589, 1923-1955, 2038-2042, 2266-2269, 2498-2511, 2563-2565, 2830-2831, 2959-2964, 3115, 3236-3294, 3325-3374, 3396-3398, 3466, 3537-3545, 3594-3608, 3620-3621, 3647-3669, 3787-3795, 3871-3896, 3962-3974, 4212-4226, 4373-4381, 4400-4442, 4529-4530, 4773-4777, 4879-4908, 4991-5075.

CROSS-CUTTING FINDING 1 — ROLE ERRORS IN WALK-SCENE VALUES, EIGHT INSTANCES. Verified ground truth: rfc5321.txt:4881-4886 and /home/ivan/FnEmail-Model/docs/paths/helo-direct-single-recipient.md:35-36 agree that foo.com is the RECEIVING SERVER (at 192.0.2.10:25) and bar.com the CLIENT (at 203.0.113.20); Smith@bar.com is the reverse-path (4893); Jones, Green and Brown @foo.com are the forward-paths (4895, 4897, 4899). Against that, the sweeps put <Smith@bar.com> in a forward-path field four times (RecipientRejectedWithoutAddressInformation, RecipientLimitEnforcedBySitePolicy, BlindCopySentAsSeparateTransaction, DeliveryResponsibilityDeclinedTemporarily), bar.com in a receiving-host field twice (MailAcceptedDespiteMalformedTraceHeader, AcceptedMessageLost), our own listen address 192.0.2.10 in an authorized-relay-CLIENT field once (RelaySourceAuthorized), and the client's address 203.0.113.20 in a next-hop-SERVER field once (NextHopChannelEstablished). All eight are corrected above with the source named. This is rule 8 earning its place a second time: a real value put in the wrong role is visible, and a placeholder would have hidden every one of them. Two of the records had already caught one instance each, in opposite directions, which is how the pattern surfaced.

CROSS-CUTTING FINDING 2 — RULE 2, UN-WRAPPED ABNF COMMENTS. RFC 5321's ABNF comments break mid-phrase, and a quote that reflows them is not the source's words. One record caught this in TraceClauseRegistered (§4.4 3341-3348) and corrected it. I found a second, uncaught instance in SourceRouteStripped: the §4.1.2 2266-2269 quote "this form, the so-called \"source route\", MUST BE accepted, SHOULD NOT be generated, and SHOULD be ignored" un-wraps three comment lines. Both merged records must carry the quotes as they break.

CROSS-CUTTING FINDING 3 — CITATION RANGES THAT CUT THE SENTENCE THEY QUOTE. Five, all widened in the merges: §3.1 974-976 (sentence starts 973), §3.7.2 1586-1589 (starts 1585, "As another consequence"), §3.4 1208-1210 (ends 1211, "or restrict their use"), §5.1 3874-3876 whose quoted fragment starts at 3875 and drops the MUST at 3874, §4.5.3.1.8 3538-3539 (starts 3537, where the 100-recipient minimum is stated).

CROSS-CUTTING FINDING 4 — ONE QUESTION RAISED TWICE, RECORDED NOT DECIDED. May an outcome event carry a snapshot of the configuration that produced it? It surfaced as site_policy_max_recipients on RecipientLimitEnforcedBySitePolicy and as timeout_minutes on DataTerminationTimeoutExpired. I resolved both the same way — struck, because the configuration event (RecipientLimitConfigured, TimeoutPolicyConfigured) owns the value and duplicating it on the outcome is a lookup rather than a capture — and both dissents are carried in full. Resolving them differently would have made the set inconsistent; resolving them at all is not this step's job, so the question travels forward.

FIELDLESS OUTCOMES AFTER THE MERGES: four of eighteen — DataTerminationTimeoutExpired, DeliveryResponsibilityDeclinedTemporarily, MailTransactionConfirmed, NullReversePathSet. Against zero of 567 before the hygiene pass, that is the shape the method predicts: a commit, a refusal of custody, a nulled path and an abandoned wait have no payload beyond the fact that they happened.

THE ABSENCE ITSELF IS STILL THE HEADLINE. Nothing in these thirty-seven records supplied an identity key, because RFC 5321 has none to supply. Every merged survivor is identified by position — the channel while the session lives, the transaction's position in the session, the path being acted on, the registry name, the server's domain. Three survivors say this in their own notes and one says it best: a message lost before anything durable was written has no identity left at all, which is exactly why its notification obligation cannot be discharged.

---

### dedup

ALL EIGHTEEN RESOLVE TO merge_into_one, AND THAT IS ITSELF A FINDING. The brief expected renaming to be common. It is not, for these eighteen: every pair cites the same anchor section and the same actor, and in eight cases both copies say so in their own notes ("MERGE of two independent candidates of the same name", "RECOVERED BY THE RE-SWEEP" appearing on both sides). These are not name clashes between different events — they are duplicate discoveries produced by overlapping sweep coverage, chiefly of 6.1, 7.x, 8, 4.5.x and 3.4, where a chapter assembler and a re-sweep both harvested the same passage. The bias rule was applied and never fired: I found no pair citing genuinely different sections with genuinely different actors.

THE CLOSEST CALL, recorded so it can be reopened: DeliveryAttemptFailed. Two actors were offered, "Sending SMTP client" and "Relay SMTP server acting as SMTP client", which invites a split. It merges because 3.6.2 1450-1453 says a relay that accepts the task "then becomes an SMTP client", making the second a subtype of the first; because both copies quote 3.6.3 1494-1496 word for word; and because 4.5.4.1 3690-3693 speaks generically of a mail queue entry. The genuinely separate arm is per-address (5.1 3873-3877, DeliveryAttemptFailedForAddress) and neither copy claimed it.

FIELD-LEVEL DISAGREEMENTS SETTLED, all with the losing argument carried into the survivor's notes: seven ambient host names struck where the host is the actor recording the event (accepting_host, suppressing_host, receiving_host, refusing_host, writing_host, stripping_host, announcing_server); delivering_host kept on ReturnPathHeaderStrippedBeforeDelivery because both copies kept it and because at final delivery the host writes itself into the message, which is the principled line between the two cases; stamped_at struck as event-occurrence time while attempted_at is kept as payload, because 4.5.4.1 3703-3704 measures the mandatory retry delay from that exact moment; two constants struck (registry, used_with); two derivable registration parts struck whose example values could not be sourced anywhere in the RFC.

THREE EVENTS EMERGE FIELDLESS — DuplicateMessageReceived, MessageContentStored, NotificationSuppressedForNullReturnPath — and in each the emptiness is the finding, not a gap: RFC 5321 asserts duplicates exist and supplies nothing with which to identify one; the stored-but-unaccepted message is the one moment no part of the spec can name, the receiver not yet having stamped its optional 4.4 ID clause; and a suppressed notification's would-be recipient is the null path itself, so the event has no handle at all. That is exactly what the audit predicted would appear once the fabricated keys came out.

NO EVENT WAS DELETED. Eighteen collisions, eighteen survivors, thirty-six records folded into them; every rejected name, every rejected stream root, every rejected field with its argument, every screen list, every cross-reference and every hotspot is carried.

CITATIONS VERIFIED THIS PASS by opening /home/ivan/FnEmail-Model/docs/rfc/rfc5321.txt, per rule 3 — headings found before citing: 2.2.2 at 532, 3.3 at 1015, 3.4 at 1162, 3.6.1 at 1420, 3.6.2 at 1445, 4.1.4 at 2443, 4.4 at 3156, 4.5.2 at 3423 (heading "Transparency" — it does not advertise that the storage-transform paragraph is inside it), 4.5.3.1.10 at 3571, 4.5.3.2.6 at 3656, 4.5.4.1 at 3685, 6.1 at 3960, 7.2 at 4212, 7.3 at 4260, 7.4 at 4284, 7.7 at 4332, 7.9 at 4355, 8 at 4383. Quote anchors confirmed at 534, 535, 567, 1087, 1088, 1105, 1117, 1454, 1463, 1578, 3237, 3242, 3453, 3455, 3576, 3578, 3581, 3591, 3600, 3703, 4009, 4270, 4288, 4380, 4445. Appendix D.1 confirmed at 4889 (8BITMIME), 4893 (MAIL FROM:<Smith@bar.com>), 4895 (RCPT TO:<Jones@foo.com>), 4897-4898 (RCPT TO:<Green@foo.com> answered 550 No such user here), 4899 (RCPT TO:<Brown@foo.com>); RFC 1652 / 8bit-MIMEtransport at 4583-4584; relay host xyz.com at 1940; per-address retry at 3873-3877.

ONE CITATION FINDING WORTH CARRYING FORWARD: there is no worked 251 exchange anywhere in RFC 5321. Every occurrence (1185, 1193, 1194, 1208, 1392, 2563, 2611, 2818, 2877, 3108, 3127, 4286, 4334) is prose or a reply-code list. So every example value for CorrectedDestinationDisclosed and AddressBookEntryUpdated is necessarily a composition, and both must stay labeled as such. The pair chosen — <Green@foo.com> to <Green@xyz.com> — uses the RFC's own tokens and its own instance of a recipient with no mailbox at the addressed host, which D.1 answers with a 550; that 550 is worth keeping visible next to the 251, since it is the disposition the RFC actually shows for the same condition.

TWO PAGE-BREAK CITATION TRAPS re-verified, both of which make a naive line range look wrong: 4.1.4's 503 sentence begins at 2516 and its second MUST resumes at 2527-2528; 3.6.2's declining sentence begins "If it declines to" at 1454 and resumes at 1463, with footer and running header at 1455-1462. Both are recorded in the survivors' notes.

US spelling gate run as a gate and tested in both directions before this output: the AGENTS.md rule 1 pattern fires on a control line of colour/behaviour/modelling/recolour/organise/analysed/artefact/authorisation/standardise and does not fire on color/modeling/organize/analysis/analyses or on advertise/surprise/exercise/compromise; run over the full text of these resolutions it returns nothing.

Working file, not a report: /tmp/claude-1000/-home-ivan-FnEmail-Model/b2d6da7d-e340-4073-a737-43964dbc16c9/scratchpad/resolutions-full.md holds the same eighteen in prose plus the gate control file alongside it.

---

### dedup

ALL 18 COLLISIONS RESOLVED AS MERGES; ZERO RENAMES. I tested each pair against the rename bias before merging and none qualified: every pair (and the one triple) shares its anchor sentence in RFC 5321 AND its actor. The differences were uniformly artifacts of independent assembly — which chapter harvested it, how far the hygiene pass went on a contested field, and which example values were reached for. Sixteen names collided in exactly two chapters, RecipientRejectedWithForwardingAddress in three (2, 11, 16). No event was deleted; every rejected alternative name, cross-reference, hotspot and dissenting field argument from a losing record is carried into the survivor's notes as described above.

CITATIONS VERIFIED, NOT REMEMBERED (rule 3). I opened /home/ivan/FnEmail-Model/docs/rfc/rfc5321.txt for every line range a decision turned on: 964-969, 1198-1211, 1359-1366, 1428-1430, 1441-1464 (the §3.6.2 550-SHOULD, whose sentence CROSSES A PAGE BREAK at 1455-1462 — that is why the citation reads "1454, 1463-1464" and it should not be "fixed"), 1520-1528, 1660-1664, 1930-1975, 2036-2046, 2530-2544, 2833-2848, 2934-2937, 3058-3062, 3107-3119, 3273-3290, 3315-3341, 3378-3398, 3500-3608, 3679-3682, 3874-3902, 3968-4005, 4118-4145, 4205-4245, 4292-4306, 4332-4336, 4388-4398, 4876-4905, 4991-5010. Every quoted claim in the merged records checked out. Two lowercase-keyword traps confirmed and flagged in the survivors: the "must" at 3275 is not RFC 2119, and line 2985 reads "SHOULD not" with a lowercase "not" — both must be quoted as printed under rule 2.

THREE EXAMPLE-VALUE CORRECTIONS FELL OUT OF THE MERGES (rule 8 — a placeholder teaches nothing, but a wrong real value teaches the wrong thing). ReturnPathLineInserted: chapter 5's pair inverted the scene, putting the forward-path <Jones@foo.com> in the Return-Path slot and naming the client bar.com as the delivery server; Appendix D.1 4881-4884 and docs/paths/WORKING-helo-direct-single-recipient-v2.md agree that Smith@bar.com is the sender, Jones@foo.com the recipient and foo.com the delivery server, so chapter 4's <Smith@bar.com>/foo.com survives. PartialDeliveryFailureDetected: <Brown@bar.com> puts Brown on the wrong host — Brown@foo.com is ACCEPTED at RCPT (4899-4900) and is therefore the only appendix recipient available to fail after acceptance, since Green is refused at 4897-4898. RecipientRejectedWithForwardingAddress: chapter 2's forward_path <Smith@bar.com> names the sender where a refused recipient belongs. Two further values improved on sourcing: MessageRepairRefusedByRelay.relay_host becomes xyz.com, which §4.1.1.3 line 1940 calls "relay host xyz.com" outright (bar.com is a client in Appendix D.1, and the original mx.bar.com was invented); RelayPolicyConfigured.relayed_for_domains becomes xyz.com with server_host foo.com, because Appendix D.3 4995-4999 makes foo.com a lower-preference MX for XYZ.COM — the RFC's own relaying arrangement, and the two values are now consistent with each other.

CONTESTED-FIELD LEDGER — where the two hygiene passes disagreed, and how each was settled on the text (the losing argument is preserved in every case, per rule 4). KEPT: peer_address on ConnectionClosedInViolation (§4.4 3325-3327 defines TCP-info as information derived from the connection — the RFC's own channel identity, and there is no session key); overflow_reply_code on RecipientLimitConfigured (§4.5.3.1.10 3601-3602 licenses "a 5yz response code" — a class, not a code, so the specific code is configured, and 3603-3608 adds a second configured option; the derivation objection holds only for the implementation arm, where 452 is fixed and therefore identity); relay_host on MessageRepairRefusedByRelay (§4.4's By-domain at 3319); server_host on RelayPolicyConfigured (the configuration-chapter precedent); BOTH line_octets and limit_octets on TextLineLengthExceeded, with the dispute flagged for a later step. STRUCK: reason on VrfySupportDisabled (one motive in the source, "for security reasons", so the field restates the citation — the same ruling both passes reached on SoftwareVersionAnnouncementDisabled); delivered_recipients on PartialDeliveryFailureDetected (accepted-set minus failed-set is a read-model fold); declining_server on DeliveryResponsibilityDeclinedPermanently; receiving_host on HeaderSectionAlteredFromDeducedEnvelopeRelationship and logging_host on NullAddressEventLoggedLocally (actor restatements); listed_in_ehlo on BilateralAgreementEstablished (the MAY at 2540-2541 addresses the server, not the two operators; its hotspot moves to the notes intact); relaying_permitted on RelayPolicyConfigured (a list's emptiness computed twice).

STREAM ROOTS SETTLED WHERE THE TWO RECORDS DIFFERED, each against a sibling already in the set: MailRejectedOnTraceHeaderFormat → Message (its paired arm MailAcceptedDespiteMalformedTraceHeader is Message-rooted in chapter 4); NullAddressEventLoggedLocally → Message (its sibling NotificationSuppressedForNullReturnPath is Message-rooted in chapter 12, and the standing decision to log is a separate configuration fact); PartialDeliveryFailureDetected → Message (custody was taken; the transaction ended at the OK reply); BilateralAgreementEstablished → Extension Registry, with Server Configuration and a dedicated Bilateral Agreement root both recorded as live alternatives.

TWO FINDINGS FOR WHOEVER WRITES THE FILES. First, the audit's central claim is confirmed independently by this pass: the two records that reason hardest about the missing identity key — ReturnPathLineInserted and MessageRepairRefusedByRelay — reach the same conclusion from opposite directions, that §4.4's ID clause (line 3336) is OPTIONAL under Opt-info (line 3329), belongs to the Received: stamp rather than to the message, and admits an Atom or an RFC 5322 msg-id, so RFC 5321 defines no message identifier of its own; and §6.4's message-id is a header field that is the SUBJECT of a prohibition, not a key. Three merged survivors are now fieldless or near it, which is the expected shape. Second, a mechanical warning: /tmp/claude-1000/-home-ivan-FnEmail-Model/b2d6da7d-e340-4073-a737-43964dbc16c9/scratchpad/merged-events.json still carries the PRE-hygiene field lists (session_id, message_ref, transaction_id, reply_code and the f2C8D14 examples are all still in that file); the cleaned field sets are the ones in the brief and the ones described above. Writing files from merged-events.json as it stands would reintroduce every fabricated key the audit found.

---

### dedup

Seventeen merges and one split, from 18 colliding names covering 21 records (AddressUpdateDisclosureRestricted and DataCommandRefusedAfterRecipientOverflow each had three). No event was deleted.

WHY SO FEW SPLITS. The bias in the brief keys on different sections plus different actors. Applying it strictly, only UndeliverableMailNotificationSent qualified: 4.4/6.1 versus 3.6.3, an accepting server versus a relay, with normative content present on one side only (the DSN-format SHOULD, the lowercase from-host "must", the grouping MUST). Every other collision turned out to be the same sentence reached from two or three chapter anchors — in ViaClauseRecorded and SizeLimitConfigured the two records cite literally the same lines. The 16 chapters overlapped on shared sections (3.4, 7.9, 4.5.3.1, 4.4, 6.1), not on shared vocabulary for different facts.

CHAPTER POLICY APPLIED. An event whose stream root is Server Configuration goes to chapter 15 (Server Configuration and Provisioning) where a chapter 15 record exists: AddressUpdateDisclosureRestricted, ExpnSupportDisabled, ReplyTextCustomized, SizeLimitConfigured. RelayAccessRestrictedToKnownSources is the exception — neither of its records was in 15, so it stays in chapter 8 with the relay-policy material and its consumer RelayingDeclined, and a facilitator applying the policy uniformly may move it. The recipient-limit family (RecipientBufferLimitReached, RecipientRejectedBelowMandatoryMinimum, DataCommandRefusedAfterRecipientOverflow) is kept together in chapter 2 rather than split across 2, 3 and 14. The two gateway acts whose grammar lives in 4.4 (ViaClauseRecorded, ReturnPathInsertedByOutboundGateway) go to chapter 9, carrying chapter 4's anchor-gap warning: 4.4's gateway bullets at 3263-3273 are not reachable from the gatewaying anchors, which stop at 3.7.5.

CITATION FIXES FOUND WHILE VERIFYING (rule 3, all confirmed by opening /home/ivan/FnEmail-Model/docs/rfc/rfc5321.txt):
- ConnectionClosedDefensively, chapter 1: 7.8 "4346-4349" is one line long at the front; line 4346 ends the previous sentence and the quote begins at 4347.
- ExpnSupportDisabled, chapter 13: 7.3 "4272-4273" is one line short; "Implementations" sits at the end of 4271. Chapter 15's 4271-4273 is correct.
- RelayAccessRestrictedToKnownSources: restriction_basis's example is sourced to 2499, which is 4.1.4 discussing denial of access to EXPN, not relay filtering. 7.9 says only "known or identifiable sources" and names no basis, so the value is borrowed. Flagged, field kept, the gap is the hotspot.
- MailRejectedForPolicyReason, chapter 16: 3.3 lines 1141-1142 sits in its rfc_refs while its own notes refuse to merge the event that section names. Kept as a flagged cross-reference so no later sweep reads 3.3 as authority.
- Already-recorded fixes preserved: 3.5.1 line 1323 (not 4.1.1.3) on SourceRoutedAddressRefused, and the truncated 7.9 4357-4358 on MailRejectedForPolicyReason.

A REPEATED EXAMPLE-VALUE DEFECT, worth naming as a pattern. Four records used <Smith@bar.com> — the SENDER in Appendix D at 4881 and 4893 — as a forward path or an addressed recipient, and two invented addresses that appear nowhere: <Brown@bar.com> (Brown is at foo.com, 4899) and <Smith@foo.com>. Verified by grep: Brown@bar.com 0 hits, Smith@foo 0 hits, Green@foo.com at 4897 and 4948, Jones@foo.com at 4895. This is the same class of defect as the fabricated identity keys — a reverse path standing in a forward-path field teaches nothing and hides that the walk was never done. Corrected in DeliveryFailedAfterAcceptance, MessageForwardedSilently and RecipientBufferLimitReached, and flagged for correction in UndeliverableMailNotificationSentByAcceptingServer.

TWO FIELD-LEVEL RULINGS MADE INSIDE MERGES, both following the same principle — a configured ceiling is the configuration event's fact, not a copy on every rejection. limit_octets moves from OversizedMessageDataRejected to SizeLimitConfigured; configured_limit moves from RecipientRejectedBelowMandatoryMinimum to RecipientLimitConfigured. Both losing arguments are preserved in the survivors' notes, and the consequence is recorded: SizeLimitConfigured is now the single home of the observation that a ceiling below the RFC's receive floor makes every rejection under it a violation. Where two records disagreed on a field, both arguments travel with the survivor — struck: peer_address and claimed_domain (facts of the channel and the greeting, recorded when those happened), prior_overflow_reply_code (already in the stream as its own event), reason on ExpnSupportDisabled (a constant of the permission); kept: message_octets (a measurement that is not a fold over anything recorded), failed_notification_recipient (the only handle the spec supplies), reply_code where the code is the subject of a configuration rather than the rendering of a decision, which is AddressUpdateDisclosureRestricted and ReplyTextCustomized only.

HELD-OVER SIBLINGS NAMED BY THE LOSING RECORDS, carried so they are not lost with the merge: DataCommandRefusedWithoutPriorRecipientRefusal / SilentRecipientOverflowRevealedAtData (all three overflow records asked for it), RelayFilteringCapabilityProvided, ForeignTransportEnvelopeCapabilityRecorded, NotificationMessageComposed / NotificationMessageSent, ObjectLengthLimitRemoved, OversizedObjectTransmissionAttempted, SizeExtensionAdvertised, DeclaredMessageSizeRejectedAtMail, RecipientListSplitIntoChunks, DisclosureConfigurationMechanismProvided.

FIELDLESS OUTCOMES after resolution: ConnectionClosedDefensively and DataCommandRefusedAfterRecipientOverflow. Both are correct — the RFC licenses the act and states no condition to record — and they are the first two of the zero-out-of-567 that the audit predicted would exist.