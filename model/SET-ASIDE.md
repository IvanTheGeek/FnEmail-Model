## 3. Message Content Transfer

Reads, lookups and displays, per the kit's "guest viewed calendar for room availability" test — none of these is a state change, and each is recorded here rather than deleted so a later step does not re-derive it.

(1) THE CLIENT'S FIRST-CHARACTER CHECK. §4.5.2 lines 3431-3432: "Before sending a line of mail text, the SMTP client checks the first character of the line." The check is a read of the line about to be sent; the state change is the insertion, which is DotStuffingApplied. Set aside.

(2) THE SERVER'S LINE CHECK. §4.5.2 lines 3435-3436: "When a line of mail text is received by the SMTP server, it checks the line." One read with three possible outcomes, all of which ARE events and all of which are carried: EndOfMailIndicatorRecognized (single period), DotUnstuffingApplied (period plus other characters), MailDataAppended (anything else). The check itself is set aside; the three outcomes are not.

(3) SIZE COMPARISONS. Measuring a line against the 1000-octet ceiling (§4.5.3.1.6) or the content against the server's ceiling (§4.5.3.1.7) is a computation over data already held. The refusals — TextLineLengthExceeded, OversizedMessageDataRejected — are the events. Set aside; note also that any `content_octets` or `line_octets` field is a derived value and is carried only because it is the fact that makes the refusal meaningful, not as a stored measurement.

(4) RECIPIENT VERIFICATION PERFORMED AFTER THE TEXT IS RECEIVED. §3.3 lines 1144-1146. The verification is a lookup against a directory or a mailbox store. Its outcomes are events and are carried or cross-referenced: RecipientFailureReportedAfterContentAccepted (this chapter, the discouraged arm) and the subsequent-failure mail message of §6.1 (Non-Delivery Notification chapter, the preferred arm). The lookup itself is set aside — and it is the clearest case in this chapter, because the RFC describes exactly the timing of a query and never a change of state.

(5) "THE SERVER DETERMINES THAT THE MESSAGE SHOULD BE REJECTED". §3.3 lines 1140-1142. The determination is a decision over content already stored — a read model's job, not an event. MessageRejectedByPolicy is the event. Set aside.

(6) INSPECTION OF THE HEADER SECTION OR BODY. §3.3 lines 1154-1156 and §4.5.3.1.8 lines 3540-3543. A relay MUST NOT do it at all and a delivery server SHOULD NOT; where it happens anyway, the inspection is a read and the refusal is the event (MessageRejectedForHeaderDefects). Set aside.

(7) TRACE RECORD INSERTION AT ACCEPTANCE. §4.1.1.4 lines 2051-2059 requires a Received line at the top of the mail data when a message is accepted for relaying or final delivery. This is NOT a read and NOT set aside as one — it is a real content mutation and a real event, but it belongs to the Trace and Provenance chapter, which holds §4.4 as its anchor. Named here only so the assembler can see that this chapter deliberately did not claim it. The same applies to Return-Path insertion at final delivery (§4.4 lines 3204-3206).

(8) NOT SET ASIDE, DELIBERATELY CROSS-REFERENCED, so the assembler can dedupe rather than lose them: EightBitTransmissionEnabledForSession (Session Lifecycle — the 8BITMIME state this chapter's HighOrderBitCleared depends on the absence of); OversizedCommandLineRejected and the 512-octet command-line limit of §4.5.3.1.4 (Protocol Fault Handling — the command channel, not the content); SizeLimitConfigured in its general seven-object form (Server Configuration and Provisioning); ResourcesBecameUnavailable (Server Configuration — the second of §3.3's three post-354 failure causes); MessageDataModifiedByRelay (Relaying); MessageHeldForUserReview and MessageRequeuedForRetry (Sending, Queueing and Retry — the client-side consequences of the two DeliveryResponsibilityDeclined events); TransactionBuffersCleared in its four-occasion merged form (Mail Transaction — this chapter split out only the DATA-completion arm); the partial-success apparatus of §4.4 lines 3275-3283 (Responsibility Handoff and Final Delivery).

---

### merge notes — 3. Message Content Transfer

SCOPE AND HARVEST. Selected by meaning across all 352 candidates, ignoring the `timeline` field entirely as instructed — the chapter's events arrived under at least eleven different sweep labels (Mail Transaction — Content, Message Data Transfer, Data Transparency and Framing, Content Conformance, Message Custody, Message Handoff, Message Storage, Inbound Transaction Limits, Delivery Responsibility, Client Timeout Discipline, Server Provisioning and Site Policy). Harvest found 24 candidates whose state change belongs here; 16 events were added by the re-sweep, giving 40.

MERGES PERFORMED (dedupe by state change, not by name; every rejected name is preserved in the surviving event's notes).
- MailDataTransferAuthorized ← MailDataTransferAuthorized + MessageTextCollectionBegan. One 354, one mode switch.
- EndOfMailDataIndicated ← the two identically-named candidates from the §3.3 and §4.1 sweeps; refs unioned, the plural-verb quote kept per rule 2.
- MessageAcceptedForProcessing ← MessageAcceptedForProcessing + the acceptance half of ResponsibilityForMessageAssumed.
- DeliveryResponsibilityAccepted ← four candidates: DeliveryResponsibilityAccepted (×3, from three different spans) + ResponsibilityForMessageAccepted. Also absorbed MessageAcceptedForFurtherProcessing, whose distinct content ("for delivery or further processing", §4.4) is a Trace-chapter fact.

MERGES REFUSED, per the instruction that a refusal is not an acceptance and a timeout is not a QUIT.
- DataCommandRejectedOutOfSequence vs DataCommandRefusedAfterRecipientOverflow: both refuse DATA before the 354, for unrelated reasons, under different strengths.
- MessageAcceptedForProcessing vs DeliveryResponsibilityAccepted: one 250 on the wire, two facts — transaction succeeded, obligation created. §4.2.5 is written entirely about the second and never mentions the first. Kept apart, overlap declared.
- MessageContentStored vs MessageAcceptedForProcessing: the split the sweeps flagged in a ⚠️ and then declined to make for budget reasons. Made here.
- DeliveryResponsibilityDeclined: split into Temporary and Permanent, as the sweep's own notes requested, because the client consequences differ.
- BareLineTerminatorTransmitted (client) vs BareLineFeedTerminationAccepted (server): either can occur without the other.
- DotStuffingApplied vs DotUnstuffingApplied: inverses, not one event.
- TerminatingLineEndingAdded vs MessageRejectedAsInvalidByOriginator: the two arms of one MUST, with nothing in common downstream.

WHAT THE RE-SWEEP RECOVERED (16, each marked in its own notes). Reading the six anchors in full, the violated arm was where the cap had bitten hardest — the sweeps consistently recorded a MUST NOT in a candidate's `notes` and never made the violation an event. Recovered: MailDataSentWithoutAuthorization, ProblematicControlCharacterTransmitted, TextLineLengthExceeded, BareLineFeedTerminationAccepted, HeaderValueEncodedForAsciiTransport, EmptyMessageContentTransmitted, ExtraLineEndingAppended, MessageRejectedAsInvalidByOriginator, MailDataBuffersCleared, MessageContentStored, MessageRejectedForIncompleteTransaction, MessageRejectedForHeaderDefects, RecipientFailureReportedAfterContentAccepted, IrreversibleStorageTransformApplied, DeliveryResponsibilityDeclinedTemporarily / …Permanently (as a split), MessageDeliveredAfterResponsibilityDeclined, SizeRestrictionImposedWithoutSizeExtension. §2.3.1 and §2.3.9 had returned almost nothing to the sweeps and yielded two events plus three corroborating citations.

CITATION WORK (rule 3 — every section heading located in /home/ivan/FnEmail-Model/docs/rfc/rfc5321.txt before citing). Headings confirmed at: §2.3.1 (608), §2.3.8 (763), §2.3.9 (791), §2.3.10 (801), §3.3 (1015), §4.1.1.4 (1992), §4.1.1.5 (2079), §4.2.2 (2778), §4.2.5 (2941), §4.3.2 (3035), §4.4 (3156), §4.5.2 (3423), §4.5.3.1.6 (3510), §4.5.3.1.7 (3516), §4.5.3.1.8 (3535), §4.5.3.1.9 (3552), §4.5.3.1.10 (3571), §4.5.3.2.6 (3656), §6.1 (3960).

THREE CITATION ERRORS FOUND IN THE INHERITED CANDIDATES, all fixed or flagged in place:
1. MessageRejectedByPolicy carried "⚠️ Reply code is not given in this span; 550 is conventional and flagged as a guess." It is given: §4.3.2 line 3117, "E: 450, 550 (rejections for policy reasons)". Now cited — and the 450/550 pair implies a temporary/permanent split the sweep could not see.
2. MessageRejectedAtEndOfData carried 554 as "an instantiation for walking, not a citation". §4.3.2 line 3115 lists "E: 552, 554, 451, 452" as the DATA error set. Now cited.
3. MessageRejectedForIncompleteTransaction's source passage was attributed to §4.1.1.4 in the harvested material. It is §3.3: the text sits at lines 1137-1142, §3.3's heading is at 1015 and §3.4's at 1162. Corrected in the event's notes rather than silently, per rule 4.

STREAM ROOTS. Three used, all in domain language and none a DDD aggregate. **Transaction** for events whose subject is the exchange in progress and which die with it (the DATA request and its grant or refusal, the confirmation, the buffers, the acceptance and the refusals, the timeout). **Message** for events whose subject is the content itself and which outlive the session (the transparency pair, the octet mutations, storage, responsibility, the duplicates). **Server Configuration** for the one standing-policy event. Two boundary cases are flagged rather than settled: EndOfMailDataIndicated and EndOfMailIndicatorRecognized were put on Transaction because the channel, not the content, is what changes state; DeliveryResponsibilityAccepted was put on Message because the obligation survives the session that created it — and that is precisely the pair of choices that decides whether this chapter and Responsibility Handoff are one chapter or two.

THE FIELDLESS-EVENT TENSION, recorded not resolved, as instructed. It bites hardest on DataRequested (DATA permits no parameters, so there is nothing but identity and time), MailDataBuffersCleared, and EndOfMailIndicatorRecognized. The kit says both "it is fine to have no fields or just the identity key" and "An event without fields is an opaque label"; every such event here carries identity plus at most one genuinely non-derived fact, and no event was padded to reach a count. One computed field was dropped on merge: `appended_octets` on MailDataAppended.

WHAT NO PROJECT SCREEN WAS ALLOWED TO CUT. Every candidate that survives only because the brief forbade the project's filters names the screen in its own notes. The pattern: the altitude tiers would remove all octet-level and buffer events (roughly a third of this chapter); the responsibility-boundary cutoff would end the chapter at DeliveryResponsibilityAccepted and delete eight events after it; the consumer test would remove everything nothing currently reads; the one-event-per-command target would collapse DataRequested/MailDataTransferAuthorized and EndOfMailDataIndicated/EndOfMailIndicatorRecognized/MailTransactionConfirmed/MessageContentStored/MessageAcceptedForProcessing into two events; the inbound-only scope would remove the four originator-side events; the HELO-only charter removes SIZE and 8BITMIME and thereby makes the unannounced-limit and high-order-bit events the only reachable arms of their rules.

THREE HOTSPOTS THIS CHAPTER PRODUCES, none of which any single RFC section states. (a) The duplicate chain: a bare-LF line ending produces no terminator, which produces DataTerminationTimeoutExpired, which produces DuplicateMessageReceived — three sections, one failure. (b) Content identity: DotUnstuffingApplied and MailDataTransformedForLocalStorage both change the octets after receipt, and the project's `content_ref` (blob:sha256, docs/paths/EXPLORE-rewalk-cadence.md line 222) does not say which side of either event it is computed on. (c) Unobservable violations: IrreversibleStorageTransformApplied, MessageDeliveredAfterResponsibilityDeclined and DuplicateMessageReceived are all detectable only from outside both MTAs, which is a modeling problem, not a specification one.

---

## 5. Responsibility Handoff and Final Delivery

Reads, lookups, displays and non-state-changes put aside for this chapter, each with the line that prompted it and the event that belongs beside it where one exists.

(1) FINAL-DELIVERY DETERMINATION ATTEMPTED — §4.4 lines 3226-3228, "It is sometimes difficult for an SMTP server to determine whether or not it is making final delivery". Deciding whether one is making final delivery is a decision over current state, and the span concedes it is often undecidable. The outcome is the event, kept as FinalDeliveryMade; the determination itself is set aside, and its undecidability is carried as a 🟥 on that event rather than as a row of its own. The inherited §4.4 sweep set this aside for the same reason and the judgment is confirmed here.

(2) RETURN-PATH HEADER PRESENCE INSPECTED — §4.4 lines 3234-3237. Not merely a read but an explicitly forbidden one for relays. ⚠️ Set aside as a read only in the honored direction: the VIOLATED arm IS kept, as RelayInspectedMessageData, because a prohibited inspection that happened is a fact about the system even though the inspection itself changes nothing. That split is the main judgment call in this chapter's set-aside pile.

(3) RETURN-PATH PURPOSE CONSULTED — §4.4 lines 3240-3243 explain what the Return-path is for. Explanatory prose; no state change. The purpose is carried as a note on ReturnPathLineInserted.

(4) TRACE RECORD INSERTED — §4.4 lines 3158-3161 and 3178-3182, §4.1.1.4 lines 2051-2058. NOT a read and NOT set aside on the read test: it is a real MUST-strength state change, and inherited candidates 178, 298 and 194 all found it. It is withheld from this chapter on ownership, not on merit — this chapter's anchors name §4.4's return-path and partial-success halves specifically (3204-3206, 3275-3283), and the trace half is a chapter of its own. Named here in full so it cannot fall between two chapters: TraceRecordInserted, plus the three MUSTs at 3178-3182 (MUST NOT change or delete a previously added Received line; MUST prepend; MUST NOT reorder or insert elsewhere) and their violated arms. If no trace chapter exists in the roster, these are unowned and must be reassigned.

(5) SLOW RELAY DETECTED / RECEIVED HEADER FIELDS COMPARED — §4.4 lines 3184-3186, "comparability of Received header fields is important for detecting problems, especially slow relays". The comparison is analysis across messages, a read. The finding it produces would be a legitimate operator-timeline event; named here, filed with the trace chapter.

(6) THE MUA/MTA BOUNDARY CAUTION — §2.3.3 lines 668-673, "the reader should be cautious about inferring the strong relationships and responsibilities that might be implied if these terms were used elsewhere". A warning about vocabulary, not an occurrence. Carried as a ⚠️ note on MessageHandedFromUserAgentToMta and MessageHandedOffToMailUserAgent.

(7) SYSTEM CLASSIFIED AS GATEWAY — §2.3.10 lines 822-824, "firewalls that rewrite addresses should be considered as gateways, even if SMTP is used on both sides of them". A classification rule for reading the specification, not a state change in any mail system. The real event behind it is an operator act nobody wrote down — AddressRewritingFirewallDeployed, a Server Configuration event — named here so it is not lost; it belongs to a provisioning chapter, not this one.

(8) CLIENT CLASSIFIED AS NOT FULLY CAPABLE — §2.1 lines 378-385, clients that transfer all traffic regardless of target domain or that "do not maintain queues for retrying message transmissions that initially cannot be completed, may otherwise conform to this specification but are not considered fully-capable". A definition applied by the reader, with no actor and no moment. The SHOULD that follows at 385-388 — that such clients "SHOULD be using the message submission protocol (RFC 4409 [18]) rather than SMTP" — does have a violated arm, LessCapableClientUsedSmtpDirectly, named here and left to a submission chapter.

(9) END-OF-DATA RESPONSE ISSUED PROMPTLY — §6.1 lines 4008-4011. Considered as the honored arm of "MUST seek to minimize the time required to respond to the final <CRLF>.<CRLF>" and refused, not set aside on the read test but because the MUST binds effort with no threshold: nothing in the RFC makes "prompt" decidable, so the honored arm has no observable moment. Only the failure is observable, and it is kept twice — DuplicateMessageReceived and DuplicateMessageCopiesDelivered. Recorded so a later reader does not go looking for the missing positive arm.

(10) SILENT MESSAGE DROPPING — §6.2 lines 4027-4028, "dropping mail without notification of the sender is permitted in practice". A real and consequential state change (inherited candidates 280 and 282), but §6.2 is not this chapter's anchor and the deliberate policy drop must not be merged with the accidental AcceptedMessageLost kept here — one is a crash, the other a decision. Named and left to whoever owns §6.2.

(11) OBSOLETE-COMMAND DELIVERY TO A TERMINAL — Appendix F.6 lines 5231-5241 (inherited candidate 319). A genuinely different delivery destination, so not a variant of MessageDepositedInMessageStore, but owned by the obsolete-commands chapter. Named so it is not lost.

(12) THE RELAY'S ONWARD SEND AND ITS BOUNCE — §2.3.10 lines 810-814 and §3.6.3 (inherited candidates 78, 182, 271, 84, 85, 87). A relay's send is not an originator's and a relay's bounce is not a delivery server's; both are cross-referenced from this chapter's events and neither is claimed. MessageForwardedToAnotherMailSystem IS claimed, because it crosses out of the SMTP environment rather than along it.

---

### merge notes — 5. Responsibility Handoff and Final Delivery

SCOPE. Chapter 5's five anchors were opened and read in full in /home/ivan/FnEmail-Model/docs/rfc/rfc5321.txt: §2.1 (heading confirmed line 348, not 346 — see the citation corrections below), §2.3.3 (656), §2.3.10 (801), §4.4 (3156), §6.1 (3960). All quoted line numbers in the events above were re-derived by numbering the file, not copied from the inherited records.

HARVEST. The `timeline` field was ignored as instructed — the candidates merged here carried nine different labels for one chapter's material ("Responsibility Handoff", "Delivery and Final Handoff", "Delivery Responsibility", "Message Custody", "Final Delivery and Message Store", "Final Delivery", "Message Handoff", "Message Handoff and Subsequent Notification", "Bounce and Notification"), which is exactly the drift the instruction anticipated. Selection was by state change: 44 of the 352 raw records were read in full, 30 were carried in, 14 were cross-referenced out to other chapters (relay, gateway, trace, queue-and-retry, submission, obsolete commands) and named in `set_aside` so nothing falls between chapters.

WHAT WAS MERGED AND WHY. Six candidates collapsed into ResponsibilityForMessageAccepted — the same instant found independently by six sweeps in §2.1, §3.3, §4.1.1.4, §4.2.5, §4.4 and §6.1, which is itself the finding: RFC 5321 states the handoff five separate times, and no other event in the document gets that treatment. Five collapsed into UndeliverableMailNotificationSent (§4.4, §6.1, §3.3, §4.1.1.4, §4.2.5). Four collapsed into MessageDepositedInMessageStore (§2.3.3, §2.3.10, §2.3.11, §4.1.1.3, §4.2.5), justified by §2.3.11 828-831 identifying the mailbox with the depository. Two collapsed into MessageHandedFromUserAgentToMta. Every rejected name is recorded in the surviving event's `notes`.

WHAT WAS DELIBERATELY NOT MERGED. One inherited merge was UNDONE: DeliveryResponsibilityDeclined had folded 4yz and 5yz to fit the sweep's cap and said so in its own notes, so it is split back into DeliveryResponsibilityDeclinedTemporarily and DeliveryResponsibilityDeclinedPermanently, which have different downstream events. Kept apart on the "looks alike" rule: a refusal at end-of-data versus a delivery failure after acceptance (same failure, opposite sides of the 250, different obligations); a relay's onward send versus a gateway's transmission out of the environment; a relay's bounce (§3.6.3) versus a delivery server's (§4.4/§6.1); a duplicate received at the wire versus duplicate copies in a mailbox (different roots, possibly days apart); too few return paths versus too many.

WHAT THE RE-SWEEP RECOVERED — eleven events, marked "RECOVERED BY THE RE-SWEEP" in their own notes. Every one is either a violated arm the truncated sweeps had no slots for, or a sibling an inherited candidate named in its notes and could not add:
 · PartialFailureSignaledInDataReply — violated arm of §4.4 3280-3281, and §3.3 1148-1150 documents it happening.
 · ReturnPathLineOmittedAtFinalDelivery — violated arm of the MUST at §4.4 3206-3207.
 · MessageOriginatedWithReturnPathHeaderPresent — violated arm of the SHOULD NOT at §4.4 3233-3234; it is the upstream cause of two repairs this chapter already carried.
 · MessageDeliveredWithAmbiguousReturnPath — violated arm of the SHOULD at §4.4 3242-3243.
 · RelayInspectedMessageData — violated arm of the MUST NOT at §4.4 3234-3237, which the permission at 3229-3231 makes unavoidable.
 · NotificationSentWithNonNullReturnPath — violated arm of a MUST stated twice, §4.4 3293-3294 and §6.1 3972-3973.
 · MessageHandedOffToMailUserAgent — the other arm of §2.3.10 807-810; candidate 50 named it as "a genuine sibling" and had no room.
 · MessageForwardedToAnotherMailSystem — §4.4 3211-3212 with §2.3.10 816-818; candidate 200 named it and had no room.
 · AcceptedMessageCommittedToStableStorage — the unnamed preventive act §6.1 3965-3967 presupposes; candidate 273 named the gap explicitly.
 · NullAddressEventLoggedLocally — §6.1 3984-3987; candidate 278 named it as a sibling the RFC "declines to forbid but never names".
 · MessageContentStored — the finer reading of §3.3's two statements of the 250, which candidate 154 flagged as folded "only to stay inside the event budget".

CITATION ERRORS FOUND IN THE INHERITED CANDIDATES (rule 3, applied to what was handed over):
 · The chapter brief gives §2.1's heading at line 346. It is at line 348; 346 is the "2.  The SMTP Model" heading. Corrected here.
 · Candidate 38 cited §2.1 lines 420-424 for "once the server has issued a success response … a formal handoff of responsibility for the message occurs". That sentence ends at line 421; 422-424 is the separate MUST clause. Split into two refs.
 · Candidate 209 correctly flagged, and this pass confirms, that the "must" at §4.4 line 3275 is LOWERCASE and not an RFC 2119 keyword, while 3280-3283 carries genuine uppercase MUSTs. Candidate 272 correctly flagged the same for §6.1 line 3964, and candidate 246 for "SHOULD not" at §4.2.5 line 2985. All three are preserved verbatim under rule 2; a pass that normalized them would have changed three strength classifications.
 · Candidate 346's §4.5.3.1.10 sentence crosses a page break (3582 to 3591); re-checked, and only the line-3591 fragment is quoted.
 · Candidate 154's §3.3 sentence crosses a page break (1116-1117 to 1127); quoted as two fragments here.
 · Candidate 39's second ref (§2.3.1 613-624) supports a definition, not the event, and is dropped rather than carried.

COMPUTED FIELDS DROPPED under the only-facts rule: `stripped_count` (candidate 203) replaced by the list of paths removed; `discharged_count` / `deferred_count` (candidate 346) replaced by the lists; `copies_delivered` (candidate 337) dropped, the duplicate delivery being the fact and the count a fold. Candidate 203's own notes had flagged its count as arguably computed and kept it anyway; the replacement resolves the tension without losing information.

THE FIELDLESS-EVENT TENSION, recorded and not resolved as instructed. It bites in exactly one place here: MessageContentStored is a bare state transition whose only honest payload is the transaction it belongs to and the moment. Under "fieldless events are legitimate" it should carry nothing beyond its identity key; under "an event without fields is an opaque label" it needs the timestamp to mean anything. It is given two fields, which is the minimum that keeps it readable, and the tension is left standing. Nothing in this chapter was padded to reach a count — six events carry three fields or fewer.

STANDING FINDING FOR THE FACILITATOR. This chapter is where the FnEmail project's responsibility-boundary cutoff sits, and the cutoff is drawn precisely at the instant the obligation BEGINS. Everything the obligation entails — commit to storage, deliver, deposit, hand off, forward onward, detect partial failure, notify, suppress the notification — falls outside the current model by construction rather than by evidence. Nineteen of the 34 events here name that screen in their notes. Four other screens do heavy work: inbound-only scope rejects every notification and every client-side event; the altitude tiers reject the message store and the delivery agent as actors the model has no place for; the "one event per command" target cannot represent a per-recipient fan-out that no command corresponds to; and the condemned status register plus the capture/decision lens would refuse all six violated-arm events on principle. None of those screens was applied.

---

## 4. Trace and Provenance

READS, LOOKUPS AND NON-CHANGES — the "guest viewed calendar for room availability" pile. Each is named so a later reader need not re-derive it, with the event that carries the actual state change.

1. ReceivedHeaderFieldsCounted — §6.3 lines 4067-4069, "Simple counting of the number of \"Received:\" header fields in a message". Counting is a read over the message; the verdict is MailLoopDetected. Confirmed against the earlier sweep's own set-aside, which reached the same conclusion independently.
2. SourceAddressReadFromTcpConnection — §4.4 lines 3167-3168 and the TCP-info comment at 3325-3327 ("Information derived by server from TCP connection", "not client EHLO"). Reading the peer address off an open socket is a read, and the fact is already carried by the session's connection event. The recordable change is the stamping of it, which is FromClauseComposed.
3. ReceivedHeaderFieldsCompared — §4.4 lines 3184-3186. Comparing trace timestamps across hops is analysis, and across messages it is analysis across streams. The *finding* is kept as SlowRelayDetected; the comparison is set aside. This is the borderline call in the chapter and the one to revisit if a facilitator disagrees.
4. ReturnPathHeaderPresenceInspected — §4.4 lines 3234-3237. Not merely a read but an expressly forbidden one for a relay ("MUST NOT inspect the message data, and especially not to the extent needed to determine if Return-path header fields are present"), so it is doubly not an event. It is also the forbidden half of the contradiction recorded on ReturnPathRemovedInTransit.
5. TraceDataReadForDebugging — §3.7.2 lines 1583-1585, "the most important use of Received: lines is for debugging mail faults". An operator reading trace data changes nothing in the mail system. The §3.7.2 sentence is cited on ReceivedLineChainAltered for the failure it names, not for the reading.
6. FinalDeliveryDeterminationAttempted — §4.4 lines 3226-3228. Deciding whether one is making final delivery is a decision, and the RFC concedes it is often undecidable at the moment it must be made; the outcome belongs to the responsibility-handoff chapter as FinalDeliveryMade.
7. TimeZoneNameSuppliedInComment — §4.4 lines 3201-3202. A field of the trace line, not an occurrence; carried by TraceTimeFormatConfigured.
8. ReturnPathPurposeConsulted — §4.4 lines 3240-3243. Explanatory prose about what the Return-path is for. No state change; the sentence is cited as rationale on ReturnPathLineInserted.
9. TraceIdClauseFormatChosen — §4.4 lines 3170-3171, "The ID clause MAY contain an \"@\" as suggested in RFC 822, but this is not required." Both arms of this MAY are spellings of one field, not two state changes. Recorded on TraceLinePrepended instead; a modeler who wants the id's *assignment* as an event should raise it, since the RFC never says who mints the value.
10. TraceLineSyntaxValidated — implied by §3.7.2 lines 1586-1589. A receiver may parse a trace line, but MUST NOT act on the result by rejecting; validation with no permitted consequence is a read. The consequences that are stated are MailAcceptedDespiteMalformedTraceHeader and its forbidden opposite.

HANDED TO OTHER CHAPTERS, not set aside — recorded here so nothing falls between chapters. FinalDeliveryMade (raw idx 200, §4.4 3204-3212) and the partial-recipient-success text at §4.4 3275-3283 go to the responsibility-handoff chapter. The undeliverable-mail notification cluster at §4.4 3281-3294, including the MUST that notifications use a null return path, goes to the non-delivery-notification chapter. RecipientListCopiedIntoHeaderSection (raw idx 292, §7.2) goes to the alias-and-list chapter; only §7.6's two disclosures are kept here. MessageDataModifiedByRelay (raw idx 81, §3.6.3) goes to the relaying chapter as the general case of ReceivedLineChainAltered. ReturnPathVerified (raw idx 82, §3.6.2) goes to relaying. The §3.7.1, §3.7.3, §3.7.4 and §3.7.5 gateway events — EnvelopeFoldedIntoHeaderSection, HeaderAddressTransformedForInternet, EnvelopeReturnPathSetAtGateway, TransportServiceTranslationPerformed (raw idx 88, 89, 90, 94, 95) — go to the gatewaying chapter; only §3.7.2 is this chapter's anchor. The one exception, argued in its own notes, is ReversePathConstructedFromForeignEnvelope: it lives at §4.4 3269-3273, which no other chapter's anchors reach, so it is kept here rather than lost.

---

### merge notes — 4. Trace and Provenance

HARVEST. Selected by meaning, not by the `timeline` field, which was useless as advertised — the chapter's material arrived under at least ten different labels: "Trace and Provenance", "Trace and Loop Detection", "Mail Gatewaying", "Relay and Gateway Transit", "Message Handoff", "Return Path", "Return Path and Error Routing", "Gateway Bridging", "Loop Control", "Registry Governance", "Service Provisioning". A grep on trace/received/return-path/gateway/loop/timestamp across all 352 records returned 180 candidates; roughly 30 were genuinely this chapter's, the rest were neighbors sharing a keyword.

MERGES, and what each cost. (1) TraceLinePrepended absorbs four candidates naming one state change — idx 193, 178, 298, 43 — plus idx 79. Rejected names carried into its notes. (2) MailLoopDetected absorbs idx 283 and idx 80, which reached the same verdict by the same evidence under different labels. (3) ReceivedLineChainAltered absorbs idx 196 and recovers ExistingReceivedLineAltered, which an earlier sweep had recorded as "folded into ReceivedLinePrependedByGateway" — folding a MUST NOT into a MUST is how a violated arm disappears, and the §3.7.2 rationale at 1583-1585 came back with it. THINGS DELIBERATELY NOT MERGED, per the brief's warning: a gateway's prepend is not a relay's (§3.7.2 is a separate MUST with a direction and a foreign environment), a relay's mid-flight return-path removal is not a delivery server's strip at the boundary (different actor, moment and evidence), removing a header is not rewriting an envelope (idx 201 versus 202 — and §4.4 3288-3290 makes the rewrite the defining difference between forwarding and aliasing), a rejection on trace format is not an acceptance despite it, and detecting a loop is not stopping one.

RE-SWEEP — nineteen of the thirty-eight events are new, which is the measure of what the 32-event cap took. Read in full: §4.4 (3156-3375, prose and ABNF), §3.7.2 (1575-1592), §6.3 (4065-4072), §7.6 (4319-4330), plus §4.1.1.4's trace paragraph (2051-2059), §3.6.3's carve-out (1524-1529), §2.3.10 (806-824), F.5 (5222-5227) and D.3's wire example (5058-5070). Recovered: FromClauseComposed, TraceSourceNameStampedWithoutConnectionAddress, FromClauseOmittedFromTrace (the whole FROM-clause topic the chapter roster names had no event of its own); TraceForClauseOmitted, MultiplePathsStampedInForClause, BlindCopyRecipientDisclosedInTraceField, InternalHostNameDisclosedInTraceField (both of §7.6's disclosures were unrepresented — a nine-line section with no events); TraceLineInsertedOutOfPosition, ExistingTraceLinePreserved; TraceDateStampedWithZoneNameInsteadOfOffset, ObsoleteDateFormUsedInTraceLine, SlowRelayDetected; UnregisteredTraceNameUsed; MailRejectedOnTraceHeaderFormat; ReturnPathSubmittedByOriginatingSystem, MultipleReturnPathsPresentAtDelivery; LoopRejectionThresholdConfigured, LoopingMessageRejected, TrivialLoopStopped, LoopedMessagePassedOnUndetected. The richest seam was exactly where the brief predicted: the violated arms. §6.3 is eight lines long and the inherited set carried one event from it; it yields five, and the one the cap dropped — TrivialLoopStopped — carries the section's only unconditional MUST while the one that survived carries a conditional SHOULD.

CITATION ERRORS FOUND IN THE INHERITED CANDIDATES, all verified against /home/ivan/FnEmail-Model/docs/rfc/rfc5321.txt by opening the file, per rule 3. (a) idx 196 cited §4.4 "3178-3180" for a sentence that ends on 3179 — corrected to 3178-3179. (b) idx 178 cited §4.1.1.4 "2054-2057" for a phrase ending on 2055, and "2051-2054" for a clause ending on 2052 — the ranges are re-cut to the sentences they quote. (c) idx 43 cited §2.3.10 "810-814" for a quotation whose last words sit on 813 — corrected to 810-813. (d) idx 79 cited §3.6.3 "1524-1527" for a carve-out that closes on 1526 — corrected to 1524-1526. (e) idx 298 cited §7.6 "4322-4323" and quoted a mid-sentence fragment, "(\"Received\") header fields produced in conformance with this", which is line 4323 alone; the full sentence runs 4321-4325 and is quoted whole here. (f) idx 195 cited "3313-3315" for the obs- prohibition, which begins on 3314 — 3313 is the date-time reference. No inherited citation was found to be wholly fabricated; every quoted string exists at or within one line of where it was said to be. Two quotes needed rule 2 care because the source wraps them mid-sentence inside ABNF comments (the SHOULD NOT against unregistered names, at 3346-3348 and 3373-3374) and because F.5 line 5225 reads "MUST BE used" with that capitalization, which is not RFC 2119's spelling and is copied as it stands.

STREAM ROOTS. Mostly Message, because trace and provenance are facts written into the message itself rather than into the session that carried it — which is the chapter's one structural finding: this is the only chapter where the state change lands on an object that outlives every SMTP session it passes through, and the object is mutable by design at every hop. Transaction for the two events that rewrite the envelope's reverse-path (ReversePathRebuiltForOnwardHop, ReversePathConstructedFromForeignEnvelope). Server Configuration for the two operator-set events. Extension Registry for the two IANA events. TWO ROOTS INVENTED, both declared in their events' notes: "Relay Host" for SlowRelayDetected, a fact about a third party observed from outside; "Transport Environment" for ErrorReportAddressDesignatedForNonSmtpTransport, a durable fact about a non-SMTP mail environment with a lifetime in years.

WHAT THIS PROJECT'S SCREENS WOULD HAVE TAKEN, named per event and counted here: the consumer test / G1 would have taken about eighteen of the thirty-eight; the four altitude tiers about fourteen; the inbound-only scope and responsibility-boundary cutoff about sixteen between them; the "one event per command" target four (FromClauseComposed, TraceRecipientNarrowed, TraceForClauseOmitted, ReversePathConstructedFromForeignEnvelope — none of which has a command); the condemned status/phase register two; the HELO-only charter three, since every trace and Return-Path event lives inside DATA. Only MailAcceptedDespiteMalformedTraceHeader and MailRejectedOnTraceHeaderFormat survive every screen, and both are about tolerating someone else's malformed output. That is the chapter's second finding: an inbound-only, HELO-only model does not trim trace and provenance, it deletes it.

TENSIONS RECORDED, NOT RESOLVED, as instructed. The fieldless-event tension bites exactly once, on ExistingTraceLinePreserved, which has nothing but its identity key and is an opaque label as a result — both halves of the kit's guidance are true of it simultaneously. Derived-value calls: received_count and rejection_threshold are kept as observed facts rather than scores; stripped_count was dropped from ReturnPathHeaderStrippedBeforeDelivery as computed and replaced by the stripped path, with the inherited argument for keeping it recorded rather than discarded; no elapsed-time field on SlowRelayDetected. Hotspots worth a 🟥 if any of this reaches the model: a relay may remove the Return-path it is forbidden to look for (3229-3231 versus 3234-3237); "exactly one return path SHOULD be present" collides with "MUST be used as the target of any mail containing delivery error messages" (3242-3243 versus 3258-3260); a stray Received: line MUST be preserved while a stray Return-Path MAY be deleted, and §4.4 never remarks on the asymmetry; §4.4 wants local time because it reveals server location while §7.6 warns about exactly that kind of disclosure, and neither cites the other; "trivial loops" is a MUST with an undefined trigger and an undefined action; a trace line's shape is mandated (3311) and its enforcement forbidden (1586-1588); and "this subsection" at 3289 is unresolvable, appearing identically at §3.9.2 1728-1730 where it means something else.

REAL DATA (rule 8). The project's standing set throughout — foo.com, bar.com, 192.0.2.10, 203.0.113.20, <Smith@bar.com>, <Jones@foo.com>, <Brown@foo.com>, f2C8D14, "Tue, 19 May 1998 09:14:07 -0700" — extended only where the chapter needed a fact the set did not cover, and each extension earned its place by exposing something a placeholder hides: <announce-owner@foo.com> (the list owner, which is what makes ReversePathRebuiltForOnwardHop a different event from ReturnPathRemovedInTransit); mail1.internal.foo.com with [192.0.2.10] (which makes the §7.6 LAN leak legible); "Tue, 19 May 1998 09:14:07 PDT" and "Tue, 19 May 98 09:14:07 -0700" (the same instant in the two non-conforming date forms, directly comparable to the compliant one); X.400 and P1 as the non-SMTP transport and protocol, taken because the RFC itself names encoded X.400 addresses at line 3466; gw.foo.com, mx.foo.com, mx.bar.com. Address literals are written bracketed — [203.0.113.20] — because that is the real form the Extended-Domain grammar produces at 3321-3323, not the bare address. Note for whoever renders this as markdown: rule 5 applies, and a bare <Smith@bar.com> outside a code span loses its angle brackets, which in SMTP are path syntax.

---

## 2. Mail Transaction

READS, LOOKUPS AND NON-EVENTS — set aside with reasons, never deleted.

(1) Server examined the forward-paths to decide whether the deferred reverse-path is acceptable (§3.3 lines 1039-1045). The examination is a read of what RecipientBuffered already recorded; only the report is a state change, and that is ReversePathProblemReportedAfterForwardPathsExamined. The guest-viewed-calendar test.

(2) Server determined whether a recipient "is known not to be a deliverable address" (§3.3 line 1071). A lookup against a mailbox directory. The refusal that follows is the event; the consultation is not.

(3) Server recognized source route syntax (§4.1.1.3 lines 1925-1926, "Receiving systems MUST recognize source route syntax"). Recognition is a read; the strip (SourceRouteStripped) or the refusal (SourceRoutedAddressRefused) is the event. An earlier sweep set the same thing aside under the name SourceRoutedPathEncountered — agreed, and recorded so the second finding is not mistaken for a new one.

(4) Server counted the recipients shown in the message header fields (§4.5.3.1.8 lines 3540-3543). A validation test on header fields, which relaying servers MUST NOT and delivery servers SHOULD NOT perform; the read itself is set aside here, but the VIOLATED ARM — MessageRejectedOnHeaderRecipientCount, a server rejecting on that count — is a real event and is held over to the message-content chapter, not rejected.

(5) Server consulted its configured recipient limit before answering (§4.5.3.1.10 lines 3598-3602, "If the server has a configured site-policy limitation"). A read of Server Configuration. The configuring act is an event (see the hold-over list); the consulting is not.

(6) Client observed that at least 100 addresses had been removed from its queue (§4.5.3.1.10 lines 3581-3596). An inference the RFC draws for the reader, not a state change, and the queue events it implies belong to the queue chapter.

(7) RSET issued in a position where it "is effectively equivalent to a NOOP (i.e., it has no effect)" — §4.1.1.5 lines 2085-2089, four such positions. A NON-EVENT: the same command that produces MailTransactionAborted elsewhere produces nothing here. Set aside rather than merged, and flagged as a hotspot on MailTransactionAborted.

(8) RSET followed immediately by EHLO, which §4.1.4 lines 2456-2459 calls "redundant, but not harmful". The RFC describing something that changes nothing twice over.

HELD OVER — genuine events cut by the 40-item schema cap or owned by a neighboring chapter. NOT rejected; listed so the next reader does not have to re-derive them.

- ObsoleteTransactionBeginningCommandReceived — §4.1.4 lines 2502-2503 and 2507 name SEND, SOML and SAML as transaction-beginning commands. Recovered by the re-sweep, cut for space.
- DataCommandRefusedWithoutPriorRecipientRefusal — §4.5.3.1.10 lines 3607-3608, "to simply return the 503 after DATA without returning any previous negative response". A materially different client experience from the 452-then-503 variant.
- MessageRejectedForIncompleteTransaction — §3.3 lines 1136-1142, the SECOND of the RFC's "only two points" where DATA can fail, after the 354. Belongs with the message-content chapter that owns the 354.
- MessageDataWithheldByClient and MessageDataSentWithoutIntermediateReply — §3.3 lines 1133-1135, the honored and violated arms of "message data MUST NOT be sent unless a 354 reply is received". Client actors; content chapter.
- UnnegotiatedParameterTransmitted — §4.1.1.3 lines 1979-1981, "The client MUST NOT transmit parameters other than those associated with a service extension offered by the server in its EHLO response." Violated arm; extension-negotiation chapter. Its server-side answer is the raw set's MailParametersRejected (455/555).
- RecipientsSplitIntoChunks — §4.5.3.1.8 lines 3546-3550, client SHOULD be prepared to transmit in 100-recipient "chunks". Queue Entry root; outbound chapter.
- RecipientLimitConfigured — the raw set has it; Server Configuration root. §4.5.3.1.10 presupposes it ("If the server has a configured site-policy limitation"), and any configured value below 100 is a stated violation of §4.5.3.1.8 lines 3537-3539.
- OversizedPathRejected — §4.5.3.1.3, a path over 256 octets. A limit, but from a span this chapter does not anchor.
- SessionReinitialized — the raw set's EHLO-mid-session event; Session root, chapter 1. Its transaction-abort arm is carried here by MailTransactionAborted with aborting_command EHLO.
- CommandRejectedWithStateUnchanged — the raw set's five-code merge (501, 503, 555, 455, 500). Undone here: its two transaction arms are TransactionCommandArgumentRejected and CommandRejectedAsOutOfSequence; the other three belong to the command-dialogue and extension chapters.
- StateTableReset — merged into TransactionBuffersCleared, with the unresolved buffer-versus-state-table tension recorded in that event's notes rather than dropped.

---

### merge notes — 2. Mail Transaction

HARVEST. Selected by meaning, never by the `timeline` field — the 91 invented labels split this chapter's material across at least fourteen of them (Mail Transaction, Mail Transaction — Envelope, Mail Transaction — Content, Inbound Transaction Limits, Source Routing, Command Dialogue, Forwarding and Address Correction, Address and Mailbox Administration, Postmaster and Abuse Response, Message Custody, Session Termination, Session Establishment, Server Provisioning and Site Policy, Reply Channel). Two of this chapter's strongest events, PostmasterRecipientAccepted and RecipientBuffered, carried labels that would have hidden them from any timeline filter.

MERGES, and why. RecipientAdded + RecipientBuffered + the folded ForwardPathStored -> RecipientBuffered (one state change, three names, three sweeps). TransactionBuffersReset + TransactionBuffersCleared + BufferCleared + StateTableReset -> TransactionBuffersCleared. MailTransactionInitiated + MailTransactionStarted -> MailTransactionStarted. SourceRouteStripped x2 + SourceRouteIgnored -> SourceRouteStripped. PostmasterRecipientAccepted x2 -> one. Every rejected alternative name is carried in the surviving event's notes.

NOT MERGED, deliberately, where two candidates only look alike. ReversePathAccepted and ReversePathProvisionallyAccepted are one 250 on the wire and two different server states. ReversePathRejectedPermanently and ReversePathRejectedTemporarily must stay apart because the RFC's MUST is precisely that they be distinguishable — collapsing them into a reply_code field erases the requirement into a payload value. RecipientBufferLimitReached (implementation, MUST 452, transient) and RecipientLimitEnforcedBySitePolicy (policy, MAY 5yz, permanent) fork the client's retry behavior. MailTransactionAborted (commanded) and PendingTransactionCanceled (connection failure, nothing on the wire) — a timeout is not a QUIT. RecipientRejectedWithForwardingAddress (551, discloses) and RecipientRejectedWithoutAddressInformation (550, deliberately silent) differ only in what is not said. The five recipient refusals are kept as five because they differ in cause, code and disclosure. The §4.2-4.3 sweep's ForwardPathAdvertised merged 251 and 551 into one event; that merge is undone, because 251 accepts custody and 551 refuses.

RE-SWEEP RECOVERIES — twelve, all read directly from the anchors, all marked in their notes. ReversePathBuffered (the insertion half of §4.1.1.2 lines 1895-1897; the clearing half was harvested and the inserting half dropped, leaving the forward-path side with a buffering event and the reverse-path side with none). NullReversePathDeclared (a raw candidate's notes said outright it had been folded "for the field cap and should be recovered by a later step"). ReversePathProblemReportedAfterForwardPathsExamined (its sibling's notes called it "the only observable consequence" and gave it no slot). TransactionCommandArgumentRejected (the 501 MUST plus the MUST-stay-in-the-same-state at §4.1.4 2514-2516, buried inside a five-code merge). NestedMailCommandRejected (a rule stated three times in the document, harvested only as a supporting citation). MailCommandRejectedForUninitializedSession (the opening MUST of §4.1.4, no event at all). RecipientRejectedBelowMandatoryMinimum, PreviouslyAcceptedRecipientsSilentlyDiscarded, OverflowReplyReclassifiedAsTransient (all three named inside raw candidates' notes as cut for slot budget; the second was called "the clearest both-arms pair in the span"). SourceRoutedReversePathSent, SourceRoutedForwardPathGenerated, SourceRouteNamesCopiedIntoReversePath. Four more were recovered and held over for the item cap — listed in set_aside.

THE PATTERN THE CAP PRODUCED. The violated arm is where the truncation bit, and it bit systematically: for every MUST/SHOULD/MAY in these anchors the eleven sweeps kept the arm the RECEIVING SERVER performs and dropped the arm the CLIENT performs or the arm a non-conforming implementation performs. Six of the twelve recoveries are client-side or non-conformance events. The §4.5.3.1.8 pair is the sharpest case: a spec that says in its own words "is a violation of this specification" and "rather than silently discarding addresses previously accepted" produced, across eleven sweeps, only the conforming events.

CITATION ERRORS FOUND IN THE INHERITED CANDIDATES (rule 3; every kept citation was re-opened in /home/ivan/FnEmail-Model/docs/rfc/rfc5321.txt). (a) SourceRoutedAddressRefused attributed "S: 550 Access Denied to You." to §4.1.1.3 line 1323 — the line number is right, the section is wrong: 1323 sits in §3.5.1 (heading 1215, §3.5.2 at 1335), a VRFY example, not a RCPT one. (b) RecipientRejectedForPolicy cited §4.1.1.3 lines 1972-1975 for the MAY-reject sentence, which runs 1973-1975; 1972 is a different sentence. (c) ReversePathDeclared cited §3.3 lines 1017-1020 for a sentence ending at 1018, and 1031-1034 for a quote ending at 1033. (d) MailTransactionConfirmed's second citation spans a page break — the sentence starts at 1116 and finishes at 1127 — so a bare range misleads; named explicitly in the ref. All corrected in place. Two flags carried FORWARD unchanged because they are honest and still true: ReversePathRejectedTemporarily's 451 is a guess (§3.3 names only 550 and 553), and RelayingDeclined's 550 is a guess (§3.3 gives no code). Three further inferences of my own are flagged in their events: the 503 for NestedMailCommandRejected and for MailCommandRejectedForUninitializedSession, and the joining of §3.3's colon-spacing trap to §4.1.4's 501.

STREAM ROOTS. Transaction for the envelope, buffers, limits and lifecycle; Recipient for the per-forward-path declaration and its five judgments, because the judgment attaches to the address and not to the transaction; Session for the two out-of-sequence refusals whose whole content is that no transaction exists; Queue Entry for OverflowReplyReclassifiedAsTransient, where the transaction is over and what survives is a retry decision. No new root was needed.

PROJECT SCREENS NAMED, NONE APPLIED, as instructed. Every event that one of them would have killed says which: the capture/decision two-kinds lens, the condemned status/phase register, the consumer test G1, the four altitude tiers, the HELO-only charter (which alone deletes every ESMTP parameter field in this chapter), the inbound-only scope (which alone deletes all six client-actor events), the responsibility-boundary cutoff, the "one event per command" design target (the most destructive here — it would erase ReversePathBuffered, RecipientBuffered, TransactionBuffersCleared and the two-kinds of 250 in one stroke), and the twelve-slice model.

---

## 6. Address Resolution and Next-Hop Selection

READS, LOOKUPS AND DISPLAYS, kept here rather than deleted, each with the line that prompted it and the event that stands beside it where one exists.

(1) THE MX LOOKUP ITSELF — §5.1 lines 3837-3838, "The lookup first attempts to locate an MX record associated with the name." Asking DNS a question changes nothing. Its five distinguishable answers ARE the events and all five are carried: TargetNameResolvedToCanonicalName, TargetDomainFoundNonExistent, MessageQueuedAfterTemporaryDnsError, ImplicitMailExchangerAssumed, NoUsableMailExchangerFound. This is the chapter's cleanest instance of the guest-viewed-calendar test: the query is a read, the answer is a fact.

(2) THE ADDRESS-RECORD QUERY ON AN EXCHANGER NAME — §5.1 lines 3854-3858. A query. Its answer is kept as MailExchangerAddressResolved, marked doubtful for exactly this reason; if a later step draws a stricter line, that event is the first to fall into this pile.

(3) SORTING THE MX RECORDS BY PREFERENCE — §5.1 lines 3883-3885 and 3927-3928, "The records are first ordered by preference". Fully re-derivable from the records themselves, so it is a computed value under the kit's no-computed-fields rule and cannot be an event. Its non-derivable half, the random tie-break, IS kept as EqualPreferenceExchangersRandomized. This pair is the clearest illustration in the chapter of where the computed-field rule actually cuts — it kills the sort and saves the draw.

(4) THE RESOLVER INTERFACE ORDERING A MULTIHOMED HOST'S ADDRESSES — §5.1 lines 3893-3895, "It is the responsibility of the domain name resolver interface to have ordered this list by decreasing preference if necessary". Done outside SMTP; the spec then treats the order as input the sender "MUST try ... in the order presented". Input, not an event of ours. What the sender does with it IS ours, and is carried as CandidateAddressesTriedOutOfOrder.

(5) INSPECTING THE MX LIST FOR THE RELAY'S OWN NAMES — §5.1 lines 3928-3930. The inspection is a read; the match is the fact, kept as SelfIdentifiedInMailExchangerList. A miss changes nothing and produces no event — re-examined in this chapter and the inherited judgment confirmed.

(6) THE RELAY DETERMINING THAT IT SHOULD RELAY — §5.1 lines 3917-3918, "If it determines that it should relay the message without rewriting the address". A decision-read over configuration and the envelope. Its four outcomes are the events: MessageRelayedByDesignatedExchanger, MessageDeliveredFinallyByDesignatedExchanger, MessageHandedOffOutsideSmtpEnvironment, and the rewrite pair.

(7) NEXT-HOP SELECTION AS SUCH — §3.6.2 lines 1453-1454, "the next SMTP server specified in the DNS (according to the rules in Section 5)". The query and the ranking are reads; NextHopChannelEstablished records the outcome that changes state.

(8) DESIGNER GUIDANCE TO STUDY THE MULTIHOMING PROCEDURES — §5.2 lines 3951-3956, "Designers of SMTP implementations ... should study the procedures above". Advice to implementers at design time, not a runtime state change. No event in any timeline.

(9) THE RFC 3974 OPERATIONAL-EXPERIENCE REFERENCE — §5.2 lines 3943-3947. A document citation the spec then declines to standardize. Its consequence — that no interoperable rule exists — is recorded as the 🟥 on AddressFamilyCandidateSkipped.

(10) AN OPERATOR INSPECTING THE OUTBOUND QUEUE — implied by the queueing MUST at 3841 and by every retry discussion, mentioned nowhere in the RFC, and it changes nothing. The nearest true guest-viewed-calendar analogue in this chapter. Named here so no later reader has to rediscover that it was considered and set down.

(11) THE STATEMENT THAT MX RECORDS MAKE SOURCE ROUTES UNNECESSARY — §3.6.1 lines 1423-1425. A statement about the environment, not a state change.

BELONGS TO ANOTHER CHAPTER, not to this pile — recorded so no one thinks the re-sweep missed them. PostmasterRecipientAccepted (§2.3.5 lines 735-737, MUST accept "postmaster" without domain qualification — an FQDN exception, but a mailbox event). ClientIdentityAsserted (§2.3.5 lines 722-726, the EHLO name MUST be a primary host name or an address literal — a session event, though AddressRecordPublished is its precondition too). RelayTaskAccepted and RelayTaskDeclined (§3.6.2 lines 1450-1454 and 1454,1463-1464 — the Relaying chapter; note the 550-refusal sentence is split by a page break and any grep-derived line range for it will be wrong). SmtpClientRoleAssumed (§3.6.2 line 1452 — Relaying; deliberately NOT fused with NextHopChannelEstablished, since role change and connection fail independently). SourceRouteStripped and DeliveryFailedAfterSourceRouteStripped (§3.6.1 — Relaying; the second is the delayed consequence of this chapter's DeliveryRoutedWithoutDnsResolution). RetryAttemptScheduled, RetryScheduleExhausted, QueueLifetimeExpired (§4.5.4.1, heading verified at line 3685 — MessageQueuedAfterTemporaryDnsError hands off to them). MailLoopDetected (§6.3, heading verified at line 4065 — §5.1 is loop prevention, §6.3 is loop detection, and only the second is ever called that). NonDeliveryNotificationSent (§6.1 — same wire outcome as MessageReturnedUndeliverableAfterSelfMatch from a different cause; the merge question is real and belongs to a later step). MessageReturnedAsUndeliverable in its §2.4 sense (a delivery system's content judgment, NOT a routing exhaustion — kept unmerged under the "a refusal is not an acceptance" rule).

---

### merge notes — 6. Address Resolution and Next-Hop Selection

SELECTION. Harvested by meaning, not by the `timeline` field, exactly as instructed — the eleven sweeps gave this chapter's material at least six different labels (Route Resolution, Routing Configuration, DNS and Routing Administration, Delivery Attempt, Relaying, Server Administration, Source Routing), and one relevant candidate, MxRecordsPublished, came from the Appendix D sweep with no routing label at all. 22 inherited records were pulled; 21 survive inside 19 events after merging.

MERGES PERFORMED. (a) MailExchangerRecordPublished: FOUR inherited candidates collapsed into one — three sharing that name from the §1.1-§2.3.5, §3.6-§3.7 and §5-§6 sweeps, plus MxRecordsPublished from Appendix D.3; MailRoutingDnsRecordPublished (§3.5 sweep) names the same act and is recorded as a rejected alternative name. Refs unioned to six, fields unioned to four (ttl_seconds survives from one sweep only and is kept because it governs when a change becomes visible). (b) NextHopChannelEstablished: the §3.6.2 candidate merged with the success arm of §5.1's "until a delivery attempt succeeds" — one state change that two sweeps each saw half of. (c) LocalAliasRejected renamed to LocalAliasAppearedInTransaction and SPLIT, with the unlicensed refusal arm kept separately as UnqualifiedDomainNameRefused.

MERGES REFUSED, per "do not merge things that merely look alike". MessageRelayedToNextHop existed twice under one name: the §4.1.1.3 instance is a source-routed relay (Relaying chapter) and the §5.1 instance is one arm of the designated-exchanger MAY; kept apart and the §5.1 one renamed MessageRelayedByDesignatedExchanger. MessageReturnedAsUndeliverable likewise existed twice: the §2.4 instance is a content judgment at the last hop, the §5.1 instance is routing exhaustion; only the §5.1 one is mine, renamed MessageReturnedUndeliverableAfterSelfMatch. TargetNameResolvedToCanonicalName and MailExchangerTargetFoundUnresolvable both involve a CNAME and are opposite verdicts on where it sits.

WHAT THE RE-SWEEP RECOVERED — 20 events, half the chapter, which confirms the 32-event cap truncated hard. Reading §5 (3822), §5.1 (3824), §5.2 (3937), §2.3.5 (691) and §3.6.2 (1446) in full produced: FIVE violated arms no sweep carried (AddressRecordsUsedDespiteMailExchangersPresent from the MUST NOT at 3848-3850, whose keyword WRAPS across the line break and is invisible to a line-scoped grep; MessageFailedWithoutQueueingAfterTemporaryError; DeliveryAbandonedAfterSingleAddress; CandidateAddressesTriedOutOfOrder; EqualPreferenceRandomizationSkipped; plus DeliveryRoutedWithoutDnsResolution from §5.1's opening MUST, which no sweep carried in EITHER arm). THREE arms of the designated-exchanger MAY, two of which an inherited note admitted were dropped "only for space". FOUR DNS-publication and configuration preconditions the spec presupposes and never states (MailExchangerRecordWithdrawn, AliasRecordPublished, AddressRecordPublished, ServerIdentityNamesConfigured — the last named twice in inherited notes and slotted by neither sweep). THREE consequence events named as siblings and never given slots (CandidateAddressesExhausted, AlternateAddressLimitReached, ForwardPathRewrittenBeforeRelay). RoutingErrorReported, the honored arm of THREE separate MUSTs, which every inherited candidate flagged and none carried. AddressFamilyCandidateSkipped, giving §5.2 an event of its own — the whole section had been folded into one candidate's notes although IPv4/IPv6 selection is in this chapter's charter. MailExchangerTargetFoundUnresolvable. And four events from §2.3.5's domain-name rules, which the sweeps had mined only for the local-alias prohibition: DeliveryDomainLexicallyIdentified, DomainNameLabelSyntaxViolated, TopLevelDomainAddressUsed, UnqualifiedDomainNameRefused.

CITATION AUDIT of the inherited candidates, rule 3 applied to every ref kept. Every line number was re-checked with `grep -n` against /home/ivan/FnEmail-Model/docs/rfc/rfc5321.txt, not read off a screen. ONE looseness found and corrected: LocalAliasRejected cited "A domain name that is not in FQDN form is no more than a local alias. Local aliases MUST NOT appear in any SMTP transaction." as §2.3.5 lines 709-712; the quoted sentences begin at 711, and 709-710 are the FQDN definition. Corrected to 711-712. ONE inherited citation I initially believed wrong turned out RIGHT and is recorded so it is not "corrected" later: MailExchangerAddressResolved's §5.2 lines 3950-3951 for the IPv6-only quote is correct — a first pass through a `cat -n` pipeline showed 3949-3950 and `grep -n` settled it at 3950-3951. That ref is nonetheless RETARGETED to lines 3941-3943 in the merged event, on relevance rather than accuracy, because the IPv6-only skip now has its own event. Everything else checked clean.

TWO CITATION TRAPS RECORDED IN THE EVENTS THEMSELVES. (i) "it MUST sort the MX records to determine candidates for delivery" is SPLIT BY A PAGE BREAK: it begins at 3917-3918 and resumes at 3927 with the single word "delivery.", with the footer at 3919-3926 in between. Any line range derived from a grep of the visible text is wrong, so it is cited as "3917-3918,3927". (ii) "MUST NOT" WRAPS at 3848-3849 ("SMTP systems MUST" / "NOT utilize"), which is why a whole MUST NOT went unfound by eleven sweeps; the inherited span notes measured five such wrapped keywords in §5-6 and warned that any tier classification built from a line-scoped grep misreads them. Both are the kind of thing that costs an hour twice if it is not written down once.

FIELDLESS-EVENT TENSION, recorded where it applies and NOT resolved, per instruction. No event in this chapter is fieldless, so the kit's "an event without fields is an opaque label" never bites here. The opposite pressure does: the risk throughout is padding, and three inherited fields were DROPPED as redundant with the event name rather than kept to make a count look respectable — dns_result on TargetDomainFoundNonExistent ("non-existent domain") and on MessageQueuedAfterTemporaryDnsError ("temporary error"), and offering_verb on LocalAliasRejected where offering_command is the domain-language form. Two fields were kept against that pressure and the reason is in each event's notes: ttl_seconds on MailExchangerRecordPublished, because it is what decides when a routing change becomes visible; and untried_addresses on AlternateAddressLimitReached and DeliveryAbandonedAfterSingleAddress, because "what was NOT tried" is the whole content of a policy stop.

STREAM ROOTS. Next-Hop Destination carries 15 events and is plainly this chapter's spine. Message (4), Queue Entry (4), Transaction (3), DNS Zone (4), Server Configuration (2), Session (1), Recipient (1). DNS ZONE IS INVENTED, as the instruction permits: none of the offered roots fits the four publication events, and Server Configuration would wrongly imply the zone is ours — it is another organization's asset, on a lifetime of years, and that is exactly the fact worth preserving. ForwardPathRewrittenBeforeRelay is rooted at Recipient rather than Transaction because a forward path is per-recipient and a three-RCPT message can have one rewritten and two not.

WHAT THIS CHAPTER LOOKS LIKE THROUGH THE PROJECT'S SCREENS, named per event and summarized here because the pattern is the finding: of 40 events, roughly 24 belong to a sending or relaying system (inbound-only scope deletes them), 6 to configuration or DNS operation on a timescale of months or years (altitude tiers file them Infrastructure), and 4 to DNS operators in another organization entirely. The consumer test (G1) would delete every MX event outright, since nothing in the current twelve-slice model consumes an MX at all. Applying this project's screens to this chapter does not trim it — it deletes it, essentially whole. That is worth deciding consciously rather than by omission, and it is the same conclusion the §3.6-§3.7 sweep reached independently about its own span.

THE CHAPTER'S SHARPEST 🟥s, so a later step does not re-derive them. (a) "Reported as an error" appears three times as a MUST and never once with a mechanism, a code or an audience — RoutingErrorReported carries the hole. (b) "Usable" is never defined anywhere in §5, and NoUsableMailExchangerFound turns on it. (c) An MX target that resolves to a CNAME "lies outside the scope of this Standard", so two conforming clients may do opposite things with the same broken zone. (d) The MAY at 3876-3877 permits a configurable limit with no minimum and the SHOULD at 3878 sets a floor of two — three lines apart, pointing opposite ways. (e) §5.2 contains ZERO RFC 2119 keywords across 3937-3956, so every IPv6 event is permission-less, and RFC 3974 is named and then explicitly declined as a standard. (f) Nothing bounds CNAME chain length or detects a cycle. (g) The randomization MUST has an escape hatch — "no clear reason to favor one" — with no criteria, which makes its violated arm nearly unfalsifiable.

REAL DATA. Every example is a real value. Where RFC 5321's own worked scenarios supply one, it is used in preference to the project set: Appendix D.3 gives xyz.com, XYZ.COM, foo.com, bar.com, <JQP@bar.com>, <Jones@XYZ.COM> and "Thu, 21 May 1998 05:33:29 -0700", and D.3 instantiates SelfIdentifiedInMailExchangerList, LowerPreferenceExchangersDiscarded, DeliveryAttemptFailedForAddress and NextHopChannelEstablished as one continuous story with real hosts. RFC 5321 gives no top-level-domain-only address anywhere, so TopLevelDomainAddressUsed uses museum, a real top-level domain, rather than a placeholder. No <address>, no <domain>, nothing invented.

---

## 1. Session Lifecycle

READS, LOOKUPS AND DISPLAYS (the kit's "guest viewed calendar for room availability" test)

1. GreetingBannerRead — a client reading the 220 banner to learn server type and version (§7.5, lines 4292-4306). Pure query: the client displays or logs what it read and no state changes on either side. Note the contrast with ClientNameVerificationRecorded, which was KEPT: there the RFC says the information IS captured, so a durable artifact exists. The banner has no such sentence.
2. EhloKeywordListRead — a client reading the advertised extension list. The read is a query; what changes state is the acceptance, carried as ServiceExtensionNegotiated.
3. VerificationLookupPerformed — the DNS query behind ClientNameVerificationRecorded (§4.1.4, lines 2481-2482). The lookup is the query; the recorded result is the event.
4. ReplyCodeInterpreted — a client determining its actions from a reply code (§4.2, lines 2610-2612) and interpreting only the first digit (§4.3.2, lines 3041-3043). A read of something already delivered.

NON-EVENTS — THE SPEC DESCRIBING SOMETHING THAT CHANGES NOTHING

5. ResetTreatedAsNoOp — §4.1.1.5 lines 2085-2089 name four positions where RSET "is effectively equivalent to a NOOP (i.e., it has no effect)": immediately after EHLO, before EHLO is issued in the session, after an end of data indicator has been sent and acknowledged, and immediately before a QUIT. In those positions BufferCleared does not occur, which makes the same command sometimes an event and sometimes not. 🟥 Hotspot: recorded here rather than as an event, because its content is that nothing happened.
6. RedundantGreetingAfterReset — §4.1.4 lines 2456-2459, "the sequence of RSET followed immediately by EHLO is redundant, but not harmful other than in the performance cost of executing unnecessary commands." A stated non-consequence.

BELONGS TO ANOTHER CHAPTER — cross-referenced, not rejected

7. MailTransactionAborted (RSET, a new EHLO, or QUIT; §4.1.1.5 lines 2081-2084, §4.1.1.10 lines 2221-2222, §4.1.4 lines 2505-2506) — Transaction chapter. Its aborting_command field is what ties it back to three events in this chapter. Also carries the boundary that RSET MUST NOT close the connection, "that action is reserved for QUIT".
8. PendingTransactionCanceled (§4.1.1.10 lines 2216-2219) — Transaction chapter. The durability boundary: MUST cancel the pending transaction, MUST NOT undo any completed one. Only has meaning because a session may carry several transactions (§4.1.4 lines 2506-2507, "There may be zero or more transactions in a session"), so a single-transaction walk never surfaces it. It is the consequence of ConnectionLossTreatedAsQuit.
9. ConnectionAttemptFailed (Appendix D.3 lines 4997-4999, narrated in prose, never on the wire) — Queue / Next-Hop Destination chapter. No session ever existed and no reply code exists for it, which is what makes it invisible to a model built from command-and-reply pairs.
10. PendingMailRetryTriggeredByInboundConnection — RECOVERED BY THE RE-SWEEP at §4.5.4.2 lines 3783-3785: "when the SMTP server receives mail from a particular host address, it could activate its own SMTP queuing mechanisms to retry any mail pending for that host address." No inherited candidate carried it. An inbound session causing outbound queue activation is a genuine cross-stream effect; the trigger is in this chapter, the effect belongs to the Queue chapter.
11. ServiceExtensionEnabled / ServiceExtensionRegistered — Server Configuration and Extension Registry chapters. The advertisement is per-session, the configuration is not, and the registration (IANA, Standards-Track RFC) is on a timeline measured in years.
12. ResourcesBecameUnavailable (§3.3 lines 1137-1141) — Transaction or Queue chapter. The precondition behind ServiceShutdownDetected and behind a 4yz on DATA, named by the RFC only in a parenthesis.
13. QuitOrResetRejectedForArguments (§4.3.2 lines 3067-3071, §4.1.1 lines 1777-1781) — RECOVERED BY THE RE-SWEEP, belongs to the Command Dialogue chapter. Commands specified as not accepting arguments SHOULD draw a 501 in the absence of EHLO-advertised extensions; clients MUST NOT send such parameters and servers SHOULD reject them.
14. UninitializedSessionCommandRefused (§4.1.4 lines 2490-2494) — RECOVERED BY THE RE-SWEEP, Command Dialogue chapter. NOOP, HELP, EXPN, VRFY and RSET can be used without initializing a session and servers SHOULD process them normally, "that is, not return a 503 code"; the violated arm is a server that returns 503 anyway.

CUT FOR THE FORTY-EVENT CAP, NOT REJECTED — each is written up in the notes of the event it sits beside

15. ConcurrentConnectionLimitConfigured — beside ConcurrentConnectionLimitReached.
16. SoftwareVersionAnnouncementEnabled and ConnectionRefusalLifted — the complements of two configuration events.
17. ResetAcknowledged — the "250 OK" MUST on an argument-free RSET (§4.1.1.5 line 2084), the exact counterpart of SessionInitialStateConfirmed. Beside BufferCleared.
18. CommandTimerStarted — the per-command timer arming (§4.5.3.2 lines 3615-3616). Marked doubtful in the raw set as close to the read/lookup line; beside CommandTimeoutExpired.
19. EightBitTransmissionEnabledForSession — Appendix A lines 4713-4718; beside ServiceExtensionNegotiated, with its 🟥 hotspot (the spec never says at what moment the rule changes) preserved there.
20. ServerHostNameConfigured — implied by §4.3.1 lines 3003-3006, where a host with no meaningful name uses an address literal in its greeting. Beside ServiceGreetingSent.
21. AddressLiteralTagRegistered — §4.1.3 lines 2403-2405, a Standardized-tag "MUST be specified in a Standards-Track RFC and registered with IANA". Extension Registry chapter; actors are the IETF and IANA.

---

### merge notes — 1. Session Lifecycle

HARVEST. Selected by meaning, not by the `timeline` field, as instructed — the 91 labels put this chapter's material under at least fourteen different strings (Session Lifecycle, Session Initiation, Session Establishment, Session Termination, Session and Channel, Service Lifecycle, Client Identification and Extension Negotiation, Protocol Negotiation, Client Timeout Discipline, Client Delivery Attempt, Conformance Observation, Abuse Defense, Service Provisioning, Mail Transaction). Fifty-seven raw candidates were pulled and read in full; forty events and twenty-one set-aside entries came out.

MERGES PERFORMED (dedupe by state change, best name kept, refs and fields unioned, every rejected name carried in the target's notes):
- ClientIdentityDeclared <- ClientIdentityAsserted + ClientIdentityDeclared + ClientIdentityDeclaredWithoutExtensions + ClientIdentityAnnounced (4).
- ServiceUnavailableAnnounced <- ServiceUnavailableAnnounced + ShutdownNoticeSent + ServiceShutdownAnnounced (3). The ShutdownNoticeSent candidate had itself flagged this merge for a facilitator; taken, with the strength difference between the two source paragraphs preserved in the notes rather than flattened.
- BufferCleared <- BufferCleared + TransactionBuffersReset + TransactionBuffersCleared (3).
- Two-way merges: TransmissionChannelOpened, ServiceGreetingSent, MailSessionRefusedAtConnectionOpening, HeloFallbackPerformed, ServiceExtensionsAdvertised, TransmissionChannelClosed, SessionEndRequested, ServerInactivityTimeoutExpired, TimeoutPolicyConfigured, ConnectionLostWithoutQuit, ConnectionClosedDefensively.

MERGES REFUSED, and why (a refusal is not an acceptance; a timeout is not a QUIT):
- SoftwareVersionAnnouncementDisabled was NOT merged with GreetingDisclosureConfigured: §3.1 gives a MAY to hide, §7.5 gives a SHOULD to disclose. Merging opposite-signed rules would erase a live tension in the spec.
- ServiceExtensionsAdvertised was NOT merged with ServiceExtensionNegotiated: the RFC's own words separate the offer from the acceptance.
- ServiceShutdownDetected was NOT merged with ServiceForciblyShutDown: different initiator, different normative strength (an exception to a MUST NOT versus a SHOULD).
- UnknownCommandRejected and ConnectionClosedInViolation kept as the honored and violated arms of one §3.8 paragraph.
- BufferCleared and StateTableReset kept apart on §2.3.6's own two-device split, with the caveat that the same sentence attributes both consequences to one of them.

SPLITS PERFORMED — three fused candidates were separated, which is the other half of what the cap did: SessionClosureRequested fused QUIT + 221 + close (now SessionEndRequested, ClosingReplySent, TransmissionChannelClosed); ClientIdentityAnnounced fused the client's claim and the mutual initial state (now ClientIdentityDeclared and SessionInitialStateConfirmed); ConnectionLostUnexpectedly fused the loss and the server's interpretation (now ConnectionLostWithoutQuit and ConnectionLossTreatedAsQuit). Each fused candidate had asked for its own split in its notes and been unable to spend the slot.

RE-SWEEP — SEVEN EVENTS RECOVERED, plus three more assigned to other chapters. Every anchor was read in full: §2.3.6 (739), §3.1 (959), §3.2 (985), §3.8 (1640), §4.1.1.1 (1783), §4.1.1.5 (2079), §4.1.1.10 (2205), §4.5.3.2 (3610), §4.5.4.2 (3774), plus §4.1.4, §4.2.2, §4.3.1, §4.3.2, §4.5.1, §7.5, §7.8, Appendix A, D.1 and F.1 for cross-cited material.
Recovered into the roster: ConcurrentConnectionLimitReached (§4.5.4.2 3778-3781 — untouched by all 352 candidates); InterveningCommandRefusedAfterSessionRefusal (§3.1 976-979 — folded away in a note); LegacyClientIdentificationStringSent (§4.1.1.1 1802-1807 — untouched, and its advice is non-2119 prose, invisible to a keyword sweep); SessionInitialStateConfirmed (§4.1.1.1 1822-1825 — split out of a fusion); ClientAbandonedSessionWithoutQuit (§4.1.1.10 2212-2214 — the violated arm of a MUST NOT, left as a hotspot note); ConnectionLossTreatedAsQuit (§4.1.1.5 2100-2102 — split out of a fusion); ConnectionFailureTreatedAsTransientError (§3.8 1671-1676).
Recovered but assigned elsewhere: PendingMailRetryTriggeredByInboundConnection (§4.5.4.2 3783-3785), QuitOrResetRejectedForArguments (§4.3.2 3067-3071), UninitializedSessionCommandRefused (§4.1.4 2490-2494).
The single clearest measurement of the cap's cost: the inherited ConnectionLost candidate quoted §3.8 lines 1671-1674 up to the closing parenthesis and stopped, capturing the subordinate clause and dropping the main clause — the SHOULD, the 451, and "act accordingly" — which is an entire normative arm and the hinge between this chapter and the Queue chapter.

CITATION ERRORS FOUND IN THE INHERITED CANDIDATES (rule 3 — every kept citation was re-opened in /home/ivan/FnEmail-Model/docs/rfc/rfc5321.txt and confirmed by finding its heading line first):
1. ServiceExtensionsAdvertised carried ehlo_greet = "foo.com Simple Mail Transfer Service Ready" — that is the 220 greeting text, not the ehlo-greet production. D.1 line 4888 shows "S: 250-foo.com greets bar.com", so under the ABNF at lines 1841-1844 the Domain is foo.com and the ehlo-greet is "greets bar.com". Corrected in the field example.
2. ConnectionClosedForAbuse cited §7.8 lines 4351-4353 for a quote sitting on line 4352 alone; the sentence runs 4349-4353 and is cited that way in ConnectionClosedDefensively.
3. TimeoutPolicyConfigured's source candidate had already caught its own inference and said so; that precision is preserved rather than dropped — the "easily reconfigurable" SHOULD is addressed to clients, and extending it to the server side is an inference, not a quotation.
No inherited citation was found to be fabricated; all section headings resolved where claimed.

RFC INCONSISTENCY FOUND IN THE RE-SWEEP, carried in GreetingRejected: §4.1.4 lines 2461-2462 enumerates 501, 500, 502 and 550 as the greeting-failure replies, while the §4.3.2 sequence table at lines 3096-3099 gives 504, 550 and 502 for the same command. Neither is marked illustrative; 501 and 500 are absent from the table and 504 is absent from the prose.

FIELDLESS-EVENT TENSION, recorded where it applies and not resolved, per the brief: SessionInitialStateConfirmed and TransmissionChannelClosed are the two events where the kit's permission ("it is fine to have no fields or just the identity key") and its warning ("An event without fields is an opaque label") both bite. In both cases a small payload was kept because it is what distinguishes the arms — closed_by and preceding_command separate a graceful close from a unilateral one — and the tension is named in each event's notes rather than argued away. The inherited TransactionBuffersReset candidate was genuinely fieldless; merging it into BufferCleared gave it a session key, which is the one field its own notes said was worth adding.

PROJECT SCREENS: none was used to reject anything. Every candidate that a project screen would have killed is in the roster with the screen named in its notes — HELO-only charter (7 events), inbound-only scope (9), altitude tiers (11), consumer test G1/G2 (12), one-event-per-command target (6), condemned status/phase register (4), responsibility-boundary cutoff (4), twelve-slice model (5). Status-shaped events kept deliberately: ConnectionRefusalConfigured, StateTableReset, SessionInitialStateConfirmed, MailSessionRefusedAtConnectionOpening.

STREAM ROOTS: Session (28), Server Configuration (4), SMTP Service Instance (3, invented — Server Configuration is a settings root and these are runtime-instance facts about a listening process, so a new root was coined and is named as invented in each event's notes), Next-Hop Destination (1), Transaction (1). US spelling checked across this output: color/behavior/modeling/organize/analyze/recognize forms only; the grep in AGENTS.md rule 1 was run against the composed text and returns nothing.

---

## 7. Sending, Queueing and Retry

Reads, lookups, displays and computations from this chapter's material. Each names the event that DOES change state, so nothing is lost.

1. Consulting the list of hosts the client cannot reach before attempting delivery (§4.5.4.1, 3716-3718) — a lookup. HostMarkedUnreachable is the write.
2. Reading a cached negative response instead of re-contacting a server (§4.5.4.1, 3746-3750) — a read. NegativeResponseCached is the write, and note the RFC's caution attaches to the write, not the read.
3. Selecting the next queue entry or destination to attempt in a retry cycle (§4.5.4.1, 3736-3739) — a query over the queue. QueueRunStarted, DeliveryAttemptFailed and RetryScheduled are the writes.
4. Determining that a delivery attempt has failed by waiting out a timeout (§4.5.4.1, 3739-3744) — an inference from a timer. DeliveryAttemptFailed is the recorded fact.
5. An ETRN-style "send queues now" request (§4.5.4.1, 3728-3730) — a command, not an event. QueueFlushTriggeredByPeerContact is the resulting state change, and the RFC's point at 3728-3730 is that the command often becomes unnecessary.
6. Reading the configured retry parameters before scheduling an attempt (§4.5.4.1, 3713-3714) — a read of state written by RetryParametersConfigured.
7. Comparing the fallback timeout against the bounce timeout to check conformance (§2.2.3, 600-602) — a computation over two configured values. ExtensionFallbackTimeoutElapsed and MessageBouncedBeforeFallbackAttempted are the events at each outcome.
8. Determining whether the next hop supports a required extension (§2.2.3, 595-598) — a lookup against the EHLO response. MessageRequeuedForExtensionSupport and AlternateMailExchangerTried are the writes.
9. Counting how many queued recipients share a destination server before batching (§4.5.4.1, 3759-3762) — a query. RecipientsGroupedIntoSingleTransaction is the write.
10. Reading how long a message has been in the queue to decide whether the first hour has passed (§4.5.4.1, 3720-3723) — derived from the queued_at already carried by MessageQueuedForDelivery. DestinationBackoffWidened is the write.
11. A timer running between arming and expiry (§4.5.4, 3681-3682) — the passage of time is not an event. QueueActivityTimedOut is the expiry.
12. A server accepting a TCP connection on the listening socket, and refusing one at capacity (§4.5.4.2, 3776-3781) — arguably events, but §4.5.4.2 is the session-lifecycle chapter's anchor and claiming it here would duplicate. Set aside to avoid taking another chapter's material.
13. An operator inspecting the outbound queue — the nearest true "guest viewed calendar for room availability" analogue in this chapter. Implied by the queueing MUST at 3690-3692 and by every retry discussion, stated nowhere in the RFC, and it changes nothing. Recorded so a later reader need not rediscover that it was considered and set down.

---

### merge notes — 7. Sending, Queueing and Retry

SCOPE AND METHOD. Harvested by meaning across all eleven sweeps, ignoring the `timeline` field entirely as instructed — the chapter's material arrived under at least eight different labels (Queue and Retry, Outbound Queue and Retry, Client Delivery Attempt, Delivery Attempts and Queue, Message Relay, Route Resolution, Command Dialogue, DNS and Reachability, Final Delivery). Twenty-three inherited candidates were pulled; they merged down to 14 events. The re-sweep of §4.5.4 (3671), §4.5.4.1 (3685) and §2.2.3 (586), read in full at the file, added 21 more. Final count 35.

MERGES, with every rejected name preserved on the surviving event. MessageQueuedForDelivery absorbed MessageQueuedForSubsequentDelivery and MessageQueuedAfterTemporaryDnsError (same state change, different trigger; the §5.1 MUST is carried on the merged event so the address-resolution chapter can cross-reference instead of re-declaring). DeliveryAttemptFailed absorbed a second identical candidate and ConnectionAttemptFailed from Appendix D.3. RetryScheduled absorbed DeliveryRetryScheduled, preserving the fact that the same state change is reached by the accepting server discharging its §4.2.5 obligation and by the client's own queue runner. MessageRequeued absorbed MessageRequeuedByClient and MessageRequeuedForRetry. DeliveryGivenUp absorbed a second identical candidate and the timeout arm of MessageBouncedAsUndeliverable.

REFUSED MERGES, per the instruction that a refusal is not an acceptance. DeliveryAttemptFailedForAddress (§5.1 3874-3876) stays with the address-resolution chapter — it is the per-address fact inside one host-level attempt. MessageRequeuedForExtensionSupport stays separate from MessageRequeued: a capability mismatch found before any failure, starting a second clock. CommandRetryAttempted stays separate from every queue-level retry — the RFC's 4yz test depends on a command being "repeated identically", which has no queue-level analogue. QueuedMessageReturnedToUser stays separate from DeliveryGivenUp: a local hand-back generates no mail. MessageCopyCreated and DuplicateMessageCopiesDelivered were harvested, judged to belong to the list-expansion and final-delivery chapters, and cross-referenced from RecipientsGroupedIntoSingleTransaction and DeliveryAttemptFailed rather than claimed.

FIELDS DROPPED AS DERIVED, per the merge rule: attempt_number and attempts_made (foldable from DeliveryAttemptFailed), messages_released and copies_delivered (counts), cached_until (cached_at plus an unstated lifetime), and any queue depth. per_connection_timeout_seconds and rcpt_limit_per_mail were kept — those are configured facts, not computations.

WHAT THE RE-SWEEP RECOVERED — 21 events, marked individually in their notes. The largest single miss was QueuedMessageTransmitted: line 3709 states two terminal arms for the retry loop and every inherited candidate carried only the failure one, leaving a queue that could end only badly. §4.5.4's own opening paragraph (3671-3683) had been skipped entirely by all eleven sweeps, which jumped to §4.5.4.1; it yielded QueueActivityTimedOut and ErrorMessageSentInResponseToErrorMessage, the latter from the most absolute wording in the chapter ("under any circumstances", 3683). Violated arms recovered where only the honored arm existed: UnsentMessageDiscardedWithoutQueuing (MUST 3690-3692), DestinationRetriedWithoutDelay (MUST 3703-3704), MailCommandRejectionCached (MUST NOT 3749-3750), SeparateCopyTransmittedPerRecipient (SHOULD 3762-3767), SendingSystemBlockedByRetryCycle (the consequence arm of SHOULD 3716-3718), MessageBouncedBeforeFallbackAttempted (SHOULD 600-602). Named-in-notes-but-never-modeled siblings recovered: QueueEntryFlaggedForImmediateAttention, OutboundConcurrencyLimitReached, AlternateMailExchangerTried, MessageDowngradedToUnextendedFormat, QueuedMessageReturnedToUser, RecipientListSplitAcrossTransactions. Unmodeled paragraphs recovered: DestinationBackoffWidened (3720-3723, the chapter's named per-destination backoff topic, which existed only as commentary), QueueRunStarted (3687-3688 and 3737-3738), NotificationDeliveryAbandoned (3710-3713), ExtensionListChangedWithinSession (3747-3749), RetryStrategyAdjustedForMultipleAddresses (3732-3734). Two invented, both doubtful, both hotspots: HostReachabilityRestored and CachedNegativeResponseInvalidated — the RFC writes to both the unreachable list and the negative cache and never erases from either.

CITATION CHECK, rule 3. Every section heading was located before citing: §2.2.3 at 586, §3.6.3 at 1475, §4.1.1.10 at 2205, §4.2.1 at 2642 with the 4yz text at 2667-2680, §4.2.5 at 2941, §4.5.4 at 3671, §4.5.4.1 at 3685, §4.5.4.2 at 3774, §4.5.5 at 3787, §5.1 at 3824. All quotes re-read at their lines. ONE INHERITED CITATION ERROR FOUND: MessageRequeuedForRetry cited §4.1.1.10 lines 2215-2219 as authority for a CLIENT requeueing after a premature close, but the text reads "the server MUST cancel any pending transaction, but not undo any previously completed transaction, and generally MUST act as if the command or transaction in progress had received a temporary error (i.e., a 4yz response)" — the obligation named there is the server's. The reference was dropped from the merged MessageRequeued and replaced with §4.2.5 2961-2964, which does state the client's retained responsibility. Also worth flagging for whoever owns §4.1.1.10: the RFC hedges its own MUST with "generally" at 2218. Two inherited line ranges were slightly loose and are tightened here without comment (3690-3692 for the queueing MUST; 3709-3710 for the give-up sentence). A note on §4.5.4.2: it sits inside my §4.5.4 anchor but is the session-lifecycle chapter's own anchor, so I cite 3783-3785 only where an inherited candidate already did (QueueFlushTriggeredByPeerContact) and set aside the listener and inbound-capacity events rather than claim them.

STREAM ROOTS. Queue Entry carries most of the chapter and is the right root because the RFC itself defines the entry as message plus envelope (3692-3693) with a lifetime measured in days. Next-Hop Destination carries everything scoped per destination rather than per message — the delay MUST is explicitly per destination (3703-3704), and that scoping is the same insight behind the unreachable list and the negative cache. Server Configuration carries RetryParametersConfigured. Session carries the two events that live inside one connection. OUTBOUND QUEUE IS INVENTED and named as such: none of the offered roots covers the queue as a whole rather than one entry, and four events need it (QueueRunStarted, SendingSystemBlockedByRetryCycle, OutboundConcurrencyLimitReached, ErrorMessageSentInResponseToErrorMessage). Message is used once, for UnsentMessageDiscardedWithoutQueuing, precisely because no queue entry ever came into existence there.

TENSIONS RECORDED, NOT RESOLVED. (a) The kit's fieldless-event tension bites on QueueEntryFlaggedForImmediateAttention and QueueRunStarted, which are near-identity-only; noted on the first. (b) DestinationRetriedWithoutDelay covers a MUST violation and a SHOULD shortfall in one event, distinguished only by a field value; splitting is defensible. (c) SeparateCopyTransmittedPerRecipient is a SHOULD violation when destinations match and correct behavior when they differ — same shape, different cause. (d) Whether HostReachabilityRestored and QueueFlushTriggeredByPeerContact are one event or two is open. (e) Two notions of "copy" collide: §3.9 1701-1703 forwards copies per expanded mailbox, §4.5.4.1 3759-3762 says one copy on the wire.

WHICH SCREENS WOULD HAVE KILLED THIS CHAPTER. Effectively all of it. The inbound-only scope takes every event here except ExtensionListChangedWithinSession. The responsibility-boundary cutoff takes the entire queue lifetime, which begins exactly where that cutoff ends. The twelve-slice model has no queue, no timer and no configuration actor, so most of these have nowhere to land rather than being rejected on merit. The one-event-per-command target cannot express the five time-triggered events (ExtensionFallbackTimeoutElapsed, DestinationBackoffWidened, QueueActivityTimedOut, DeliveryGivenUp, NotificationDeliveryAbandoned) because no command causes them. The condemned status/phase register would take HostMarkedUnreachable and every violation-arm event. The HELO-only charter takes the four extension events outright. Named on each event as instructed; nothing was rejected on any of these grounds.

---

## 8. Relaying

Reads, lookups, displays and non-changes, each with the reason it is not an event, per the \"guest viewed calendar for room availability\" test. None deleted silently.

1. NEXT-HOP SELECTION FROM THE DNS — §3.6.2 lines 1452-1454, \"establishes a transmission channel to the next SMTP server specified in the DNS (according to the rules in Section 5)\". Consulting the DNS to decide where to go is a lookup; what changes state is the channel existing (NextHopChannelEstablished) or failing to (NextHopChannelRefused). The sorting and preference rules the sentence defers to live in §5.1 (heading line 3824, verified) and belong to the routing chapter, where sweep candidates SelfIdentifiedInMailExchangerList (#264) and LowerPreferenceExchangersDiscarded (#265) already sit.

2. TRACE-HEADER EXAMINATION FOR LOOP DETECTION — §3.6.3 lines 1526-1528, \"to attempt to detect looping in the mail system\", and §6.3 lines 4067-4069, counting Received: fields. The counting and comparing is a read of the message. The VERDICT is kept as MessageLoopDetected, because after it the relay does something it would not otherwise do.

3. THE RETURN-PATH VERIFICATION ATTEMPT ITSELF — §3.6.2 lines 1470-1471, \"A server MAY attempt to verify the return path\". The attempt is a query against SPF, DKIM or whatever the operator chose; only the recorded verdict is kept, split into ReturnPathVerified and ReturnPathVerificationFailed. Flagged rather than resolved: whether a verdict is an event or a decision-model fold is exactly the question the kit leaves open.

4. CONSULTING THE RELAY POLICY AT RCPT TIME — the read of whatever RelayPolicyConfigured and RelayAccessRestrictedToKnownSources established. §3.6.2 line 1463 says only \"for policy reasons\"; the consultation is a fold over prior configuration events, and the state change is RelayTaskAccepted or RelayTaskDeclined.

5. RECOGNIZING SOURCE ROUTE SYNTAX — §4.1.1.3 lines 1925-1926, \"Receiving systems MUST recognize source route syntax\". Recognition is parsing, not a state change, even though it carries a MUST. The state changes that follow it are kept: SourceRoutedAddressAccepted, SourceRoutedAddressRefused, SourceRouteStripped, SourceRouteHonored. Sweep #144 had already set aside a candidate SourceRoutedPathEncountered on the same ground and it is not resurrected here.

6. THE CLIENT REFRAINING FROM GENERATING A SOURCE ROUTE — the honored arm of the SHOULD NOT at §3.6.1 lines 1427-1428. An absence, not a completed state change; there is no moment at which not-doing-it happens. Named here so that the two-arm sweep is visibly complete rather than silently short. Contrast MessageContentPassedThroughUnmodified, which IS kept, because §2.3.10 lines 811-814 make non-modification constitutive of the relay role rather than merely the absence of an act — a distinction worth arguing about in Step 2.

7. DETERMINING WHETHER A RECIPIENT IS LOCAL OR REQUIRES RELAYING — implied by §3.6.2 line 1450-1451, \"in the same way it accepts or rejects mail for a local user\". The determination is a read of configuration; accept and decline are the events.

8. THE ASSESSMENT THAT SMTP IS UNSUITED TO SUBMISSION — §3.6.3 lines 1483-1484, \"SMTP, as specified here, is not ideally suited for this role.\" An editorial judgment. No state changes.

9. THE SCOPE DISCLAIMER ON PRIVATE SUBMISSION ARRANGEMENTS — §3.6.3 lines 1486-1488. A statement about the document, not about a mail system.

10. MX RECORDS POINTING AT GATEWAYS — §3.6.3 lines 1490-1492, \"It is important to note that MX records can point to SMTP servers that act as gateways into other environments\". A statement of possibility, and the publication act itself belongs to the routing chapter, where sweep candidates MailExchangerRecordPublished (#63, #64) and MxRecordsPublished (#315) already carry it. Deliberately NOT given a slot in this chapter to avoid duplicating a neighbor; the sibling MailExchangerPointedAtGateway is named here so it is not lost.

11. §7.9's COUNSEL OF RESTRAINT — lines 4359-4365, \"considerable care should be taken and balance maintained\". Advice to operators with a lowercase should; the adoptable decision is kept as SelectiveAcceptancePolicyAdopted, and there is no observable moment at which care is or is not taken.

12. THE IMPLEMENTOR'S FILTERING CAPABILITY — §7.9 lines 4377-4379, \"implementations SHOULD provide the capability to perform this type of filtering\". A real state change, but of a software product rather than of a mail system: the actor is a vendor and the stream root would be something like Product Release. Named here as RelayFilteringCapabilityProvided rather than spent as one of the forty slots; recover it if the roster ever admits product-altitude events.

---

### merge notes — 8. Relaying

HARVEST. Selection was by state change, not by the timeline label, exactly as instructed — the eleven sweeps used 91 labels and the relay material was scattered across at least nine of them (\"Relaying\", \"Routing Configuration\", \"Source Routing\", \"Return Path\", \"Non-Delivery Notification\", \"Trace and Loop Detection\", \"Mail Transaction — Envelope\", \"Relay and Routing\", \"Session and Transaction Policy\", \"Message Repair\"). The §3.6 span sweep contributed a contiguous block (candidates 64-87); §7.9 contributed one (302); the rest came from spans covering §3.3, §4.1.1.3, §5.1, §6.1, §6.4 and Appendix C.

MERGES PERFORMED, all by state change and never by name similarity:
- RelayTaskDeclined absorbed RelayingDeclined (§3.3) and RecipientRejectedForPolicy (§4.1.1.3) — one refusal seen from three sections.
- SmtpClientRoleAssumed absorbed RelayRoleAssumed (§2.1/§2.3.2).
- MailRelayedOnward absorbed both candidates named MessageRelayedToNextHop (§4.1.1.3 and §5.1).
- RelayTaskAccepted absorbed RelayResponsibilityAccepted (Appendix D.3/§7.1).
- SourceRouteStripped absorbed SourceRouteIgnored (§3.3) and the second SourceRouteStripped (§4.1.1.3).
- RelayPolicyConfigured absorbed RelayingPolicyConfigured (§3.3) and RelayDomainAuthorized (§3.5.3).
Every displaced name is recorded in the surviving event's notes.

MERGES REFUSED, per the instruction that a refusal is not an acceptance and a relay's send is not an originator's:
- DeliveryAttemptFailed kept apart from DeliveryDeterminedImpossible (one attempt versus a final verdict — conflating them is how retry logic disappears).
- UndeliverableMailSilentlyDiscarded kept apart from NotificationSuppressedToPreventLoop (identical silence, opposite conformance).
- MailRelayedOnward kept apart from MessageSubmittedForDistribution (a relay's send is not an originator's).
- RelayTaskAccepted kept apart from DeliveryResponsibilityAccepted (§6.1's 250-after-DATA custody transfer, left to the transaction chapter).
- SourceRouteStripped kept apart from OwnDomainRemovedFromForwardPath (Appendix C's narrower MUST, left to the appendix chapter).
- RelayPolicyConfigured kept apart from RelayAccessRestrictedToKnownSources (destination-based versus source-based) and from SelectiveAcceptancePolicyAdopted (relaying versus acceptance at large).
Two inherited candidates were SPLIT because they had packed opposite outcomes into one record: ExplicitSourceRouteGenerated yielded InvalidSourceRouteGenerated (SHOULD NOT with an escape hatch versus MUST NOT without one), and ReturnPathVerified yielded ReturnPathVerificationFailed (a Boolean field was hiding two events with different consequences).

RE-SWEEP. Thirteen events were recovered by reading §3.6, §3.6.1, §3.6.2, §3.6.3 and §7.9 in full and asking, for every MUST, SHOULD and MAY, what the missing arm would be: RelayDeclinedWithNonStandardCode, NextHopChannelRefused, UndeliverableMailSilentlyDiscarded, NotificationSentWithNonNullReversePath, NotificationLoopGenerated, MessageContentPassedThroughUnmodified, MessageContentInspectedByRelay, InvalidSourceRouteGenerated, SourceRoutedAddressAccepted, SourceRouteHonored, ReturnPathVerificationFailed, RelayAccessRestrictedToKnownSources, RelaySourceAuthorized, SelectiveAcceptancePolicyAdopted, RelayUsedToObscureOrigin. The pattern in what the cap had cut is consistent and worth stating: the violated arms of the strongest rules, and the honored arms of the prohibitions. The chapter's own charter phrase \"content left untouched\" had NO event at all in the inherited set — only its violation, MessageDataModifiedByRelay — which is the clearest single sign the raw set was truncated rather than exhausted. Three of the recoveries came from one sentence, §3.6.3 lines 1524-1527, because a single MUST NOT there governs two verbs (\"inspect or act upon\") and carries two carve-outs.

CITATION WORK, under rule 3 — every section heading was located before citing and every quoted line range re-read in /home/ivan/FnEmail-Model/docs/rfc/rfc5321.txt. Headings confirmed: §3.6 line 1419, §3.6.1 line 1421, §3.6.2 line 1446, §3.6.3 line 1475, §7.9 line 4355; cross-referenced headings confirmed at §2.3.10 line 801, §3.3 line 1015, §3.7.1 line 1547, §4.1.1.3 line 1916, §4.4 line 3156, §4.5.5 line 3787, §5.1 line 3824, §6.3 line 4065, §6.4 line 4074.

ERRORS FOUND IN THE INHERITED CANDIDATES:
(a) Candidate #302 cited §7.9 lines \"4357-4358\" for a sentence that runs 4357-4359, and stored the quote truncated mid-clause (\"an SMTP server may refuse to\") in a form no checker could match; both corrected.
(b) Candidate #80 reported loop detection as simply optional on the strength of §3.6.3 line 1527. §6.3 lines 4071-4072 say \"servers MUST contain provisions for detecting and stopping trivial loops.\" Both are in force and the tension is now recorded on the event rather than resolved.
(c) Candidate #81 used \"Received:\" as its example of a header field a relay modified. Modifying a Received: line is separately and more strictly forbidden by §4.4 lines 3178-3179, so the example conflated two prohibitions; changed to \"Subject:\".
(d) Candidate #75 was RIGHT about the page-break trap and it is preserved: the 550 sentence begins at the end of line 1454 and resumes at line 1463, with the footer and running header at 1455-1462. Confirmed by reading the file.
(e) Candidate #85 was RIGHT that the \"must\" at line 1503 is lowercase and therefore not an RFC 2119 keyword. Confirmed.
Every other quote used here was checked character by character against the file and copied exactly, British spellings and all, per rule 2 (none appear in this chapter's quotes; the RFC uses US spelling throughout the anchored sections).

CHAPTER BOUNDARY. §3.6.3 contains the undeliverable-notification obligation and the null reverse-path rule, which a bounce or queue chapter may also claim. They are kept here because §3.6.3 is one of this chapter's anchors and because the MUST at line 1494 hangs off RelayTaskAccepted — separating them would leave the obligation with no visible trigger. Whoever merges chapters should dedupe against a notification chapter rather than assume a gap. Conversely, MailExchangerRecordPublished was DROPPED from this chapter despite being cited from §3.6.2, because the routing chapter carries it twice already; it is named in set_aside so the drop is visible.

STREAM ROOTS. Two were invented because none of the offered candidates fits, and both are logical roots in domain language, not DDD aggregates. \"Relay Task\" comes from the RFC's own noun at line 1450 (\"the task of relaying the mail\") and is per-address — Recipient was the alternative and was rejected because the task outlives the RCPT that created it, surviving into the queue. \"Notification Message\" is the bounce as its own thing: it has a reverse-path of its own, rules of its own, and a prohibition on generating one about another.

FIELDLESS-EVENT TENSION, recorded where it applies rather than resolved: SmtpClientRoleAssumed carries two fields, one of which is only an identity key, and is the closest thing here to \"an opaque label\". It has not been padded. NullReversePathSet and NotificationSuppressedToPreventLoop are similarly thin by nature.

SCREEN ACCOUNTING. Not one candidate was rejected by a project screen. Where a screen would have killed something it is named on the event: the responsibility-boundary cutoff and inbound-only scope account for most of this chapter (relaying is by definition the outbound half), the consumer test / G1 would kill every non-send and every violation event, the condemned status register would kill SmtpClientRoleAssumed, the altitude tiers would file all four Server Configuration events as Infrastructure, and the one-event-per-command target has no shape for MailRejectedForPolicyReason, which lands on three different commands.

---

## 9. Gatewaying

Reads, lookups, displays and non-changes from this chapter's anchors — the "guest viewed calendar for room availability" pile. (1) INSPECTING THE MESSAGE BODY AT A GATEWAY, §3.7.1 lines 1550-1552 ("This may involve inspecting the message body") — the inspection changes nothing; HeaderFieldRewrittenAtGateway is the event it feeds. (2) INTERPRETING THE LOCAL-PART OF THE DESTINATION ADDRESS, §3.7.1 lines 1551-1552 — also a read, but flagged rather than filed quietly: §2.3.11 lines 847-849 makes it a MUST-level trespass ("the local-part MUST be interpreted and assigned semantics only by the host specified in the domain part of the address"), so a facilitator may want a card for the trespass even though the act itself produces no state change. This is the one item in this pile I would most expect to be argued back. (3) DEBUGGING USE OF RECEIVED: LINES, §3.7.2 lines 1583-1585 — an operator reading trace data changes nothing in the mail system; it is the rationale for the MUST NOT, not an event. (4) THE DEFINITIONAL SENTENCE naming a boundary system a "gateway" or "gateway SMTP", §3.7 lines 1536-1539 — vocabulary, not an occurrence; the crossing it describes is kept as MessageCrossedTransportEnvironmentBoundary. (5) THE FOUR-ROLE TAXONOMY, §2.3.10 lines 803-805 ("This specification makes a distinction among four types of SMTP systems, based on the role those systems play") — a classification scheme, not a state change. Worth recording that role is PER-MESSAGE, not a fixed property of a host: one host is originator, relay, gateway and delivery system across different messages, which is why every event in this chapter carries an actor role rather than assuming one. (6) CLASSIFYING A FIREWALL AS A GATEWAY, §2.3.10 lines 822-824 — the classification is a fold over the fact that the box rewrites addresses; AddressRewrittenByGateway is the fact. Kept out on the "queries are not events" test, not on any project screen. (7) THE SCOPE CONCESSION, §3.7 lines 1541-1543 ("is complex and does not easily yield to standardization") — a statement about the specification, not about mail. (8) CHECKING WHETHER THE FAR-SIDE TRANSPORT MAINTAINS THE ENVELOPE SENDER SEPARATELY, §4.4 lines 3264-3266 ("unless it is known that") — the checking is a read of configuration; ForeignTransportEnvelopeCapabilityRecorded is the event that put the fact there. (9) COUNTING RECEIVED: HEADER FIELDS to look for a loop, §6.3 lines 4067-4069 — the count is a read; the loop-control chapter's MailLoopDetected is the verdict. Cited here only because ReceivedLineOmittedAtBoundaryCrossing corrupts the count. (10) BORDERLINE, FLAGGED NOT FILED: ViaClauseOmittedByGateway is kept as an event rather than set aside, though an omission produces no record at the gateway and is only observable downstream. If a facilitator moves it into this pile, ReceivedLineOmittedAtBoundaryCrossing must move with it — the two stand or fall on the same reasoning, and splitting them would be incoherent.

---

### merge notes — 9. Gatewaying

WHAT WAS HARVESTED AND MERGED. Selection was by state change, not by the `timeline` field — the eleven sweeps labeled this chapter's material "Mail Gatewaying", "Relay and Gateway Transit", "Gateway Translation", "Gateway Bridging", "Trace and Loop Detection", "Return Path and Error Routing" and "Origination and Submission", seven labels for one chapter. Fifteen inherited candidates were claimed; one merge was performed (TransportServiceTranslationPerformed folded into MessageCrossedTransportEnvironmentBoundary, its name carried into notes). Four inherited candidates were deliberately NOT merged despite surface similarity, each cross-referenced instead: MessageDataModifiedByRelay against MessageTransformedByGateway (same act, opposite verdict by role); ReceivedLineChainAltered (§4.4) against ExistingReceivedLineAlteredByGateway (general integrity rule versus the gateway "fix"); ReturnPathRemoved (§4.4 lines 3229-3231, a MAY for any forwarding system) against ReturnPathDeletedByInboundGateway (§4.4 lines 3268-3269, a SHOULD for a gateway); MailLoopDetected against MessageRemailedIntoOriginatingEnvironment (a detection is not the thing detected). CEDED TO OTHER CHAPTERS, with the reason, so nothing falls between: MessageIntroducedIntoTransportEnvironment (origination — a first entry, not a crossing); LocalPartRewrittenByIntermediateHost (addressing — general intermediate host, not a gateway); MessageRelayedToNextHop and ReversePathRewrittenBeforeRelay (relay); TraceLinePrepended, TraceRecordInserted, ReturnPathLineInserted (trace); AddressCorrectedToFqdn (message repair — but §3.7.1 explicitly licences a gateway to override §6.4, so the two chapters must stay aware of each other); MailExchangerRecordPublished (routing, overlapping GatewayDesignatedForTransportBoundary).

WHAT THE RE-SWEEP RECOVERED — thirteen events the cap cut. Five were named as folded-away candidates in the inherited notes and given no slot: ExistingReceivedLineAlteredByGateway, PrivateRecipientDisclosedByEnvelopeFolding, NonstandardAddressFormatAccepted, ForeignErrorRoutedToReversePath, ReturnPathDefaultedToOriginatorAddress. Six are VIOLATED ARMS that no sweep carried at all — the pattern the brief predicted: ReceivedLineOmittedAtBoundaryCrossing, MailRejectedOnTraceHeaderFormat, ValidAddressFormatRefusedByGateway, NonconformingHeaderAddressForwardedIntoInternet, ForeignErrorRoutedToHeaderOriginator, MessageGatewayedFromHeaderFieldsAlone, plus ViaClauseOmittedByGateway. Two came from APPENDIX E, which the roster's anchor list does not name even though its heading is "Other Gateway Issues" (verified at line 5128): LayeringSemanticsPreservedAcrossBoundary and EnvelopeInformationLostInTranslation. Two more from cross-reference sentences that state no rule of their own and are therefore the first thing a truncated pass drops: SourceRouteIgnoredByGateway (§3.7.3 lines 1600-1602 into §3.3) and ForeignTransportEnvelopeCapabilityRecorded (the unexplained "unless it is known that" at §4.4 lines 3264-3266). GatewayDesignatedForTransportBoundary and MessageRemailedIntoOriginatingEnvironment complete the set.

CITATION CHECK ON THE INHERITED CANDIDATES (AGENTS.md rule 3). Every section heading was located before citing: §2.3.10 at line 801, §3.7 at 1531, §3.7.1 at 1547, §3.7.2 at 1575, §3.7.3 at 1594, §3.7.4 at 1604, §3.7.5 at 1631, and for cross-references §3.3 at 1015, §6.3 at 4065, §6.4 at 4074, §7.2 at 4212, Appendix B at 4720, Appendix E at 5128. Every inherited line range in this chapter's material checked out; no bad citation was found among the fifteen claimed candidates. Two corrections to inherited CLAIMS rather than line numbers: (a) the ViaClauseRecorded candidate proposed a field via_protocol with example "P1", which the RFC does not support — §4.4's ABNF gives Via only a Link (line 3332; Link = "TCP" / Addtl-Link at 3350) and puts Protocol in the separate With clause (line 3334), so §3.7.2's request for "the environment and protocol in the 'via' clauses" cannot be satisfied literally. The field is renamed via_link and the contradiction is recorded as a hotspot. (b) The MailAcceptedDespiteMalformedTraceHeader candidate's range "3.7.2 L1586-1589" starts one line late — the sentence begins "As another consequence" at line 1585; corrected here.

LOWERCASE KEYWORDS — the chapter's recurring trap. Four sentences read as normative and are not: §2.3.10 line 823 "firewalls that rewrite addresses should be considered as gateways"; §3.7.5 line 1637 "the gateway must select and use a best approximation"; Appendix B line 4776 "must come from some source in the other environment"; Appendix E line 5138 "must be written with the understanding". The §3.6-3.7 sweep flagged two of these in extras.md as a pattern; the re-sweep found two more. Reporting any of them as MUST or SHOULD overstates the requirement, which is exactly the failure rule 3 records from 2026-08-06.

WHAT THIS CHAPTER LOOKS LIKE. Thirty-two events, of which twenty-nine have a gateway as actor, one an operator, one a foreign remailer, and two an ordinary receiving server. Only MailAcceptedDespiteMalformedTraceHeader and MailRejectedOnTraceHeaderFormat survive the project's inbound-only charter; the other thirty are deleted by it, not trimmed. Named screens that would have rejected candidates kept here, per the brief: inbound-only scope (all 32), responsibility-boundary cutoff (11), four altitude tiers (12), consumer test / G1 (5), "one event per command" (4), twelve-slice model (2), capture/decision lens (2), HELO-only charter (2). Four 🟥 hotspots earned rather than assumed: addresses that "MUST be effective and useful for sending replies" with no way to verify it (§3.7.4 lines 1611-1612); §3.7.1 floating the envelope-into-header fold that Appendix E condemns as "inadequate in important ways"; §3.7.1 licensing local-part interpretation that §2.3.11 forbids by MUST; and the Via/With ABNF contradiction. The kit's tension about fieldless events bites twice, at ReceivedLineOmittedAtBoundaryCrossing and ViaClauseOmittedByGateway — both are near-fieldless because an omission has little payload, and both are recorded with that tension named rather than resolved by padding.

---

## 10. Alias and List Expansion

Reads, lookups, displays and non-changes — the \"guest viewed calendar for room availability\" pile — plus material that belongs to a neighboring chapter. Each carries the reason and, where one exists, the event that DOES belong beside it, so nothing has to be re-derived.

READS AND LOOKUPS
1. Resolving a pseudo-mailbox against the alias or list table — §3.9 lines 1706-1708. The lookup that precedes expansion; PseudoMailboxExpanded is the event, the table read is not. (Inherited from the §3.8-3.9 sweep's pile and re-confirmed.)
2. Reading the addresses on the list — §3.9 lines 1703-1704, \"Servers SHOULD simply utilize the addresses on the list\". The canonical guest-viewed-calendar case: an unmodified read of stored membership. The writes are ListMemberAdded and ListMemberRemoved.
3. Classifying a pseudo-mailbox as alias or list AT EXPANSION TIME — §3.9 lines 1706-1708. The classification was decided when the pseudo-mailbox was defined, so reading it during expansion is a lookup; the decision is carried by MailingListDefined.expansion_kind.
4. The header-field check at Appendix B lines 4741-4744, \"the remaining header fields SHOULD be checked to verify that at least one TO, CC, or BCC header field remains\". A check; the insertion it may trigger is kept as EmptyBccHeaderFieldInserted.
5. EXPN as a command — §4.1.1.7 lines 2137-2145 and §3.5.1 lines 1272-1274. The purest read in RFC 5321, and the RFC says so itself at 2143-2145: it \"has no effect on the reverse-path buffer, the forward-path buffer, or the mail data buffer\". Set aside as a read AND as chapter 13's material; its unwritten precondition is kept here as ListMemberAdded / ListMemberRemoved.

NON-CHANGES (🟤 nothing)
6. Observing that the header section was left unchanged during an expansion — §3.9 lines 1694-1696. The absence of a change is not a change. The violating arm IS an event and has been recovered as HeaderSectionModifiedByExpander.
7. \"Alias expanded with return path intact\" — §3.9.1 lines 1712-1715. The intactness is the absence of a rewrite, carried by AliasExpanded itself; no ReturnPathLeftIntact event is offered.
8. The honored arm of §7.2's SHOULD NOT at lines 4218-4223 — a client or server that did not copy the RCPT arguments into the header section. Nothing happened.
9. Recursive expansion, where an expanded address is itself a pseudo-mailbox. Not a new state change — PseudoMailboxExpanded fires again on the derived address. Recorded because the RFC is silent on it in §3.9 and the nearest text is §6.3's loop control; a hotspot for whoever owns loops, not an event here.

BELONGS TO ANOTHER CHAPTER
10. MailingListAccessRestricted — §3.5.1 lines 1322-1323, the configuration behind \"S: 550 Access Denied to You.\" A genuine event with a real actor, but it governs who may READ a list, not how one expands; chapter 13 (Address Diagnostics) owns \"access denied\" and chapter 15 owns the configuration. Left there, named here so it is not lost between the two.
11. MailingListMembershipDisclosedViaExpn — §3.5.1 lines 1315-1318. The disclosure consequence of a read. Chapter 13's, and worth noting that the RFC's own three members sit at three different organizations, which makes it a cross-organizational leak.
12. EnvelopeGeneratedFromHeaderFields (Appendix B lines 4734-4735) and MessageCopyGenerated (Appendix B lines 4737-4739). Real events, and siblings of BlindCopyRecipientCopiedFromBccHeaderField which this chapter does take. ⚠️ FLAG FOR THE FACILITATOR: none of the sixteen chapters claims Appendix B. Its BCC mechanics were taken here on the charter phrase \"blind-copy handling\"; its TO/CC envelope construction has no home at all.
13. Delivery or onward forwarding of each expanded copy — §3.9.1 lines 1715-1716, \"The message is then delivered or forwarded to each expanded address.\" The fan-out is this chapter's; what happens to each copy afterward is chapter 5 (final delivery), chapter 7 (queueing) and chapter 8 (relaying).
14. ForwardingAliasProvisioned, MailboxRelocationRecorded, MailForwardingRuleCreated, ReplacementAddressAssociatedWithMailbox — §3.4, §7.3 and §7.7. Address forwarding for a single mailbox is chapter 11's, and §3.9.2's self-description as \"forwarding\" at line 1729 is the trap that would pull them in here; see the ambiguity recorded under ListExpanded.
15. Composition of the bounce itself, the null reverse-path, and one-notification-per-failed-recipient — §6.1, §4.4 lines 3275-3283. Chapter 12's. This chapter keeps only WHERE the bounce landed: ErrorMessageReturnedToListAdministrator and its opposite ErrorMessageReturnedToOriginator.

---

### merge notes — 10. Alias and List Expansion

SCOPE AND METHOD. Chapter anchors verified by locating each heading line before citing anything: §3.9 at 1687, §3.9.1 at 1710, §3.9.2 at 1718, §7.2 at 4212 — all four match the roster. Additional headings confirmed for cross-references: §3.4 at 1162, §3.5.1 at 1215, §3.5.4 at 1407, §4.1.1.7 at 2135, §4.5.3.2.3 at 3633, §6.1 at 3960, §7.3 at 4237. The full text of §3.9 (1687-1746) and §7.2 (4212-4235) was read line by line for the re-sweep, plus Appendix B lines 4730-4750 and §3.5.4 lines 1407-1417.

HARVEST. Selected by meaning, not by the `timeline` field — the eleven sweeps used at least six different labels for this chapter's material (\"Address Expansion\", \"List Administration\", \"List Expansion\", \"List Expansion at Source\", \"Message Submission\", \"Trace and Provenance\"), and one of the chapter's events arrived under \"Mailing List Administration\". Fourteen inherited candidates were taken: indices 30, 31, 115-127, 290-292, 308, 309 of raw-candidates.json.

MERGES PERFORMED, with the losing names carried into each event's notes so no naming research is lost.
- PseudoMailboxExpanded absorbed RecipientAddressExpandedByListExploder (§7.2 sweep) — the same state change found from opposite ends of the RFC, one calling it expansion and the other explosion.
- MailingListDefined absorbed MailingListCreated and AliasEntryCreated from the §3.5 sweep, and gained §3.5.1 lines 1276-1279, which says the alias/list distinction on real hosts \"is a bit fuzzy\" — that complicates its own expansion_kind field usefully and neither original carried it.
- ListMembershipChanged{change: added|removed}, MailingListMemberAdded (§3.5.1) and MailingListMemberAdded (§4.1.1.7) were merged and then SPLIT into ListMemberAdded and ListMemberRemoved. The enum form made the name a category rather than a state change; the two arms have different consequences. The single-event form is recorded as the rejected alternative and the split is reversible.
- ReturnPathRewritten was renamed ReturnPathRewrittenToListAdministrator to keep it distinct from the gateway and relay rewrites in chapters 8 and 9.

DELIBERATELY NOT MERGED, per the prompt's warning that a relay's send is not an originator's: PseudoMailboxExpanded vs MailingListExpandedAtSource (different actor, different moment, no return path to rewrite); MessageCopyCreated vs Appendix B's MessageCopyGenerated (expansion-time vs submission-time, different motive); ListReturnPathLeftUnchanged vs AliasExpanded (a violated MUST is not a permitted absence); HeaderSectionModifiedByExpander vs MessageModifiedByList (a broken MUST is not a reclassification); RecipientListCopiedIntoHeaderSection vs HeaderSectionAlteredFromDeducedEnvelopeRelationship (sender writing a trace field vs receiver rewriting for delivery).

CITATION ERRORS FOUND IN THE INHERITED CANDIDATES — rule 3 applied to material this chapter inherited rather than wrote. Four line-range errors and three severed quotes, all corrected in place and recorded in the affected events' notes.
- MessageModifiedByList cited §3.9.2 lines 1743-1745 for \"There exist mailing lists that perform additional, sometimes extensive, modifications to a message and its envelope.\" The sentence ends mid-line 1744; line 1745 belongs to the next sentence. Corrected to 1743-1744.
- DeliveryAcceptedByList and MessagePostedByList both cited 1745-1746 for \"Such mailing lists need to be viewed as full MUAs...\". The sentence begins with \"Such mailing\" at the end of line 1744. Corrected to 1744-1746 in both.
- ListAdministratorChanged cited 1692-1693 for \"the address of a person or other entity who administers the list\". Line 1692 ends at \"MUST be changed to be\"; the phrase is entirely on 1693. Narrowed to 1693.
- ErrorMessageReturnedToListAdministrator cited §6.1 lines 3971-3974 for the notification MUST. The sentence starts on 3970 and the null-reverse-path MUST ends on 3973. Corrected to 3970-3973.
- Three §7.2 and Appendix B quotes were accurate but cut across line breaks into unreadable fragments (\"SMTP clients and servers SHOULD NOT copy\"; \"separate message transaction containing only a single RCPT command.\"; \"remains.  If none do, then a BCC header field with no additional\"). Widened to complete sentences. Rule 2 respected throughout: every quote is copied exactly, including the RFC's double spaces after sentence periods.
Every remaining inherited citation was opened and confirmed against the file.

WHAT THE RE-SWEEP RECOVERED — eleven events, and three of them were named in the sweeps' own overflow notes as dropped for the thirty-two-event cap, which is direct evidence the cap was the binding constraint rather than the spec.
Named as dropped by the sweeps and now restored: AddressExpansionSupportEnabled (§3.9 1689-1690, the only obligation §3.9 places on the host itself, and the SHOULD every other event presupposes); ExpansionDeferredUntilAfterAcceptance (§4.5.3.2.3 3635-3636, the only sentence in RFC 5321 that fixes WHEN expansion happens, which decides whether this whole chapter is visible inside a session or outside it); HeaderSectionModifiedByExpander (dropped as \"a violation record rather than a state change\" — contestable, and not the method's reason).
Violated arms the sweeps carried only one side of: ListReturnPathLeftUnchanged and its consequence ErrorMessageReturnedToOriginator (the MUST at 1692); AddressExpansionModelLeftUnsupported (the SHOULD at 1689); HeaderSectionAlteredFromDeducedEnvelopeRelationship (the SHOULD NOT at 4231-4232, folded into a different event by the §7 sweep); BlindCopyRecipientDisclosed (the harm named at 4234-4235).
Missing from §7.2's opening entirely: BlindCopyRecipientAddressedInEnvelopeOnly. §7.2 names two reasons an envelope recipient appears in no header field; the inherited set had an event for the list exploder and nothing for blind copies, the thing the section is titled after.
Missing from the Appendix B blind-copy sequence: BlindCopyRecipientCopiedFromBccHeaderField — the sweeps carried the removal and the empty-field insertion but not the copy that creates the blind copy in the first place.
Both arms of the MTA/MUA classification: ListTreatedAsTransitContinuation (constrained, 1730-1733) and ListReclassifiedAsFullMua (modifying, 1743-1746). The §3.9 sweep set the classification aside as a fold over MessageModifiedByList; that reasoning is preserved verbatim in both events' notes rather than overturned, because it may well win at a later step, and the chapter charter names the promotion explicitly.

CONVENTIONS. Mailbox values are written WITHOUT angle brackets throughout, following the §3.9 sweep and the project's settled path-vs-mailbox distinction — the brackets are envelope path syntax, not part of an address. The exception is the null reverse-path \"<>\" quoted from §6.1, which is path syntax and keeps them. Real values are the RFC's own wherever it supplies them: Smith@bar.com, Jones@foo.com, Green@foo.com and Brown@foo.com from Appendix D.1 lines 4893-4899 (Green is the recipient that fails with \"550 No such user here\" at line 4898); Admin.MRC@foo.com from D.5 line 5114; Example-People and its three members from §3.5.1 lines 1315-1318. staff@foo.com and owner-staff@foo.com are constructed in the RFC's own domain space because §3.9 supplies no worked example of its own — an absence worth noticing in a section this consequential.

STRENGTH DISTRIBUTION, since reading these as uniform would misclassify events. §3.9's first paragraph holds two hard MUSTs pulling opposite ways (the envelope MUST change, the header section MUST be left unchanged, 1690-1696) and one SHOULD on the host (1689-1690). Everything from line 1698 to 1746 — the entire mechanism, both subsections, and the MTA/MUA reclassification that decides how many messages exist — carries NO RFC 2119 keyword at all; the heuristics rule is a SHOULD plus \"strongly discouraged\", a SHOULD NOT written in prose. §7.2 is the inverse: three prohibitions in eighteen lines (SHOULD NOT copy, SHOULD NOT deduce and alter, SHOULD NOT use Apparently-to), one MAY, and an explicit admission that the main rule \"is often violated in practice, and cannot be enforced\" (4223-4224). A permission that exists only because a prohibition failed is a shape worth naming.

SCREENS NAMED, NOT APPLIED. Every event carries the FnEmail screen that would have killed it. The tally: the responsibility-boundary cutoff reaches roughly two thirds, because expansion happens after MessageAccepted by definition; the inbound-only scope reaches every submission-side and originator-side event; the four altitude tiers reach all six configuration and administration events; the consumer test G1 reaches every disclosure and violation event. Exactly one event in the chapter survives all four — HeaderSectionAlteredFromDeducedEnvelopeRelationship, which fires inside a receiving server at delivery.

THE STRUCTURAL FINDING, if only one thing carries forward. PseudoMailboxExpanded breaks an assumption the current model rests on without ever stating it: that the recipient set is fixed at RCPT time and closed by MessageAccepted{recipients[]}. Here the set grows after acceptance, and §4.5.3.2.3 confirms the growth may happen inside the session or long outside it, at the implementer's choice. Stream roots in this chapter divide cleanly along that fault: List for the administrative facts that exist before any message, Message for the expansion and its consequences, Queue Entry for the copies that acquire independent delivery fates, Transaction for the blind-copy events that live in one client-server exchange, and Server Configuration for the three capability events. No new root had to be invented.

---

## 11. Forwarding and Address Correction

Reads, lookups, displays and checks from this chapter's anchors, each with the write that DOES belong beside it.

(1) The server's consultation of its own address-change record when it becomes "aware of an address change" — §3.4 lines 1191-1192. A directory read; the directory is unchanged. The writes are ForwardingAliasProvisioned and MailboxRelocationRecorded.

(2) The client's determination of the server's authenticity before acting on a 251 or 551 — §7.4 lines 4288-4289, "it should be certain of the server's authenticity". A check. The rewrite it gates is AddressBookEntryUpdated; the case where the check is skipped is kept as AddressCorrectionAcceptedFromUnauthenticatedServer.

(3) The client returning the address-updating information to the user — §3.4 lines 1195-1196 and 1205-1206, "or even return that information to the user". A display, the guest-viewed-calendar case verbatim. The persistence that may follow it is AddressBookEntryUpdated.

(4) Parsing the forward-path out of a 251 or 551 reply text — §4.2 lines 2563-2565, "are associated with message text that must be parsed and interpreted by machines", and lines 2610-2612, "An SMTP client MUST determine its actions only by the reply code, not by the text (except for the "change of address" 251 and 551". A parse of a received reply. This is the only place in RFC 5321 where reply text is normative for a client, so the parse is unusually load-bearing — but nothing changes until the client acts, and the acts are AddressBookEntryUpdated and MessageResubmittedToCorrectedAddress. Worth a facilitator's attention: a server that emits unparseable text under 251 or 551 breaks the one case where text carries meaning, and no event in this chapter records that.

(5) The implementer's careful evaluation of 251 and 551 — §3.4 lines 1185-1187, "must be evaluated carefully by implementers". A design-time judgment with no runtime state change; the §7 sweep already flagged Implementer as a doubtful role for exactly this reason. What it produces is either the presence of the codes or the lever, and the lever's use is AddressUpdateDisclosureRestricted.

(6) A site's conclusion that its use of 251/551 "would undesirably disclose information" — §3.4 lines 1209-1211. An assessment. The write is AddressUpdateDisclosureRestricted.

(7) The internal audit that reads forwarding rules to find sensitive mail leaving the organization — §7.3 lines 4267-4269. A read of provisioned state; the write is MailForwardingRuleCreated.

(8) The VRFY and EXPN commands as such — §3.5.1 lines 1217-1218, "SMTP provides commands to verify a user name or obtain the content of a mailing list". Pure lookups, and owned by another chapter. Only their 251/551 disclosure consequence is kept here, as VerifiedAddressReportedAsForwarded.

(9) A client's inspection of a reply code's first digit to decide severity — §4.2 lines 2615-2617. A read of a received reply, listed because it is the alternative to (4) and is what a client does when it declines to parse the corrected address.

---

### merge notes — 11. Forwarding and Address Correction

WHAT WAS HARVESTED. The timeline field was ignored as instructed: this chapter's material was spread across six of the eleven sweeps under labels including "Forwarding and Address Correction", "Address Directory Administration", "Non-Delivery Handling", "Sender Address Book", "Client Routing Memory", "Address Change and Directory", "Mailbox and Alias Administration" and "Server Capability Configuration". Selection was by state change. Thirteen inherited candidates were pulled in; four were rejected as belonging elsewhere and are cross-referenced rather than merged: MailboxUnavailableReported (§4.2.2 line 2830 — a genuine mailbox failure, not a suppressed correction; noted inside RecipientRejectedWithoutAddressInformation), VerificationDeclinedWithDeliveryPromised (252, a VRFY event), AliasExpanded and ListExpanded (§3.9 redistribution — and note the documented RFC ambiguity the §3.8-3.9 sweep found: §3.9.2 lines 1728-1730 calls list handling "forwarding" eight lines after saying a list works by "redistribution" rather than by "forwarding", so "forwarding" in that subsection does NOT mean this chapter's forwarding).

WHAT WAS MERGED, AND WHY. Three merges, all by state change rather than by name. (a) ForwardPathAdvertised (§4.2-4.3 sweep) had folded 251 and 551 into one event and said so — "Merged to fit the count; split if custody is drawn." The custody differs materially: 251 accepts the message and undertakes to forward it, 551 refuses and hands the problem back. Split here into CorrectedDestinationDisclosed and RecipientRejectedWithForwardingAddress, with ForwardPathAdvertised carried as a rejected name in both. (b) SenderAddressRecordUpdated (§3.4-3.5 sweep) and AddressBookEntryUpdated (§7 sweep) are the same client-side write found from opposite ends of the document; kept under the RFC's own words at lines 4287-4288. (c) ReplacementAddressAssociatedWithMailbox (§7 sweep) names the same state change as MailboxRelocationRecorded and was folded into it. Nothing was merged across the refusal/acceptance line: MessageForwardedSilently and CorrectedDestinationDisclosed are acceptances, RecipientRejectedWithForwardingAddress and RecipientRejectedWithoutAddressInformation are refusals, and MessageRelayedToCorrectedAddress (a relay's send) is deliberately not merged with MessageResubmittedToCorrectedAddress (an originator's send).

WHAT THE RE-SWEEP RECOVERED — eight events, all from reading §3.4 (1162-1211), §7.4 (4284-4290) and §7.7 (4332-4338) in full. The pattern the cap had cut is exactly as predicted: the merged list carried the honored arm of every rule and almost none of the violated arms. MessageRelayedToCorrectedAddress — the forwarding that the 250 and 251 arms both promise, which nothing recorded. MessageResubmittedToCorrectedAddress — 551 says "please try" and nothing carried the trying. CorrectedAddressFoundUnreachableBySender — line 1183 states the corrected address may not be reachable by the sender at all, cited by the merged list only as background. AddressCorrectionAcceptedFromUnauthenticatedServer — the violated arm of §7.4, whose entire content is one lowercase "should" and one named attack. SensitiveAddressInadvertentlyDisclosed — the harm §7.7 exists to name, of which the merged list carried only the presupposition and the remedy. ForwardingEntryRetiredOnAssumedClientUpdate — the violated arm of the chapter's only MUST NOT, stated twice in identical words at 1194-1196 and 1204-1206. VerifiedAddressReportedAsForwarded — §3.5.3 lines 1391-1394 extends 251/551 to VRFY and EXPN, cited in an inherited note but never given an event, so the entire non-transaction arm was missing. ServerChosenForDisclosureBehavior — §7.7 names select AND configure; only configure was carried. Eighteen events in total, from thirteen inherited candidates that reduced to ten after merging.

CITATION ERRORS FOUND IN THE INHERITED CANDIDATES. All section headings were located in /home/ivan/FnEmail-Model/docs/rfc/rfc5321.txt before citing: §3.4 at 1162, §3.5 at 1213, §3.5.3 at 1371, §3.6 at 1419 (so §3.4's span ends at 1212), §4.2 at 2546, §4.2.2 at 2778, §4.2.3 at 2863, §4.3.2 at 3035, §7.3 at 4237, §7.4 at 4284, §7.5 at 4292, §7.7 at 4332, §7.8 at 4340. Every quote was re-read at its line. Nine line-range errors were found and fixed, all of the same kind — a range wider than the quote it labels, which is how a remembered citation drifts. "Forwarding support is most often required..." cited 1164-1167, ends at 1165. "may forward 'silently' and return a 250 code" cited 1192-1194, begins at 1193. "information hiding..." cited 1171-1174 in two candidates, ends at 1173. "the 251 (corrected destination) and 551 reply codes from RCPT must be evaluated carefully" cited 1184-1187 — that sentence begins at 1185; line 1184 is the RFC 821 pointer. "Servers MAY reject messages or return them as non-deliverable..." cited 1200-1203 in one candidate and 1200-1202 in another; it ends at 1201. "may reject the message as undeliverable with a 550 code" cited 1202-1204, begins at 1203. Two candidates quoted bare mid-sentence line fragments cut at the line break — AddressBookEntryUpdated's "to automatically update its future behavior (e.g., updating the" (line 4287 alone) and ReplacementAddressAssociatedWithMailbox's "identify the replacement address associated with a mailbox may" (line 4335 alone); both are extended to whole sentences here, whose ranges then match the labels that were given. No quote in this chapter crosses a page break: line 1174's sentence continues at 1183 across the running header and footer, so line 1183 is quoted alone rather than reconstructed. The §3.5.3 citation at 1391-1394, the §4.2.2 citations at 2818 and 2835, the §4.2 citation at 2563-2565 and the §4.3.2 citations at 3108 and 3109 were all correct as inherited.

TENSIONS RECORDED, NOT RESOLVED. (1) The fieldless-event tension bites hardest on RecipientRejectedWithoutAddressInformation, where the withheld address is the fact and padding the payload with it would invert the event's meaning. (2) The consumer test and the RFC disagree in writing on 251/551: the screen asks whether anything consumes the corrected address, and the RFC forbids the emitter from assuming anything does (1194-1196, 1204-1206). (3) One wire act raising two events — CorrectedDestinationDisclosed on the Recipient root and SensitiveAddressInadvertentlyDisclosed on the Mailbox root — is proposed here and should be ruled on rather than left standing by default. (4) Two stream roots needed care: Correspondent Address Record is INVENTED, for a sender's stored belief about someone else's mailbox, which is neither the Mailbox nor the Message; Server Configuration was taken from the offered list. (5) §3.4 has no de-provisioning event because the RFC states no lifetime for a forwarding entry — that is silence, not a ruling, and a later reader should not take it for one.

---

## 12. Non-Delivery Notification

Reads, lookups and non-changes, each with the reason it was set down — the "guest viewed calendar for room availability" pile.

1. **Reading the reverse-path in order to address a notification** — §3.6.3 lines 1497-1499, "as indicated by the reverse-path". A read of envelope state already established when the reverse-path was declared. The addressing that results is carried by NonDeliveryNotificationSent.notification_recipient. (Also set aside by an earlier sweep, for the same reason.)

2. **Reading the Return-Path: header line to find where the notification goes** — §6.1 lines 3973-3974, "(or the Return-Path: line)". A read of stored message state, used when the envelope is no longer available. Which of the two sources was consulted IS a fact and is kept as NonDeliveryNotificationSent.recipient_source; the consulting is not.

3. **VRFY probing of a putative sending system to test a return address** — §6.2 lines 4049-4052. A query, and the RFC concedes in the same breath that the system may not "fully and accurately support VRFY". The verdict is kept as ReturnAddressDeterminedInvalid; the probe is not.

4. **Inspecting a message for hostile content** — §6.2 lines 4057-4059. The inspection is a read, and the RFC explicitly places the decision "outside the scope of an SMTP server as defined in this document". The verdict is kept as HostileContentDetected, which is a state change; the inspection is not.

5. **Checking whether a message's return path is null before deciding to notify** — §6.1 lines 3973-3984. A read over the envelope. Its two outcomes are both kept, as NonDeliveryNotificationSent and NotificationSuppressedForNullReturnPath.

6. **Consulting the site's drop policy** — §6.2 lines 4052-4053. A read over configuration. The policy's adoption is kept as InvalidReturnAddressDropPolicyAdopted and its application as MessageDroppedForInvalidReturnAddress.

7. **An operator reading a mail log to find out why a bounce never arrived** — implied by §6.1 lines 3984-3987, which is the only place the RFC sanctions such a record existing. Mentioned nowhere as an act, and it changes nothing. Named here so a later reader does not have to rediscover that it was considered and set down; it is the closest true "guest viewed calendar" analogue in this chapter.

---

**Not set aside as non-events, but deliberately NOT claimed by this chapter** — real state changes that belong to a neighboring chapter, listed so the roster's seams are visible and nothing is assumed lost:

- **The DATA OK reply given under partial failure** — §4.4 lines 3280-3281, "In such cases, the response to the DATA command MUST be an OK reply." A genuine MUST and a genuine event, but it is a DATA-reply event; it belongs to the acceptance chapter. It is the precondition that makes PartialDeliveryFailureDetected possible.
- **DeliveryResponsibilityAccepted** — §6.1 lines 3962-3964, the 250-after-DATA moment. The custody line that every event in this chapter is positioned relative to; it belongs to the acceptance chapter.
- **DeliveryGivenUp / retry exhaustion** — §4.5.4.1 lines 3709-3711 (heading verified at 3685). The commonest trigger for a bounce, but it is a Queue Entry event and belongs to the queue and retry chapter.
- **MessageReturnedAsUndeliverable when no MX candidates remain** — §5.1 lines 3932-3934. Same thing on the wire as NonDeliveryNotificationSent, different cause and different point in the lifecycle; belongs to the address-resolution chapter.
- **MessageReturnedAsUndeliverable for garbled or high-bit content** — §2.4 lines 913-915. A MAY, and a content judgment; belongs to the conformance chapter.
- **NonDeliveryNotificationReturned by a sending client after retries fail** — §4.2.5 lines 2953-2957 (heading verified at 2941). An originator's act, not a receiver's, and kept apart on the brief's warning that a relay's send is not an originator's.
- **ErrorMessageReturnedToListAdministrator** — §3.9.2 lines 1723-1727 (heading verified at 1718). The same bounce landing somewhere else because a list rewrote the backward-pointing address; belongs to the list chapter.
- **Source-route stripping from a FORWARD path on receipt** — §4.1.1.3 lines 1925-1928 and §3.3 lines 1080-1082. A different act in a different direction from this chapter's SourceRouteStrippedFromReturnPath; belongs to the envelope chapter.
- **Received:-field counting and loop detection** — §6.3 lines 4067-4072 (heading verified at 4065). Adjacent in the document and adjacent in purpose, but a distinct mechanism; belongs to the trace and loop chapter.
- **Appendix C's instruction to ignore the source route when returning a message** — outside this chapter's anchors. Recorded in the notes of SourceRouteStrippedFromReturnPath so the two rules are not mistaken for a contradiction: both yield user@d for @a,@b:user@d.

---

### merge notes — 12. Non-Delivery Notification

**What was harvested.** Selection was by meaning, not by the `timeline` field, exactly as instructed — the chapter's material was scattered across at least eleven distinct labels in the raw set ("Non-Delivery Notification", "Failure Notification", "Bounce and Notification", "Notification and Bounce Handling", "Delivery and Bounce", "Delivery Notification", "Disposition Policy", "Queue and Retry", "Message Handoff and Subsequent Notification", "Delivery Attempts and Queue", "Return Path and Error Routing"). Roughly 30 raw records were read in full; 17 were merged or kept, and 9 more were read and consciously assigned to a neighboring chapter (listed in `set_aside`).

**What was merged, and why.** Three merges, each by state change rather than by name.
1. *DeliveryFailedAfterAcceptance* absorbed `DeliveryDeterminedImpossible` (§3.6.3, relay vantage) and `DeliveryFailedAfterAcceptance` (§6.1, receiver vantage). Justified by §3.6.3 lines 1503-1505, which attach the obligation to "the host that first determines that delivery cannot be accomplished" — one datable determination, two roles.
2. *NonDeliveryNotificationSent* absorbed six raw candidates (`UndeliverableMailNotificationSent` ×2, `NonDeliveryNotificationSent`, `NotificationSentWithNullReversePath`, `SubsequentFailureNotificationReturned`, `DeliveryFailureReportGenerated`) that all name the bounce leaving, under five different section citations. All rejected names are recorded in that event's `notes`.
3. *NotificationSuppressedToPreventLoop* absorbed the §4.5.4 half of `ErrorNotificationSuppressedToPreventLoop`; its §4.5.5 half was **split back out** as `AutoReplySentToNullReversePathMessage`, because the raw record's own notes flagged the collapse as questionable and the two rules differ in strength, actor and act.

**What was split rather than merged.** `NonDeliveryNotificationComposed` was separated from `NonDeliveryNotificationSent`. Three independent raw candidates each flagged this split without taking it — the roster item is literally "Bounce composed" — and a composed bounce that never sends is a real failure. Deliberately NOT merged despite surface similarity: the §5.1 routing-failure return, the §2.4 content return, the §4.2.5 client-side give-up return, and the §3.9.2 list-administrator return. A refusal is not an acceptance and a relay's send is not an originator's.

**What the re-sweep recovered — 10 events, all marked in their own `notes`.** Every one is a MUST/SHOULD/MAY arm or a presupposed precondition that the 32-event cap cut off:
- `AcceptedMessageLost` — the violated arm of §6.1's opening MUST NOT (3965-3967). Eleven sweeps read this paragraph and none recorded it. The single largest recovery: it is the reason the notification obligation exists.
- `NullAddressEventLoggedLocally` — §6.1 3984-3987, the RFC's only explicit sanction for a site keeping its own record of something the protocol makes invisible. Named as a sibling in a raw record's notes with no slot.
- `NotificationSentAboutNotificationFailure` — the violated arm of the chapter's most absolute MUST NOT, restated twice with "under any circumstances".
- `BounceSentDespiteHostileContent` — the escape-clause and violated arms of §6.2's SHOULD NOT (4059-4061).
- `HostileContentDetected` and `MessageJudgedSeriouslyFraudulent` — the two determinations §6.2 gates irreversible behavior on and then refuses to specify. Both were named as slot-less siblings in raw notes.
- `MessageDroppedForInvalidReturnAddress` and `InvalidReturnAddressDropPolicyAdopted` — the raw set carried only the determination; §6.2 4045-4055 keeps determination, act and policy apart in one sentence and the model now does too.
- `FaultyMessageDeliveredAnyway` — the honored arm of the §6.2 principle that every other §6.2 event departs from. Marked `doubtful`, with the reason recorded.
- `PostmasterRedirectConfigured` — the raw record admitted on the page that it folded this in "for slot budget". The cap, confessing.
- `NullReversePathUsedForNonNotification` — the violated arm of §4.5.5 3803-3805, recorded in neither arm by any sweep.

**Citation errors found in the inherited candidates** (rule 3, applied to our own work):
1. **Three raw candidates attribute the null-reverse-path and undeliverable-mail text at lines 1494-1522 to §3.6.1.** It is **§3.6.3** — heading verified at line 1475. §3.6.1 begins at 1421 and ends at 1446, where §3.6.2 begins. Corrected throughout; every `rfc_ref` here says §3.6.3.
2. `MessageSilentlyDropped` cited lines **4028-4030** for "However, it is extremely dangerous…" — the sentence begins at **4029**. Corrected.
3. One inherited flag **confirmed correct**: line 3791 does point non-delivery notifications at "Section 3.7", and §3.7 (verified at line 1531) is *Mail Gatewaying*. The RFC's own pointer is wrong; the substantive text is §3.6.3 and §4.4.
4. **New RFC-internal fault found by this re-sweep**: §6.2 line 4027 says "As discussed in Section 7.8 and Section 7.9 below, dropping mail without notification of the sender is permitted in practice." Both sections were opened and read. §7.8 (heading 4340) discusses closing a connection after generating 5yz replies to an address-harvesting attack; §7.9 (heading 4355) discusses **refusing** to accept mail with a 550. Neither discusses silent dropping. The only support for the claim is the sentence itself.
5. Two lowercase-"must" traps **verified rather than trusted**: line **1503** ("This notification message must be from the SMTP server at the relay host…") and line **3275** ("The server must give special treatment…"). Both are non-2119. Reporting either as a MUST would repeat the failure recorded in the project's rules for 2026-08-06.

**Section headings confirmed by locating the heading line before citing** — §3.6 (1419), §3.6.1 (1421), §3.6.2 (1446), §3.6.3 (1475), §3.7 (1531), §3.9.2 (1718), §4.1.1.4 (1992), §4.2.5 (2941), §4.4 (3156), §4.5.4 (3671), §4.5.4.1 (3685), §4.5.5 (3787), §5.1 (3824), §6 (3958), §6.1 (3960), §6.2 (4013), §6.3 (4065), §6.4 (4074), §7.8 (4340), §7.9 (4355). All five chapter anchors were read in full, with line numbers printed, not recalled.

**Stream roots.** One was invented and is flagged as such: **Notification**. The candidate list offers Message, but a bounce is a *new* message about a previous one with a lifecycle of its own — composed, given a null reverse-path, addressed after stripping, sent, possibly failing, possibly redirected to a postmaster — and collapsing it into Message loses the distinction between the message that failed and the message that reports the failure, which is the distinction this entire chapter turns on. Events about the *failed* message (loss, drop, suppression, judgment) keep `Message`; events about the *bounce* take `Notification`. Two configuration events take `Server Configuration`.

**Tensions recorded and deliberately not resolved**, per the brief:
- *The kit's fieldless-event tension bites hardest at* `NotificationSuppressedForNullReturnPath` *and* `HostileContentDetected` — both carry little beyond identity, both are legitimate under "do not pad events with fields just to reach a count", and both are close to "an opaque label". Recorded at those events, not resolved.
- *A live contradiction inside the RFC*: §6.1 3970-3971 makes notification a MUST on any post-acceptance delivery failure; §6.2 4059-4061 makes it a SHOULD NOT for hostile content. The available reconciliation (rejection versus post-acceptance failure) is contradicted by §6.2 4018-4020, which routes messages that cannot be rejected in-transaction back into the bounce path and therefore back under §6.1's MUST. Flagged at `BounceSuppressedForHostileContent`, where it bites.
- *A second gap*: `MessageDroppedForInvalidReturnAddress` is a non-delivery, so §6.1's MUST would seem to require a notification — addressed to the very path just judged invalid. The RFC says nothing. Flagged at that event.

**Project screens named, never obeyed** — every kept event records which of the project's own filters would have rejected it. The most-cited are the responsibility-boundary cutoff (this chapter lives entirely past it), the inbound-only scope (this chapter *sends* mail), the consumer test G1, the four altitude tiers, the "one event per command" target, and the condemned status/phase register. Three events — `AcceptedMessageLost`, `PostmasterRedirectConfigured`, `InvalidReturnAddressDropPolicyAdopted` — would have been killed by every screen at once.

---

## 13. Address Diagnostics

READS, LOOKUPS AND DISPLAYS — the "guest viewed calendar for room availability" pile. Each is named with the line that prompted it and, where one exists, the event kept beside it.

(1) THE VRFY COMMAND AS SUCH — §4.1.1.6 lines 2110-2111, "This command asks the receiver to confirm that the argument identifies a user or mailbox", reinforced by §3.5.1 lines 1217-1218, "SMTP provides commands to verify a user name or obtain the content of a mailing list". Verifying is a lookup against a directory; the directory is unchanged. Kept beside it: MailboxExistenceDisclosedViaVrfy, because the information crossing the boundary is not recallable, and the several refusal events, because a refusal is not a lookup result.

(2) THE EXPN COMMAND AS SUCH — §4.1.1.7 lines 2137-2139, "asks the receiver to confirm that the argument identifies a mailing list, and if so, to return the membership of that list". The purest read in RFC 5321. Kept beside it: MailingListMembershipDisclosedViaExpn and AddressesHarvestedViaExpn.

(3) THE ACT OF VERIFICATION ITSELF — §3.5.3 lines 1373-1374, the server having "actually verified the address". Reads the directory, writes nothing. Its outcome is what the kept events carry.

(4) SYNTAX CHECKING OF A DIAGNOSTIC ARGUMENT — §3.5.3 lines 1375-1376 and 1386-1387, and §7.3 lines 4247-4248. A validation read, and one the RFC says explicitly is not enough to justify a 250. Kept beside it: VerificationFalselyClaimed, which is what happens when a server answers as though the read were more than it was.

(5) MORE AGGRESSIVE VERIFICATION FOR VRFY THAN FOR RCPT — §3.5.3 lines 1394-1396, "Implementations generally SHOULD be more aggressive about address verification in the case of VRFY than in the case of RCPT, even if it takes a little longer to do so." The extra work is still a read. The policy choice behind it is arguably configuration and could be promoted; it is set aside rather than promoted because the RFC states it as advice to implementers, not as a per-site setting.

(6) THE CLIENT EXPOSING A 553 RESULT TO THE USER — §3.5.1 lines 1260-1261. A display, the guest-viewed-calendar case verbatim.

(7) THE CLIENT CONSULTING A DIRECTORY SERVICE BEFORE REPORTING — §3.5.1 lines 1268-1270. A read of a third system. Flagged because this project has dissolved a consulted view once before (commit 18f9f3a, "A consulted view is a decision-model fold; step 7 dissolves"), so the earlier reasoning probably transfers — but that is a later step's call, not Step 1's.

(8) A CLIENT TRANSLATING A REPLY OR TAKING AUTOMATED ACTION — §3.5.1 lines 1265-1268. Presentation and client policy.

(9) INTERPRETING A DIAGNOSTIC REPLY "very carefully, if at all" AND ONLY FOR DIAGNOSTIC PURPOSES — §3.5.1 lines 1330-1333. A constraint on how a read is used.

(10) INTERNAL AUDITS OF HOW EMAIL GETS ROUTED — §7.3 lines 4266-4269. The audit is a read; the forwarding rule it looks for is an event in a directory-administration chapter.

(11) PRESENTATION RULES FOR DIAGNOSTIC REPLIES — one mailbox per line in a multiline EXPN reply, §3.5.1 lines 1299-1301; pointed brackets around returned addresses, §3.5.2 lines 1351-1352; the recommended 553 wording and RFC 3463 extended codes, §3.5.1 lines 1261-1264. Formatting of a read.

(12) READING SERVER TYPE OR VERSION OUT OF A HELP RESPONSE OR A GREETING — §7.5 lines 4294-4298. A display. The operator's choice about what to reveal is kept as ServerIdentityDisclosurePolicySet.

(13) THE HELP RESPONSE CONTENT ITSELF, considered purely as text for a person — §4.2.2 line 2796, "this reply is useful only to the human user". This is the RFC stating that nothing in the protocol consumes it. HelpInformationSent is kept anyway, marked doubtful, because §7.5 treats the sending as a disclosure with consequences; the read half is set aside here.

(14) A NOOP CONSIDERED AS ITS OWN DEFINITION — §4.1.1.9 lines 2193-2195, "It specifies no action other than that the receiver send a '250 OK' reply." The RFC's own definition of a non-event. NoopAcknowledged is kept, marked doubtful, on the §4.5.1 MUST-support and §4.5.3.2.7 timer grounds recorded in its notes; the "no action" reading is set aside here so both halves survive.

(15) THE FOUR "no effect on the reverse-path buffer, the forward-path buffer, or the mail data buffer" SENTENCES read as anti-events — §4.1.1.6 lines 2114-2115, §4.1.1.7 2143-2145, §4.1.1.8 2157-2159, §4.1.1.9 2197-2199. The honored arm changes nothing and is set aside; the violated arm is kept as TransactionStateDestroyedByDiagnosticCommand.

(16) THE ARGUMENT-SYNTAX PRODUCTIONS — vrfy at §4.1.1.6 line 2119, expn at §4.1.1.7 line 2149, help at §4.1.1.8 line 2166, noop at §4.1.1.9 line 2203. Grammar. They license rejections, which are kept as events; the productions themselves change nothing.

(17) THE STATEMENT THAT DIAGNOSTIC ARGUMENTS CANNOT BE FURTHER RESTRICTED — §3.5.1 lines 1325-1330, including the observation that on some systems an EXPN argument may be a file name for a file containing a mailing list. A fact about the space of implementations. Its configurable half is kept as UserNameRecognitionRuleConfigured.

(18) IPR AND PROCESS MATERIAL, BIBLIOGRAPHY AND THE RFC 1123 / RFC 821 HISTORICAL NOTES at §3.5.2 lines 1364-1365 — provenance of the rules, not state changes in any mail system.

---

### merge notes — 13. Address Diagnostics

HOW THE HARVEST WAS DONE. The `timeline` field was ignored entirely, as instructed. Selection was by meaning: a regex sweep of all 352 records for vrfy|expn|verif|ambigu|mailing list|252|help|noop|expand|harvest|debug returned 177 records, which were read down to the seventeen whose state change actually belongs to Address Diagnostics. Those came from four different sweeps under six different timeline labels — "Address Debugging Session", "Address Disclosure", "Server Capability Configuration", "Mailing List Administration", "Command Dialogue", "Server Configuration and Policy", "Address Verification" — which is by itself the evidence that filtering on timeline would have lost half the chapter.

WHAT WAS MERGED, AND WHY.
- AddressVerificationDeclinedAsUnverifiable (candidate 24, §3.5.3) + VerificationDeclinedWithDeliveryPromised (candidate 240, §4.2.2). One state change seen from two spans. The proof they are one is textual, not inferred: the 252 code's own description at line 2821 says "(See Section 3.5.3)". The §4.2.2 record supplied the fact the §3.5 record lacked — that 252 carries a commitment to accept the message and attempt delivery, and is listed as a SUCCESS reply for both VRFY and EXPN at §4.3.2 lines 3127 and 3132.
- AddressVerificationDisabled (candidate 293) was NOT merged into a single event. It was split across VrfySupportDisabled and ExpnSupportDisabled, because §3.5.2 line 1360-1361 says "either or both" and the consequences diverge: EXPN's disablement removes an EHLO service extension (MUST, 1365-1366), VRFY's does not, and VRFY alone is on the §4.5.1 minimum-implementation list at line 3392. A merged event would have hidden the only clean asymmetry between the two commands. The merged name is carried in both records as a rejected alternative.
- AddressVerificationRefused (candidate 294) was renamed VerificationDeclinedAsDisabled and kept SEPARATE from AddressVerificationDeclinedAsUnverifiable, although both return 252. Different cause, different modal strength (MUST at §7.3 4243-4245 versus SHOULD at §3.5.3 1389-1390), different duration of the fact the requester learns. Merging them would have collapsed a policy decision into an inability. A third 252 exists — "252 Unable to verify members of mailing list" at §3.5.1 line 1283 — and is recorded inside CrossTypeVerificationRefused rather than given a fourth event; a later step should not conflate the three.
- GreetingDisclosureConfigured (candidate 297) was renamed ServerIdentityDisclosurePolicySet, because §7.5 governs the HELP response and the greeting in one sentence and the old name concealed half its scope.
- DuplicateRecipientEliminatedBySourceExpansion (candidate 31) was renamed to name the attempt rather than a result §3.5.4 says does not work, and was kept separate from MailingListExpandedAtSource (candidate 30): expanding is not deduplicating, and §3.5.4 deprecates only the second.
- Nothing else was merged. AmbiguousMailboxAlternativesDisclosed and MailboxNameReportedAsAmbiguous are two arms of one MAY and both stand; ExpnAccessDenied, CrossTypeVerificationRefused, VerificationDeniedWithoutChecking and VerificationRefusedAsUnimplemented are four different 5yz refusals with four different causes and are not one event.

WHAT THE RE-SWEEP RECOVERED — thirteen events, forty per cent of the chapter, and the pattern in what was lost is consistent: the cap left the honored arms standing and cut the violated ones, and it cut whole commands that a set-aside rule had already touched.
Violated arms recovered: VerificationFalselyClaimed (§7.3 4241-4248, MUST NOT stated twice, no inherited candidate); VerificationDeniedWithoutChecking (§7.3 4249-4251, the other named non-conformance); SourceRouteReturnedInVerificationReply (§3.5.2 1356-1357); NoopParameterActedUpon (§4.1.1.9 2199); DiagnosticCommandRejectedAsOutOfSequence (§4.1.4 2492-2493); TransactionStateDestroyedByDiagnosticCommand (the four repeated no-effect sentences).
Opposite arms recovered: MailboxNameReportedAsAmbiguous (the non-disclosing half of the MAY at 1240, which the inherited record explicitly declined to model on a reason the re-read shows to be wrong — line 1260-1261 says the client exposes the 553 to the user); MailingListVerifiedAsUser (1279-1282); UserNameExpandedAsSingletonList (1295-1297).
Whole commands recovered: HelpInformationSent, HelpRequestRefused, HelpSupportConfigured, NoopAcknowledged. HELP and NOOP produced no candidates at all in the inherited 352. The §4.1 sweep's set-aside pile shows why — it disposed of both with the RFC's own "no effect on the buffers" sentence and named HelpTextConfigured as "too thin to earn a slot" — and both of those are screens this chapter is instructed not to apply.
Also recovered: AddressDiagnosticsRestrictedToAuthenticatedRequestors (§7.3 4270-4271, which the §7 sweep's span notes named as a sibling it had no room for) and DiagnosticCommandAcceptedWithoutSessionInitialization (§4.1.4 2448-2451 and 2490-2494, an anchor outside this chapter's listed set and the only place the spec says what these commands' relationship to a session is).

CITATION ERRORS FOUND IN THE INHERITED CANDIDATES. Every section heading was located in /home/ivan/FnEmail-Model/docs/rfc/rfc5321.txt before any citation was written: §3.5 at 1213, §3.5.1 at 1215, §3.5.2 at 1335, §3.5.3 at 1371, §3.5.4 at 1407, §4.1.1.6 at 2108, §4.1.1.7 at 2135, §4.1.1.8 at 2151, §4.1.1.9 at 2191, §7.3 at 4237 — all ten as given in the task. Additionally located and used: §4.1.4 at 2443, §4.2.2 at 2778 with its code list at 2792-2821, §4.3.2 at 3035 with the command-reply sequences at 3125-3150, §4.5.1 at 3378, §4.5.3.2.7 at 3666, §7.5 at 4292, D.1 at 4878 and D.4 at 5103. Three defects were found and fixed:
(a) CrossTypeVerificationRefused cited lines "1283-1297" for the EXPN-on-a-user quote. That range crosses the page break at 1285-1294 (blank lines, the "Klensin  Standards Track  [Page 23]" footer, and the RFC header). The quote is cut to 1295-1297 so no quoted range silently spans a page break.
(b) UserNameRecognitionRuleConfigured cited "1303-1305" for "\"User name\" is a fuzzy term and has been used deliberately." That sentence is on line 1303 alone; 1304-1305 is the next sentence, which the record cited separately.
(c) VerificationDeclinedAsDisabled (as candidate 294, AddressVerificationRefused) quoted only sentence fragments — "server MUST return a 252 response, rather than a code that could be" and "appear to have verified addresses that are not, in fact, verified." Both are accurate as line-slices but read as truncated claims; both are completed here across their real line ranges.
Everything else checked verified clean, including candidate 11's tricky 1364-1366 span and candidate 297's 4304-4305 fragment.

THE FINDING THAT CHANGES HOW ExpnAccessDenied MUST BE READ. §3.5.1 lines 1322-1323 show `C: EXPN Executive-Washroom-List` / `S: 550 Access Denied to You.` — and §4.1.4 lines 2496-2500, sixty pages later, declares that same example, carried over from RFC 821, INCORRECT unless an EHLO precedes the EXPN or the denial rests on the client's IP address or on authentication. RFC 5321 therefore prints an example in §3.5 and disqualifies it in §4.1, with no cross-reference in either direction. Neither the inherited candidates nor any span note caught it. It supplies the missing precondition for ExpnAccessDenied and is the strongest evidence that AddressDiagnosticsRestrictedToAuthenticatedRequestors is a real event.

STRENGTH TRAPS VERIFIED IN THE FILE, recorded so nobody restates them from memory. The four "This command has no effect on the reverse-path buffer..." sentences contain NO RFC 2119 keyword — they are flat declaratives, not MUST NOTs. §7.3's "may want to disable" (4239) and "may choose" (4270) are lowercase, not MAYs. "is equally not in conformance" (4250-4251) is a statement of fact with no keyword. Conversely §7.3 line 4241 and line 4244 are real MUSTs and are the chapter's sharpest obligations.

STANDING CONTRADICTIONS THE SPEC LEAVES OPEN, all four flagged on the events where they bite rather than in an appendix, per rule 12. (i) VRFY: SHOULD support (1219, 1359), MUST support to conform (§4.5.1 3381-3392), MAY provide a way to disable (1360), and disabling yields 500/502 which 1380-1381 calls not in full compliance. (ii) 502/500 SHOULD be returned in the syntax-only case (1376-1377) while returning 502/500 for VRFY is called non-compliant (1380-1381). (iii) §3.4 goes to length to avoid disclosing one forwarding address while §3.5.1 hands over an entire list membership on request. (iv) D.4 lines 5111-5114 advertise VRFY as an EHLO extension keyword although VRFY is a base command, then answer 250 with a real full name without saying whether the address was verified — which §7.3 4247-4248 would call a violation if it was not.

REAL DATA (rule 8). Where the RFC supplies its own worked values they are used verbatim, because they are real by construction and traceable: Example-People, Executive-Washroom-List, Crispin, Mark Crispin, <Admin.MRC@foo.com>, <jsmith@foo.com>, <hsmith@foo.com>, <dweep@foo.com>, Joe Smith, Harry Smith, Melvin Smith, <Postel@isi.edu>, <Fonebone@physics.foo-u.edu>, <SQSmith@specific.generic.com>, "220 foo.com Simple Mail Transfer Service Ready", "@ONE,@TWO:JOE@THREE". Everywhere else the project's standing values: foo.com, bar.com, 203.0.113.20, <Smith@bar.com>, <Jones@foo.com>, f2C8D14. No placeholder appears in any field. If this output is rendered as markdown, note rule 5: a bare <Smith@bar.com> outside a code span loses its angle brackets, and in SMTP those brackets are path syntax.

STREAM ROOTS. One root was invented and is named as such: "Address Diagnostic Request" carries the twelve events that are the life of one VRFY or EXPN exchange — asked, answered, refused, declined, faked. None of the offered candidates fit: it is not a Transaction (§4.1.1.6 and §4.1.1.7 say these commands touch none of the transaction buffers), not a Session (a diagnostic request can precede any session initialization, per §4.1.4 2490-2491), not a Mailbox (the subject may not exist, which is the whole point of asking), and not a Message (none is ever sent). The remaining roots are Session for the HELP, NOOP and order-of-commands events, List for the EXPN-on-a-real-list and list-administration events, Server Configuration for the six configuration events, Extension Registry for the EHLO listing, Transaction for the one event that destroys one, and Message for the source-expansion event. Per the kit these are logical roots, not DDD aggregates.

CONFIDENCE DISTRIBUTION AND WHY IT SKEWS. Two events are marked certain, seventeen likely, thirteen doubtful. The doubtful ones cluster in exactly two places: the violated arms of rules the RFC never illustrates, and the HELP/NOOP events the spec itself defines as having no effect. Both clusters are kept deliberately under this chapter's instruction, and every one names the screen that would delete it, so a later gate rejects on evidence rather than by reflex.

---

## 14. Protocol Fault Handling

READS AND LOOKUPS (never events — the \"guest viewed calendar for room availability\" test):

1. Examining a reply code to decide the next action. §4.2 lines 2558-2559, \"The number is for use by automata to determine what state to enter next\", and §4.2.1 lines 2644-2652, the first/second/third-digit inspection. Reading a code is a query against a reply already recorded by ReplyIssued; only the resulting state change is an event, and that is UnrecognizedReplyCodeInterpretedByFirstDigit or MailTransactionTerminatedOnInvalidReply.

2. Consulting the §4.3.2 command-reply sequence table (lines 3087-3155) to decide which code to send. A lookup against a static table in the specification; the decision it feeds produces the refusal events, not an event of its own.

3. Consulting §4.2.2 and §4.2.3 (lines 2778-2930) for a code's meaning or numeric order. Pure reference material — the numeric-order list at §4.2.3 is a re-presentation of the same codes and adds no state change anywhere.

4. Checking whether a command verb is in the §4.5.1 required subset (lines 3384-3392). A lookup against a fixed list; the event is RequiredCommandRejectedAsUnrecognized when the check is skipped or ignored.

5. Measuring a line, path or message against a configured ceiling. The measurement is a read of data already on the wire; the events are the rejections.

6. Reading the advertised EHLO capability list to decide whether a parameter is safe to send. A query against ServiceExtensionsAdvertised (raw candidate 161); the fault event is UnimplementedCapabilityAdvertised, which is about the list being wrong, not about it being read.

CANDIDATES HANDED TO OTHER CHAPTERS (kept, not dropped — recorded so a later reader can find them and so this chapter is not silently claiming them):

- ClientStateAdvanced (raw 226) — the generic client automaton transition; belongs with whoever owns the client state machine. Named in full inside UnrecognizedReplyCodeInterpretedByFirstDigit's notes.
- CommandAcceptedPendingFurtherInput (raw 232) — the 354 intermediate reply; belongs to the DATA/content chapter, since \"held in abeyance\" is a DATA-phase state with its own timeout and parsing mode.
- CommandRetryAttempted (raw 235) — command-level retry after a 4yz; belongs to the retry-and-queueing chapter. Distinct from a queued delivery attempt.
- GreetingRejected (raw 162) and HeloFallbackPerformed (raw 163) — EHLO/HELO refusal and downgrade; belong to session establishment. GreetingRejected carries a fact this chapter would otherwise lose and which is recorded here for cross-reference: a mid-session EHLO that is REJECTED does not clear the buffers, while one that is ACCEPTED clears everything (§4.1.4 lines 2453-2456), so a rejected EHLO during an open transaction leaves that transaction open.
- ServiceShutdownAnnounced (raw 230), the 421 that any command may draw (§4.3.2 line 3073) — session lifecycle. It is a fault in the operational sense but not a protocol fault: nothing the client did caused it.
- ServiceRefusedAtConnection (raw 229), the 554 at connection opening — session lifecycle.
- StorageLimitReached (raw 242) — a resource limit, not a protocol limit; belongs with delivery and mailbox handling. The 552 collision with OversizedMessageDataRejected is noted in that event.
- ServiceExtensionNegotiated (raw 255) — the extension chapter. It matters here because §4.3.2 lines 3049-3054 say the sequencing rules and the codes themselves can be modified by negotiated extensions, so several MUSTs in this chapter are conditional on it; the conditionality is recorded in the events that depend on it rather than duplicating the event.
- MessageRejectedAtEndOfData (raw 180) and the §4.2.5 responsibility rules (lines 2941-2988) — the post-DATA responsibility chapter. §4.2.5 sits between two of this chapter's anchors and was read in full to place it; its MUST NOTs on retrying after a 4yz or 5yz are noted inside PermanentFailureReturned as the contrast that makes the SHOULD NOT there weak.

NOT AN EVENT, RECORDED SO IT IS NOT RE-DERIVED: the fieldless-event tension the kit names applies squarely to this chapter and is left unresolved as instructed. Several refusals here — UnknownCommandRejected above all — carry almost nothing beyond identity plus a code, and the kit says both that a fieldless event is legitimate and that an event without fields is an opaque label. Every refusal in this chapter was given a reply_code because the code is the fact the client acts on, not padding; but session_id plus reply_code is close to the floor, and a facilitator who wanted them fieldless would not be wrong.

---

### merge notes — 14. Protocol Fault Handling

HARVEST. Selected by meaning across all 352 candidates, ignoring the timeline field as instructed — the events for this chapter arrived under twelve different labels (Command Dialogue, Reply Channel, Session Termination, Conformance Observation, Mail Transaction, Mail Transaction — Envelope, Mail Transaction — Content, Inbound Transaction Limits, Message Repair, Server Configuration, Server Provisioning and Site Policy, Trace and Loop Detection), which is on its own evidence that the label field carried no information. 36 inherited candidates were pulled; they merged down to 24 rows, and 16 rows are new.

MERGES, and what each cost. (1) CommandRejectedWithStateUnchanged (raw 173) was a five-fault super-event held together by a reply_code field; it is SPLIT across UnknownCommandRejected, CommandRejectedAsMalformed, CommandRejectedAsUnimplemented, CommandRejectedAsOutOfSequence and MailParametersRejected, with its invariant — nothing on the server changes — preserved in each meaning. Its own notes made the case for splitting and it is followed. (2) CommandRejectedAsOutOfSequence absorbs raw 236, 149 and 155, which are the same state change found at three different verbs; the per-verb strength differences (MUST for RCPT-without-MAIL, MUST for general out-of-order, MAY with two codes for DATA) are preserved in notes rather than flattened. (3) UnknownCommandRejected absorbs raw 102 and the unrecognized-verb arm of 237, keeping 102's distinctive fact that nothing closes. (4) RequiredCommandRejectedAsUnrecognized absorbs raw 25, whose VRFY case is one instance of a nine-command rule. (5) SizeLimitConfigured merges raw 320 and 60 — the same act found from §2.3.8 and from §4.5.3, and merging them discharged raw 60's own ⚠️ that its 512 was unverified. Every rejected alternative name is carried in the notes of the surviving event, as required.

WHAT WAS DELIBERATELY NOT MERGED. TransientFailureReturned and PermanentFailureReturned (a temporary refusal is not a permanent one, and the client's retry forks on nothing else). RecipientBufferLimitReached and RecipientLimitEnforcedBySitePolicy (capacity exhaustion MUST be 452; policy refusal MAY be 5yz). MalformedMessageRejected and MalformedMessagePassedOnUnchanged (two arms of an unsettled debate). UnknownCommandRejected and ConnectionClosedInViolation (the honored and violated arms of the same rule, different actors). MessageIdAdded and MessageRepairRefusedByRelay (the identical edit, legal for an originator and forbidden to a relay).

RE-SWEEP — 16 rows added, every one from an anchor read in full. Both-arms recoveries, honored arm already present, violated arm missing: ShortCommandLineRejectedAsTooLong (§4.3.2 3063-3065), RequiredCommandRejectedAsUnrecognized (§4.3.2 3061-3063), PreviouslyAcceptedRecipientsDiscarded (§4.5.3.1.8 3543-3546), RecipientRejectedBelowMandatoryMinimum (§4.5.3.1.8 3537-3539), InconsistentMultilineReplyEmitted (§4.2.1 2771-2772), UnimplementedCapabilityAdvertised (§4.2.4 2937-2939), NonStandardReplyCodeInvented (§4.2.1 2730-2732 with §4.3.1 3021-3022), CommandSentWithoutAwaitingReply (§4.3.1 2995-2997 and 3000-3001). Siblings the cap cut that earlier sweeps named in their own notes and never gave rows: BareReplyCodeTolerated and MachineReadableReplyTextInterpreted (from raw 224), UnrecognizedReplyCodeInterpretedByFirstDigit (from raw 226), TextLineLengthExceeded (from raw 331, whose notes said it \"deserves its own event\"), PreviouslyAcceptedRecipientsDiscarded and OverflowReplyReclassifiedAsTransient (from raw 326, whose notes said the first \"was cut only for slot budget and should be restored\"), RepairDocumentedInTraceHeader (from raw 284). Fresh from the §6.4 read: MalformedMessageRejected, MalformedMessagePassedOnUnchanged. Folded into notes rather than given rows, purely for the 40-row cap, and named so they are recoverable: MultilineReplyStarted, OutOfRangeReplyCodeTransmitted, RepairAppliedByRelayInViolation, RecipientListSplitIntoChunks, CapabilityAdvertisementAligned, ReplyCodeVocabularyExtended, ReplyTextOmitted, LocalPartLengthExceeded, DomainLengthExceeded, SizeExtensionAdvertised, DeclaredMessageSizeRejectedAtMail, SilentRecipientOverflowRevealedAtData, OversizedObjectTransmissionAttempted.

CITATION ERRORS FOUND IN THE INHERITED CANDIDATES (rule 3 — every retained citation was checked by opening docs/rfc/rfc5321.txt, and every chapter anchor heading was confirmed at the line the roster gave: 2546, 2642, 2931, 2989, 2991, 3035, 3552, 4074). Three fixes, all understatements of where a quotation begins rather than wrong sections: raw 25 cited §3.5.3 \"1378-1381\" for a sentence that begins at 1380; raw 93 cited §3.7.2 \"1586-1589\" for a sentence whose opening clause is at 1585; raw 224 cited §4.2 \"2606-2608\" for a sentence ending at 2607. One citation that LOOKS wrong and is not: raw 227's §4.2 \"2628-2639\" spans a page break, the sentence resuming with \"or 5.\" at 2639 after the running header — verified, not corrected. Nothing else in the retained set misattributed a section, and no retained quotation was found altered. One field was dropped as a fiction rather than as padding: raw 329 carried command_verb on OversizedCommandLineRejected, but an over-length line is refused before it is parsed, so the server does not know the verb.

STRENGTH TRAPS worth carrying forward, all confirmed by reading rather than remembering. §4.1.1.11's 555 and 455 are \"it will return\" and \"it should return\" — lowercase, no RFC 2119 keyword. §4.1.5's 500 for an unknown X-command is \"is expected to reply\". §3.8's tolerance of unknown commands is \"Servers are expected to be tolerant\" — plain prose, immediately after a sentence calling the opposite a violation, so the effective obligation exceeds the wording. §4.5.3.1.10's 503-after-DATA is \"it would be reasonable to\", weaker than MAY. §4.2.4 answers its own question with two SHOULDs, so the 500/502 split is advisory. §4.2's out-of-range rule is SHOULD plus \"normally\", the weakest obligation in the chapter. Conversely §4.5.3.1.7 writes \"MUST BE\" in capitals and §4.1.2's MUST is qualified in its own parenthesis, \"this rule, like others, could be overridden by appropriate SMTP extensions\". And several MUSTs in this chapter are conditional on negotiated extensions, so the same wire behavior can be a fault in one session and correct in the next.

PROJECT SCREENS NAMED, NOT APPLIED, per the brief. The screens that would have killed the most rows here are the consumer test/G1 (every refusal that leaves server state unchanged), the condemned status/phase register (every severity- and compliance-shaped event), the inbound-only scope and responsibility-boundary cutoff (every client-side and peer-observation event), the HELO-only charter (the whole ESMTP parameter family and the size/limit events reached through extensions), and the one-event-per-command target (which has no room for a command that produces a refusal instead of an event). Each is named in the notes of the row it would have taken.

---

## 15. Server Configuration and Provisioning

READS, LOOKUPS AND DISPLAYS — kept, not deleted, with the reason each fails the \"guest viewed calendar for room availability\" test.

1. VrfyRequestAnswered / MailboxExistenceDisclosedViaVrfy (§3.5.1 1217-1229; §3.5.3 1373-1381) — the canonical query. A client asks whether <Smith@bar.com> is a user name and the server answers; nothing changes state. NOTE THE PAIRING: MinimumCommandSetProvisioned (VRFY on the mandatory list, §4.5.1 3392) is an event and stays, while the VRFY exchange itself is set aside. \"Queries are not events\" rejects the USE, not the provisioning — they are two different candidates and a sweep that merges them loses the provisioning one.

2. MailingListMembershipDisclosedViaExpn (§3.5.1 1299-1301, 1312-1318) — same test, list-shaped. The multiline 250- reply is a display of list membership, not a change to it. The state change that WOULD be an event is a membership change, which belongs to the List chapter.

3. EhloExtensionListReturned / ServiceExtensionsAdvertised (§4.1.1.1 1874-1877; §3.5.2 1365-1366) — the EHLO keyword list is a projection of ServiceExtensionEnabled and ExpnSupportEnabled. It is the read model, not the write. Recorded here because ExpnServiceExtensionRegistered was a raw candidate for exactly this and was merged into ExpnSupportEnabled rather than kept as a display.

4. GreetingBannerRead (§3.1 964-966; §7.5 4292-4306; Appendix D.1 line 4886) — a client observing 220 foo.com Simple Mail Transfer Service Ready learns the server's type and version. The disclosure DECISION is SoftwareVersionAnnouncementDisabled; the observation is a read. This is the half of §7.5 that is not an event.

5. HelpResponseRead (§7.5 4296-4297, \"in the greeting response or in response to the HELP command\") — same shape as the banner, different command.

6. RelayAuthorizationChecked (§7.9 4377-4379; §3.5.3 1386-1389) — consulting the known-sources list at RCPT time, or the expected-relay-domains set during verification. A read of configuration written by RelaySourceAuthorized and RelayDomainAuthorized.

7. ExtensionRegistryConsulted (§2.2.2 577-580) — a client or implementer checking whether a non-X keyword corresponds to a registered extension. The registry is written by ServiceExtensionRegistered and read here.

8. RetryParametersConsulted / TimeoutValueConsulted (§4.5.4.1 3703-3714; §4.5.3.2 3612-3621) — the queue runner reading its configured interval, or a timer being set from a configured value. Reads of what RetryParametersConfigured and TimeoutValuesReconfigured wrote.

9. RecipientCountCompared (§4.5.3.1.8 3537-3539; §4.5.3.1.10 3598-3603) — testing the running RCPT count against the configured maximum. A comparison against a read model; the REJECTION that follows is an event and belongs to the recipient/transaction chapter.

10. MessageSizeCompared (§4.5.3.1.7 3518-3519) — same shape for octets.

11. PostmasterObligationChecked (§4.5.1 3398-3402) — deciding whether a given RCPT names postmaster at a served domain. A lookup against the set MailServiceDomainAdded populates.

12. MxRecordsLookedUp (§5.1 3837-3849) — a read, and another chapter's besides.

ADJACENT EVENTS CEDED TO OTHER CHAPTERS, cross-referenced so they are not lost: MailRejectedForPolicyReason and RelayingDeclined (§7.9 4379-4381, §3.3 1082-1083 — runtime arms of MailAcceptancePolicyConfigured and RelayPolicyConfigured, belonging to the session/transaction chapters); MailExchangerRecordPublished / MxRecordsPublished / MailRoutingDnsRecordPublished (three raw candidates for one DNS publication event, belonging to routing/next-hop); MailboxProvisioned, MailboxDefined, LocalMailboxProvisioned, ForwardingAliasProvisioned, ErrorHandlingMailboxDesignated, MailboxPolicyBlockApplied, PrimaryMailboxAddressEstablished (Mailbox chapter — only the POSTMASTER mailbox is claimed here, because §4.5.1 makes it server configuration rather than user provisioning); MailingListAccessRestricted, MailingListCreated (List chapter); TraceTimeFormatConfigured (§4.4 3186-3202, three SHOULDs on Received: date format — genuinely server configuration, ceded only because §4.4 is another chapter's anchor and the 40-event cap bound); SubmissionArrangementEstablished (§3.6.3 1477-1488, submission/relay chapter).

---

### merge notes — 15. Server Configuration and Provisioning

WHAT WAS HARVESTED. Selected by meaning across all eleven sweeps, ignoring the `timeline` field entirely as instructed — the 34 candidates that fed this chapter carried at least nine different timeline labels (Abuse Defense, Client Routing Memory, Mailbox Administration, Message Custody and others), and several of the strongest, such as MinimumCommandSetProvisioned and RetryParametersConfigured, sat under labels naming the span rather than the subject.

WHAT WAS MERGED, and why.
- PostmasterAddressProvisioned into PostmasterMailboxProvisioned. Same state change; the §3.1 sweep reached it through a passing mention and correctly flagged that it had not opened §4.5.1, where the MUST actually lives. MailProblemContactPointPublished was SPLIT BACK OUT of that record, because the sentence's whole point is that the two are different states.
- AddressVerificationDisabled into VrfySupportDisabled. Rejected its one-event-with-a-command-field shape: §3.5.2 1360-1362 says "either or both", and the consequences diverge (disabling EXPN withdraws an EHLO line under a MUST; disabling VRFY does not). Two events, cross-referenced.
- ExpnServiceExtensionRegistered into ExpnSupportEnabled. Its own sweep marked it doubtful on the correct ground that the EHLO listing is a display; the enabling act is the event, the listing is the read model, and the VRFY/EXPN asymmetry survives in the notes either way.
- RelayingPolicyConfigured (§3.3) into RelayPolicyConfigured (§3.6.1). One state change described in two sections. Absorbed RelayServiceRefused as the empty-list value of the same fact.
- TimeoutPolicyConfigured into TimeoutValuesReconfigured; SmtpServiceStarted into SmtpListenerStarted; LineLengthLimitImposed into SizeLimitConfigured; GreetingDisclosureConfigured split across SoftwareVersionAnnouncementDisabled and ReplyTextCustomized; the two same-named ServiceExtensionRegistered candidates unioned.
NOT MERGED although they look alike, per the brief: RelayPolicyConfigured / RelayDomainAuthorized / RelaySourceAuthorized are three different sets in three different sections with three different consumers; RecipientLimitConfigured (server, §4.5.3.1) and ClientRecipientsPerTransactionLimitConfigured (client, §4.5.4.1) are opposite sides of the wire; RequiredCommandLeftUnimplemented (implementer never built it) and VrfySupportDisabled (operator switched it off) produce the same 502 from different actors; SmtpListenerStarted's inbound concurrency cap and ConcurrentOutboundTransactionLimitConfigured are different resources.

WHAT THE RE-SWEEP RECOVERED — sixteen events, all read directly from the anchors:
§3.1 → MailProblemContactPointPublished; the §3.1 973-976 MAY behind ConnectionRefusalConfigured (the inherited record cited only §4.5.1, which points at §3.1 without quoting it); the §3.1 980-983 SHOULD on refusal reply text, added to ReplyTextCustomized.
§3.5.2 / §7.3 → ExpnSupportEnabled (the honored arm — the raw set had only the disabling arms); DisabledVerificationReplyCodeConfigured (the MUST-252 rule and its three named non-conforming answers, carried by nothing); VerificationRestrictedToAuthenticatedRequestors (the middle position on a spectrum the raw set flattened to two).
§4.5.1 → RequiredCommandLeftUnimplemented (violated arm of the MUST); DomainlessPostmasterSupportProvisioned (the RCPT TO:<Postmaster> MUST); PostmasterMailBlockLifted (the closing arm the RFC never states).
§4.5.3.1 → NonConformingRecipientLimitConfigured (violated arm — a limit below 100, which 3538-3539 names as a violation); SizeServiceExtensionImplemented (had been flattened to a boolean field on another event); the orderly-behavior MUST at 3543-3546, added to RecipientLimitConfigured.
§4.5.4 / §4.5.4.1 → ClientRecipientsPerTransactionLimitConfigured; ConcurrentOutboundTransactionLimitConfigured; QueuingStrategyAdopted.
§7.9 → RelaySourceAuthorized; RelayFilteringCapabilityImplemented (the SHOULD is laid on IMPLEMENTATIONS, a different actor); MailAcceptancePolicyConfigured (the standing-policy arm; the raw set had only the runtime rejection).
§8 → AddressLiteralTagRegistered, TransmissionTypeRegistered, TraceHeaderFieldRegistered. §8 names THREE registries and the raw set carried one; the other two, plus the section's only RFC 2119 keyword, existed solely as names inside another event's fold list — the clearest single instance of what the 32-event cap cost.

CITATION WORK (rule 3). Every heading was located by line before citing: §3.1 at 959, §3.5.2 at 1335, §4.5.1 at 3378, §4.5.3.1 at 3460, §4.5.4.1 at 3685, §7.9 at 4355, §8 at 4383, plus §3.4 at 1160ff, §3.5.1 at 1215, §3.5.3 at 1371, §4.5.3.2 at 3610, §4.5.4.2 at 3774, §7.3 at 4237, §7.5 at 4292, §2.2.2 at 532ff, §4.1.5 at 2534. Inherited citations spot-checked against the file and found sound in every case tested (§3.5.2 1359-1362; §3.5.1 1219-1220; §3.5.3 1380-1381; §4.5.1 3394-3409; §4.3.1 3022-3033; §7.5 4304-4306; §2.2.2 534-538 and 567-569; §4.5.4.2 3776-3777; §3.8 1651). One inherited record carried its OWN ⚠️ flag — LineLengthLimitImposed marked its 512-octet example as unverified because its span deferred to Section 4 and it never opened §4.5.3.1. The re-sweep opened it: 512 for a command line is confirmed at line 3500, and the flag is discharged in SizeLimitConfigured's notes rather than silently dropped. UserNameRecognitionRuleConfigured's inherited refs split one paragraph three ways with the MUST sentence straddling two of them; re-cited as 1303-1305 and 1305-1310. Two example values were changed to project standing data per rule 8 (jsmith@foo.com to Smith@bar.com; a placeholder-ish contact point to <mail-problems@foo.com>).

STRENGTH FACTS WORTH NOT RE-DERIVING. Lowercase modal verbs that read like RFC 2119 keywords and are not: §4.5.1 3407 "may block mail directed to Postmaster" (weaker than the MUST it overrides, while the narrow-tailoring constraint immediately after IS an uppercase SHOULD); §7.9 4357 "may refuse to accept mail" and 4363 "considerable care should be taken"; §7.3 4270 "may choose"; §4.5.4.1 3710 "generally needs to be at least 4-5 days" — no keyword at all behind the best-known number in the section. §8's only RFC 2119 keyword is the trace-field MUST at 4444-4447.

FIELDLESS-EVENT TENSION, recorded and not resolved per instruction: nothing in this chapter came out fieldless. Every configuration event carries at least the host it configures, because a configuration fact with no subject is unusable — which is the "opaque label" objection biting from a direction the kit does not discuss. The nearest approach is DomainlessPostmasterSupportProvisioned, whose two fields are arguably one identity key plus a constant; it was not padded further.

CHAPTER-BOUNDARY AND CAP NOTE. The 40-event schema cap bound again, on a chapter that had roughly 46 defensible candidates. Rather than drop material silently, four events were folded into other events' notes WITH THEIR CITATIONS INTACT, and are recoverable by name: AlternateAddressLimitConfigured (§5.1 3876-3878, 3898-3902, inside RetryParametersConfigured), AdditionalClauseRegistered and ExtensionApprovedByIesg (§8 4419-4442 and 4396-4398, inside ServiceExtensionRegistered), VersionDisclosureProvisionImplemented (§3.1 967-968, inside SoftwareVersionAnnouncementDisabled). A further set was ceded to neighboring chapters and listed at the end of set_aside. If this chapter is ever re-run without a cap, those eight are the first additions.

PROJECT SCREENS NAMED, NEVER APPLIED. Per instruction, no candidate was rejected using a FnEmail screen. Every event's notes name the screen or screens that would have killed it — most often the four altitude tiers (which file all configuration as Infrastructure), the consumer test G1 (nothing in the current model reads configuration), the HELO-only charter (which erases VRFY, EXPN and every extension event at once), the inbound-only scope (which erases the client-side and IANA events), and the condemned status/phase register (which would reject ConnectionRefusalConfigured, PostmasterMailBlocked and every enabled/disabled pair). Taken together those five screens would delete this entire chapter, which is itself the finding worth reporting: RFC 5321 specifies a substantial body of state that changes outside any session, and the current model has no lane for it.

---

## 16. Abuse, Security and Registry

Reads, lookups, displays and checks from this chapter's anchors, each with the reason and with the event that DOES belong beside it. (1) Reading server type or version from the greeting, and HELP command output — 7.5 lines 4294-4298: a display; the guest-viewed-calendar case. The operator's choice about what to reveal is kept as ServerIdentityDisclosureConfigured, and the disclosure's effect on the world is kept, doubtfully, as ServerTypeAndVersionDisclosed. (2) The client's determination of a server's authenticity before acting on a 251 or 551 — 7.4 lines 4288-4289, "it should be certain of the server's authenticity": a check. The rewrite it gates is AddressBookEntryUpdated and the failure it fails to prevent is AddressBookPoisonedByForgedReply. (3) Internal audits of how email gets routed, "to check and to make sure no one is automatically forwarding sensitive mail outside the organization" — 7.3 lines 4266-4269: the audit is a read of forwarding configuration. What it reads was created by ReplacementAddressAssociatedWithMailbox and the inherited MailForwardingRuleCreated. (4) Syntax-only validation of a VRFY address — 7.3 lines 4247-4248: a validation read, and one the RFC says is not enough to justify a 250. Kept nearby, because the REPLY is not a read: VerificationFakedAfterSyntaxOnlyCheck. (5) VRFY and EXPN as commands — the purest queries in RFC 5321; already set aside by the 3.5 sweep with fifteen companions. This chapter sets them aside again and keeps only what they change: disclosure, refusal, configuration, harvest. (6) Consulting the IANA registries, and the note that "IANA does not guarantee the locations associated with the URLs" — 8 lines 4387-4389: reads of a registry whose writes are kept as ServiceExtensionRegistered, ProtocolNameRegistered, TraceClauseRegistered and TraceHeaderFieldRegistered. (7) A client checking the EHLO keyword list before using a command — 3.5.2 lines 1366-1369: a read, and the RFC says clients need not even perform it. The offering is kept as UnregisteredKeywordOffered. (8) The debugging use of announced type and version — 7.5 lines 4298-4303, "The utility of the debugging information is beyond doubt": an operator reading a banner changes nothing in the mail system. (9) The whole of 7.1's argument about end-to-end methods, digital signatures, PGP and S/MIME — 4169-4173: a recommendation pointing outside SMTP entirely, with no state change inside it; nothing here is an event of this system, and no signing event is claimed. (10) "Sites are encouraged to evaluate the tradeoff" (7.5 line 4303), "sites SHOULD carefully evaluate the tradeoffs" (7.3 lines 4272-4273), and "considerable care should be taken and balance maintained" (7.9 lines 4363-4364) — three instructions to deliberate. Deliberation is not a state change; the decisions it produces are, and they are kept as ServerIdentityDisclosureConfigured, VerificationCommandDisabled and RelayAccessRestrictedToKnownSources. (11) Section 8's statement that IANA maintains three registries and that this document expands the third (4385-4387) — a description of standing arrangements, not an occurrence; the expansion itself is kept as TraceClauseRegistered. (12) BORDERLINE, flagged rather than filed: the omission of a FOR clause on a multi-recipient message, 7.6 line 4328 "or not at all". Omitting an optional clause is indistinguishable in the result from never supporting it, so it is recorded as a non-event here — but the brief's both-arms instruction argues for promoting it as ForClauseOmittedToProtectRecipients, and it is named in BlindCopyRecipientDisclosedInForClause's notes so a facilitator can.

---

### merge notes — 16. Abuse, Security and Registry

WHAT WAS HARVESTED AND MERGED. Selection was by meaning, never by the timeline field, as instructed: a keyword pass over name, meaning, notes and rfc_refs pulled 145 of the 352 inherited candidates for inspection, of which 50 were read in full and 26 belong to this chapter. Nine merges collapsed 21 of those into 9 events, in every case because they name one state change, never because the names looked alike: VerificationCommandDisabled absorbed AddressVerificationDisabled + VrfySupportDisabled + ExpnSupportDisabled; RelayingDeclined absorbed RelayingDeclined + RelayTaskDeclined + RecipientRejectedForPolicy; ServerIdentityDisclosureConfigured absorbed GreetingDisclosureConfigured + SoftwareVersionAnnouncementDisabled; HostileTrafficDetected absorbed HostileTrafficDetected + AbusePatternDetected; ConnectionClosedDefensively absorbed ConnectionClosedDefensively + ConnectionClosedForAbuse; ReplacementAddressAssociatedWithMailbox absorbed MailboxRelocationRecorded; RelayAccessRestrictedToKnownSources absorbed RelayPolicyConfigured (and the never-promoted RelayServiceRefused); ServiceExtensionRegistered absorbed its own duplicate from a second sweep plus AddressLiteralTagRegistered and RegistrySubsectionAdded; ProtocolNameRegistered absorbed TransmissionTypeRegistered. Every rejected name is carried in the surviving event's notes. Kept apart deliberately, against the temptation to merge: the three 252 replies (7.3's refusal-to-disclose, 3.5.3's cannot-verify-in-real-time, 4.2.2's promise-to-deliver — same code, three unrelated causes, two of them anchored in other chapters); the 251 acceptance and the 551 refusal; MailRejectedForPolicyReason (pre-DATA, at EHLO/MAIL/RCPT) and the inherited MessageRejectedByPolicy (post-354, after the content was read); RecipientListCopiedIntoHeaderSection (copying what the envelope gave you) and HeaderSectionAlteredFromDeducedEnvelopeRelationship (inventing a correspondence); AddressBookEntryUpdated and AddressBookPoisonedByForgedReply; MessageSpoofedToAppearFromElsewhere and EnvelopeReturnPathSetToAnotherUsersAddress, which are the same wire act with opposite intent and which the RFC itself refuses to separate.

WHAT THE RE-SWEEP RECOVERED — 14 events, 35 percent of the chapter. Reading the anchors in full found that section 7.1 had produced NO inherited candidates at all: seven subsections of Security Considerations yielded events, and the first one, on spoofing, yielded none, because the sweep that covered lines 4156-5323 hit the 32-event cap and spent it on the later subsections and the appendices. Recovered from 7.1: MessageSpoofedToAppearFromElsewhere, SpoofedOriginDetected, EnvelopeReturnPathSetToAnotherUsersAddress, ReturnPathAlterationRestricted, PermanentMailboxAddressEstablishedForUser, TransportLevelAuthenticationCompleted. Recovered from 7.2: HeaderSectionAlteredFromDeducedEnvelopeRelationship, the second paragraph's separate SHOULD NOT. Recovered from 7.3: VerificationFakedAfterSyntaxOnlyCheck and VerificationAlwaysRefusedWith550 — the two non-conformances the RFC names outright — plus AddressesHarvestedFromMailingList, MailingListProtectionInstalled and VerificationRestrictedToAuthenticatedRequestors, the last of which was named in an inherited candidate's own notes as a sibling and never promoted. Recovered from 7.4: AddressBookPoisonedByForgedReply, the man-in-the-middle arm. Recovered from 7.6: InternalHostNamesDisclosedInTraceField and BlindCopyRecipientDisclosedInForClause — the whole subsection had contributed only a cross-reference. Recovered from 7.8: ServerRenderedInaccessibleByAttack, the second of the two attack purposes at line 4344. Recovered from 8 and 2.2.2: ExtensionApprovedByIesg, UnregisteredKeywordOffered, TraceHeaderFieldRegistered. The pattern the brief predicted held exactly: the violated arms were what the cap cut. Every MUST, SHOULD and MAY in the anchors was read for both arms, and the recovered set is dominated by the arms nobody honored — a faked 250, a blanket 550, an unregistered keyword offered, a poisoned address book, a leaked internal host name, a disclosed blind copy.

CITATION WORK, PER RULE 3. Every section heading was located before citing: 2.2.2 at 532, 7 at 4156, 7.1 at 4158, 7.2 at 4212, 7.3 at 4237, 7.4 at 4284, 7.5 at 4292, 7.6 at 4319, 7.7 at 4332, 7.8 at 4340, 7.9 at 4355, 8 at 4383, 6.2 at 4013. Every quote kept was re-read at its line rather than trusted. Errors found in the inherited candidates: (a) TraceClauseRegistered's quote from 4.4 lines 3341-3348 had been silently reflowed out of its ABNF comment wrapping, which rule 2 forbids — corrected, and the same correction applied preemptively to ProtocolNameRegistered's 4.4 quote; (b) several refs gave ranges wider than the quoted text (HostileTrafficDetected's "L4347-4349" for text wholly on 4347; RecipientListCopiedIntoHeaderSection's "L4220-4221" for text wholly on 4220; BlindCopySentAsSeparateTransaction's "L4224-4226" for a sentence that begins at 4223) — none is a false citation, but each truncates mid-sentence in a way that hides the governing clause, so the quotes have been extended to whole sentences and the ranges corrected. No inherited citation in this chapter was found pointing at text that is not there. One inherited note claimed 7.8 "contains no RFC 2119 keyword at all" — verified true, lines 4340-4353, and it is the only subsection in section 7 of which that is true.

TWO STANDING WARNINGS FOR WHOEVER TAKES THIS FURTHER. First, normative strength in section 7 cannot be read off the words: at least five load-bearing sentences are LOWERCASE shoulds that a reader will take for RFC 2119 SHOULDs — line 4193 (establish a primary mailbox address), 4288 (be certain of the server's authenticity), 4326-4327 (be aware of trace disclosure), 4327 (supply the FOR clause with caution), 4336-4338 (configure servers appropriately). Anyone tiering these events by strength must read the case, not the keyword. The genuine 2119 keywords in the whole of section 7 are: SHOULD NOT at 4220, 4231 and 4235; MUST NOT at 4241; MUST at 4244; SHOULD at 4272 twice; SHOULD at 4304; SHOULD at 4378; SHOULD at 4380. Section 8's only one is the MUST at 4445. Second, an ANCHOR GAP: section 7.9, Scope of Operation of SMTP Servers (heading at 4355), is not in this chapter's anchor list and appears in no other chapter's either, yet it is the source of MailRejectedForPolicyReason and half of RelayAccessRestrictedToKnownSources and it is what the roster line "unsolicited mail refused" most directly names. Both are claimed here and flagged rather than dropped. Section 6.2, "Unwanted, Unsolicited, and \"Attack\" Messages" (heading at 4013), is anchored to the sections 5-6 chapter and carries BounceSuppressedForHostileContent; it is cross-referenced, not claimed.

PROJECT SCREENS NAMED, NOT APPLIED. Per the instruction, no candidate was rejected by any FnEmail filter, and each event's notes name the screen that would have killed it. The tally is stark: 11 of the 40 have an actor that never opens an SMTP connection (site operator, mailbox administrator, list administrator, IANA, IESG, two operators agreeing out of band, an attacker); 8 would die to the consumer test alone; every one of the 8 registry and configuration events would die to all four altitude tiers at once; the HELO-only charter kills everything VRFY-, EXPN- and EHLO-shaped, which is 9 events; and the condemned status/phase register would kill the disclosure-capability and conformance events, which the brief explicitly re-licenses since both authors use status-shaped events in their own worked material. The chapter also contains three events with no command anywhere behind them — InternalHostNamesDisclosedInTraceField, BlindCopyRecipientDisclosedInForClause and ConnectionClosedDefensively — which the "one event per command" design target has no shape for at all.

FIELDLESS-EVENT TENSION, RECORDED NOT RESOLVED. No event here ended up with zero fields, but two came close and were held to three rather than padded: SpoofedOriginDetected and ServerRenderedInaccessibleByAttack, both of which are substantively "this happened, to this party, on this day". The kit permits an event with no meaningful payload beyond its identity and separately warns that an event without fields is an opaque label; both applied, and the resolution taken was the minimum that keeps the event readable without padding to reach a count. Two computed fields were dropped from inherited candidates on the only-facts rule — rejected_recipient_count (48) from AbusePatternDetected and replies_issued_before_close (12) from ConnectionClosedForAbuse — and in both cases the counter-argument, that section 7.8's own criterion IS a count, is preserved in the surviving event's notes rather than settled.