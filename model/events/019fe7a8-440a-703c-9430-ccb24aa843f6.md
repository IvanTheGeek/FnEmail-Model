# `ViaClauseRecorded`

A gateway named the environment and protocol it had forwarded from in the via clause of the Received: header field it supplied.

| Grouping | Value | Fixed at |
|:--|:--|:--|
| Workflow | 4. Trace and Provenance | Step 1 — timeline discovery |
| Stream root | Message | Step 1 — stream roots |
| Ownership swimlane | — | Step 6 — events section, per team or system |
| Originating actor | Gateway SMTP | Step 3 — actor swimlane; provenance, not this event's row |

🟧 Event · confidence *likely* · recorded 2026-08-09, Step 1 Brain Storming, whole-spec sweep of RFC 5321 · `019fe7a8-440a-703c-9430-ccb24aa843f6`

## Data

| Field | Type | Cardinality | Example | Example source |
|:--|:--|:--|:--|:--|
| `gateway_host` | Domain | Single | gw.foo.com | illustrative — no RFC source |
| `via_environment` | Text | Single | X.400 | rfc5321.txt:3466 |
| `via_protocol` | Text | Single | P1 | illustrative — no RFC source |
| `via_link` | Text | Single | X.400 | illustrative — no RFC source as a Link value; X.400 appears at rfc5321.txt:3466 as an address form, and Addtl-Link at rfc5321.txt:3352 admits any Atom, with unregistered names a SHOULD NOT at rfc5321.txt:3356-3357 |

## Where RFC 5321 says so

| Section | Lines | Text |
|:--|:--|:--|
| §3.7.2 | 1591-1592 | *"The gateway SHOULD indicate the environment and protocol in the "via" clauses of Received header field(s) that it supplies."* |
| §4.4 | 3332 | *"Via = CFWS "VIA" FWS Link"* |
| §4.4 | 3355-3356 | *"Via" is primarily of value ; with non-Internet transports."* |

Line numbers are into [`docs/rfc/rfc5321.txt`](../../docs/rfc/rfc5321.txt), the archived copy. Quotations keep the specification's own words and spellings (AGENTS.md rule 2).

## Notes

Carried over from idx 92 and strengthened by the re-sweep with two §4.4 citations the original lacked: the Via production at 3332 and the comment at 3355-3356 saying the clause is 'primarily of value with non-Internet transports', which independently confirms that this event belongs to the gateway case and not to ordinary relaying. Kept separate from ReceivedLinePrependedByGateway on strength grounds, which the inherited record argued well and I am keeping verbatim in substance: prepending is MUST, populating the via clause is SHOULD, and collapsing them would silently promote a SHOULD to a MUST — the exact failure rule 3 records from the 2026-08-06 session. Note the plural 'header field(s)' at 1592: one gateway may supply more than one line. The legal values of via_protocol come from the registry, so ProtocolNameRegistered is this event's unwritten precondition and UnregisteredTraceNameUsed is what happens when it was skipped. FnEmail screens: the consumer test / G1, the altitude tiers, and inbound-only scope. ⚠️ FIELD HYGIENE PASS: struck the fabricated `message_ref` identity key — RFC 5321 supplies none and f2C8D14 is this project's minted queue id. Identity is the supplied line, and the plural 'header field(s)' at 1592 means even a hop is not necessarily singular here. Sourcing exposes a real asymmetry between the prose and the grammar: §3.7.2 1591-1592 asks for 'the environment and protocol' while the ABNF gives Via = Link (3332) and With = Protocol (3334), so these two fields span two clauses despite the field names. X.400 is at least the RFC's own token, named at line 3466; P1 is invented — the RFC names no protocol of any foreign environment.

COLLISION RESOLVED — merged from 2 independently brainstormed records. The two records cite exactly the same three references — 3.7.2 lines 1591-1592, 4.4 line 3332, 4.4 lines 3355-3356 — name the same actor (Gateway SMTP) and the same stream root, Message. Identical event, assembled twice from the same lines. Merge. Chapter 9 takes it because the actor is a gateway and 3.7.2 is the gatewaying section, matching the ReturnPathInsertedByOutboundGateway resolution; the merge also settles a genuine field disagreement in favor of the reading the ABNF supports.

From the duplicate in 9. Gatewaying: Kept separate from ReceivedLinePrependedByGateway ON PURPOSE because the strengths differ: prepending is MUST, populating the via clause is SHOULD. Collapsing them would silently promote a SHOULD to a MUST — precisely the failure AGENTS.md rule 3 records from the 2026-08-06 session. Note the plural "header field(s)" at line 1592: a gateway may supply more than one. CITATION FINDING from the re-sweep, not previously recorded: §3.7.2 asks for "the environment and protocol in the 'via' clauses", but §4.4's ABNF gives Via only a Link (line 3332, Link = "TCP" / Addtl-Link at line 3350) and puts Protocol in the separate With clause (line 3334). The two clauses cannot both live in Via, so the §3.7.2 sentence cannot be followed literally — a real defect in the document and a legitimate 🟥 hotspot. The field is therefore named via_link, matching the grammar, not the prose. Line 3355-3356 corroborates the gateway use: Via is "primarily of value with non-Internet transports". Rejected alternative names: TransitEnvironmentDeclared, GatewayViaClausePopulated. Which project screen would have killed it: consumer test / G1, altitude tiers, inbound-only scope.

FIELD HYGIENE PASS: struck message_ref (example f2C8D14) — RFC 5321 names no message identifier; the via clause belongs to one Received: header field, and the gateway that supplied it is what identifies the record. Re-verified the ABNF while sourcing via_link and the re-sweep's citation finding holds: Via = CFWS "VIA" FWS Link at line 3332, Link = "TCP" / Addtl-Link at line 3350, Addtl-Link = Atom at line 3352. One consequence worth adding: "X.400" is not a registered link name, and lines 3356-3357 say SMTP servers SHOULD NOT use unregistered names — so the illustrative value is itself in tension with a SHOULD NOT, which is a second small hotspot on the same clause.
