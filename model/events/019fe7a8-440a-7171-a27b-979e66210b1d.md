# `MailingListAccessRestricted`

An access control was placed on a mailing list so that expansion requests from unauthorized parties are refused.

| Grouping | Value | Fixed at |
|:--|:--|:--|
| Workflow | 13. Address Diagnostics | Step 1 — timeline discovery |
| Stream root | List | Step 1 — stream roots |
| Ownership swimlane | — | Step 6 — events section, per team or system |
| Originating actor | Mailing list administrator | Step 3 — actor swimlane; provenance, not this event's row |

🟧 Event · confidence *likely* · recorded 2026-08-09, Step 1 Brain Storming, whole-spec sweep of RFC 5321 · `019fe7a8-440a-7171-a27b-979e66210b1d`

## Data

| Field | Type | Cardinality | Example | Example source |
|:--|:--|:--|:--|:--|
| `list_name` | Text | Single | Executive-Washroom-List | rfc5321.txt:1322 |
| `permitted_requestors` | Text | List | authenticated requestors within foo.com | illustrative — no RFC source; the RFC shows only the refusal at rfc5321.txt:1322-1323 and never the configuration behind it |

## Where RFC 5321 says so

| Section | Lines | Text |
|:--|:--|:--|
| §3.5.1 | 1322-1323 | *"C: EXPN Executive-Washroom-List S: 550 Access Denied to You."* |
| §3.5.1 | 1315-1318 | *"C: EXPN Example-People S: 250-Jon Postel \<Postel@isi.edu>"* |
| §4.1.4 | 2496-2500 | *"the example in RFC 821 that shows "550 access denied to you" in response to an EXPN command is incorrect unless an EHLO command precedes the EXPN or the denial of access is based on the client's IP address or other authentication or authorization-determining mechanisms"* |

Line numbers are into [`docs/rfc/rfc5321.txt`](../../docs/rfc/rfc5321.txt), the archived copy. Quotations keep the specification's own words and spellings (AGENTS.md rule 2).

## Notes

Inherited (candidate 13), with §4.1.4 added by the re-sweep. Strength: none in §3.5.1 — the RFC shows only the refusal and never the configuration that produces it, a precondition nobody wrote down. The cleanest evidence that access is per-list rather than per-command is the RFC's own pair of examples fifteen lines apart: Example-People at 1315 expands freely, Executive-Washroom-List at 1322 does not, on the same server in the same section. §4.1.4 supplies the basis the configuration must rest on if the refusal is to be legitimate — session identity, client IP address, or authentication. Rejected alternative names: ListVisibilityRestricted, ExpnAccessPolicySet, MailingListProtected. Runtime arm: ExpnAccessDenied. Sibling at server scope: AddressDiagnosticsRestrictedToAuthenticatedRequestors. Which screen would have killed it: the four altitude tiers, the inbound-only scope, the twelve-slice model, and the responsibility-boundary cutoff — the configuration happens long before any session opens. FIELD HYGIENE PASS: nothing struck. No reply code and no invented identity key. The sourcing pass sharpened what the note already said in prose: the list name is quoted from the RFC at 1322, and the permitted-requestor set has no source anywhere, because the RFC never writes down the configuration that produces its own '550 Access Denied to You.' — the precondition nobody wrote down, now visible in an example_source rather than only in a note. Identity here is the list itself, which is the stream root; RFC 5321 supplies no configuration key of any kind.
