# `VerificationAlwaysRefusedWith550`

A server answered every VRFY with 550 regardless of whether the address was valid, claiming to support the command while conveying nothing.

| Grouping | Value | Fixed at |
|:--|:--|:--|
| Workflow | 16. Abuse, Security and Registry | Step 1 — timeline discovery |
| Stream root | Session | Step 1 — stream roots |
| Ownership swimlane | — | Step 6 — events section, per team or system |
| Originating actor | Receiving SMTP server | Step 3 — actor swimlane; provenance, not this event's row |

🟧 Event · confidence *likely* · recorded 2026-08-09, Step 1 Brain Storming, whole-spec sweep of RFC 5321 · `019fe7a8-440a-71df-8845-883e52d8fe78`

## Data

**No data.** The method sanctions this: *"If an event has no meaningful payload beyond its identity (e.g., a simple state transition), it is fine to have no fields or just the identity key."* It also warns, of a different case, that *"An event without fields is an opaque label"* — both are recorded here rather than reconciled, because reconciling them is a later step's work, not Brain Storming's.

## Where RFC 5321 says so

| Section | Lines | Text |
|:--|:--|:--|
| §7.3 | 4249-4251 | *"Of course, an implementation that "supports" VRFY by always returning 550 whether or not the address is valid is equally not in conformance."* |

Line numbers are into [`docs/rfc/rfc5321.txt`](../../docs/rfc/rfc5321.txt), the archived copy. Quotations keep the specification's own words and spellings (AGENTS.md rule 2).

## Notes

RECOVERED BY THE RE-SWEEP; no inherited candidate carried it. The second named non-conformance in 7.3 and the mirror of the first: 4247-4248 fakes a yes, 4249-4251 fakes a no, and the RFC calls both violations of the same rule. Strength: 'equally not in conformance' — a conformance verdict with no 2119 keyword, which is why confidence is likely: the RFC states the practice and condemns it, never specifying the act. Rejected names: BlanketVerificationRefusal, VrfyStubbedWith550, AllAddressesReportedInvalid. Sibling in another chapter: VerificationRefusedAsUnimplemented (3.5.3 lines 1375-1381, the 500/502 case), deliberately not merged; note the unresolved ambiguity that 1376-1377 says 502 or 500 SHOULD be returned in the syntax-only case while 1380-1381 calls returning 500 or 502 for VRFY not full compliance. Screens that would have killed it: the condemned status register, the query filter. FIELD HYGIENE: THIS EVENT IS NOW FIELDLESS, and that is the correct outcome, not a defect. All three fields were fixed by the event's own name and carried no information: 4249-4251 speaks only of VRFY, so command could never be anything else; 550 is in the name and is in any case the rendering of the decision; server_host restated the actor. The method sanctions this outright — an event with no payload beyond its identity is fine with no fields — and zero fieldless events across all 567 candidates is what exposed the padding. Confidence stays likely: fieldlessness is not doubt, and the justification at 4249-4251 never rested on the struck fields. If a facilitator re-roots this at Server Configuration rather than Session — defensible, since it is a standing implementation behavior, not a session act — the server becomes its identity field and it stops being fieldless.
