# `AddressRecordPublished`

An A or AAAA record was published giving a host name an IP address — the record an exchanger name must resolve to.

| Grouping | Value | Fixed at |
|:--|:--|:--|
| Workflow | 6. Address Resolution and Next-Hop Selection | Step 1 — timeline discovery |
| Stream root | DNS Zone | Step 1 — stream roots |
| Ownership swimlane | — | Step 6 — events section, per team or system |
| Originating actor | Domain administrator / DNS zone operator | Step 3 — actor swimlane; provenance, not this event's row |

🟧 Event · confidence *doubtful* · recorded 2026-08-09, Step 1 Brain Storming, whole-spec sweep of RFC 5321 · `019fe7a8-440a-7025-9fa7-983b8f6a42ac`

## Data

| Field | Type | Cardinality | Example | Example source |
|:--|:--|:--|:--|:--|
| `host_name` | DomainName | Single | mail.foo.com | illustrative — no RFC source |
| `ip_address` | IpAddress | Single | 192.0.2.10 | walk scene |

## Where RFC 5321 says so

| Section | Lines | Text |
|:--|:--|:--|
| §5.1 | 3856-3858 | *"That domain name, when queried, MUST return at least one address record (e.g., A or AAAA RR) that gives the IP address of the SMTP server to which the message should be directed."* |
| §2.3.5 | 714-718 | *"names that can be resolved to MX RRs or address (i.e., A or AAAA) RRs (as discussed in Section 5) are permitted"* |
| §5.2 | 3941-3943 | *"The host domains to which MX records point may, consequently, contain "A RR"s (IPv4), "AAAA RR"s (IPv6), or any combination of them."* |

Line numbers are into [`docs/rfc/rfc5321.txt`](../../docs/rfc/rfc5321.txt), the archived copy. Quotations keep the specification's own words and spellings (AGENTS.md rule 2).

## Notes

RECOVERED BY THE RE-SWEEP — the third of the DNS-publication family named as siblings in the inherited notes and never carried. Precondition for MailExchangerAddressResolved; its absence is MailExchangerTargetFoundUnresolvable, which is the only reason the MUST at 3856-3858 can be violated at all. Also the precondition for §2.3.5's EHLO primary-host-name rule at lines 722-724, which belongs to the Session chapter — cross-referenced rather than claimed. Whether the three publication events (MX, CNAME, address) are one event with a record_type field or three is a genuine merge question for a later step; kept as THREE because the branch each one feeds in §5.1 is different — MX feeds the lookup, CNAME feeds the chase, address feeds the delivery attempt. Merging them would erase that. record_type distinguishes A from AAAA and is what AddressFamilyCandidateSkipped reads. ⚠️ FIELD HYGIENE PASS: struck record_type as a derived value — the address family is readable off ip_address itself (192.0.2.10 is IPv4, therefore an A RR), so the tag records nothing the address does not already fix, and the inherited claim that AddressFamilyCandidateSkipped reads it is answered the same way. The same strike was applied to record_type on MailExchangerAddressResolved. Note this weakens the inherited argument just above for keeping the three publication events separate — that argument now rests entirely on the §5.1 branch each one feeds, which is still sound. No fabricated identity key was present. host_name is illustrative because RFC 5321 names no exchanger host outside D.3's own domains. Which project screen would have killed it: all of the scope screens.
