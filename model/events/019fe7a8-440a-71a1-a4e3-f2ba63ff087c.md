# `SmtpClientRoleAssumed`

Having accepted the relaying task, the server changed role and became an SMTP client for the onward leg.

| Grouping | Value | Fixed at |
|:--|:--|:--|
| Workflow | 8. Relaying | Step 1 — timeline discovery |
| Stream root | Relay Task | Step 1 — stream roots |
| Ownership swimlane | — | Step 6 — events section, per team or system |
| Originating actor | Relay SMTP server | Step 3 — actor swimlane; provenance, not this event's row |

🟧 Event · confidence *likely* · recorded 2026-08-09, Step 1 Brain Storming, whole-spec sweep of RFC 5321 · `019fe7a8-440a-71a1-a4e3-f2ba63ff087c`

## Data

**No data.** The method sanctions this: *"If an event has no meaningful payload beyond its identity (e.g., a simple state transition), it is fine to have no fields or just the identity key."* It also warns, of a different case, that *"An event without fields is an opaque label"* — both are recorded here rather than reconciled, because reconciling them is a later step's work, not Brain Storming's.

## Where RFC 5321 says so

| Section | Lines | Text |
|:--|:--|:--|
| §3.6.2 | 1451-1452 | *"If it accepts the task, it then becomes an SMTP client"* |
| §2.1 | 409-412 | *"An SMTP server may be either the ultimate destination or an intermediate "relay" (that is, it may assume the role of an SMTP client after receiving the me"* |
| §2.3.2 | 651-654 | *"Since a given host may act both as server and client in a relay situation, "receiver" and "sender" terminology is still used where needed for clarity."* |

Line numbers are into [`docs/rfc/rfc5321.txt`](../../docs/rfc/rfc5321.txt), the archived copy. Quotations keep the specification's own words and spellings (AGENTS.md rule 2).

## Notes

Merged from #76 and #42; rejected names: RelayRoleAssumed, ClientRoleAdopted, RoleFlippedToClient. Deliberately kept as a STATUS-SHAPED event and flagged as such: a role transition with almost no payload. The kit's tension is recorded, not resolved - 'If an event has no meaningful payload beyond its identity (e.g., a simple state transition), it is fine to have no fields or just the identity key,' against 'An event without fields is an opaque label.' Strength: declarative ('it then becomes', 1452), a stated consequence of acceptance. 2.3.2 lines 651-654 keep the old sender/receiver vocabulary alive precisely because client/server is inadequate here - a hint that any model with a fixed 'we are the server' stance will mis-fit relaying. Screens: the condemned status/phase register, the two-kinds lens, inbound-only scope. --- FIELD HYGIENE - NOW FIELDLESS. Struck queue_id (f2C8D14 appears nowhere in rfc5321.txt; it is this project's own minted value, and no queue exists at the moment of the role change, which 1451-1452 puts immediately after acceptance) and host (recorder's own domain). Nothing survives, which is the outcome the inherited note was already pointing at, and the method sanctions it explicitly. Identity is position in the session: this follows RelayTaskAccepted on the same channel.
