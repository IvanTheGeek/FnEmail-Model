# `AddressFamilyCandidateSkipped`

A client passed over candidate records or addresses of an address family it cannot use — an IPv6-only client skipping A RRs and IPv4-only servers.

| Grouping | Value | Fixed at |
|:--|:--|:--|
| Workflow | 6. Address Resolution and Next-Hop Selection | Step 1 — timeline discovery |
| Stream root | Next-Hop Destination | Step 1 — stream roots |
| Ownership swimlane | — | Step 6 — events section, per team or system |
| Originating actor | SMTP client | Step 3 — actor swimlane; provenance, not this event's row |

🟧 Event · confidence *doubtful* · recorded 2026-08-09, Step 1 Brain Storming, whole-spec sweep of RFC 5321 · `019fe7a8-440a-701c-b692-e778614b1be2`

## Data

| Field | Type | Cardinality | Example | Example source |
|:--|:--|:--|:--|:--|
| `exchanger` | DomainName | Single | mail.foo.com | illustrative — no RFC source |
| `client_stack` | String | Single | IPv6-only | rfc5321.txt:3950 |

## Where RFC 5321 says so

| Section | Lines | Text |
|:--|:--|:--|
| §5.2 | 3939-3943 | *"In the contemporary Internet, SMTP clients and servers may be hosted on IPv4 systems, IPv6 systems, or dual-stack systems that are compatible with either version of the Internet Protocol."* |
| §5.2 | 3947-3951 | *"The appropriate actions to be taken either will depend on local circumstances, such as performance of the relevant networks and any conversions that might be necessary, or will be obvious (e.g., an IPv6-only client need not attempt to look up A RRs or attempt to reach IPv4-only servers)."* |
| §5.2 | 3943-3946 | *"While RFC 3974 [39] discusses some operational experience in mixed environments, it was not comprehensive enough to justify standardization, and some of its recommendations appear to be inconsistent with this specification."* |

Line numbers are into [`docs/rfc/rfc5321.txt`](../../docs/rfc/rfc5321.txt), the archived copy. Quotations keep the specification's own words and spellings (AGENTS.md rule 2).

## Notes

RECOVERED BY THE RE-SWEEP. §5.2 was folded entirely into another event's notes by the inherited sweep and left with no event of its own, although IPv4/IPv6 selection is named in this chapter's charter. Strength: NONE — §5.2 contains zero RFC 2119 keywords across lines 3937-3956, measured rather than estimated by the inherited span notes and confirmed here; 'will depend on local circumstances' and 'need not attempt' are the strongest language available, so this event is permission-less by construction. 🟥, and an unusually deep one: RFC 3974 is named and then explicitly declined as a standard (3943-3946), so there is no interoperable rule for mixed environments at all, and two conforming clients may build entirely different candidate lists from the same zone. Doubtful for that reason, not because the state change is unreal. Rejected alternative names: IpVersionCandidateFiltered, AddressRecordTypeIgnored. Which project screen would have killed it: every scope screen, plus the consumer test. ⚠️ FIELD HYGIENE PASS: struck skipped_family as a derived value — the RFC's own clause at 3949-3951 makes the skip follow from the stack ('an IPv6-only client need not attempt to look up A RRs or attempt to reach IPv4-only servers'), so given client_stack the skipped family is the rule restated, not an observation. The inherited claim on AddressRecordPublished that this event 'reads record_type' is answered the same way: the address family is readable off the address itself. No fabricated identity key was present. client_stack takes the RFC's own wording at 3950; exchanger stays illustrative because §5.2 supplies no worked instance.
