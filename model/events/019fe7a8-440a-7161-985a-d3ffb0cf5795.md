# `CrossTypeVerificationRefused`

The server refused a diagnostic request because the named entry was the wrong kind — a mailing list asked about as a user, or a user asked about as a list.

| Grouping | Value | Fixed at |
|:--|:--|:--|
| Workflow | 13. Address Diagnostics | Step 1 — timeline discovery |
| Stream root | Address Diagnostic Request | Step 1 — stream roots |
| Ownership swimlane | — | Step 6 — events section, per team or system |
| Originating actor | Receiving SMTP Server | Step 3 — actor swimlane; provenance, not this event's row |

🟧 Event · confidence *likely* · recorded 2026-08-09, Step 1 Brain Storming, whole-spec sweep of RFC 5321 · `019fe7a8-440a-7161-985a-d3ffb0cf5795`

## Data

| Field | Type | Cardinality | Example | Example source |
|:--|:--|:--|:--|:--|
| `command` | Text | Single | VRFY | rfc5321.txt:1359 |
| `named_entry` | Text | Single | Example-People | rfc5321.txt:1315 |
| `actual_kind` | Text | Single | mailing list | rfc5321.txt:1282 |

## Where RFC 5321 says so

| Section | Lines | Text |
|:--|:--|:--|
| §3.5.1 | 1279-1283 | *"otherwise an error SHOULD be reported (e.g., "550 That is a mailing list, not a user" or "252 Unable to verify members of mailing list")"* |
| §3.5.1 | 1295-1297 | *"positive response consisting of a list containing one name, or an error MAY be reported (e.g., "550 That is a user name, not a mailing list")"* |
| §3.5.1 | 1276-1279 | *"a common data structure may hold both types of entries, and it is possible to have mailing lists containing only one mailbox"* |

Line numbers are into [`docs/rfc/rfc5321.txt`](../../docs/rfc/rfc5321.txt), the archived copy. Quotations keep the specification's own words and spellings (AGENTS.md rule 2).

## Notes

Inherited (candidate 27). Citation correction from the re-sweep: the inherited record cited lines '1283-1297' for the EXPN-on-a-user quote, a range that crosses the page break at 1285-1294 (footer, header, blank lines). The quote is now cut to 1295-1297 so it does not silently span a page break — the material fact is unchanged. Strength asymmetry, deliberately preserved rather than smoothed: VRFY-on-a-list says an error SHOULD be reported (1281-1282), EXPN-on-a-user says an error MAY be reported (1296) — so the two directions do not carry equal weight and a facilitator may split this event on that ground. Two reply codes are offered for the same refusal at 1282-1283, 550 and '252 Unable to verify members of mailing list', and they mean different things: 550 says wrong kind, 252 says cannot see inside. That is a THIRD distinct use of 252 in this chapter, alongside the §3.5.3 unverifiable case and the §7.3 disabled case; a later step must not conflate the three. Rejected alternative names: WrongEntryKindReported, VrfyAppliedToListRefused (splits the event), ExpnAppliedToUserRefused (the other half). Which screen would have killed it: 'one event per command', the capture/decision lens, the twelve-slice model. Reachable only because 1276-1279 says one data structure holds both kinds. FIELD HYGIENE PASS: struck reply_code (550), and this strike cost something real, recorded rather than papered over. The note above says 550 and 252 are two different refusals at the same moment; with the code gone the field set no longer distinguishes them, and only the note and the rfc_refs carry the distinction. It still goes: a reply code is the rendering of a decision, and RFC 5321 does not carry one as data beyond the wire. Whether to split the event on that distinction is a later step's decision, not a field. command is kept because it genuinely varies — VRFY-on-a-list versus EXPN-on-a-user, and the two directions carry different modal strength. actual_kind is kept because the finding about the entry is what makes the refusal meaningful, and its value is the RFC's own word at line 1282. No invented identity key was present.
