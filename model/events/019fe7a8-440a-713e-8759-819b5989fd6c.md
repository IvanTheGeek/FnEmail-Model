# `ExpansionDeferredUntilAfterAcceptance`

A server was set to postpone mailing list and alias processing until after the message has been accepted, rather than performing it while the client waits on RCPT.

| Grouping | Value | Fixed at |
|:--|:--|:--|
| Workflow | 10. Alias and List Expansion | Step 1 — timeline discovery |
| Stream root | Server Configuration | Step 1 — stream roots |
| Ownership swimlane | — | Step 6 — events section, per team or system |
| Originating actor | Site operator | Step 3 — actor swimlane; provenance, not this event's row |

🟧 Event · confidence *doubtful* · recorded 2026-08-09, Step 1 Brain Storming, whole-spec sweep of RFC 5321 · `019fe7a8-440a-713e-8759-819b5989fd6c`

## Data

| Field | Type | Cardinality | Example | Example source |
|:--|:--|:--|:--|:--|
| `host` | Domain | Single | foo.com | rfc5321.txt:4882 |

## Where RFC 5321 says so

| Section | Lines | Text |
|:--|:--|:--|
| §4.5.3.2.3 | 3635-3636 | *"A longer timeout is required if processing of mailing lists and aliases is not deferred until after the message was accepted."* |

Line numbers are into [`docs/rfc/rfc5321.txt`](../../docs/rfc/rfc5321.txt), the archived copy. Quotations keep the specification's own words and spellings (AGENTS.md rule 2).

## Notes

RECOVERED BY THE RE-SWEEP. PseudoMailboxExpanded's inherited notes name this candidate and say it was "dropped for room" — the cap again. §4.5.3.2.3 heading confirmed at line 3633 ("RCPT Command: 5 Minutes"), outside this chapter's four anchors but cited because it is the only place in RFC 5321 that fixes WHEN expansion happens, and the timing decides whether every other event in this chapter is visible to the client inside the session or invisible outside it. That is the single most consequential fact about expansion for a model built around session boundaries, and it is stated in one sentence in a timeouts subsection. Strength: stated as a consequence ("A longer timeout is required if..."), with no RFC 2119 keyword and no preference expressed between the two arrangements. Rejected alternative names: ExpansionTimingConfigured, ListProcessingDeferred, InlineExpansionDisabled. Chapter 1 (Session Lifecycle, which owns §4.5.3.2) and chapter 15 both have a claim; recorded here because the state change is about expansion. A per-message arm — that a particular transaction's expansion was deferred — is conceivable and not offered, because the RFC frames it as an arrangement rather than a per-message choice. FIELD HYGIENE PASS, AND A CHANGE OF CONFIDENCE: deferred struck — 1 field. It was a Boolean whose only sanctioned value, true, restates the event's own name; a `deferred: false` instance would contradict the name it sits under, which is the test that shows the field carries no information. CONFIDENCE LOWERED from likely to doubtful, and the reason is the strike: with the flag gone the record is a host and a name, and what it was recording is a setting on a server's configuration — a field wearing an event's name, which is the exact collapse the field-hygiene brief describes. The counter-argument is preserved and is not weak: §4.5.3.2.3 lines 3635-3636 is the only sentence in RFC 5321 that fixes when expansion happens, and the arrangement it names is what decides whether the whole of this chapter falls inside a session or outside it, which is a genuine state of the world an operator brought about. Kept, not dropped; deciding is a later step's job. Compare AddressExpansionSupportEnabled, which survived at likely because supported_models is a real value set the spec names rather than a restatement of the event name.
