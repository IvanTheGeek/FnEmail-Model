# `ErrorReportAddressDesignatedForNonSmtpTransport`

A system using RFC 822 syntax over a non-SMTP transport nominated an unambiguous address, tied to the transport envelope, for non-delivery reports.

| Grouping | Value | Fixed at |
|:--|:--|:--|
| Workflow | 4. Trace and Provenance | Step 1 — timeline discovery |
| Stream root | Transport Environment | Step 1 — stream roots |
| Ownership swimlane | — | Step 6 — events section, per team or system |
| Originating actor | Administrator of a non-SMTP system | Step 3 — actor swimlane; provenance, not this event's row |

🟧 Event · confidence *doubtful* · recorded 2026-08-09, Step 1 Brain Storming, whole-spec sweep of RFC 5321 · `019fe7a8-440a-7048-ba4e-6318d98295eb`

## Data

| Field | Type | Cardinality | Example | Example source |
|:--|:--|:--|:--|:--|
| `transport` | Text | Single | X.400 | rfc5321.txt:3466 |
| `error_report_address` | Mailbox | Single | \<postmaster@foo.com> | rfc5321.txt:3395 for the reserved postmaster name; walk scene for foo.com |

## Where RFC 5321 says so

| Section | Lines | Text |
|:--|:--|:--|
| §4.4 | 3243-3246 | *"Systems using RFC 822 syntax with non-SMTP transports SHOULD designate an unambiguous address, associated with the transport envelope, to which error reports (e.g., non-delivery messages) should be sent."* |

Line numbers are into [`docs/rfc/rfc5321.txt`](../../docs/rfc/rfc5321.txt), the archived copy. Quotations keep the specification's own words and spellings (AGENTS.md rule 2).

## Notes

Carried over from idx 208. SHOULD. Doubtful, and honestly so: the span states this as a requirement on those systems rather than describing an occasion when it happens, so the event is inferred from the requirement rather than observed in the text — that is exactly the kind of inference this chapter should show its working on rather than quietly make. Kept because Step 1 keeps what it is unsure of, and because it is the outer edge of 'who else acts': the actor is not running SMTP at all and never appears in any session. STREAM ROOT INVENTED: 'Transport Environment'. None of the offered roots fits — this is a durable fact about a whole non-SMTP mail environment, not about a Session, Message, Server Configuration, or anything with a lifetime shorter than years. Kept separate from the list-maintainer's error mailbox (ErrorHandlingMailboxDesignated, idx 207, §4.4 3214-3219) because actor and object differ: that one nominates a mailbox for a list, this one nominates an address for an entire transport. FnEmail screens: every altitude tier, and the responsibility-boundary cutoff twice over. ⚠️ FIELD HYGIENE PASS: nothing struck — this event never carried `message_ref`, correctly, since it predates and outlives any message. Sourcing is now stated and is better than expected: postmaster is the RFC's own reserved mailbox name at line 3395, and X.400 is its own token at 3466, so both values trace to the source even though the event itself is inferred. The confidence stays doubtful for the reason already recorded — the span states a requirement, not an occasion — and this pass finds nothing in the fields to change that either way.
