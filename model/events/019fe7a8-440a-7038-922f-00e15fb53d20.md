# `TraceClauseRegistered`

A new named clause became legal inside a Received: line's optional information, changing what every conforming server may stamp.

| Grouping | Value | Fixed at |
|:--|:--|:--|
| Workflow | 4. Trace and Provenance | Step 1 — timeline discovery |
| Stream root | Extension Registry | Step 1 — stream roots |
| Ownership swimlane | — | Step 6 — events section, per team or system |
| Originating actor | IANA registrant | Step 3 — actor swimlane; provenance, not this event's row |

🟧 Event · confidence *likely* · recorded 2026-08-09, Step 1 Brain Storming, whole-spec sweep of RFC 5321 · `019fe7a8-440a-7038-922f-00e15fb53d20`

## Data

| Field | Type | Cardinality | Example | Example source |
|:--|:--|:--|:--|:--|
| `clause_name` | Atom | Single | TLS | illustrative — no RFC source |
| `registry` | Text | Single | Mail Transmission Types | rfc5321.txt:4407 |
| `registered_by` | Text | Single | standardization | rfc5321.txt:4436 |

## Where RFC 5321 says so

| Section | Lines | Text |
|:--|:--|:--|
| §4.4 | 3341-3345 | *"Additional-Registered-Clauses = CFWS Atom FWS String ; Additional standard clauses may be added in this ; location by future standards and registration with"* |
| §4.4 | 3346-3348 | *"SMTP servers SHOULD NOT use unregistered names."* |
| §8 | 4435-4437 | *"As with link and protocol identifiers, additional clauses may be registered only by standardization or by way of an RFC-documented, IESG-approved, Experimental protocol extension."* |
| §8 | 4419-4422 | *"An additional subsection has been added to the "VIA link types" and "WITH protocol types" subsections of this registry to contain registrations of "Additional-registered-clauses" as described above."* |

Line numbers are into [`docs/rfc/rfc5321.txt`](../../docs/rfc/rfc5321.txt), the archived copy. Quotations keep the specification's own words and spellings (AGENTS.md rule 2).

## Notes

Carried over from idx 197. Answers 'who else acts' and 'over time, not just in a session' directly: the vocabulary of a legal trace line is mutable state held outside every SMTP server and it changes on a timescale of years. Strength: lowercase 'may' for the addition, since it describes a process rather than granting a protocol permission; uppercase SHOULD NOT for using unregistered names, whose violated arm is now its own event (UnregisteredTraceNameUsed, recovered by the re-sweep). Rule 2 note kept from the inherited record and re-verified: the quotes are ABNF comment lines and wrap mid-sentence in the source, with 'IANA.' and 'names.' beginning the continuation lines at 3346 and 3348; they are copied as they break. Kept separate from ProtocolNameRegistered because the two registries govern different things — clause *names* versus clause *values* — and merging them would violate the kit's unique-semantics rule in the direction that hides information. FnEmail screens: all four altitude tiers (this sits above Domain, in method governance) and the consumer test. ⚠️ FIELD HYGIENE PASS: no identity key struck — this event never carried one, correctly, since a registration is not about a message. Two example values were fabricated and are corrected rather than removed, and the old values are recorded here per rule 4. `registry` read 'mail parameters', which is what line 3372 calls the registry for *protocol* names; the clause registry is named at line 4407, 'Mail Transmission Types', with clause registrations held in an added subsection (4419-4422), so the field now carries the RFC's own name for the right registry. `registered_by` read 'IETF standards action', which is RFC 5226's registration-policy vocabulary and appears nowhere in RFC 5321; §8 line 4436 states the actual routes — 'registered only by standardization or by way of an RFC-documented, IESG-approved, Experimental protocol extension' — so the value is now 'standardization', one of the two the source names. clause_name remains invented: TLS is not in RFC 5321, and it cannot be otherwise, because the values this event creates are by definition the ones the RFC does not list.

COLLISION RESOLVED — merged from 2 independently brainstormed records. Same act, same actor, same stream root, same three fields, same corrected registry value reached independently by both. The records differ only in citation fidelity, and the file settles that: the ABNF comment wraps mid-phrase and must be quoted as it breaks.

From the duplicate in 16. Abuse, Security and Registry: Merged from one inherited candidate; section 8 refs added by the re-sweep, where the registration procedure actually lives — 4.4 only forbids using unregistered names. CITATION CORRECTED: the inherited candidate quoted 4.4 lines 3341-3348 as 'Additional standard clauses may be added in this location by future standards and registration with', silently UN-WRAPPING an ABNF comment whose lines break mid-phrase; its own notes admitted the reflow. Rule 2 says a quotation keeps its own words, and that includes its breaks, so the quote above is copied as it breaks, semicolons and all. The vocabulary of a legal trace line is mutable state held outside every SMTP server and changes on a timescale of years — the clearest answer in the RFC to 'what changes over time rather than in a session'. Strength: lowercase 'may be added' for the addition, uppercase SHOULD NOT for using unregistered names. Section 8 lines 4437-4442 add the IESG's criterion. Rejected names: AdditionalClauseRegistered, ReceivedClauseNameRegistered, OptInfoClauseRegistered. Sibling for the violated arm, not promoted at this cap: UnregisteredTraceNameUsed. Screens that would have killed it: every altitude tier, the consumer test. FIELD HYGIENE: nothing struck; two example values corrected against the source — rule 3 applied to a value rather than to a citation. registry read 'mail parameters', which is the registry the ABNF names at line 3372 for additional PROTOCOL names, not for clauses; section 8 lines 4419-4422 puts Additional-registered-clauses in a new subsection of the THIRD registry, 'Mail Transmission Types' (4407), so that is the value now — and the very reason this event is kept separate from ProtocolNameRegistered is what exposed the mix-up. registered_by read 'IETF standards action'; 4435-4437 names two routes and neither is called that, so the value now uses the RFC's own alternatives. clause_name has no RFC source at all — the specification names no registered additional clause, TLS included. Identity: the clause name, supplied by the registry rather than fabricated.
