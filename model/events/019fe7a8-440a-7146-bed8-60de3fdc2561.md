# `RecipientListCopiedIntoHeaderSection`

A client or server wrote the full set of RCPT command arguments into the message header section, exposing recipients that the envelope had kept out of it.

| Grouping | Value | Fixed at |
|:--|:--|:--|
| Workflow | 10. Alias and List Expansion | Step 1 — timeline discovery |
| Stream root | Message | Step 1 — stream roots |
| Ownership swimlane | — | Step 6 — events section, per team or system |
| Originating actor | SMTP client or receiving server | Step 3 — actor swimlane; provenance, not this event's row |

🟧 Event · confidence *likely* · recorded 2026-08-09, Step 1 Brain Storming, whole-spec sweep of RFC 5321 · `019fe7a8-440a-7146-bed8-60de3fdc2561`

## Data

| Field | Type | Cardinality | Example | Example source |
|:--|:--|:--|:--|:--|
| `header_field_name` | String | Single | Apparently-to | rfc5321.txt:4233 |
| `disclosed_recipients` | Mailbox | List | Jones@foo.com, Brown@foo.com | rfc5321.txt:4895, 4899 |
| `writing_host` | Domain | Single | foo.com | rfc5321.txt:4886 |

## Where RFC 5321 says so

| Section | Lines | Text |
|:--|:--|:--|
| §7.2 | 4218-4223 | *"Especially when more than one RCPT command is present, and in order to avoid defeating some of the purpose of these mechanisms, SMTP clients and servers SHOULD NOT copy the full set of RCPT command arguments into the header section, either as part of trace header fields or as informational or private-extension header fields."* |
| §7.2 | 4233-4235 | *"The popular "Apparently-to" header field is a violation of this principle as well as a common source of unintended information disclosure and SHOULD NOT be used."* |
| §7.2 | 4219-4223 | *"in order to avoid defeating some of the purpose of these mechanisms, SMTP clients and servers SHOULD NOT copy the full set of RCPT command arguments into the header section, either as part of trace header fields or as informational or private-extension header fields"* |

Line numbers are into [`docs/rfc/rfc5321.txt`](../../docs/rfc/rfc5321.txt), the archived copy. Quotations keep the specification's own words and spellings (AGENTS.md rule 2).

## Notes

CITATION WIDENED during the merge: the inherited candidate quoted "SMTP clients and servers SHOULD NOT copy" at 4220-4221 and "The popular "Apparently-to" header field is a violation of this" at 4233 — both fragments accurate but severed from their objects. Replaced with the complete sentences at 4218-4223 and 4233-4235. The violated arm of a SHOULD NOT, kept in the RFC's text precisely because it happens routinely. A separate candidate ApparentlyToHeaderFieldUsed was considered in the re-sweep and merged here instead: the Apparently-to field IS this state change with a particular field name, and header_field_name carries the difference. Rejected alternative names: ApparentlyToHeaderFieldUsed, RcptArgumentsWrittenToHeaderSection, RecipientsDisclosedInHeaderSection. Three prohibitions converge in this short section and they are three different acts: line 4220 (do not copy RCPT arguments — this event), lines 4231-4232 (do not deduce envelope/header relationships and alter the header for delivery — a separate event below), line 4235 (do not use Apparently-to — merged here). The honored arm is 🟤 nothing and is in set_aside. Sibling failure in another chapter: blind-copy recipients disclosed in a trace field, §7.6 lines 4327-4330, which is chapter 4's. Screens: the consumer test G1, and the altitude tiers would file it as Infrastructure. FIELD HYGIENE PASS: nothing struck. All three fields survive on sourced values — the field name is the RFC's own "Apparently-to" at line 4233, the recipients are Appendix D.1's, and writing_host names which host acted, which the text makes load-bearing by addressing "SMTP clients and servers" jointly at 4220. This is the writing event; the reading of it by a recipient is BlindCopyRecipientDisclosed, from which the duplicate of header_field_name was struck in this pass, so the field name now lives in one place only, here, where the act of choosing it happened.

COLLISION RESOLVED — merged from 2 independently brainstormed records. Same subsection, same actor, same violated SHOULD NOT, same surviving header_field_name with the same RFC-sourced value. The disagreement was writing_host: one copy kept it because 4220 addresses 'SMTP clients and servers' jointly, so which one acted is load-bearing; the other struck it as an actor restatement. Struck — the actor is already recorded on the event, so the field restates it — with the keeping argument preserved. The recipient examples differ only in which mailboxes were chosen; the Appendix D.1 multi-RCPT pair is preferred because 4218-4219 conditions the SHOULD NOT on more than one RCPT command being present, and D.1 is the RFC's own multi-RCPT scene.

From the duplicate in 16. Abuse, Security and Registry: Merged from one inherited candidate; citation extended — the inherited refs quoted only 'SMTP clients and servers SHOULD NOT copy' (4220) and 'The popular "Apparently-to" header field is a violation of this' (4233), both accurate and both truncated mid-sentence, hiding that 4222-4223 names THREE carriers. The violated arm of the SHOULD NOT, kept in the text because it happens routinely (4223-4224). Rejected names: RcptArgumentsCopiedToHeader, BlindCopyRecipientsExposedInHeader, ApparentlyToHeaderFieldAdded. Distinct from HeaderSectionAlteredFromDeducedEnvelopeRelationship; sibling BlindCopyRecipientDisclosedInForClause (7.6). Screens that would have killed it: the consumer test, the altitude tiers. FIELD HYGIENE: struck writing_host, an actor restatement — the actor field already names client or server. header_field_name survives as a genuine variable, not a constant: 4219-4223 names three carriers (trace header fields, informational, private-extension), so which one was used is a fact, and Apparently-to is the RFC's own named instance at line 4233. disclosed_recipients are walk-scene mailboxes. No identity key: RFC 5321 gives a message none.
