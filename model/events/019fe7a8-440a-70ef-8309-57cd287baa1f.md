# `SendingSystemBlockedByRetryCycle`

A retry cycle attempted every queued message for one unavailable host, consuming the sending system for a long period.

| Grouping | Value | Fixed at |
|:--|:--|:--|
| Workflow | 7. Sending, Queueing and Retry | Step 1 — timeline discovery |
| Stream root | Outbound Queue | Step 1 — stream roots |
| Ownership swimlane | — | Step 6 — events section, per team or system |
| Originating actor | Sending SMTP client | Step 3 — actor swimlane; provenance, not this event's row |

🟧 Event · confidence *likely* · recorded 2026-08-09, Step 1 Brain Storming, whole-spec sweep of RFC 5321 · `019fe7a8-440a-70ef-8309-57cd287baa1f`

## Data

| Field | Type | Cardinality | Example | Example source |
|:--|:--|:--|:--|:--|
| `unavailable_host` | Domain | Single | foo.com | walk scene |
| `per_connection_timeout_seconds` | Integer | Single | 60 | rfc5321.txt:3741-3742 |

## Where RFC 5321 says so

| Section | Lines | Text |
|:--|:--|:--|
| §4.5.4.1 | 3736-3739 | *"An SMTP client may have a large queue of messages for each unavailable destination host. If all of these messages were retried in every retry cycle, there would be excessive Internet overhead and the sending system would be blocked for a long period."* |
| §4.5.4.1 | 3739-3744 | *"Note that an SMTP client can generally determine that a delivery attempt has failed only after a timeout of several minutes, and even a one-minute timeout per connection will result in a very large delay if retries are repeated for dozens, or even hundreds, of queued messages to the same host."* |

Line numbers are into [`docs/rfc/rfc5321.txt`](../../docs/rfc/rfc5321.txt), the archived copy. Quotations keep the specification's own words and spellings (AGENTS.md rule 2).

## Notes

Recovered by the re-sweep as the consequence arm of the SHOULD at 3716-3718 — what happens when the unreachable list is not kept. The RFC names the outcome in its own words at 3738-3739, so this is not inferred harm. No keyword: it is a consequence, not a requirement, which is precisely the class of event a keyword-hunting sweep walks past. Dropped as derived: any count of messages retried or minutes blocked; per_connection_timeout_seconds is a configured fact and stays. Sibling: HostMarkedUnreachable, the avoidance. Screens: inbound-only scope, altitude tiers (a resource-exhaustion fact reads as Infrastructure), consumer test G1. ⚠️ FIELD HYGIENE PASS: STRUCK blocked_from as a bare occurrence stamp — the blocking has no recorded end in the RFC, so the start time is not the beginning of any measured interval. The earlier decision to drop the counts was already correct under the method's derived-value rule and is confirmed here. unavailable_host is the RFC's own term at 3736-3737 and carries identity for this event; the Outbound Queue stream root has no identifier of its own, because RFC 5321 supplies none and a host may hold "one or more areas for queuing messages in transit" (rfc5321.txt:3673-3675). per_connection_timeout_seconds keeps the RFC's own one-minute figure from 3741-3742.
