# `RecipientValidationDeferred`

The server accepted a recipient it could not validate — because DNS was soft-failing, the target was a mailing list, or it was relaying with no access to the delivering system.

| Grouping | Value | Fixed at |
|:--|:--|:--|
| Workflow | 5. Responsibility Handoff and Final Delivery | Step 1 — timeline discovery |
| Stream root | Recipient | Step 1 — stream roots |
| Ownership swimlane | — | Step 6 — events section, per team or system |
| Originating actor | Receiving SMTP server | Step 3 — actor swimlane; provenance, not this event's row |

🟧 Event · confidence *likely* · recorded 2026-08-09, Step 1 Brain Storming, whole-spec sweep of RFC 5321 · `019fe7a8-440a-706c-8f24-7cc3acc474b5`

## Data

| Field | Type | Cardinality | Example | Example source |
|:--|:--|:--|:--|:--|
| `forward_path` | ForwardPath | Single | \<Smith@bar.com> | walk scene |
| `deferral_reason` | Text | Single | soft domain system error | rfc5321.txt:4003 |
| `accepting_host` | Domain | Single | bar.com | walk scene |

## Where RFC 5321 says so

| Section | Lines | Text |
|:--|:--|:--|
| §6.1 | 4001-4004 | *"it may be impossible for the receiving SMTP server to validate all the delivery addresses in RCPT command(s) due to a "soft" domain system error"* |
| §6.1 | 4003-4006 | *"because the target is a mailing list (see earlier discussion of RCPT), or because the server is acting as a relay and has no immediate access to the delivering system."* |
| §3.3 | 1144-1147 | *"However, in practice, some servers do not perform recipient verification until after the message text is received. These servers SHOULD treat a failure for one or more recipients as a "subsequent failure" and return a mail message"* |
| §6.1 | 4000-4006 | *"Some delivery failures after the message is accepted by SMTP will be unavoidable. For example, it may be impossible for the receiving SMTP server to validate all the delivery addresses in RCPT command(s) due to a "soft" domain system error, because the target is a mailing list (see earlier discussion of RCPT), or because the server is acting as a relay and has no immediate access to the delivering system."* |

Line numbers are into [`docs/rfc/rfc5321.txt`](../../docs/rfc/rfc5321.txt), the archived copy. Quotations keep the specification's own words and spellings (AGENTS.md rule 2).

## Notes

Inherited candidate 279, kept, refs unioned with the §3.3 deferred-verification passage that candidate 158 found independently. Not normative in §6.1 — the RFC is explaining WHY post-acceptance failure is unavoidable — but it names a real state change: a recipient accepted with validation still outstanding. §3.3 1144-1147 supplies the normative half at SHOULD strength for servers that do this deliberately. The three stated reasons are three different events in disguise and the deferral_reason field is the seam to split on; the mailing-list one has its own sibling, MailingListExpanded, owned by whoever walks §3.9. This event is the precondition that makes PartialDeliveryFailureDetected possible, so the two belong side by side on the wall. Screens: the capture/decision two-kinds lens and G1. ⚠️ FIELD HYGIENE PASS 2026-08-09 — NOTHING STRUCK; this and SourceRouteStrippedFromReturnPath are the chapter's two clean records. It carried no invented key, no f2C8D14, no reply code and no occurrence timestamp, and the reason is instructive: its stream root is Recipient and the forward-path is a real identity the RFC actually supplies, so the sweep had no gap to fill with a fabrication. deferral_reason is now sourced to line 4003, where the RFC's own '"soft" domain system error' sits — quoted with the RFC's quotation marks around soft, per rule 2.

COLLISION RESOLVED — merged from 2 independently brainstormed records. Same passage, same actor, same stream root, same two surviving fields with the same RFC-sourced value; both copies were themselves unions of an inherited candidate with an independently found 3.3 passage. Both arrived essentially clean, and both gave the same reason for it: the stream root is Recipient and the forward-path is an identity the RFC actually supplies, so the sweep had no gap to fill with a fabrication. That is the audit's control case, found twice independently — the same lesson SourceRouteStrippedFromReturnPath carries.

From the duplicate in 2. Mail Transaction: NOT normative — §6.1 is the RFC explaining why post-acceptance failure is unavoidable — but it names a real state change: a recipient accepted with its validation still outstanding, which is what makes a later subsequent-failure notification possible at all. Three stated reasons are three different events in disguise (soft DNS failure, mailing-list target, relay with no access to the delivering system), and deferral_reason carries which; the mailing-list arm has a sibling owned by §3.9 outside this chapter. §3.3 1144-1147 is the same practice from the other side and continues into a SHOULD about treating the result as a subsequent failure, which belongs to the notification chapter. Screens: the capture/decision two-kinds lens and the consumer test G1. Rejected alternative names: RecipientAcceptedUnvalidated, ValidationPostponed.

FIELD HYGIENE PASS: struck nothing — this is the one event in the chapter that arrived clean. Both fields were checked and both hold: forward_path is the Recipient stream's identity and its value is walk-scene data, and deferral_reason is a stated cause rather than a computed one, with a value taken from the RFC's own words at rfc5321.txt:4003 (the source writes a "soft" domain system error, with quotation marks around soft, which rule 2 preserves in the quoted ref above while the field value carries the phrase plain). The RFC names exactly three reasons at 4000-4006, so the field has a real and closed range: soft domain system error, mailing-list target, relay with no immediate access to the delivering system. No identity key was present to strike, and none is needed: the deferral attaches to the forward-path just accepted.
