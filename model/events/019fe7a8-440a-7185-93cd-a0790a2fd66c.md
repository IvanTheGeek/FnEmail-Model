# `ReplyTextCustomized`

An operator changed the human-readable text a server sends with a reply code, without changing the code.

| Grouping | Value | Fixed at |
|:--|:--|:--|
| Workflow | 15. Server Configuration and Provisioning | Step 1 — timeline discovery |
| Stream root | Server Configuration | Step 1 — stream roots |
| Ownership swimlane | — | Step 6 — events section, per team or system |
| Originating actor | Server operator | Step 3 — actor swimlane; provenance, not this event's row |

🟧 Event · confidence *likely* · recorded 2026-08-09, Step 1 Brain Storming, whole-spec sweep of RFC 5321 · `019fe7a8-440a-7185-93cd-a0790a2fd66c`

## Data

| Field | Type | Cardinality | Example | Example source |
|:--|:--|:--|:--|:--|
| `server_domain` | Domain | Single | foo.com | walk scene |
| `reply_code` | ReplyCode | Single | 220 | rfc5321.txt:4886 |
| `new_text` | Text | Single | foo.com Simple Mail Transfer Service Ready | rfc5321.txt:4886 |
| `server_host` | Domain | Single | bar.com | rfc5321.txt:4881 |

## Where RFC 5321 says so

| Section | Lines | Text |
|:--|:--|:--|
| §4.3.1 | 3022-3033 | *"A receiver MAY substitute text in the replies, but the meanings and actions implied by the code numbers and by the specific command reply sequence MUST be preserved."* |
| §4.2 | 2565-2567 | *"In the general case, the text may be receiver dependent and context dependent, so there are likely to be varying texts for each reply code"* |
| §4.2 | 2606-2608 | *"An SMTP server SHOULD use the text shown in the examples whenever appropriate."* |
| §3.1 | 980-983 | *"a server returning a 554 response on connection opening SHOULD provide enough information in the reply text to facilitate debugging of the sending system"* |
| §4.2 | 2607-2608 | *"An SMTP server SHOULD use the text shown in the examples whenever appropriate."* |
| §4.2.1 | 2727-2728 | *"Each reply text is recommended rather than mandatory, and may even change according to the command with which it is associated."* |

Line numbers are into [`docs/rfc/rfc5321.txt`](../../docs/rfc/rfc5321.txt), the archived copy. Quotations keep the specification's own words and spellings (AGENTS.md rule 2).

## Notes

UNION with GreetingDisclosureConfigured's greeting_text, and the §3.1 980-983 reference is added by the re-sweep: it is the one place the RFC puts a SHOULD on the CONTENT of customized text rather than on the code, and its violated arm — a 554 refusal whose text tells the sender nothing — is a real, recoverable event this record now covers. Three different keywords about one string: MAY to substitute, MUST to preserve the meaning, SHOULD to use the documented text. This is the origin of the project's monospace convention (code fixed, text variable) and the reason a greeting is quotable as real data at all: the RFC's own range runs from 220 foo.com Simple Mail Transfer Service Ready (Appendix D.1, line 4886) to 250 xyz.com is on the air (D.3, line 5057). Screens: the Infrastructure altitude tier and G1. FIELD HYGIENE: struck changed_by (\<postmaster@foo.com>) — an invented field; RFC 5321 records no author for a text substitution, and the acting party is already on the record's actor. reply_code is KEPT, and the lines that make it data rather than the rendering of a decision are §4.2 2565-2567, which says there are 'varying texts for each reply code', and §4.3.1 3022-3033, which holds the code invariant while the text moves: the code is the key the stored text hangs on, not something this event emits. Both surviving examples are the same real greeting line, verified at rfc5321.txt:4886. No session, transaction or connection key was present; the setting is per server and per code, which is exactly its identity.

COLLISION RESOLVED — merged from 2 independently brainstormed records. Both records cite 4.3.1 lines 3022-3033 as the primary and 4.2 lines 2565-2567 alongside it, name the same actor (server operator) and the same stream root, Server Configuration, and describe the same act: the text changed, the code left alone. The differing extras — 4.2.1 in one, 3.1 in the other — are further statements about the same string. Same section, same actor: merge. Chapter 15 takes it under the configuration-root policy.

From the duplicate in 14. Protocol Fault Handling: Inherited from candidate 254; citations checked and correct (the 3022-3033 range legitimately spans a page break). Its previous_text field is dropped here — it is recoverable from the prior event in the stream, so carrying it is padding under the essential-fields rule. Three keywords about one string: MAY to substitute, MUST to preserve the meaning, SHOULD to use the documented text. This is the protocol-level source of the project's own monospace convention — the code is fixed, the text varies — and the RFC's greeting examples show the range it expects: 220 ISIF.USC.EDU Service ready and 220 mail.example.com SuperSMTP v 6.1.2 Service ready (§4.3.1 lines 3011-3015). Lifetime is the server's, not the session's. Kept in this chapter rather than a configuration chapter because §4.3.1 is a chapter anchor and because it is the honored arm of NonStandardReplyCodeInvented. Screens: the altitude tiers (Infrastructure) and G1.

FIELD HYGIENE PASS (chapter 14): changed_by struck — RFC 5321 supplies no operator identity anywhere, and the field only restated what the record's actor already says. reply_code KEPT, and the line that makes it data beyond the wire is 3022-3026: "A receiver MAY substitute text in the replies, but the meanings and actions implied by the code numbers ... MUST be preserved" — the code keys the configured text and exists in the configuration before any session opens, which is exactly the test. new_text KEPT as the event's whole content.
