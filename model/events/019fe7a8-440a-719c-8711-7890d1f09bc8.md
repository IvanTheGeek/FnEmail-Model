# `TraceHeaderFieldRegistered`

A newly created trace header field was added to the BCP 90 registry, as required whenever a trace field beyond Return-path and Received comes into existence.

| Grouping | Value | Fixed at |
|:--|:--|:--|
| Workflow | 15. Server Configuration and Provisioning | Step 1 — timeline discovery |
| Stream root | Extension Registry | Step 1 — stream roots |
| Ownership swimlane | — | Step 6 — events section, per team or system |
| Originating actor | IANA | Step 3 — actor swimlane; provenance, not this event's row |

🟧 Event · confidence *certain* · recorded 2026-08-09, Step 1 Brain Storming, whole-spec sweep of RFC 5321 · `019fe7a8-440a-719c-8711-7890d1f09bc8`

## Data

| Field | Type | Cardinality | Example | Example source |
|:--|:--|:--|:--|:--|
| `field_name` | Text | Single | Received-SPF | illustrative — no RFC source; RFC 5321 names only Return-path and Received, at line 4445 |
| `registry` | Text | Single | the IANA registry established by BCP 90 (RFC 3864) | rfc5321.txt:4446 |

## Where RFC 5321 says so

| Section | Lines | Text |
|:--|:--|:--|
| §8 | 4444-4447 | *"In addition, if additional trace header fields (i.e., in addition to Return-path and Received) are ever created, those trace fields MUST be added to the IANA registry established by BCP 90 (RFC 3864) [11] for use with RFC 5322 [4]."* |

Line numbers are into [`docs/rfc/rfc5321.txt`](../../docs/rfc/rfc5321.txt), the archived copy. Quotations keep the specification's own words and spellings (AGENTS.md rule 2).

## Notes

RECOVERED BY THE RE-SWEEP. This is THE ONLY RFC 2119 KEYWORD IN THE WHOLE OF §8 — everything else in the section is descriptive prose about registry procedure — and the raw set carried it only as a name inside ServiceExtensionRegistered's fold list, with its citation range given as 4444-4447 and quoted as the three words 'those trace fields MUST'. Quoted here in full so the obligation is legible without reopening the file. Two events hide in one sentence and both matter: a trace header field being CREATED (elsewhere, by someone else, and RFC 5321 does not govern it) and the field being REGISTERED (the MUST). The violated arm is a trace field in wide use that was never registered, which is common in practice. Screens: all four altitude tiers. FIELD HYGIENE: struck registry ('BCP 90 (RFC 3864) message header field registry') — a constant, since 4445-4447 names exactly one registry and this event exists only for it. field_name survives as the identity of what was registered, and its example is labeled honestly: Received-SPF appears nowhere in RFC 5321, which names only Return-path and Received, so the value illustrates the class the sentence is about rather than quoting the spec. No session, transaction or connection key was present. On requirement 3, checked here because this is the trace-header event: §4.4's Opt-info at line 3329 supplies an optional ID clause within a Received field, and §4.4 3170 adds that it 'MAY contain an "@"' — but that is a clause inside one trace field, not a message identifier the model may hang on events, and nothing in RFC 5321 supplies one.

COLLISION RESOLVED — merged from 2 independently brainstormed records. One citation, one sentence, one surviving field; the copies are the same recovery of the same MUST from the same three lines. Confidence resolves to certain because the obligation is an unambiguous MUST at 4444-4447, verified at 4445 — the 'likely' of the other copy is about the registry's empty population at publication, which is a fact about the event's instances, not a doubt about whether the event exists.

From the duplicate in 16. Abuse, Security and Registry: RECOVERED BY THE RE-SWEEP. Named in the inherited ServiceExtensionRegistered notes as 'a MUST at 4444-4447, the only 2119 keyword in section 8' and never promoted — the cap took the one normatively strongest fact in the whole IANA section. Strength: MUST, conditional on a trace header field being created at all, and it is indeed the only RFC 2119 keyword in section 8; everything else there is lowercase 'may'. Note what the parenthetical fixes: as of RFC 5321 the trace fields are exactly Return-path and Received (4444-4445), so this event's population is empty at publication and any later member arrives through it — the cleanest example in the chapter of a registry whose current value is a fact with a history. Rejected names: AdditionalTraceFieldRegistered, TraceFieldAddedToBcp90Registry, NewTraceHeaderFieldCreated. The creation and the registration are arguably two events and the MUST binds only the second; a facilitator may want TraceHeaderFieldCreated split out, since 4444-4445 speaks of fields being 'created' before they are added. Screens that would have killed it: all four altitude tiers, the consumer test, the responsibility-boundary cutoff; the registry it writes is not even an SMTP registry, it is a message-format one. FIELD HYGIENE: struck used_with (RFC 5322) — the MUST at 4444-4447 fixes it, so every instance would carry the same value. field_name has no source in rfc5321.txt, and that is the finding rather than an oversight: 4444-4445 says the trace fields are exactly Return-path and Received as of this specification, so the population is empty at publication and any example must come from outside; Received-SPF is stated plainly as illustrative for that reason. Identity: the field name; no key fabricated.
