# `ReversePathConstructedFromForeignEnvelope`

A gateway derived the SMTP MAIL command's reverse-path either by copying the return-path header it had just deleted or by combining it with the foreign transport's own envelope information.

| Grouping | Value | Fixed at |
|:--|:--|:--|
| Workflow | 4. Trace and Provenance | Step 1 — timeline discovery |
| Stream root | Transaction | Step 1 — stream roots |
| Ownership swimlane | — | Step 6 — events section, per team or system |
| Originating actor | Gateway (non-SMTP to SMTP) | Step 3 — actor swimlane; provenance, not this event's row |

🟧 Event · confidence *likely* · recorded 2026-08-09, Step 1 Brain Storming, whole-spec sweep of RFC 5321 · `019fe7a8-440a-7047-8490-d2adb6c13ca7`

## Data

| Field | Type | Cardinality | Example | Example source |
|:--|:--|:--|:--|:--|
| `constructed_reverse_path` | Path | Single | \<Smith@bar.com> | walk scene |
| `construction_method` | Text | Single | combined with foreign envelope | rfc5321.txt:3270-3271 |
| `gateway_host` | DomainName | Single | gw.foo.com | illustrative — no RFC source; built on the walk scene domain foo.com |

## Where RFC 5321 says so

| Section | Lines | Text |
|:--|:--|:--|
| §4.4 | 3269-3273 | *"either copy that information to the SMTP envelope or combine it with information present in the envelope of the other transport system to construct the reverse-path argument to the MAIL command in the SMTP envelope."* |
| §4.4 | 3258-3260 | *"The reverse-path address (as copied into the Return-path) MUST be used as the target of any mail containing delivery error messages."* |
| §4.4 | 3255-3258 | *"Historical note: Text in RFC 822 that appears to contradict the use of the Return-path header field (or the envelope reverse-path address from the MAIL command) as the destination for error messages is not applicable on the Internet."* |
| §4.4 | 3268-3273 | *"a gateway from elsewhere -> SMTP SHOULD delete any return-path header field present in the message, and either copy that information to the SMTP envelope or combine it with information present in the envelope of the other transport system to construct the reverse-path argument to the MAIL command in the SMTP envelope."* |
| §B | 4773-4777 | *"A submission protocol based on Standard RFC 822 information alone MUST NOT be used to gateway a message from a foreign (non-SMTP) mail system into an SMTP environment. Additional information to construct an envelope must come from some source in the other environment, whether supplemental header fields or the foreign system's envelope."* |

Line numbers are into [`docs/rfc/rfc5321.txt`](../../docs/rfc/rfc5321.txt), the archived copy. Quotations keep the specification's own words and spellings (AGENTS.md rule 2).

## Notes

Carried over from idx 206 and KEPT HERE RATHER THAN HANDED OFF, deliberately: the gatewaying chapter's envelope-reconstruction anchor is §3.7.5, which produces a different event (EnvelopeReturnPathSetAtGateway, idx 95), and these lines at 3269-3273 sit inside §4.4 where only this chapter is looking. Dropping it would lose it entirely. The construction_method field is not decoration: the RFC offers two genuinely different derivations and does not say which to prefer, so a message's reverse-path may be a copy or a synthesis and only this field records which. Downstream the distinction matters because of the MUST at 3258-3260 — whatever lands here becomes the mandatory target of every delivery error message, so a badly synthesized address silently sends bounces nowhere. The historical note at 3255-3258 exists purely to overrule RFC 822 text that appears to contradict this, which is why it is cited. Pairs with ReturnPathDeletedByInboundGateway. FnEmail screens: the 'one event per command' target, decisively — this happens *before* any MAIL command exists, so it has no command to be the one event of. ⚠️ FIELD HYGIENE PASS: struck the fabricated `message_ref` identity key — RFC 5321 supplies no message or transaction identifier despite this record's 'Transaction' stream root, and f2C8D14 is this project's own minted queue id. Identity is the constructed reverse-path itself, and the timing makes that unusually clear: as the inherited note says, this happens before any MAIL command exists, so there is no transaction to hold a key even in principle. construction_method survives the derived-values test — it records which of two branches the gateway took, not a computed result — and its value is now sourced to the RFC's own words at 3270-3271, 'combine it with information present in the envelope of the other transport system'.

COLLISION RESOLVED — merged from 2 independently brainstormed records. Same section, same sentence, same actor, same two substantive fields with the same values and the same reasoning for keeping construction_method. Only the stream root and one actor-restating field differ, and the first record's own notes resolve the root against itself.

From the duplicate in 9. Gatewaying: construction_method is not decoration: the RFC offers two genuinely different derivations and does not say which to prefer, so a message's reverse-path may be a copy or a synthesis, and only this field records which. It matters downstream because of the MUST at 3258-3260 — whatever lands here becomes the mandatory target of every delivery error, so a badly synthesized address sends bounces nowhere and nobody can tell. The Appendix B reference is the outer constraint on both methods: header fields alone are not enough, by MUST NOT. Pairs with ReturnPathDeletedByInboundGateway; distinct from EnvelopeReturnPathSetAtGateway, which takes the foreign environment's declared error address rather than deriving one. Rejected alternative names: MailCommandReversePathSynthesized, InboundEnvelopeReconstructed. Which project screen would have killed it: the "one event per command" target most sharply — this happens BEFORE any MAIL command exists, so it has no command to be the one event of. Also inbound-only scope.

FIELD HYGIENE PASS: struck message_ref (example f2C8D14) — RFC 5321 supplies no message identifier, and this event is the sharpest illustration of why: it occurs BEFORE the MAIL command exists, so there is not yet a transaction, an envelope, or anything else to key on. The gateway and the path it is building are all there is. construction_method survives the derived-value test — it records which of the two derivations the RFC offers was taken, which is a fact about the act, not a computation over it — and its example is now quoted from the source at lines 3270-3271 rather than paraphrased.
