# `SelfIdentifiedInMailExchangerList`

A relay found one of its own names or addresses among the sorted MX candidates for the destination.

| Grouping | Value | Fixed at |
|:--|:--|:--|
| Workflow | 6. Address Resolution and Next-Hop Selection | Step 1 — timeline discovery |
| Stream root | Next-Hop Destination | Step 1 — stream roots |
| Ownership swimlane | — | Step 6 — events section, per team or system |
| Originating actor | Relay host | Step 3 — actor swimlane; provenance, not this event's row |

🟧 Event · confidence *certain* · recorded 2026-08-09, Step 1 Brain Storming, whole-spec sweep of RFC 5321 · `019fe7a8-440a-7011-9b65-551bd6ce98c0`

## Data

| Field | Type | Cardinality | Example | Example source |
|:--|:--|:--|:--|:--|
| `relay_host` | DomainName | Single | foo.com | rfc5321.txt:5049 |
| `matched_record` | DomainName | Single | foo.com | rfc5321.txt:5050 |

## Where RFC 5321 says so

| Section | Lines | Text |
|:--|:--|:--|
| §5.1 | 3928-3930 | *"The relay host MUST then inspect the list for any of the names or addresses by which it might be known in mail transactions."* |
| §5.1 | 3917-3918,3927 | *"If it determines that it should relay the message without rewriting the address, it MUST sort the MX records to determine candidates for delivery."* |
| §D.3 | 5049-5051 | *"foo.com, having received the message, now does a DNS lookup on xyz.com. It finds the same set of MX records, but cannot use the one that points to itself (or to any other host as a worse preference)."* |

Line numbers are into [`docs/rfc/rfc5321.txt`](../../docs/rfc/rfc5321.txt), the archived copy. Quotations keep the specification's own words and spellings (AGENTS.md rule 2).

## Notes

Carried from the inherited candidate, with the RFC's own worked instance from Appendix D.3 added — real values xyz.com and foo.com, so this event does not need invented data. Strength: MUST. CITATION TRAP recorded for later readers: the sort sentence is split by a page break, beginning at 3917-3918 and resuming at 3927 with the single word 'delivery.', with the footer occupying 3919-3926 — any line range taken from a grep of the visible text will be wrong, which is why the ref above is written 3917-3918,3927. Precondition nobody wrote down, now carried: ServerIdentityNamesConfigured. §5.1 is loop *prevention* and §6.3 (heading verified at 4065) is loop *detection*; only the second is ever called that, and 130 lines separate two halves of one story. The miss arm changes nothing and is in set_aside. Rejected alternative names: OwnNameFoundInMxList, RelaySelfMatchDetected. Which project screen would have killed it: the inbound-only scope. ⚠️ FIELD HYGIENE PASS: struck matched_preference. The preference is read off the MX record set, which a read model already holds, and where it actually does work is on LowerPreferenceExchangersDiscarded as threshold_preference — carrying it twice records it once too often. Its example was also unsourceable: RFC 5321 states no preference numbers anywhere, D.3 says only 'best preference' and 'a lower preference'. No fabricated identity key was present; identity is the relay host, and both surviving values come from the RFC's own worked relay at D.3 rather than from invention.
