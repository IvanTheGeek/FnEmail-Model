# `MailRejectedForPolicyReason`

A server refused mail for an operational, technical or policy reason, answering 550 to EHLO, MAIL or RCPT.

| Grouping | Value | Fixed at |
|:--|:--|:--|
| Workflow | 8. Relaying | Step 1 — timeline discovery |
| Stream root | Session | Step 1 — stream roots |
| Ownership swimlane | — | Step 6 — events section, per team or system |
| Originating actor | Receiving SMTP server | Step 3 — actor swimlane; provenance, not this event's row |

🟧 Event · confidence *certain* · recorded 2026-08-09, Step 1 Brain Storming, whole-spec sweep of RFC 5321 · `019fe7a8-440a-71ba-99c0-8357d7685937`

## Data

| Field | Type | Cardinality | Example | Example source |
|:--|:--|:--|:--|:--|
| `rejected_command` | Text | Single | EHLO | rfc5321.txt:4381 |
| `peer_address` | IpAddress | Single | 192.0.2.10 | walk scene |
| `claimed_domain` | Domain | Single | bar.com | rfc5321.txt:4887 |

## Where RFC 5321 says so

| Section | Lines | Text |
|:--|:--|:--|
| §7.9 | 4357-4359 | *"It is a well-established principle that an SMTP server may refuse to accept mail for any operational or technical reason that makes sense to the site providing the server."* |
| §7.9 | 4379-4381 | *"When mail is rejected for these or other policy reasons, a 550 code SHOULD be used in response to EHLO (or HELO), MAIL, or RCPT as appropriate."* |
| §3.3 | 1141-1142 | *"or if the server determines that the message should be rejected for policy or other reasons"* |

Line numbers are into [`docs/rfc/rfc5321.txt`](../../docs/rfc/rfc5321.txt), the archived copy. Quotations keep the specification's own words and spellings (AGENTS.md rule 2).

## Notes

Carried from #302, with its line ranges re-verified against the file - the inherited citation gave '4357-4358' for a quote that in fact runs 4357-4359, one line short, and the inherited quote text was truncated mid-sentence in a way that would have failed a checker. Unconditional permission to refuse; SHOULD only on WHICH code. Three landing points are named explicitly at 4381, modeled here as one event with a field naming the command, but splitting into three is equally defensible and probably better at protocol altitude, because a 550 at EHLO ends the whole session before any transaction exists - which is why stream_root is Session rather than Transaction. Broader than RelayTaskDeclined. Rejected names: MailRefusedByPolicy, TrafficRejected, PolicyRejectionIssued. --- FIELD HYGIENE: struck reply_code (the 550 at 4380-4381 is the code to be USED IN RESPONSE, a wire rendering under a SHOULD), peer_address and claimed_domain (7.9 says nothing about who the peer is or what it claimed; both were padding against the cited sentence). No session key existed to strike and none is available: while the session lives the channel is its identity, which is exactly why the stream root is Session. rejected_command survives because 4381 names its three values as data.

COLLISION RESOLVED — merged from 2 independently brainstormed records. Both records cite the same two sentences of 7.9, name the same actor (receiving SMTP server) and the same stream root, Session, and describe one act: a refusal on operational, technical or policy grounds answered with 550. Same section, same actor: merge. Chapter 16 takes it because 7.9 is the security and scope-of-operation section and chapter 16 already holds the 7.8 material, while the refusal itself is general and not relay-specific; chapter 8 keeps the relay-specific RelayingDeclined and RelayAccessRestrictedToKnownSources.

From the duplicate in 16. Abuse, Security and Registry: Merged from one inherited candidate; the 3.3 ref added as the post-DATA landing point 7.9 does not name. NOTE THE ANCHOR GAP: section 7.9 (heading confirmed at line 4355) is NOT in this chapter's anchor list, which jumps from 7.8 to 2.2.2, yet the chapter's description says 'unsolicited mail refused' and no other chapter's anchors reach it. Claimed here and flagged so a facilitator can move it. Strength: unconditional permission to refuse (4357-4359, 'may refuse'); SHOULD only on which code (4380). Three landing points at 4381 — EHLO (or HELO), MAIL, RCPT — modeled as one event with a command field, though splitting into three is equally defensible and probably better at protocol altitude, since a 550 at EHLO ends the session before any transaction exists. Rejected names: MessageRejectedByPolicy (an inherited sibling at 3.3 lines 1141-1142 that fires AFTER the 354, cross-referenced and deliberately not merged, a pre-DATA refusal and a post-DATA refusal being different state changes), MailRefusedByServerPolicy, TrafficRejectedBySitePolicy. 7.9 also warns against overuse (4359-4365). Cross-reference: 6.2 (heading confirmed at line 4013) carries BounceSuppressedForHostileContent. FIELD HYGIENE: struck reply_code (550) as rendering — 4379-4381 specifies which code goes on the wire for a policy rejection, which is precisely what makes it wire syntax rather than a fact the server keeps. rejected_command survives because 4381 enumerates the landing points, so the value genuinely varies. Identity: no session or transaction key existed here to strike, and at an EHLO rejection there is not yet a transaction to identify — the peer address (4.4 lines 3325-3327) and the domain the client claimed are all the server has, which is the honest state of affairs and worth seeing on the event; claimed_domain's value is the RFC's own C: EHLO bar.com at D.1 line 4887.
