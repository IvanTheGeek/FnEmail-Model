# `AmbiguousMailboxAlternativesDisclosed`

The server answered an ambiguous VRFY by naming every mailbox that matched, handing over addresses the requester never asked for by name.

| Grouping | Value | Fixed at |
|:--|:--|:--|
| Workflow | 13. Address Diagnostics | Step 1 — timeline discovery |
| Stream root | Address Diagnostic Request | Step 1 — stream roots |
| Ownership swimlane | — | Step 6 — events section, per team or system |
| Originating actor | Receiving SMTP Server | Step 3 — actor swimlane; provenance, not this event's row |

🟧 Event · confidence *likely* · recorded 2026-08-09, Step 1 Brain Storming, whole-spec sweep of RFC 5321 · `019fe7a8-440a-715f-a771-72a7caddfd0a`

## Data

| Field | Type | Cardinality | Example | Example source |
|:--|:--|:--|:--|:--|
| `queried_user_name` | Text | Single | Smith | illustrative — no RFC source for the argument itself; the surname is the RFC's own at rfc5321.txt:1249-1251 |
| `disclosed_alternatives` | Mailbox | List | \<jsmith@foo.com>, \<hsmith@foo.com>, \<dweep@foo.com> | rfc5321.txt:1249-1251 |
| `disclosed_full_names` | Text | List | Joe Smith, Harry Smith, Melvin Smith | rfc5321.txt:1249-1251 |

## Where RFC 5321 says so

| Section | Lines | Text |
|:--|:--|:--|
| §3.5.1 | 1239-1241 | *"the server MAY either note the ambiguity or identify the alternatives"* |
| §3.5.1 | 1248-1251 | *"553- Ambiguous; Possibilities are 553-Joe Smith \<jsmith@foo.com> 553-Harry Smith \<hsmith@foo.com> 553 Melvin Smith \<dweep@foo.com>"* |
| §3.5.1 | 1255-1258 | *"553-Ambiguous; Possibilities 553- \<jsmith@foo.com>"* |
| §3.5.1 | 1261-1264 | *"Use of exactly the forms given, and the "user ambiguous" or "ambiguous" keywords"* |

Line numbers are into [`docs/rfc/rfc5321.txt`](../../docs/rfc/rfc5321.txt), the archived copy. Quotations keep the specification's own words and spellings (AGENTS.md rule 2).

## Notes

Inherited (candidate 23), kept unchanged in substance; its citations were checked line by line against the file and are correct. Strength: MAY with two arms (1240); only this arm discloses. Two disclosing forms exist and differ in what they leak — 1248-1251 leaks three full names as well as three mailboxes, 1255-1258 leaks the mailboxes only — so a later step choosing a form is choosing a disclosure level, which is worth keeping visible. Sibling and opposite: MailboxNameReportedAsAmbiguous, recovered by the re-sweep as the arm this candidate declined to model. Rejected alternative names: AlternativesIdentified, AmbiguousVrfyExpanded, MatchingMailboxesListed. Which screen would have killed it: the consumer test (G1), the capture/decision lens, and 'one event per command'. Note the standing asymmetry the span sets up and does not resolve: §3.4 goes to length to avoid disclosing one forwarding address, while §3.5.1 hands over three matching mailboxes on request. FIELD HYGIENE PASS: struck reply_code (553), on the same test applied to its sibling — the code is the rendering of the decision and the RFC never carries it as data beyond the wire. Nothing else struck, and the reason is worth stating: both disclosure fields are quoted straight from the RFC's own worked reply at lines 1249-1251, which is rule 8 already satisfied at sweep time. Re-verified against the file in this pass. No invented identity key was present; the query itself and the channel carry identity.
