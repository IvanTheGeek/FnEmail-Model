# `ReturnPathHeaderStrippedBeforeDelivery`

A server making final delivery removed the Return-Path header fields the message already carried, before writing its own.

| Grouping | Value | Fixed at |
|:--|:--|:--|
| Workflow | 4. Trace and Provenance | Step 1 — timeline discovery |
| Stream root | Message | Step 1 — stream roots |
| Ownership swimlane | — | Step 6 — events section, per team or system |
| Originating actor | Delivery SMTP server | Step 3 — actor swimlane; provenance, not this event's row |

🟧 Event · confidence *likely* · recorded 2026-08-09, Step 1 Brain Storming, whole-spec sweep of RFC 5321 · `019fe7a8-440a-7042-9d9a-4436d861a944`

## Data

| Field | Type | Cardinality | Example | Example source |
|:--|:--|:--|:--|:--|
| `stripped_return_path` | Path | Single | \<Smith@bar.com> | walk scene |
| `delivering_host` | Domain | Single | foo.com | walk scene |

## Where RFC 5321 says so

| Section | Lines | Text |
|:--|:--|:--|
| §4.4 | 3237-3238 | *"SMTP servers making final delivery MAY remove Return-path header fields before adding their own."* |
| §4.4 | 3233-3234 | *"A message-originating SMTP system SHOULD NOT send a message that already contains a Return-path header field."* |
| §4.4 | 3242-3243 | *"exactly one return path SHOULD be present when the message is delivered"* |

Line numbers are into [`docs/rfc/rfc5321.txt`](../../docs/rfc/rfc5321.txt), the archived copy. Quotations keep the specification's own words and spellings (AGENTS.md rule 2).

## Notes

Carried over from idx 203. MAY. Distinct from ReturnPathRemovedInTransit by actor and by moment — that one is a relay mid-flight, this one is the delivering server at the boundary — and the two produce different downstream evidence. The precondition is itself an event that should not have happened: 3233-3234 makes it a SHOULD NOT for an originating system to submit a message already carrying a Return-path, so every firing of this event implies either an upstream violation (ReturnPathSubmittedByOriginatingSystem, recovered by the re-sweep) or a legitimate gateway insertion under 3263-3266 (ReturnPathInsertedByOutboundGateway). CHANGE FROM THE INHERITED RECORD: idx 203 carried a stripped_count field and flagged it as arguably computed; I dropped it under the kit's no-derived-values rule and replaced it with the stripped path itself, which is a fact rather than a tally. The inherited argument for keeping it — that zero-versus-more is the only distinguishing fact — is recorded here rather than silently discarded, and it is answered by MultipleReturnPathsPresentAtDelivery, which names that condition as its own event. FnEmail screens: the responsibility-boundary cutoff. ⚠️ FIELD HYGIENE PASS: struck the fabricated `message_ref` identity key — RFC 5321 supplies none, and f2C8D14 is this project's own minted queue id. Identity is the stripped path plus the delivering host, at the moment the message leaves SMTP. Note that the re-sweep's own earlier decision recorded above — dropping stripped_count as a computed tally — is the same rule this pass applies to received_count on the loop events, reached independently; it is the strongest evidence in the chapter that the no-derived-values rule is workable rather than merely stated. Both surviving values come from the walk scene.

COLLISION RESOLVED — merged from 2 independently brainstormed records. Same MAY at 3237-3238, same actor, same moment, same stream root, same two surviving fields; the copies differ only in which second citation they carried, which is a union. Cardinality was the only genuine choice and the RFC settles it: 3237-3238 says 'Return-path header fields', plural, and 3242-3243 makes 'exactly one return path' a SHOULD at delivery, which presupposes that more than one can be present beforehand. List, therefore, with ReversePath the more precise of the two offered types.

From the duplicate in 5. Responsibility Handoff and Final Delivery: Inherited candidate 203, kept, with one change: the inherited stripped_count: 1 is replaced by the list of paths actually removed. The inherited notes flagged the count as 'arguably a computed field under the kit's no computed fields rule' and kept it anyway; carrying the paths instead keeps the same zero-versus-more information as facts and drops the fold, so the tension resolves without losing anything. MAY strength. Distinct from ReturnPathRemoved by actor and by moment — that one is a relay mid-flight, this one is the delivering server at the boundary, and the two leave different downstream evidence. Every firing implies an upstream event that should not have happened: MessageOriginatedWithReturnPathHeaderPresent, below, or a gateway insertion under 3263-3266. Screens: the responsibility-boundary cutoff and inbound-only scope. ⚠️ FIELD HYGIENE PASS 2026-08-09 — one field struck. message_id: RFC 5321 supplies no message identifier; §4.4's ID clause (line 3336) is optional and sits in the receiver's own Received: stamp, msg-id belongs to RFC 5322, and f2C8D14 is this project's minted queue_id value from commit 6acfc68. Identity here is the stripped paths themselves — which is the same resolution the inherited pass reached when it replaced stripped_count with the list, one step further along: the removed values are simultaneously the payload and the only handle the RFC offers.
