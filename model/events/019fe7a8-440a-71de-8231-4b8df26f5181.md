# `VerificationFakedAfterSyntaxOnlyCheck`

A server returned 250 to a VRFY after checking only the syntax of the address, so it appeared to have verified an address it had not.

| Grouping | Value | Fixed at |
|:--|:--|:--|
| Workflow | 16. Abuse, Security and Registry | Step 1 — timeline discovery |
| Stream root | Session | Step 1 — stream roots |
| Ownership swimlane | — | Step 6 — events section, per team or system |
| Originating actor | Receiving SMTP server | Step 3 — actor swimlane; provenance, not this event's row |

🟧 Event · confidence *certain* · recorded 2026-08-09, Step 1 Brain Storming, whole-spec sweep of RFC 5321 · `019fe7a8-440a-71de-8231-4b8df26f5181`

## Data

| Field | Type | Cardinality | Example | Example source |
|:--|:--|:--|:--|:--|
| `queried_name` | Text | Single | Crispin | rfc5321.txt:5113 |
| `echoed_address` | Mailbox | Single | \<Admin.MRC@foo.com> | rfc5321.txt:5114 |

## Where RFC 5321 says so

| Section | Lines | Text |
|:--|:--|:--|
| §7.3 | 4247-4248 | *"Returning a 250 reply code with the address listed in the VRFY command after having checked it only for syntax violates this rule."* |
| §7.3 | 4241-4242 | *"implementations that permit this MUST NOT appear to have verified addresses that are not, in fact, verified."* |

Line numbers are into [`docs/rfc/rfc5321.txt`](../../docs/rfc/rfc5321.txt), the archived copy. Quotations keep the specification's own words and spellings (AGENTS.md rule 2).

## Notes

RECOVERED BY THE RE-SWEEP. The violated arm of the MUST NOT at 4241-4242, stated by the RFC as a named violation rather than left to inference — precisely the arm truncated sweeps lose, and no inherited candidate carried it. Strength: MUST NOT violated. Rejected names: SyntaxOnlyVerificationReported, FalseVerificationAsserted, AddressAppearedVerified. Real instance inside the RFC: D.4 lines 5113-5114 show C: VRFY Crispin answered S: 250 Mark Crispin \<Admin.MRC@foo.com> and never say whether anything beyond syntax was checked — one of two defects the 7.x span notes find in the RFC's own scenarios, and the cleanest real-data instantiation available (rule 8). Opposite arm: MailboxExistenceDisclosedViaVrfy, another chapter. Screens that would have killed it: the consumer test, the HELO-only charter. FIELD HYGIENE: struck reply_code (250) as the rendering of the decision; 4247 describes the code as the vehicle of the violation, not as a fact the server retains. What makes the event meaningful survives intact and comes straight from the RFC's own scenario at lines 5113-5114. Identity: none needed and none fabricated — a VRFY exchange is identified by its position in the session, RFC 5321 naming no session key.
