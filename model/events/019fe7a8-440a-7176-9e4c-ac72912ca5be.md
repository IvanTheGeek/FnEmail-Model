# `HelpSupportConfigured`

An implementation settled what its HELP command answers — whether it responds without an argument, whether it accepts an argument, and what it says.

| Grouping | Value | Fixed at |
|:--|:--|:--|
| Workflow | 13. Address Diagnostics | Step 1 — timeline discovery |
| Stream root | Server Configuration | Step 1 — stream roots |
| Ownership swimlane | — | Step 6 — events section, per team or system |
| Originating actor | Site administrator | Step 3 — actor swimlane; provenance, not this event's row |

🟧 Event · confidence *doubtful* · recorded 2026-08-09, Step 1 Brain Storming, whole-spec sweep of RFC 5321 · `019fe7a8-440a-7176-9e4c-ac72912ca5be`

## Data

| Field | Type | Cardinality | Example | Example source |
|:--|:--|:--|:--|:--|
| `server_host` | Domain | Single | foo.com | walk scene |
| `argumentless_supported` | Boolean | Single | true | rfc5321.txt:2161-2162 |
| `argument_supported` | Boolean | Single | false | rfc5321.txt:2161-2162 |
| `help_text` | Text | Single | Commands supported: EHLO HELO MAIL RCPT DATA RSET NOOP QUIT VRFY HELP | illustrative — no RFC source; the RFC shows no HELP output anywhere, and the command list is the §4.5.1 minimum at rfc5321.txt:3384-3392 |

## Where RFC 5321 says so

| Section | Lines | Text |
|:--|:--|:--|
| §4.1.1.8 | 2161-2162 | *"SMTP servers SHOULD support HELP without arguments and MAY support it with arguments."* |
| §4.1.1.8 | 2153-2155 | *"The command MAY take an argument (e.g., any command name) and return more specific information as a response."* |

Line numbers are into [`docs/rfc/rfc5321.txt`](../../docs/rfc/rfc5321.txt), the archived copy. Quotations keep the specification's own words and spellings (AGENTS.md rule 2).

## Notes

RECOVERED BY THE RE-SWEEP — this is the §4.1 sweep's own cut candidate, named there as HelpTextConfigured and dropped with the words 'too thin to earn a slot'. Recovering a candidate whose only recorded reason for exclusion was thinness is exactly what the cap-recovery instruction asks for; the method's fieldless-events note is the counterweight, and the tension is recorded rather than resolved: this event has four fields, three of which are near-identity, and if the help_text is dropped it becomes an opaque label. Strength: SHOULD for the argumentless form, MAY for the argument form (2161-2162) — a two-armed configuration decision, not a single switch, which is why two Booleans are carried rather than one. Rejected alternative names: HelpTextConfigured (the original), HelpCapabilityProvisioned, HelpContentAuthored. Overlapping event kept separate: ServerIdentityDisclosurePolicySet governs how much type and version information the help text reveals, which is a policy question about the same string; merging them would lose the §7.5 security framing. Which screen would have killed it: the four altitude tiers, the consumer test, the twelve-slice model, and 'one event per command' if configuration is not modeled at all. FIELD HYGIENE PASS: nothing struck. No reply code and no invented identity key. The two Booleans were tested against the bar on computed and derived values and kept: they are not derivations of anything, they are the two arms of the configuration decision the RFC states in a single sentence at 2161-2162 — SHOULD support HELP without arguments, MAY support it with arguments — and the whole event is that pair being settled. help_text is labeled invented for the same reason as on HelpInformationSent: the RFC shows no HELP output anywhere. The four-fields-three-of-them-near-identity tension the inherited note records is unchanged by this pass; sourcing the fields did not make them fewer, and that is itself the honest result. Left doubtful.
