# `SizeLimitConfigured`

A receiving implementation's ceiling for one named protocol object was set to a specific octet count, changing what it will accept from then on.

| Grouping | Value | Fixed at |
|:--|:--|:--|
| Workflow | 15. Server Configuration and Provisioning | Step 1 — timeline discovery |
| Stream root | Server Configuration | Step 1 — stream roots |
| Ownership swimlane | — | Step 6 — events section, per team or system |
| Originating actor | Server operator | Step 3 — actor swimlane; provenance, not this event's row |

🟧 Event · confidence *likely* · recorded 2026-08-09, Step 1 Brain Storming, whole-spec sweep of RFC 5321 · `019fe7a8-440a-718f-a830-b1d4ad7b96a5`

## Data

| Field | Type | Cardinality | Example | Example source |
|:--|:--|:--|:--|:--|
| `server_host` | Domain | Single | foo.com | walk scene |
| `limit_name` | Text | Single | command line | rfc5321.txt:3498 |
| `limit_octets` | Count | Single | 512 | rfc5321.txt:3500 |

## Where RFC 5321 says so

| Section | Lines | Text |
|:--|:--|:--|
| §4.5.3.1 | 3462-3464 | *"There are several objects that have required minimum/maximum sizes. Every implementation MUST be able to receive objects of at least these sizes."* |
| §4.5.3.1 | 3467-3470 | *"Clients MAY attempt to transmit these, but MUST be prepared for a server to reject them if they cannot be handled by it. To the maximum extent possible, implementation techniques that impose no limits on the length of these objects should be used."* |
| §4.5.3.1.4 | 3500-3502 | *"The maximum total length of a command line including the command word and the \<CRLF> is 512 octets. SMTP extensions may be used to increase this limit."* |
| §4.5.3.1.7 | 3518-3519 | *"The maximum total length of a message content (including any message header section as well as the message body) MUST BE at least 64K octets."* |
| §2.3.8 | 770-771 | *"Limits MAY be imposed on line lengths by servers (see Section 4)."* |
| §4.5.3.1.4 | 3501-3502 | *"SMTP extensions may be used to increase this limit."* |
| §4.5.3.1 | 3469-3470 | *"To the maximum extent possible, implementation techniques that impose no limits on the length of these objects should be used."* |

Line numbers are into [`docs/rfc/rfc5321.txt`](../../docs/rfc/rfc5321.txt), the archived copy. Quotations keep the specification's own words and spellings (AGENTS.md rule 2).

## Notes

MERGE: absorbs LineLengthLimitImposed (rejected name; same state change, narrower object). ⚠️ CITATION CORRECTION carried forward: the LineLengthLimitImposed record flagged its own example value of 512 as unverified, because §2.3.8 defers to Section 4 and its sweep never opened §4.5.3.1. The re-sweep opened it — 512 for a command line is confirmed at 3500. All seven objects, verified line by line: local-part 64 (§4.5.3.1.1, 3486-3487), domain 255 (§4.5.3.1.2, 3491), path 256 (§4.5.3.1.3, 3495-3496), command line 512 (§4.5.3.1.4, 3500), reply line 512 (§4.5.3.1.5, 3506-3507), text line 1000 (§4.5.3.1.6, 3512-3513), message content at least 64K (§4.5.3.1.7, 3518-3519). The RFC writes 'MUST BE' in capitals at 3519 — rule 2 keeps it. The MUST is a RECEIVE FLOOR, not a send ceiling, so a limit configured BELOW the floor is itself non-conforming and this same event records the violation. Two arms the RFC states and the raw set lost: an extension may RAISE a limit (3501-3502 and §4.5.3.1.6 3514), and 3469-3470 says implementations should impose no limits at all — the opposite event, ObjectLengthLimitRemoved. Modeled separately from RecipientLimitConfigured because §4.5.3.1.8 and .1.10 give the recipient limit its own reply-code behavior. Screens: the altitude tiers (Protocol/Infrastructure) and the inbound-session scope. FIELD HYGIENE: nothing struck. All three fields are essential and every value is now sourced to a line rather than remembered: limit_name to the §4.5.3.1.4 heading at rfc5321.txt:3498, limit_octets to rfc5321.txt:3500. limit_octets is a configured setting, not a computed one — the RFC's seven numbers are the floors it must clear, listed above, not values derived from anything. No session, transaction or connection key was present; identity is the server plus the named object whose ceiling was set.

COLLISION RESOLVED — merged from 2 independently brainstormed records. Both records cite 4.5.3.1 lines 3462-3464 as the primary with 2.3.8 lines 770-771 alongside, carry the same three fields with the same example values, name the same actor and the same stream root, and even record the same merge of LineLengthLimitImposed and the same discharged warning about the unverified 512. This is one event assembled twice. Same section, same actor: merge. Chapter 15 takes it under the configuration-root policy.

From the duplicate in 14. Protocol Fault Handling: Merged from candidates 320 (SizeLimitConfigured) and 60 (LineLengthLimitImposed), which are the same state change found by two different sweeps from two different sections. Rejected alternative name: LineLengthLimitImposed — too narrow, since the same act sets the path, message and text-line ceilings too. IMPORTANT: candidate 60 carried a ⚠️ on its own example, warning that its 512 was taken from memory rather than from §4.5.3, and that warning is now discharged — 512 for a command line is verified at line 3500 of the file. The seven ceilings §4.5.3.1 names, each verified: local-part 64 (3486-3487), domain 255 (3491), path 256 (3495-3496), command line 512 (3500-3501), reply line 512 (3506-3507), text line 1000 (3512-3513), message content at least 64K (3518-3520). The MUST at 3463 is a RECEIVE FLOOR, not a send ceiling, so a configured value below the floor makes the configuration non-conforming and every rejection under it a violation — see ShortCommandLineRejectedAsTooLong and RecipientRejectedBelowMandatoryMinimum for the runtime arms. The effective limit is configuration plus whatever extensions were negotiated, so it can change within a session. Sibling from 3467-3468, named not given a row: OversizedObjectTransmissionAttempted, the client's explicit MAY to try anyway. Screens: the altitude tiers, the condemned configuration register, and the inbound-only scope — nothing inside a session configures a limit.

FIELD HYGIENE PASS (chapter 14): nothing struck — no session key, no f2C8D14, and all three fields are configuration facts the RFC states outright. This event is now the single home for every ceiling in the chapter: limit_octets was struck from OversizedCommandLineRejected, OversizedPathRejected, TextLineLengthExceeded and OversizedMessageDataRejected, all four of which were carrying a copy of what this event records. The example 512 for a command line is verified at lines 3500-3501, and the other six ceilings are already sourced by line in the note above.
