# `UserNameRecognitionRuleConfigured`

The host's definition of which strings count as a user name for VRFY and EXPN was set.

| Grouping | Value | Fixed at |
|:--|:--|:--|
| Workflow | 13. Address Diagnostics | Step 1 — timeline discovery |
| Stream root | Server Configuration | Step 1 — stream roots |
| Ownership swimlane | — | Step 6 — events section, per team or system |
| Originating actor | Site administrator | Step 3 — actor swimlane; provenance, not this event's row |

🟧 Event · confidence *likely* · recorded 2026-08-09, Step 1 Brain Storming, whole-spec sweep of RFC 5321 · `019fe7a8-440a-7172-a1e0-51858240e65e`

## Data

| Field | Type | Cardinality | Example | Example source |
|:--|:--|:--|:--|:--|
| `server_host` | Domain | Single | foo.com | walk scene |
| `accepted_form` | Text | Single | local-part@domain | rfc5321.txt:1307-1309 |

## Where RFC 5321 says so

| Section | Lines | Text |
|:--|:--|:--|
| §3.5.1 | 1303 | *"User name" is a fuzzy term and has been used deliberately."* |
| §3.5.1 | 1304-1305 | *"An implementation of the VRFY or EXPN commands MUST include at least recognition of local mailboxes as "user names"."* |
| §3.5.1 | 1306-1310 | *"hosts, especially hosts that provide this functionality, SHOULD accept the "local-part@domain" form as a "user name"; hosts MAY also choose to recognize other strings as "user names"."* |
| §3.5.1 | 1325-1330 | *"The character string arguments of the VRFY and EXPN commands cannot be further restricted due to the variety of implementations of the user name and mailbox list concepts."* |
| §3.5.1 | 1303-1305 | *"User name" is a fuzzy term and has been used deliberately. An implementation of the VRFY or EXPN commands MUST include at least recognition of local mailboxes as "user names"."* |
| §3.5.1 | 1305-1310 | *"However, since current Internet practice often results in a single host handling mail for multiple domains, hosts, especially hosts that provide this functionality, SHOULD accept the "local-part@domain" form as a "user name"; hosts MAY also choose to recognize other strings as "user names"."* |
| §3.5.1 | 1325-1329 | *"The character string arguments of the VRFY and EXPN commands cannot be further restricted due to the variety of implementations of the user name and mailbox list concepts. On some systems, it may be appropriate for the argument of the EXPN command to be a file name for a file containing a mailing list"* |

Line numbers are into [`docs/rfc/rfc5321.txt`](../../docs/rfc/rfc5321.txt), the archived copy. Quotations keep the specification's own words and spellings (AGENTS.md rule 2).

## Notes

Inherited (candidate 12). Its citation of '1303-1305' for the fuzzy-term quote was one line too long — the sentence is on 1303 alone — and is corrected here; the substance is unchanged. All three modal strengths appear in one paragraph: MUST recognize local mailboxes (1304-1305), SHOULD accept local-part@domain (1306-1309), MAY recognize other strings (1309-1310). The RFC calls the term fuzzy on purpose, so this is configuration the spec explicitly declines to pin down — the clearest case in the chapter for rule 8, since 'Smith', 'jsmith@foo.com', 'Crispin' and 'Example-People' are all candidate user names under different rules and only instantiating them shows the rules diverge. Line 1327-1330 adds that on some systems an EXPN argument may be a file name for a file containing a mailing list, so the accepted forms are not even all address-shaped. A strength field was dropped from the inherited version: 'SHOULD' is metadata about the spec, not a fact about the configured host, and the 'only facts' rule excludes it. Rejected alternative names: UserNameGrammarSet, DiagnosticArgumentFormsAccepted, VrfyArgumentRuleConfigured. Which screen would have killed it: the altitude tiers, the twelve-slice model. FIELD HYGIENE PASS: struck example_accepted_string ('jsmith@foo.com'). It was a field whose content was an illustration of another field — accepted_form already says local-part@domain, and a conforming sample string adds no fact about the configuration, only a demonstration of it. That is padding by the method's own test. Its value was perfectly sourceable at line 1249, which is the second time in this chapter that sourcing a value has failed to save a field. The inherited note about rule 8 is not weakened by the strike: 'Smith', 'jsmith@foo.com', 'Crispin' and 'Example-People' are still the instantiations that show the MUST/SHOULD/MAY tiers at 1304-1310 diverge — they belong in the walk, not in a field. No reply code and no invented identity key were present; server_host is the stream's identity on a Server Configuration root.

COLLISION RESOLVED — merged from 2 independently brainstormed records. Same section, same paragraph, same actor, same two fields with the same values, and both struck the same third field for the same reason. The records differ only in how they carve the citation ranges.

From the duplicate in 15. Server Configuration and Provisioning: All three modal strengths in one paragraph (1303-1310): MUST recognize local mailboxes, SHOULD accept local-part@domain, MAY recognize other strings. The RFC calls the term fuzzy on purpose (1303), so this is configuration the spec explicitly declines to pin down — and rule 8 is what makes that visible: 'Smith', 'Smith@bar.com', 'Example-People' and a file name are all candidate user names under different rules, and only instantiating them shows the rules diverge. The example value was changed in the merge from jsmith@foo.com to Smith@bar.com to use the project's standing real data. Citation corrected during the re-sweep: the inherited record split this into 1303-1305 / 1304-1305 / 1306-1310 with the MUST sentence straddling two refs; the paragraph actually runs 1303-1310 and is quoted here in two clean halves. Screens: altitude tiers, twelve-slice model. FIELD HYGIENE: struck example_accepted_string ('Smith@bar.com'). It is an instance of accepted_form rather than a second fact — an illustration derived from the rule the event configures — and the method sends derived material to read models; the walked instances that make the divergence visible are kept in these notes, where rule 8's point survives intact. accepted_form itself survives and genuinely varies across the paragraph's three strengths, with the quoted form 'local-part@domain' verified at rfc5321.txt:1308. No session, transaction or connection key was present; identity is the host named by its domain.
