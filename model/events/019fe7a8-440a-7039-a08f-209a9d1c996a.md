# `ProtocolNameRegistered`

A new protocol or link name became legal for the WITH and VIA clauses of a trace line by being registered with IANA.

| Grouping | Value | Fixed at |
|:--|:--|:--|
| Workflow | 4. Trace and Provenance | Step 1 — timeline discovery |
| Stream root | Extension Registry | Step 1 — stream roots |
| Ownership swimlane | — | Step 6 — events section, per team or system |
| Originating actor | IANA registrant | Step 3 — actor swimlane; provenance, not this event's row |

🟧 Event · confidence *likely* · recorded 2026-08-09, Step 1 Brain Storming, whole-spec sweep of RFC 5321 · `019fe7a8-440a-7039-a08f-209a9d1c996a`

## Data

| Field | Type | Cardinality | Example | Example source |
|:--|:--|:--|:--|:--|
| `name` | Atom | Single | ESMTPS | illustrative — no RFC source |
| `clause` | Text | Single | WITH | rfc5321.txt:3334 |
| `registry` | Text | Single | mail parameters | rfc5321.txt:3372 |

## Where RFC 5321 says so

| Section | Lines | Text |
|:--|:--|:--|
| §4.4 | 3369-3374 | *"Attdl-Protocol = Atom ; Additional standard names for protocols are ; registered with the Internet Assigned Numbers ; Authority (IANA) in the "mail parameters" ; registry [9]. SMTP servers SHOULD NOT ; use unregistered names."* |
| §4.4 | 3352-3357 | *"Addtl-Link = Atom ; Additional standard names for links are ; registered with the Internet Assigned Numbers ; Authority (IANA). "Via" is primarily of value ; with non-Internet transports. SMTP servers ; SHOULD NOT use unregistered names."* |
| §4.4 | 3367 | *"Protocol = "ESMTP" / "SMTP" / Attdl-Protocol"* |
| §4.4 | 3370-3374 | *"; Additional standard names for protocols are ; registered with the Internet Assigned Numbers ; Authority (IANA) in the "mail parameters" ; registry [9]. SMTP servers SHOULD NOT ; use unregistered names."* |
| §8 | 4407-4411 | *"The third, "Mail Transmission Types" [46], established by RFC 821 and renewed by this specification, is a registry of link and protocol identifiers to be used with the "via" and "with" subclauses of the time stamp ("Received:" header field) described in Section 4.4."* |
| §8 | 4411-4417 | *"Link and protocol identifiers in addition to those specified in this document may be registered only by standardization or by way of an RFC-documented, IESG-approved, Experimental protocol extension. This name space is for identification and not limited in size"* |

Line numbers are into [`docs/rfc/rfc5321.txt`](../../docs/rfc/rfc5321.txt), the archived copy. Quotations keep the specification's own words and spellings (AGENTS.md rule 2).

## Notes

Carried over from idx 198, citations re-verified against the ABNF block. The base grammar fixes only three values — Link = "TCP" (3350) and Protocol = "ESMTP" / "SMTP" (3367) — and everything else arrives through this event, which makes the with_protocol: SMTP field on TraceLinePrepended legal only because of a prior registration. That is the 'precondition nobody wrote down' in its purest form: a per-message field whose legal values are set by an organization on a multi-year clock. Both the Link and Protocol blocks end in the same SHOULD NOT against unregistered names (3356-3357, 3373-3374), which is why one event covers both clauses and the clause is carried as a field rather than split. Sibling: TraceClauseRegistered. Violated arm: UnregisteredTraceNameUsed. FnEmail screens: all four altitude tiers and the consumer test; the roster's own note at the sixteen-chapter table proposes recording registry facts without chaptering them, and this event is the reason that question is live. ⚠️ FIELD HYGIENE PASS: nothing struck — no fabricated identity key was present, correctly. Sourcing is now stated and one value is honestly invented: 'mail parameters' is the RFC's own registry name at line 3372 and 'WITH' is the RFC's own clause keyword at 3334, but ESMTPS appears nowhere in RFC 5321. It cannot: the names this event registers are precisely those the base grammar at 3367 does not fix. §8 line 4407 additionally names the registry 'Mail Transmission Types', which is the same registry under a second name in the same document — recorded here rather than changed, since 3372 sources the value as written.

COLLISION RESOLVED — merged from 2 independently brainstormed records. Same act, same actor, same stream root, same three fields, same illustrative name value. The records differ only in which of the RFC's two names for one registry they put in the field, and both had already noticed the duplication — that is a merge with a recorded finding, not two events.

From the duplicate in 16. Abuse, Security and Registry: MERGE of the inherited ProtocolNameRegistered (4.4) with the sibling TransmissionTypeRegistered that ServiceExtensionRegistered's notes named from 8 lines 4407-4417 and never promoted — the same registry seen from two sections. Names rejected: TransmissionTypeRegistered, LinkTypeRegistered, WithProtocolNameRegistered, ViaLinkNameRegistered. Kept SEPARATE from TraceClauseRegistered because the two govern different things — clause NAMES versus clause VALUES — and merging them would break the unique-semantics rule in the direction that hides information; section 8 confirms the split by describing them as distinct subsections of one registry (4419-4422). The base grammar fixes only three values: Link = "TCP" (3350) and Protocol = "ESMTP" / "SMTP" (3367); everything else arrives through this event, which makes the with_protocol field on TraceLinePrepended legal only because of a prior registration — a precondition nobody wrote down, in its purest form. ABNF comment quotes are copied as they break in the source (rule 2). Strength: SHOULD NOT on using unregistered names (3373-3374 and 3356-3357); lowercase 'may be registered only by' on the registration. Screens that would have killed it: the altitude tiers, the consumer test. FIELD HYGIENE: nothing struck. name has no source inside rfc5321.txt — the grammar fixes only ESMTP and SMTP at line 3367 and everything else arrives through this very event — so ESMTPS is stated plainly as illustrative; what the RFC does supply is the document that registers such types, reference [9] at line 4529, 'ESMTP and LMTP Transmission Types Registration', recorded here rather than smuggled into example_source. clause is real, from the ABNF at line 3334, and genuinely two-valued (VIA at 3332, WITH at 3334). registry is the third IANA registry named at line 4407. Identity: the registered name.
