# `MessageLoopDetected`

A relay concluded from the trace information that the message was circulating in the mail system, and can now stop it.

| Grouping | Value | Fixed at |
|:--|:--|:--|
| Workflow | 8. Relaying | Step 1 — timeline discovery |
| Stream root | Message | Step 1 — stream roots |
| Ownership swimlane | — | Step 6 — events section, per team or system |
| Originating actor | Relay SMTP server | Step 3 — actor swimlane; provenance, not this event's row |

🟧 Event · confidence *likely* · recorded 2026-08-09, Step 1 Brain Storming, whole-spec sweep of RFC 5321 · `019fe7a8-440a-71aa-a8d0-1ef7dbce9151`

## Data

**No data.** The method sanctions this: *"If an event has no meaningful payload beyond its identity (e.g., a simple state transition), it is fine to have no fields or just the identity key."* It also warns, of a different case, that *"An event without fields is an opaque label"* — both are recorded here rather than reconciled, because reconciling them is a later step's work, not Brain Storming's.

## Where RFC 5321 says so

| Section | Lines | Text |
|:--|:--|:--|
| §3.6.3 | 1526-1528 | *"and, optionally, to attempt to detect looping in the mail system (see Section 6.3)"* |
| §6.3 | 4067-4070 | *"Simple counting of the number of "Received:" header fields in a message has proven to be an effective, although rarely optimal, method of detecting loops in mail systems. SMTP servers using this technique SHOULD use a large rejection threshold, normally at least 100 Received entries."* |
| §6.3 | 4071-4072 | *"Whatever mechanisms are used, servers MUST contain provisions for detecting and stopping trivial loops."* |

Line numbers are into [`docs/rfc/rfc5321.txt`](../../docs/rfc/rfc5321.txt), the archived copy. Quotations keep the specification's own words and spellings (AGENTS.md rule 2).

## Notes

Carried from #80, with a strength correction the re-sweep found by opening 6.3. The inherited note called loop detection simply 'optional' on the authority of 1527. But 4071-4072 says 'servers MUST contain provisions for detecting and stopping trivial loops.' Both are in force: the second carve-out in the 3.6.3 MUST NOT is permissive about whether a relay looks, while 6.3 makes provisions for trivial loops mandatory. A real tension between two sections of the same document, recorded and not resolved. The EXAMINATION of the trace headers is a read and sits in set_aside; the VERDICT is what changes state. Siblings not named here: LoopingMessageDiscarded, LoopingMessageReported. Rejected names: MailLoopIdentified, TrivialLoopStopped. --- FIELD HYGIENE - NOW FIELDLESS, and a correction to the inherited reasoning. The inherited note defended received_field_count as 'a fact the relay observed, not a derived score'; 6.3 lines 4067-4070 describe it as SIMPLE COUNTING of the Received: header fields in the message, which is a fold over the trace and exactly what the method sends to a read model. Struck. repeated_host struck too: 6.3 names only counting and undefined 'trivial loops', so a repeated host has no source. Struck relay_host. The verdict is the whole content of the event; its evidence is a read model over the Received: fields.
