# `ConnectionClosedInViolation`

A server closed the connection on an unrecognized command, ending a session the specification required it to keep open.

| Grouping | Value | Fixed at |
|:--|:--|:--|
| Workflow | 1. Session Lifecycle | Step 1 — timeline discovery |
| Stream root | Session | Step 1 — stream roots |
| Ownership swimlane | — | Step 6 — events section, per team or system |
| Originating actor | remote SMTP server (peer) | Step 3 — actor swimlane; provenance, not this event's row |

🟧 Event · confidence *doubtful* · recorded 2026-08-09, Step 1 Brain Storming, whole-spec sweep of RFC 5321 · `019fe7a8-440a-7094-bf9a-8ee5df69c4b2`

## Data

| Field | Type | Cardinality | Example | Example source |
|:--|:--|:--|:--|:--|
| `command_verb` | Text | Single | XVERP | illustrative — no RFC source (the X-prefix form for local verbs is licensed at rfc5321.txt:582-584) |
| `peer_address` | IPAddress | Single | 203.0.113.20 | walk scene |

## Where RFC 5321 says so

| Section | Lines | Text |
|:--|:--|:--|
| §3.8 | 1660-1662 | *"In particular, a server that closes connections in response to commands that are not understood is in violation of this specification."* |

Line numbers are into [`docs/rfc/rfc5321.txt`](../../docs/rfc/rfc5321.txt), the archived copy. Quotations keep the specification's own words and spellings (AGENTS.md rule 2).

## Notes

Doubtful on purpose, and kept as the VIOLATED arm whose honored arm is UnknownCommandRejected — the same §3.8 paragraph states both in consecutive sentences, which is why they are carried as a pair rather than merged into one event with a boolean. This is a fact about a peer, observed from the client side; the responsibility-boundary cutoff and the inbound-only scope would both kill it. Recorded because the spec spends a whole paragraph naming the behavior, which means it is common enough to specify against, and because a client that meets it lands in ConnectionLostWithoutQuit with failure_kind close and cannot distinguish this cause from a crash — if an outbound lane is ever built, this is the event that makes peer misbehavior visible instead of silently folding into a transient failure. The open question, left open: whether a conformance observation is a domain event at all, since the RFC states a violation rather than a state change. FIELD HYGIENE PASS: struck 2 fields. session_id struck: no such key in the spec. peer_address struck as the channel's, recorded at TransmissionChannelOpened. command_verb survives as the fact that earns the violation — the command that was not understood. Its value XVERP appears nowhere in RFC 5321, so it is marked illustrative; the X-prefix convention it follows is real, at lines 582-584, where verbs beginning with X are named as local extensions that may not be registered. Confidence was already doubtful and is left there: the strike neither rescued nor collapsed it, since the doubt is about whether a conformance observation is an event at all.

COLLISION RESOLVED — merged from 2 independently brainstormed records. Identical citation (§3.8 1660-1662), identical actor (the remote peer), identical stream root and confidence — the same fact harvested twice. Moving the survivor to chapter 14 keeps it beside its honored arm, UnknownCommandRejected, which this pass also lands in 14; splitting the two arms of one §3.8 paragraph across chapters would hide the pairing that gives each its meaning.

From the duplicate in 14. Protocol Fault Handling: Inherited from candidate 103; citation checked and correct. The violated arm of UnknownCommandRejected, and the RFC spends a whole sentence naming the behavior, which means it is common enough to legislate against. Doubtful on purpose, and the reason is worth keeping: it is a fact about a peer, observable only from the client side, and a client that meets it cannot distinguish this cause from a crash or a reset — it lands in ConnectionLost (raw candidate 111) with no way to tell why. Recording it separately is what makes peer misbehavior visible instead of silently folding into a transient failure. The open Step 1 question, left open per the kit: whether a conformance observation is a domain event at all — the RFC states a violation, and the state change (the connection ended) is real while the classification is an interpretation. Screens: the responsibility-boundary cutoff and the inbound-only scope both kill it.

FIELD HYGIENE PASS (chapter 14): session_id struck — RFC 5321 names no session, transaction or connection identifier, and f2C8D14 is nowhere in the RFC. What carries identity here is the channel itself, and section 4.4 line 3325 is where the RFC writes that down — TCP-info, "Information derived by server from TCP connection" — which is why peer_address is KEPT rather than being padding. command_verb KEPT as the only record of what drew the close. The doubtful confidence is unchanged and is NOT a consequence of this pass: it predates it and rests on the open question of whether a conformance observation is a domain event, which the field surgery does not touch.
