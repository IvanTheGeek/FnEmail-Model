# `SpoofedOriginDetected`

An examiner determined that a message's claimed origin was fabricated.

| Grouping | Value | Fixed at |
|:--|:--|:--|
| Workflow | 16. Abuse, Security and Registry | Step 1 — timeline discovery |
| Stream root | Message | Step 1 — stream roots |
| Ownership swimlane | — | Step 6 — events section, per team or system |
| Originating actor | Expert examiner (human or downstream analyzer) | Step 3 — actor swimlane; provenance, not this event's row |

🟧 Event · confidence *doubtful* · recorded 2026-08-09, Step 1 Brain Storming, whole-spec sweep of RFC 5321 · `019fe7a8-440a-71d7-b986-855c39abb064`

## Data

| Field | Type | Cardinality | Example | Example source |
|:--|:--|:--|:--|:--|
| `claimed_originator` | Mailbox | Single | \<Jones@foo.com> | walk scene |

## Where RFC 5321 says so

| Section | Lines | Text |
|:--|:--|:--|
| §7.1 | 4163-4166 | *"Constructing such a message so that the "spoofed" behavior cannot be detected by an expert is somewhat more difficult, but not sufficiently so as to be a deterrent to someone who is determined and knowledgeable."* |

Line numbers are into [`docs/rfc/rfc5321.txt`](../../docs/rfc/rfc5321.txt), the archived copy. Quotations keep the specification's own words and spellings (AGENTS.md rule 2).

## Notes

RECOVERED BY THE RE-SWEEP; the roster line 'Spoofing detected or tolerated' names both arms and only this one has RFC text behind it. Doubtful and deliberately kept: the examination is a read, but the verdict outlives it, the shape kept as MessageLoopDetected and MessageRejectedByPolicy. Strength: none; 4164 concedes detection is possible and 4165-4166 says it is no deterrent. The 'tolerated' arm has no distinct text. Rejected names: SpoofingDetected, ForgeryIdentified, OriginDeterminedFalse. Screens that would have killed it: the consumer test and the altitude tiers, which have no Analysis tier. FIELD HYGIENE: struck examined_message (MessageId, \<f2C8D14@foo.com>). RFC 5321 supplies no message identifier; 4.4's ID clause at line 3336 — ID = CFWS "ID" FWS ( Atom / msg-id ) — is an OPTIONAL trace clause whose msg-id is defined in RFC 5322, not a field the spec puts on events. f2C8D14 appears nowhere in rfc5321.txt: it is this project's own minted queue_id (commit 6acfc68), and nothing here is a queue id. Struck detection_basis: 4163-4166 says an expert can detect spoofing and says nothing about how, so any basis vocabulary is invented. Identity: the message this spec cannot identify; the claimed originator is all that names it. Confidence unchanged — the justification never rested on the struck fields.
