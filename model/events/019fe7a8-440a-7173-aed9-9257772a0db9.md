# `ExpnServiceExtensionRegistered`

EXPN entered the set of extensions this server is obliged to advertise in its EHLO response.

| Grouping | Value | Fixed at |
|:--|:--|:--|
| Workflow | 13. Address Diagnostics | Step 1 — timeline discovery |
| Stream root | Extension Registry | Step 1 — stream roots |
| Ownership swimlane | — | Step 6 — events section, per team or system |
| Originating actor | Receiving SMTP Server | Step 3 — actor swimlane; provenance, not this event's row |

🟧 Event · confidence *doubtful* · recorded 2026-08-09, Step 1 Brain Storming, whole-spec sweep of RFC 5321 · `019fe7a8-440a-7173-aed9-9257772a0db9`

## Data

| Field | Type | Cardinality | Example | Example source |
|:--|:--|:--|:--|:--|
| `advertising_host` | Domain | Single | foo.com | walk scene |

## Where RFC 5321 says so

| Section | Lines | Text |
|:--|:--|:--|
| §3.5.2 | 1364-1366 | *"Since they were both optional in RFC 821, but VRFY was made mandatory in RFC 1123 [3], if EXPN is supported, it MUST be listed as a service extension in an EHLO response."* |
| §3.5.2 | 1366-1369 | *"VRFY MAY be listed as a convenience but, since support for it is required, SMTP clients are not required to check for its presence on the extension list before using it."* |
| §D.4 | 5111-5112 | *"S: 250-VRFY S: 250 HELP"* |

Line numbers are into [`docs/rfc/rfc5321.txt`](../../docs/rfc/rfc5321.txt), the archived copy. Quotations keep the specification's own words and spellings (AGENTS.md rule 2).

## Notes

Inherited (candidate 11), kept under the method's instruction to include when unsure and mark it. The argument against: the EHLO listing is a display of configuration state and the guest-viewed-calendar test kills a display. The argument for: MUST strength at 1365 means enabling EXPN changes what the server is obliged to emit, and an obligation that did not exist before is state that came into existence at a moment. The re-sweep added D.4 lines 5111-5112 as live wire data, and it cuts against the RFC's own rule in two ways worth recording rather than resolving: the scenario advertises 250-VRFY, exercising the MAY at 1366-1367 even though VRFY support is required, and it advertises 250 HELP — a keyword for a base command that §3.5.2 never discusses at all. Whatever a later step does with this event, the VRFY/EXPN asymmetry at 1364-1369 is the content that must survive: EXPN MUST be listed if supported, VRFY MAY be listed and clients need not check for it. Rejected alternative names: ExpnAdvertisedInEhlo, ExtensionKeywordPublished, ExpnCapabilityAnnounced. Which screen would have killed it: the HELO-only charter outright — EHLO extensions are out of charter by definition — plus the consumer test and the altitude tiers. Inverse: ExpnSupportDisabled, into which this event's fact should fold if the facilitator rules the listing a display. FIELD HYGIENE PASS: struck extension_keyword ('EXPN'). Its value cannot vary — this event is about EXPN by name — so the field restated the event's own name and could carry no information, which is padding by definition. The same test does NOT strike the command field on VerificationDeclinedAsDisabled or CrossTypeVerificationRefused, where the value genuinely ranges over VRFY and EXPN; the rule is that a field fixed by the event's name is not a field. One field survives, the host that now owes the advertisement. No reply code and no invented identity key were present. The thinning strengthens rather than weakens the doubt already recorded: with the keyword gone, the record says only that a host acquired an obligation, which is exactly the question a later step has to rule on. Left doubtful.
