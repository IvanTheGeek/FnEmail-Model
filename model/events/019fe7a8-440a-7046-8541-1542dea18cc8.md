# `ReturnPathDeletedByInboundGateway`

A gateway bringing mail from another transport into SMTP removed the return-path header field it found in the message.

| Grouping | Value | Fixed at |
|:--|:--|:--|
| Workflow | 4. Trace and Provenance | Step 1 — timeline discovery |
| Stream root | Message | Step 1 — stream roots |
| Ownership swimlane | — | Step 6 — events section, per team or system |
| Originating actor | Gateway (non-SMTP to SMTP) | Step 3 — actor swimlane; provenance, not this event's row |

🟧 Event · confidence *likely* · recorded 2026-08-09, Step 1 Brain Storming, whole-spec sweep of RFC 5321 · `019fe7a8-440a-7046-8541-1542dea18cc8`

## Data

| Field | Type | Cardinality | Example | Example source |
|:--|:--|:--|:--|:--|
| `deleted_return_path` | Path | Single | \<Smith@bar.com> | walk scene |
| `source_transport` | Text | Single | X.400 | rfc5321.txt:3466 |
| `gateway_host` | DomainName | Single | gw.foo.com | illustrative — no RFC source; built on the walk scene domain foo.com |

## Where RFC 5321 says so

| Section | Lines | Text |
|:--|:--|:--|
| §4.4 | 3268-3269 | *"a gateway from elsewhere -> SMTP SHOULD delete any return-path header field present in the message, and either copy that"* |
| §4.4 | 3240-3243 | *"The primary purpose of the Return-path is to designate the address to which messages indicating non-delivery or other mail system failures are to be sent. For this to be unambiguous, exactly one return path SHOULD be present when the message is delivered."* |
| §4.4 | 3268-3273 | *"a gateway from elsewhere -> SMTP SHOULD delete any return-path header field present in the message, and either copy that information to the SMTP envelope or combine it with information present in the envelope of the other transport system to construct the reverse-path argument to the MAIL command in the SMTP envelope."* |

Line numbers are into [`docs/rfc/rfc5321.txt`](../../docs/rfc/rfc5321.txt), the archived copy. Quotations keep the specification's own words and spellings (AGENTS.md rule 2).

## Notes

Carried over from idx 205. SHOULD. Deliberately separated from the reverse-path construction the same sentence goes on to require, because the deletion and the construction can be observed independently — a gateway can delete and fail to construct, producing a message with no error destination at all, which is the exact failure 3240-3243 warns against. Pairs with ReversePathConstructedFromForeignEnvelope. Rule 2 note: the quoted range ends mid-sentence at 'and either copy that' because that is where line 3269 ends and the clause continues into the paired event's citation; the fragment is copied as it breaks rather than silently completed. Mirror event: ReturnPathInsertedByOutboundGateway. FnEmail screens: the HELO-only charter and inbound-only scope; also, at the protocol tier this is invisible, since the gateway's output looks like an ordinary MAIL command on the wire — which is the most useful thing about it, because it means an inbound-only model cannot tell this provenance from any other. ⚠️ FIELD HYGIENE PASS: struck the fabricated `message_ref` identity key — RFC 5321 supplies no message identifier and f2C8D14 is not in the RFC. Identity is the deleted path, which is the last trace of the foreign envelope before the SMTP envelope replaces it. The inherited note's closing observation gains force from the strike: if an inbound-only model cannot tell this provenance from any other, then a fabricated key on the event was inventing exactly the continuity the protocol denies.

COLLISION RESOLVED — merged from 2 independently brainstormed records. Same section, same sentence, same SHOULD, same actor, same stream root, same two surviving fields with the same values. The only difference is one extra field whose example was invented.

From the duplicate in 9. Gatewaying: Deliberately separated from the construction that the SAME SENTENCE goes on to require, because the two are independently observable: a gateway can delete and then fail to construct, which produces a message with no error destination at all — the exact ambiguity 3240-3243 warns against. Fusing them would make that failure unrepresentable. Strength: SHOULD. Pairs with ReversePathConstructedFromForeignEnvelope. Note this event is invisible at the protocol tier: the gateway's output on the wire is an ordinary MAIL command, so nothing downstream can tell a deleted-and-reconstructed path from an original one — a legitimate 🟥 hotspot about the limits of what a receiving model can know. Related but NOT merged: the return-path chapter's ReturnPathRemoved cites §4.4 lines 3229-3231, a MAY granted to "forwarding, gateway, or relay" systems for a different purpose (ensuring exactly one line survives); this one is the gateway-specific SHOULD at 3268-3269. Two rules, two strengths, one verb. Rejected alternative names: InboundReturnPathStripped, ForeignReturnPathDiscarded. Which project screen would have killed it: HELO-only charter, inbound-only scope.

FIELD HYGIENE PASS: struck message_ref (example f2C8D14) — no message identifier in RFC 5321; the deleted value is what makes the deletion recoverable and is the only identity the act has. Kept deleted_return_path deliberately: the note's whole argument is that a delete-without-construct produces a message with no error destination, and only recording the deleted value makes that loss legible. Value sourced to the walk scene rather than to the RFC, since §4.4 lines 3268-3273 supplies the rule and no address.
