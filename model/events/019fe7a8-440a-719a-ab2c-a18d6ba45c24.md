# `AddressLiteralTagRegistered`

A tag identifying a new form of domain literal was entered in the Address Literal Tags registry.

| Grouping | Value | Fixed at |
|:--|:--|:--|
| Workflow | 15. Server Configuration and Provisioning | Step 1 — timeline discovery |
| Stream root | Extension Registry | Step 1 — stream roots |
| Ownership swimlane | — | Step 6 — events section, per team or system |
| Originating actor | IANA | Step 3 — actor swimlane; provenance, not this event's row |

🟧 Event · confidence *likely* · recorded 2026-08-09, Step 1 Brain Storming, whole-spec sweep of RFC 5321 · `019fe7a8-440a-719a-ab2c-a18d6ba45c24`

## Data

| Field | Type | Cardinality | Example | Example source |
|:--|:--|:--|:--|:--|
| `tag` | Text | Single | IPv6 | rfc5321.txt:4403 |
| `defining_document` | Text | Single | RFC 5321 | rfc5321.txt:4403 |

## Where RFC 5321 says so

| Section | Lines | Text |
|:--|:--|:--|
| §8 | 4400-4405 | *"The second registry, "Address Literal Tags" [47], consists of "tags" that identify forms of domain literals other than those for IPv4 addresses (specified in RFC 821 and in this document). The initial entry in that registry is for IPv6 addresses (specified in this document). Additional literal types require standardization before being used; none are anticipated at this time."* |
| §4.1.3 | 2403-2405 | *"a Standardized-tag MUST be specified in a Standards-Track RFC and registered with IANA"* |

Line numbers are into [`docs/rfc/rfc5321.txt`](../../docs/rfc/rfc5321.txt), the archived copy. Quotations keep the specification's own words and spellings (AGENTS.md rule 2).

## Notes

RECOVERED BY THE RE-SWEEP. §8 names three registries and the raw set carried only the first; this is the second. It existed in the inherited notes twice — once inside ServiceExtensionRegistered's fold list and once inside ServiceExtensionEnabled's, both times named and not carried, which is precisely how the 32-event cap ate whole families. MUST strength on the standardization requirement (§4.1.3 2403-2405). The RFC's own initial entry is IPv6, so the example is real and drawn from the spec rather than invented. 'none are anticipated at this time' (4404-4405) is a rare forward-looking statement in a registry definition and dates badly, which is itself the reason registry events belong on a timeline. Screens: every altitude tier. FIELD HYGIENE: struck registry ('Address Literal Tags') — a constant the event name already carries, since this event exists for exactly one registry. The two survivors both trace to rfc5321.txt:4403, where the RFC names IPv6 as the initial entry 'specified in this document', which is what makes RFC 5321 the defining document for it. No session, transaction or connection key was present; identity is the tag, which is the registry's own key.
