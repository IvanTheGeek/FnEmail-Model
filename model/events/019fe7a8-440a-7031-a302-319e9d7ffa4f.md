# `ReceivedLineChainAltered`

An existing Received: line in a message's header section was changed, deleted, or reordered by a later system — the forbidden arm of the trace-integrity rule.

| Grouping | Value | Fixed at |
|:--|:--|:--|
| Workflow | 4. Trace and Provenance | Step 1 — timeline discovery |
| Stream root | Message | Step 1 — stream roots |
| Ownership swimlane | — | Step 6 — events section, per team or system |
| Originating actor | Relay or gateway (non-conforming) | Step 3 — actor swimlane; provenance, not this event's row |

🟧 Event · confidence *likely* · recorded 2026-08-09, Step 1 Brain Storming, whole-spec sweep of RFC 5321 · `019fe7a8-440a-7031-a302-319e9d7ffa4f`

## Data

| Field | Type | Cardinality | Example | Example source |
|:--|:--|:--|:--|:--|
| `altering_host` | Domain | Single | gw.foo.com | illustrative — no RFC source |
| `alteration` | Text | Single | reordered | rfc5321.txt:3181 |

## Where RFC 5321 says so

| Section | Lines | Text |
|:--|:--|:--|
| §4.4 | 3178-3179 | *"An Internet mail program MUST NOT change or delete a Received: line that was previously added to the message header section."* |
| §4.4 | 3180-3182 | *"SMTP servers MUST prepend Received lines to messages; they MUST NOT change the order of existing lines or insert Received lines in any other location."* |
| §3.7.2 | 1577-1579 | *"When forwarding a message into or out of the Internet environment, a gateway MUST prepend a Received: line, but it MUST NOT alter in any way a Received: line that is already in the header section."* |
| §3.7.2 | 1583-1585 | *"the most important use of Received: lines is for debugging mail faults, and this debugging can be severely hampered by well-meaning gateways that try to "fix" a Received: line."* |

Line numbers are into [`docs/rfc/rfc5321.txt`](../../docs/rfc/rfc5321.txt), the archived copy. Quotations keep the specification's own words and spellings (AGENTS.md rule 2).

## Notes

MERGE of idx 196 (ReceivedLineChainAltered) with ExistingReceivedLineAltered, which an earlier sweep recorded as folded into ReceivedLinePrependedByGateway rather than kept — recovering it here is one of the re-sweep's finds, because the §3.7.2 quote at 1583-1585 is the only place in RFC 5321 that says this violation actually happens, and it happens for a *good* motive. CITATION CORRECTION to the inherited record: idx 196 cited §4.4 '3178-3180' for a sentence that ends on 3179. Confidence raised from the inherited 'doubtful' to 'likely' on that evidence: the RFC treats it as a live failure mode, not a hypothetical. Three MUST NOTs converge here (3178-3179 change or delete; 3180-3182 reorder; 1578-1579 alter in any way at a gateway) and the compliant arm of all three is inaction — see ExistingTraceLinePreserved for that tension, deliberately unresolved. Broader sibling in a neighboring chapter: MessageDataModifiedByRelay (idx 81, §3.6.3) covers any modification to header or body and belongs to the relaying chapter; this event is the trace-specific case. Opposite: TraceLineInsertedOutOfPosition, which adds in the wrong place rather than touching what is there. FnEmail screens: the consumer test — nothing inbound consumes a tampering finding — and the responsibility-boundary cutoff, since the altering host is by definition upstream. ⚠️ FIELD HYGIENE PASS: struck the fabricated `message_ref` identity key — RFC 5321 names no message identifier, and f2C8D14 is this project's minted queue id, absent from the RFC. Identity is the altered chain itself, which is the only durable record of the act. The `alteration` value is now sourced rather than free text: 'reordered' is the RFC's own third prohibition at line 3181, 'change the order of existing lines', and the other two legal values of this field are the RFC's own verbs at 3178, 'change or delete'. gw.foo.com is invented and now says so — the RFC names no gateway host.
