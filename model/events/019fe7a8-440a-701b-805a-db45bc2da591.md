# `NextHopChannelEstablished`

The sending system opened a transmission channel to the SMTP server at a selected candidate address, ending the resolution sequence for this attempt.

| Grouping | Value | Fixed at |
|:--|:--|:--|
| Workflow | 6. Address Resolution and Next-Hop Selection | Step 1 — timeline discovery |
| Stream root | Next-Hop Destination | Step 1 — stream roots |
| Ownership swimlane | — | Step 6 — events section, per team or system |
| Originating actor | Sending SMTP system acting as SMTP client | Step 3 — actor swimlane; provenance, not this event's row |

🟧 Event · confidence *certain* · recorded 2026-08-09, Step 1 Brain Storming, whole-spec sweep of RFC 5321 · `019fe7a8-440a-701b-805a-db45bc2da591`

## Data

| Field | Type | Cardinality | Example | Example source |
|:--|:--|:--|:--|:--|
| `sending_host` | DomainName | Single | foo.com | rfc5321.txt:5049 |
| `next_hop_host` | DomainName | Single | xyz.com | rfc5321.txt:5052 |
| `next_hop_address` | IpAddress | Single | 203.0.113.20 | walk scene |

## Where RFC 5321 says so

| Section | Lines | Text |
|:--|:--|:--|
| §3.6.2 | 1452-1454 | *"it then becomes an SMTP client, establishes a transmission channel to the next SMTP server specified in the DNS (according to the rules in Section 5), and sends it the mail"* |
| §5.1 | 3874-3876 | *"each of the relevant addresses in this list in order, until a delivery attempt succeeds"* |
| §D.3 | 5052-5053 | *"It tries to open a connection to xyz.com itself and succeeds. Then we have:"* |

Line numbers are into [`docs/rfc/rfc5321.txt`](../../docs/rfc/rfc5321.txt), the archived copy. Quotations keep the specification's own words and spellings (AGENTS.md rule 2).

## Notes

MERGED: the inherited NextHopChannelEstablished (§3.6.2) with the success arm of §5.1's 'until a delivery attempt succeeds' — one state change reached by two routes, and neither sweep saw the other's half. Kept DISTINCT from SmtpClientRoleAssumed and from the transfer itself, both other chapters': the role change, the connection and the transfer are three state changes that the RFC runs together in one sentence at 1452-1454 and each can fail independently. The next-hop *selection* is a lookup and is set aside; what changes state is the channel existing. D.3 line 5052 supplies the worked success with real host names, and D.3's step 1 supplies the fallback case — the source host failed to xyz.com and succeeded to the lower-preference foo.com (4997-4999). Rejected alternative names: NextHopConnectionOpened, DeliveryAttemptSucceededForAddress (accurate but frames a connection as a delivery, which it is not — nothing has been transferred yet). Opposite events: DeliveryAttemptFailedForAddress, CandidateAddressesExhausted. Which project screen would have killed it: the inbound-only scope and the altitude tiers. ⚠️ FIELD HYGIENE PASS: struck opened_at as event metadata — every event has a time of occurrence, and carrying it as a field pads the payload; the same strike was applied to queued_at on MessageQueuedAfterTemporaryDnsError. No fabricated identity key was present. A channel has two ends and both are kept, since neither host is recoverable from the other; next_hop_address is kept because the RFC's candidate list is a list of addresses (3891-3893) and the channel is opened to one of them, which a host name does not fix. The two host names come from D.3's worked success; the address is a walk-scene value because RFC 5321 gives no IP address in that scenario.

COLLISION RESOLVED — merged from 2 independently brainstormed records. Both cite §3.6.2 1452-1454 as the primary reference for the same act, on the same stream root, with the same host-name example from D.3. The actor labels differ only in wording ('sending SMTP system' versus 'relay SMTP server'), and §3.6.2's sentence is about a relay becoming a client, so they name the same role. One record's references are a strict superset of the other's — the superset case is a merge, not a rename.

From the duplicate in 8. Relaying: Carried from #77 unchanged in substance. Kept separate from SmtpClientRoleAssumed and MailRelayedOnward: the RFC runs role change, connection and transfer together in one sentence (1451-1454), but each fails independently and a facilitator will want them separable. The next-hop SELECTION is a lookup and sits in set_aside; what changes state is the channel existing. Strength: declarative, inside the 'If it accepts the task' consequence chain - no keyword. 5.1 'Locating the Target Host' (heading 3824, verified) holds the selection rules. Opposite arm: NextHopChannelRefused. Rejected names: OnwardConnectionOpened, TransmissionChannelToNextHopOpened. Screens: inbound-only scope; altitude tiers would push it to Infrastructure. --- FIELD HYGIENE: struck relay_host (recorder's own domain), opened_at (event time restated) and next_hop_ip - 203.0.113.20 was the walk scene's CLIENT address applied to a next-hop server, a role error, and the address is in any case the result of the DNS lookup 1453 defers to Section 5, i.e. a derived value belonging to the resolution read model, which is where the inherited note had already put the selection. Value re-based on Appendix D.3 line 5052, where the relay opens the onward channel to xyz.com.
