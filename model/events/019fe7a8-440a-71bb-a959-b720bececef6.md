# `SubmissionArrangementEstablished`

A private arrangement was put in place for a limited mail-sending client to send all of its messages to one designated server for processing and onward distribution.

| Grouping | Value | Fixed at |
|:--|:--|:--|
| Workflow | 8. Relaying | Step 1 — timeline discovery |
| Stream root | Server Configuration | Step 1 — stream roots |
| Ownership swimlane | — | Step 6 — events section, per team or system |
| Originating actor | Mail system operator or administrator, on behalf of a mail-sending client | Step 3 — actor swimlane; provenance, not this event's row |

🟧 Event · confidence *likely* · recorded 2026-08-09, Step 1 Brain Storming, whole-spec sweep of RFC 5321 · `019fe7a8-440a-71bb-a959-b720bececef6`

## Data

| Field | Type | Cardinality | Example | Example source |
|:--|:--|:--|:--|:--|
| `client_identity` | Address | Single | \<JQP@bar.com> | rfc5321.txt:5008 |
| `submission_server` | Domain | Single | foo.com | rfc5321.txt:5001 - the RFC's relay host, which 3.6.3 titles 'Message Submission Servers as Relays' |

## Where RFC 5321 says so

| Section | Lines | Text |
|:--|:--|:--|
| §3.6.3 | 1481-1483 | *"For these clients, it is common practice to make private arrangements to send all messages to a single server for processing and subsequent distribution."* |
| §3.6.3 | 1477-1481 | *"Many mail-sending clients exist, especially in conjunction with facilities that receive mail via POP3 or IMAP, that have limited capability to support some of the requirements of this specification, such as the ability to queue messages for subsequent delivery attempts."* |
| §3.6.3 | 1486-1488 | *"In any event, because these arrangements are private and fall outside the scope of this specification, they are not described here."* |

Line numbers are into [`docs/rfc/rfc5321.txt`](../../docs/rfc/rfc5321.txt), the archived copy. Quotations keep the specification's own words and spellings (AGENTS.md rule 2).

## Notes

Carried from #66. The RFC names this and then explicitly refuses to specify it (1486-1488), and that refusal is exactly why Step 1 should record it: the arrangement is real, it changes state, and it is the precondition for MessageSubmittedForDistribution. Strength: none; descriptive of common practice. RFC 4409 [18] is named at 1486 as the standardized replacement - 'A standardized mail submission protocol has been developed that is gradually superseding practices based on SMTP' (1484-1486) - so a sibling SubmissionProtocolAdopted belongs to a different specification and is deliberately not given a slot here. Rejected names: SmartHostConfigured, DesignatedSubmissionServerAssigned. Screens: inbound-only scope, responsibility-boundary cutoff. --- FIELD HYGIENE: struck client_ip - 3.6.3 lines 1477-1488 describe the arrangement and never mention addresses, and the sweep's 192.0.2.10 was the walk scene's own server address besides, the same role error found in RelaySourceAuthorized. The two survivors are the arrangement's two parties, both re-based onto Appendix D.3.
