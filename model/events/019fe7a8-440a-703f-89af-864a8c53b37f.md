# `ReturnPathLineInserted`

A delivery server wrote a Return-Path line at the head of the mail data, preserving the reverse-path from the MAIL command as the message left the SMTP environment.

| Grouping | Value | Fixed at |
|:--|:--|:--|
| Workflow | 4. Trace and Provenance | Step 1 — timeline discovery |
| Stream root | Message | Step 1 — stream roots |
| Ownership swimlane | — | Step 6 — events section, per team or system |
| Originating actor | Delivery SMTP server | Step 3 — actor swimlane; provenance, not this event's row |

🟧 Event · confidence *certain* · recorded 2026-08-09, Step 1 Brain Storming, whole-spec sweep of RFC 5321 · `019fe7a8-440a-703f-89af-864a8c53b37f`

## Data

| Field | Type | Cardinality | Example | Example source |
|:--|:--|:--|:--|:--|
| `reverse_path` | Path | Single | \<Smith@bar.com> | walk scene |
| `delivering_host` | Domain | Single | foo.com | walk scene |
| `inserting_host` | Domain | Single | bar.com | walk scene |

## Where RFC 5321 says so

| Section | Lines | Text |
|:--|:--|:--|
| §4.4 | 3204-3206 | *"When the delivery SMTP server makes the "final delivery" of a message, it inserts a return-path line at the beginning of the mail data."* |
| §4.4 | 3206-3207 | *"This use of return-path is required; mail systems MUST support it."* |
| §4.4 | 3221-3224 | *"The text above implies that the final mail data will begin with a return path line, followed by one or more time stamp lines."* |
| §4.4 | 3240-3243 | *"The primary purpose of the Return-path is to designate the address to which messages indicating non-delivery or other mail system failures are to be sent. For this to be unambiguous, exactly one return path SHOULD be present when the message is delivered."* |
| §4.4 | 3300 | *"Return-path-line = "Return-Path:" FWS Reverse-path \<CRLF>"* |
| §4.4 | 3240-3242 | *"The primary purpose of the Return-path is to designate the address to which messages indicating non-delivery or other mail system failures are to be sent."* |

Line numbers are into [`docs/rfc/rfc5321.txt`](../../docs/rfc/rfc5321.txt), the archived copy. Quotations keep the specification's own words and spellings (AGENTS.md rule 2).

## Notes

Carried over from idx 199, citations verified line by line. Strength asymmetry that is easy to misreport and is preserved here: supporting return-path is a MUST (3206-3207) but *exactly one* being present at delivery is only a SHOULD (3242-3243) — the RFC accepts that duplicates happen and hands relays a MAY to clean up (3229-3231). Ordering is specified at 3221-3224: return path line first, then one or more time stamp lines, then the balance of the header section and the body — so this event and TraceLinePrepended are not commutative, which is the kind of fact a wall of cards loses unless someone writes it down. CHAPTER OVERLAP, declared: the roster gives §4.4 3204-3206 to the responsibility-handoff chapter as well, and FinalDeliveryMade (idx 200) is handed to that chapter rather than duplicated here; this chapter keeps only the provenance act, because the chapter's own Covers line names 'Return-Path: inserted at final delivery'. Violated arms recovered by the re-sweep: MultipleReturnPathsPresentAtDelivery, ReturnPathSubmittedByOriginatingSystem. FnEmail screens: the responsibility-boundary cutoff and the inbound-only scope, since final delivery is by definition the moment the message leaves SMTP. ⚠️ FIELD HYGIENE PASS: struck the fabricated `message_ref` identity key — RFC 5321 supplies no message identifier, and f2C8D14 is this project's own minted queue id rather than anything the spec carries. What identifies this event is the reverse-path itself, which is the RFC's own answer: the Return-path-line production at 3300 is 'Return-Path:' followed by the Reverse-path and nothing else, so the path *is* the line's content and its identity. Both surviving values come from the walk scene.

COLLISION RESOLVED — merged from 2 independently brainstormed records. Identical section, identical five citations, identical actor, identical stream root and confidence, and the same two-field payload under two field names. The chapters both claimed §4.4's return-path half; chapter 4's Covers line settles it, and the merge additionally catches an inverted example that would have taught the wrong thing under rule 8.

From the duplicate in 5. Responsibility Handoff and Final Delivery: Inherited candidate 199, kept. The strength asymmetry here is easy to misreport and rule 3 was applied to it: supporting return-path is a MUST (3206-3207), but exactly one being present at delivery is only a SHOULD (3242-3243), and the RFC accepts that duplicates happen and gives relays a MAY to clean up (3229-3231). Ordering is specified and non-commutative — 3221-3224 puts the return path line first, then the time stamp lines, then the balance of the header section and the body — so this event and the trace insertion cannot be reordered even though nothing else links them. Cross-reference: TraceRecordInserted (inherited candidates 178 and 298) is set aside to whichever chapter owns §4.4's trace half; only the return-path half of §4.4 is claimed here, per this chapter's stated anchors. Screens: the responsibility-boundary cutoff and inbound-only scope, since final delivery is by definition the moment the message leaves SMTP. ⚠️ FIELD HYGIENE PASS 2026-08-09 — one field struck. message_id: RFC 5321 supplies no message identifier. This is the event where the point is sharpest, because §4.4's ID clause — the one place in the whole document that even resembles a message key — sits four lines from this event's own grammar rule: `Return-path-line = "Return-Path:" FWS Reverse-path \<CRLF>` at line 3300 and `ID = CFWS "ID" FWS ( Atom / msg-id )` at line 3336. The ID clause is OPTIONAL (`Opt-info = [Via] [With] [ID] [For]`, line 3329), belongs to the Received: stamp rather than to the message, and takes an Atom or an RFC 5322 msg-id, so RFC 5321 defines no identifier of its own. The struck example f2C8D14 is this project's minted queue_id value from commit 6acfc68. Identity here is the reverse_path being written — the value IS the payload of the line — plus the host writing it.
