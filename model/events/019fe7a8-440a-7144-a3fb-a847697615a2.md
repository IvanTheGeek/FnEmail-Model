# `EmptyBccHeaderFieldInserted`

A submission MTA inserted an empty BCC header field because stripping the blind recipients left the message with no TO, CC or BCC field at all.

| Grouping | Value | Fixed at |
|:--|:--|:--|
| Workflow | 10. Alias and List Expansion | Step 1 — timeline discovery |
| Stream root | Message | Step 1 — stream roots |
| Ownership swimlane | — | Step 6 — events section, per team or system |
| Originating actor | Submission client MTA | Step 3 — actor swimlane; provenance, not this event's row |

🟧 Event · confidence *certain* · recorded 2026-08-09, Step 1 Brain Storming, whole-spec sweep of RFC 5321 · `019fe7a8-440a-7144-a3fb-a847697615a2`

## Data

**No data.** The method sanctions this: *"If an event has no meaningful payload beyond its identity (e.g., a simple state transition), it is fine to have no fields or just the identity key."* It also warns, of a different case, that *"An event without fields is an opaque label"* — both are recorded here rather than reconciled, because reconciling them is a later step's work, not Brain Storming's.

## Where RFC 5321 says so

| Section | Lines | Text |
|:--|:--|:--|
| §Appendix B | 4741-4745 | *"Once this process is completed, the remaining header fields SHOULD be checked to verify that at least one TO, CC, or BCC header field remains. If none do, then a BCC header field with no additional information SHOULD be inserted as specified in [4]."* |

Line numbers are into [`docs/rfc/rfc5321.txt`](../../docs/rfc/rfc5321.txt), the archived copy. Quotations keep the specification's own words and spellings (AGENTS.md rule 2).

## Notes

CITATION WIDENED during the merge: the inherited candidate quoted a fragment cut across a line break — "remains.  If none do, then a BCC header field with no additional" at lines 4744-4745 — which is accurate to the source but unreadable as a claim. Replaced with the full two sentences at 4741-4745; the fragment was correctly placed, so this is a completeness fix, not a correction of fact. Substantively a fieldless event: nothing about it varies except where it happened. The kit permits exactly this ("If an event has no meaningful payload beyond its identity ... it is fine to have no fields or just the identity key") and its own counter-warning applies simultaneously ("An event without fields is an opaque label") — the two fields kept are the minimum that keeps it readable without padding to reach a count, and the tension is recorded here rather than resolved, per the prompt. Strength: SHOULD, with the field's format delegated outward to RFC 5322 ("as specified in [4]", line 4745). The check that precedes it is a read and is in set_aside. Rejected alternative names: PlaceholderBccInserted, EmptyRecipientFieldAdded. Screens: the consumer test G1, since nothing anywhere reads an empty BCC field. FIELD HYGIENE PASS — THIS EVENT IS NOW FIELDLESS, THE ONLY ONE IN THE CHAPTER. Both fields struck. inserted_field could only ever hold "BCC" on an event named EmptyBccHeaderFieldInserted, so it restated the name. submission_host restated the record's own actor, "Submission client MTA", and the note above already conceded that "nothing about it varies except where it happened" — keeping two fields "to keep it readable" is padding in the method's own words, and the earlier pass said as much while doing it. The field is empty by definition, the message has no identifier at submission time because nothing has been accepted anywhere and no §4.4 ID clause has been stamped, and what identifies the event is its position in the Appendix B sequence, immediately after BccHeaderFieldRemoved (4740-4745). Confidence stays certain: fieldlessness is not weakness here — the SHOULD at 4744-4745 is explicit and the act is real — and the opaque-label counter-warning recorded above still stands, unresolved, which is where the prompt leaves it. Zero fieldless events across 567 is what exposed the padding; this is what one looks like when the padding is gone.
