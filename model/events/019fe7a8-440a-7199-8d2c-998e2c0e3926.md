# `ServiceExtensionRegistered`

IANA added a service extension, with its keyword, parameters and verbs, to the SMTP Service Extensions registry.

| Grouping | Value | Fixed at |
|:--|:--|:--|
| Workflow | 15. Server Configuration and Provisioning | Step 1 — timeline discovery |
| Stream root | Extension Registry | Step 1 — stream roots |
| Ownership swimlane | — | Step 6 — events section, per team or system |
| Originating actor | IANA | Step 3 — actor swimlane; provenance, not this event's row |

🟧 Event · confidence *certain* · recorded 2026-08-09, Step 1 Brain Storming, whole-spec sweep of RFC 5321 · `019fe7a8-440a-7199-8d2c-998e2c0e3926`

## Data

| Field | Type | Cardinality | Example | Example source |
|:--|:--|:--|:--|:--|
| `ehlo_keyword` | Text | Single | 8BITMIME | rfc5321.txt:4889 |
| `extension_name` | Text | Single | 8bit-MIMEtransport | rfc5321.txt:4583 |
| `defining_document` | Text | Single | RFC 1652 | rfc5321.txt:4584 |
| `added_mail_or_rcpt_parameters` | Text | List | BODY=8BITMIME | illustrative — no RFC source |
| `command_length_increment` | Text | Single | MAIL increased by 14 octets | illustrative — no RFC source |

## Where RFC 5321 says so

| Section | Lines | Text |
|:--|:--|:--|
| §8 | 4391-4398 | *"The first, "Simple Mail Transfer Protocol (SMTP) Service Extensions" [46], consists of SMTP service extensions with the associated keywords, and, as needed, parameters and verbs. As specified in Section 2.2.2, no entry may be made in this registry that starts in an "X". Entries may be made only for service extensions (and associated keywords, parameters, or verbs) that are defined in Standards-Track or Experimental RFCs specifically approved by the IESG for this purpose."* |
| §2.2.2 | 534-538 | *"The IANA maintains a registry of SMTP service extensions. A corresponding EHLO keyword value is associated with each extension. Each service extension registered with the IANA must be defined in a formal Standards-Track or IESG-approved Experimental protocol document."* |
| §2.2.2 | 567-569 | *"the increment by which the extension is increasing the maximum length of the commands MAIL and/or RCPT, over that specified in this Standard."* |
| §2.2.2 | 577-580 | *"keyword values presented in the EHLO response that do not begin with "X" MUST correspond to a Standard, Standards-Track, or IESG-approved Experimental SMTP service extension registered with IANA. A conforming server MUST NOT offer non-"X"-prefixed keyword values that are not described in a registered extension."* |
| §8 | 4391-4394 | *"The first, "Simple Mail Transfer Protocol (SMTP) Service Extensions" [46], consists of SMTP service extensions with the associated keywords, and, as needed, parameters and verbs."* |
| §8 | 4394-4395 | *"As specified in Section 2.2.2, no entry may be made in this registry that starts in an "X"."* |
| §D.1 | 4889-4892 | *"S: 250-8BITMIME S: 250-SIZE S: 250-DSN S: 250 HELP"* |

Line numbers are into [`docs/rfc/rfc5321.txt`](../../docs/rfc/rfc5321.txt), the archived copy. Quotations keep the specification's own words and spellings (AGENTS.md rule 2).

## Notes

MERGE of two independent candidates of the same name, one reached from §2.2.2 and one from §8; refs and fields unioned. §2.2.2 538-569 enumerates SEVEN mandatory parts of a registration — an unusually complete field list handed to us by the spec — of which five were carried: name, keyword, parameter syntax, additional verbs, new MAIL/RCPT parameters, behavioral description, command-length increment. The command-length increment is the surprising one: a registration ALTERS WIRE LIMITS at every conforming server, which links this event to SizeLimitConfigured across a timeline measured in years, and that point is preserved here with its §2.2.2 567-569 citation intact. A governance actor entirely outside any mail system, writing a shared read model every conforming server consults. Violated arm at 577-580, a MUST NOT: a server offering an unregistered non-X keyword. FOLDED HERE rather than given its own slot, citation intact: AdditionalClauseRegistered (§8 4419-4442, the 'Additional-registered-clauses' subsection added to VIA link types and WITH protocol types, registrable only by standardization or an IESG-approved Experimental extension) and ExtensionApprovedByIesg (§8 4396-4398 and 4412-4417, the upstream act with the IESG as actor). Screens: all four altitude tiers at once, since this sits above Domain and outside the running system. FIELD HYGIENE: struck two fields. added_mail_or_rcpt_parameters (BODY=8BITMIME) and command_length_increment (14) are both derivable from the defining document the registration names, so by the method's own rule they belong in a read model over the registry rather than on the registration event; the increment's value 14 additionally had no source anywhere in RFC 5321, which is the signal the brief describes. The three survivors are the registry key and the two facts the §2.2.2 534-538 MUST turns on, and every one is verified: 8BITMIME appears in an EHLO response at rfc5321.txt:4889, '8bit-MIMEtransport' and 'RFC 1652' in the reference at 4583-4584. No session, transaction or connection key was present; identity is the EHLO keyword, which is the registry's own key.

COLLISION RESOLVED — merged from 2 independently brainstormed records. Both copies say MERGE in their own notes — two sweeps found the same registration event, one from 2.2.2 and one from 8 — with the same actor, the same stream root, and the same identity key: the EHLO keyword that 534-535 associates with each extension, which is the one genuine RFC-supplied identity key anywhere in this material. The only disagreement was field count, and the two unsourceable values decide it: an event whose examples must be invented is padding by the audit's own definition. The spec's seven-part enumeration is preserved as a citation and a note rather than as columns, so nothing researched is lost and the five-field version can be restored by a facilitator who wants the registry modeled in full.

From the duplicate in 16. Abuse, Security and Registry: MERGE of two inherited candidates with the same name found by two sweeps, one from 2.2.2 and one from 8; refs and fields unioned. All citations re-verified line by line — both were accurate, worth recording given rule 3's warning. Names rejected: ExtensionRegistered, EhloKeywordRegistered, SmtpExtensionAdded. A configuration-over-time event with an actor entirely outside any mail system, writing a registry every conforming server consults. 2.2.2 lines 538-569 enumerate SEVEN mandatory parts; five are kept and the full seven are name, EHLO keyword, parameter syntax, additional verbs, new MAIL/RCPT parameters, behavioral description, and command-length increment. The last matters most and is easiest to miss: a registration CHANGES THE MAXIMUM LENGTH of MAIL and RCPT, so registry state alters wire limits at every server. Folded here with sections verified: AddressLiteralTagRegistered (8 lines 4400-4405) and RegistrySubsectionAdded (8 lines 4419-4422); both are separable and a facilitator may want them back. Screens that would have killed it: all four altitude tiers at once, the consumer test, every gate the project has. FIELD HYGIENE: nothing struck, and the justification for five fields is the spec's own — 2.2.2 lines 538-569 enumerate the mandatory parts, so the field list is handed over rather than invented, the rare case where 'only what the business needs' is settled by the source. Three examples are real: 8BITMIME at line 4889 (also 922-926), and '8bit-MIMEtransport' with RFC 1652 from the reference at lines 4583-4584. Two are not and say so plainly: BODY=8BITMIME does not appear in rfc5321.txt, and neither does any octet increment — 567-569 requires an increment to be stated in a registration and RFC 5321 never states one, which is the finding, not a license to invent. Identity: the EHLO keyword, which 534-535 says is associated with each extension — the one genuine identity key in this chapter, supplied by the RFC rather than fabricated.
