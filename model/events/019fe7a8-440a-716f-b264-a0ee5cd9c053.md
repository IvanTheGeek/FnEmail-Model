# `ExpnSupportDisabled`

A site turned the EXPN command off, so mailing list membership can no longer be obtained over SMTP and the EXPN keyword leaves the EHLO response.

| Grouping | Value | Fixed at |
|:--|:--|:--|
| Workflow | 13. Address Diagnostics | Step 1 — timeline discovery |
| Stream root | Server Configuration | Step 1 — stream roots |
| Ownership swimlane | — | Step 6 — events section, per team or system |
| Originating actor | Site administrator | Step 3 — actor swimlane; provenance, not this event's row |

🟧 Event · confidence *certain* · recorded 2026-08-09, Step 1 Brain Storming, whole-spec sweep of RFC 5321 · `019fe7a8-440a-716f-b264-a0ee5cd9c053`

## Data

| Field | Type | Cardinality | Example | Example source |
|:--|:--|:--|:--|:--|
| `server_host` | Domain | Single | foo.com | walk scene |
| `reason` | Text | Single | address harvesting by spammers | rfc5321.txt:4253-4254 for the motive; the RFC licenses the disabling 'For security reasons' at rfc5321.txt:1359-1360 |

## Where RFC 5321 says so

| Section | Lines | Text |
|:--|:--|:--|
| §3.5.2 | 1359-1362 | *"For security reasons, implementations MAY provide local installations a way to disable either or both of these commands through configuration options or the equivalent"* |
| §3.5.2 | 1364-1366 | *"Since they were both optional in RFC 821, but VRFY was made mandatory in RFC 1123 [3], if EXPN is supported, it MUST be listed as a service extension in an EHLO response."* |
| §7.3 | 4272-4273 | *"Implementations SHOULD still provide support for EXPN, but sites SHOULD carefully evaluate the tradeoffs."* |
| §7.3 | 4253-4254 | *"On the public Internet, the contents of mailing lists have become popular as an address information source for so-called "spammers."* |
| §7.3 | 4271-4273 | *"Implementations SHOULD still provide support for EXPN, but sites SHOULD carefully evaluate the tradeoffs."* |

Line numbers are into [`docs/rfc/rfc5321.txt`](../../docs/rfc/rfc5321.txt), the archived copy. Quotations keep the specification's own words and spellings (AGENTS.md rule 2).

## Notes

Inherited (candidate 10), kept separate from VrfySupportDisabled for the reason recorded there; candidate 293's fields and its harvesting reason are folded in. The re-sweep added §7.3 lines 4272-4273, the standing counterweight nobody cited: even in the section that explains why sites disable EXPN, the RFC says implementations SHOULD still provide support for it. So the modal picture for EXPN is SHOULD support (1359, 4272), MAY disable (1360) — with, unlike VRFY, no MUST anywhere: EXPN is absent from the §4.5.1 minimum command list at lines 3384-3392, which is the one clean asymmetry between the two commands and the reason a merged AddressVerificationDisabled event would have hidden a real difference. Rejected alternative names: AddressVerificationDisabled (the merged form), ListExpansionDisabled, ExpnTurnedOff. Which screen would have killed it: the condemned status register, the altitude tiers, the HELO-only charter. Downstream: ExpnServiceExtensionRegistered stops holding; VerificationDeclinedAsDisabled becomes the runtime answer. FIELD HYGIENE PASS: struck disabled_by (\<postmaster@foo.com>), for the reason recorded in full on VrfySupportDisabled — RFC 5321 records no administrator identity for any configuration change, and the actor field already carries the role. reason is kept and sourced the same way. server_host is the stream's own identity on a Server Configuration root, not padding. No reply code and no invented identity key were present. One consequence the surviving two fields no longer show, so the note must carry it: disabling EXPN also removes an EHLO extension line under the MUST at 1364-1366 while disabling VRFY does not, and that is a downstream event — ExpnServiceExtensionRegistered ceasing to hold — rather than a field here.

COLLISION RESOLVED — merged from 2 independently brainstormed records. Both records cite 3.5.2 lines 1359-1362 and 1364-1366 as their primary ground and 7.3 as the motive, name the same actor (site administrator / operator — one role) and the same stream root, Server Configuration, and describe the same act. Same section, same actor: merge. Chapter 15 takes it under the configuration-root policy applied across this batch.

From the duplicate in 15. Server Configuration and Provisioning: Kept separate from VrfySupportDisabled on the 'either or both' wording at 1360-1362 and on the divergent consequence: disabling EXPN also withdraws an EHLO extension line the server was under a MUST to list. §7.3 supplies the motive the §3.5.2 text only gestures at — harvesting, 4263-4265 — and immediately supplies the counterweight, a standing SHOULD to keep EXPN and a second SHOULD to weigh the tradeoff carefully (4271-4273). Both SHOULDs are addressed to different parties (implementations, sites), which is easy to miss. Opposite arm carried as ExpnSupportEnabled. Screens: HELO-only charter, altitude tiers, twelve-slice model. FIELD HYGIENE: struck two fields, on the same grounds as the VRFY twin. disabled_by duplicated actor and has no RFC counterpart. reason is a constant of the permission — §3.5.2 1360 says 'For security reasons' and offers no alternative ground; the harvesting detail it was paraphrasing is already carried verbatim as the §7.3 4253-4254 citation, which is a stronger record than a Text field. No session, transaction or connection key was present; identity is the installation named by its domain, and the withdrawal of the EHLO line is a consequence for a later session, not a field here.
