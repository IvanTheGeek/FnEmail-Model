# `AcceptedMessageLost`

A message the server had accepted responsibility for was lost — the outcome §6.1 forbids.

| Grouping | Value | Fixed at |
|:--|:--|:--|
| Workflow | 5. Responsibility Handoff and Final Delivery | Step 1 — timeline discovery |
| Stream root | Message | Step 1 — stream roots |
| Ownership swimlane | — | Step 6 — events section, per team or system |
| Originating actor | Receiving host | Step 3 — actor swimlane; provenance, not this event's row |

🟧 Event · confidence *doubtful* · recorded 2026-08-09, Step 1 Brain Storming, whole-spec sweep of RFC 5321 · `019fe7a8-440a-7056-b089-ac5d1aff8449`

## Data

| Field | Type | Cardinality | Example | Example source |
|:--|:--|:--|:--|:--|
| `holding_host` | Domain | Single | bar.com | walk scene |
| `cause` | Text | Single | host crash before spool write | rfc5321.txt:3966-3967 (the RFC's own frivolous reasons: host crash, predictable resource shortage) |
| `loss_cause` | Text | Single | because the host later crashes | rfc5321.txt:3966-3967 |

## Where RFC 5321 says so

| Section | Lines | Text |
|:--|:--|:--|
| §6.1 | 3965-3967 | *"It MUST NOT lose the message for frivolous reasons, such as because the host later crashes or because of a predictable resource shortage."* |
| §6.1 | 3967-3968 | *"Some reasons that are not considered frivolous are discussed in the next subsection and in Section 7.8."* |
| §6.1 | 3962-3964 | *"When the receiver-SMTP accepts a piece of mail (by sending a "250 OK" message in response to DATA), it is accepting responsibility for delivering or relaying the message."* |

Line numbers are into [`docs/rfc/rfc5321.txt`](../../docs/rfc/rfc5321.txt), the archived copy. Quotations keep the specification's own words and spellings (AGENTS.md rule 2).

## Notes

Inherited candidate 273, kept, and now paired with the preventive event the re-sweep recovered. Doubtful as an event to keep, not as a thing that happens: it is a prohibited outcome, and a model may prefer to record only its prevention. 'frivolous' is undefined and the RFC defers the non-frivolous list to §6.2 (heading at line 4013) and §7.8 — a 🟥, and one this chapter cannot close because neither section is its anchor. The silent-drop policy of §6.2 4027-4028 is the deliberate cousin of this accidental loss and belongs to whichever chapter owns §6.2; do not merge the two, one is a crash and the other is a decision. Screens: the altitude tiers (Infrastructure), G1, and the condemned status register. ⚠️ FIELD HYGIENE PASS 2026-08-09 — two fields struck. message_id: no message identifier exists in RFC 5321 — §4.4's ID clause (line 3336) is an optional receiver trace clause, msg-id is RFC 5322's — and f2C8D14 is this project's minted queue_id value. There is a bitter accuracy in the absence here: a message lost before anything durable was written has NO identity left at all, which is exactly why the notification obligation cannot be discharged for it. cause survives, and is the one field that matters, because the MUST NOT turns on it: 'frivolous' versus not is the whole test, and the RFC's own examples of frivolous are the host crash and the predictable resource shortage at 3966-3967. lost_after: struck as the event's own occurrence time.

COLLISION RESOLVED — merged from 2 independently brainstormed records. Same section, same MUST NOT, same actor, same stream root, same single meaningful field. The disagreement is confidence and one actor-restating field, both recorded rather than lost.

From the duplicate in 12. Non-Delivery Notification: RECOVERED BY THE RE-SWEEP, and the clearest single thing the 32-event cap cost this chapter: eleven sweeps read §6.1 and not one recorded the violated arm of its opening MUST NOT, even though that MUST NOT is the reason the notification obligation exists at all. Strength: MUST NOT, qualified by "for frivolous reasons" and illustrated with two — a later host crash, and a predictable resource shortage. The qualification creates the event's hardest edge, and the RFC knows it: line 3967-3968 concedes that some reasons are NOT frivolous and points to §6.2 and §7.8 for them, so a loss is a violation only relative to a judgment the specification hands off. loss_cause is what carries that. This is a silent failure with no wire evidence and no notification — the message simply stops existing — which puts it beside MessageSilentlyDropped as the second of the chapter's two invisible outcomes, and unlike that one it is forbidden rather than tolerated. Note also the lowercase "It must take this responsibility seriously" at 3964 immediately before the uppercase MUST NOT: two sentences, two strengths, one paragraph. Opposite event: DeliveryFailedAfterAcceptance followed by NonDeliveryNotificationSent — the conforming way to fail. Rejected alternative names: MessageLostAfterAcceptance, CustodyBroken, ResponsibilityAbandoned. Which screens would have killed it: all of them — no command, no reply, no consumer, and the responsibility-boundary cutoff puts it past the model's edge.

=== FIELD HYGIENE PASS === Struck two of three fields. (1) message_ref (f2C8D14) — fabricated; RFC 5321 supplies no message identifier. (2) custodian_host — struck as a restatement of the event-level actor. WHAT CARRIES IDENTITY HERE INSTEAD: nothing. Like MessageSilentlyDropped this event leaves no wire evidence and now no minted key either, which is the accurate picture of a message that stops existing. loss_cause survives because §6.1 3967-3968 makes the frivolous / not-frivolous distinction the entire content of the MUST NOT, and the RFC names two frivolous causes verbatim at 3966-3967. Example corrected to the RFC's own words; the previous value added machinery about a queue and stable storage that RFC 5321 does not mention anywhere in this paragraph.
