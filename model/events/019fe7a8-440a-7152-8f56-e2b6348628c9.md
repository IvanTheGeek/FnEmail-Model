# `MessageJudgedSeriouslyFraudulent`

A site concluded, with very high confidence, that a message was seriously fraudulent or otherwise inappropriate — the precondition the specification sets before a silent drop.

| Grouping | Value | Fixed at |
|:--|:--|:--|
| Workflow | 12. Non-Delivery Notification | Step 1 — timeline discovery |
| Stream root | Message | Step 1 — stream roots |
| Ownership swimlane | — | Step 6 — events section, per team or system |
| Originating actor | Receiving site | Step 3 — actor swimlane; provenance, not this event's row |

🟧 Event · confidence *doubtful* · recorded 2026-08-09, Step 1 Brain Storming, whole-spec sweep of RFC 5321 · `019fe7a8-440a-7152-8f56-e2b6348628c9`

## Data

| Field | Type | Cardinality | Example | Example source |
|:--|:--|:--|:--|:--|
| `judgment` | Text | Single | seriously fraudulent | rfc5321.txt:4042-4043 |

## Where RFC 5321 says so

| Section | Lines | Text |
|:--|:--|:--|
| §6.2 | 4040-4043 | *"So silent dropping of messages should be considered only in those cases where there is very high confidence that the messages are seriously fraudulent or otherwise inappropriate."* |

Line numbers are into [`docs/rfc/rfc5321.txt`](../../docs/rfc/rfc5321.txt), the archived copy. Quotations keep the specification's own words and spellings (AGENTS.md rule 2).

## Notes

RECOVERED BY THE RE-SWEEP. The raw candidate MessageSilentlyDropped named it as a sibling with "no criteria attached anywhere in the span" and gave it no slot — the cap again. It is separable from the drop because the RFC makes the judgment the gate: the drop is permitted only where this event has occurred at "very high confidence". confidence_level is therefore a recorded fact and not decoration, and it travels as a field for the same reason it does on ReturnAddressDeterminedInvalid: the RFC states a threshold and supplies no way to test it. Strength: no RFC 2119 keyword — "should be considered only" is lowercase, and the criterion ("seriously fraudulent or otherwise inappropriate") is left entirely undefined. Hotspot: two adjacent sections gate consequential, irreversible behavior on determinations this specification explicitly refuses to specify — here, and at HostileContentDetected. Rejected alternative names: MessageDeemedInappropriate, FraudulentMessageIdentified, SilentDropThresholdMet. Which screens would have killed it: the altitude tiers, the capture/decision two-kinds lens, and the condemned status/phase register.

=== FIELD HYGIENE PASS === Struck two of three fields. (1) message_ref (f2C8D14) — fabricated; RFC 5321 supplies no message identifier. WHAT CARRIES IDENTITY HERE INSTEAD: nothing the spec supplies. (2) confidence_level — struck: it held "very high", which is the RFC's own stated THRESHOLD at rfc5321.txt:4042 restated as instance data. There is no scale, no test, and every conforming instance carries the same value, so it measures nothing; the threshold belongs to the rule. The inherited note's defense of it is preserved above and is answered here, not deleted. The identical shape is KEPT one place — certainty_threshold on InvalidReturnAddressDropPolicyAdopted — because there a threshold genuinely is a property of the policy adopted. The same strike was made to certainty on ReturnAddressDeterminedInvalid. CONFIDENCE LOWERED TO DOUBTFUL, under the brief's one exception. Once the fabricated key and the restated threshold are gone, the entire record is a single string sourced to rfc5321.txt:4042-4043 — the same string, from the same line, as drop_reason on MessageSilentlyDropped, which the RFC states in the SAME SENTENCE as the drop it gates. That is the shape of a field wearing an event's name. The record is kept, not dropped, not renamed and not merged; deciding is a later step's job. The counter-argument, recorded so the decision is made on both: the RFC makes the judgment the gate ("should be considered only in those cases where..."), so a model that never records it cannot show why a particular drop was permitted.
